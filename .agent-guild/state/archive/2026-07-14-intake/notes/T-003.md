# T-003 self-report

## What I did

Edited `.claude/skills/constitution/SKILL.md`'s "## 1. Interview" section only. Added a
clearly-marked two-way branch at the top of the step:

- **Spec-exists branch**: read `.agent-guild/state/spec.md` end to end (including its
  provenance header), derive candidate quality bars from the spec's goal, Definition of
  Done, deliverables, constraints, and non-goals, present those candidates to the user for
  confirmation/adjustment, and ask only what the spec leaves open (severities, protected
  words, environments the spec doesn't name). Explicit instruction not to re-ask anything
  the spec already answers.
- **No-spec branch**: the original full-interview paragraph, left verbatim (word for word),
  just prefixed with the "If no spec exists" condition so its text is unchanged per the
  spec's requirement.

Left the "Done when..." line below the branch untouched since it applies to both paths.
Did not touch `interview.md` — the spec-exists branch didn't need any of its question-bank
content, so pulling it in would have been unnecessary scope.

## Verification

- `git diff -- .claude/skills/constitution/SKILL.md` shows only the intended 7-line
  insertion in the Interview section; nothing else in the file changed.
- `git status --short` confirms no other files were touched by this task (the `.gitignore`,
  `check-provenance.py`, and `.claude/skills/job/` changes visible in status belong to
  other tasks in this job, not this one).
- Ran the C-7 check command directly:
  `git diff --quiet HEAD -- .claude/settings.json .claude/agents .agent-guild/hooks/dispatch-guard.py .agent-guild/hooks/orchestrator-write-guard.py .agent-guild/hooks/stop-gate.py .agent-guild/hooks/subagent-return.py`
  — exits 0 (clean), so C-7 passes.

## Judgment calls

- Interpreted "candidate quality bars" broadly enough to include constraints and non-goals
  (not just goals/acceptance-criteria) since the spec excerpt explicitly lists "goals,
  acceptance criteria, constraints, and non-goals" as the derivation source.
- Kept the branch order spec-first, no-spec-second (matching the spec excerpt's own
  ordering) rather than preserving the original text-first ordering, since the spec
  excerpt frames the spec-exists case as the new primary path being added.

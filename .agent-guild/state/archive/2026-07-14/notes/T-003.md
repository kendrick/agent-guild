# T-003 self-report

## What I did

Created `dist/plugin/hooks/hooks.json`, mirroring the event/matcher structure of
the live `.claude/settings.json` exactly:

- `Stop` → `stop-gate.py`
- `SubagentStop` (matcher: `worker-bulk|worker-standard|worker-craft|checker-deterministic|checker-judgment|auditor`) → `subagent-return.py`
- `PreToolUse` (matcher: `Task|Agent`) → `dispatch-guard.py`
- `PreToolUse` (matcher: `Write|Edit|MultiEdit`) → `orchestrator-write-guard.py`

Every `command` keeps `timeout: 30` and targets its script via
`python3 "${CLAUDE_PLUGIN_ROOT}"/hooks/<script>.py` — quoting just the
variable, per the spec excerpt's literal instruction (`"${CLAUDE_PLUGIN_ROOT}"/hooks/<script>`),
rather than quoting the whole path. The scripts already exist under
`dist/plugin/hooks/` from T-002, so this path resolves once the plugin is
installed.

Top-level shape is `{ "hooks": { "<Event>": [ ... ] } }`, matching the spec.
Wrote nothing outside `dist/plugin/`.

## Verification

- JSON parses cleanly (`python3 -c "import json; json.load(...)"`).
- C-3 check-build script passes: no `CLAUDE_PROJECT_DIR` reference remains, `CLAUDE_PLUGIN_ROOT` is present.
- C-8 check-build script passes: `git diff --quiet HEAD -- .claude .agent-guild/hooks .agent-guild/scripts .agent-guild/templates` is clean — untouched.
- For C-4, I structurally compared events/matchers/script-basenames/timeouts between the live `.claude/settings.json` and the new file (script by script, ignoring the necessarily-different path prefix) — all four gates match on event, matcher, and target script name.

## Judgment call to flag

The spec excerpt gives two slightly different quoting conventions: the spec.md
proper says rewrite to `$CLAUDE_PLUGIN_ROOT/...` (bare), while the task's own
spec excerpt (and constitution C-3's text) says `"${CLAUDE_PLUGIN_ROOT}"/hooks/<script>`
— quoting only the variable, brace form. I followed the more specific,
more recent instruction in the task excerpt: `${CLAUDE_PLUGIN_ROOT}` (braced)
with double quotes around just the variable, e.g.
`python3 "${CLAUDE_PLUGIN_ROOT}"/hooks/stop-gate.py`. This also matches the
brace form the constitution's C-3 check regex expects (`grep -rq CLAUDE_PLUGIN_ROOT`,
which matches either form, but the constitution clause text itself uses braces).

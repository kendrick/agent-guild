# T-006 self-report

## What I built

`dist/plugin/README.md` — the plugin install story, covering the four things the spec names, in this order:

1. **Local-path install.** `/plugin marketplace add ./dist/plugin` then `/plugin install agent-guild@agent-guild-local`, with the minimal `marketplace.json` the user has to drop next to `plugin.json`, and a note that enablement lands in a settings file (`~/.claude/settings.json` or the project's `.claude/settings.json`) under `enabledPlugins`.
2. **The hybrid, with the reason.** A dedicated "What Ships / What Stays" section states plainly that the contract import and `.agent-guild/state/` stay per-project because a plugin can't contribute an always-on `CLAUDE.md`. I also listed `.agent-guild/scripts/` as project-side, since the constitution's non-goals confirm the check tooling stays with the state its clauses invoke.
3. **The import line explained.** A section titled with the literal line, breaking it into what it is / what it loads / how it got there ("you added it when you installed the guild"), so it can never read as a mystery.
4. **The portability check.** The offline `python3 hooks/test_hooks.py` (expect `49 passed, 0 failed`), then a fresh-session walk of the three demonstrable gates (dispatch-guard denies a Task-ID-less dispatch, stop-gate holds a turn open, write-guard blocks an orchestrator edit), pointing at `SMOKE.md` Part A for the full walk.

## Taste calls

- **Voice.** Matched the top-level `README.md`: plain declarative sentences, copulas over "serves as," the "`code/` holds X" construction it already uses for directory descriptions, Title Case headings. The intro deliberately echoes C-7's failing example ("tooling that's installed and an orchestrator that was never told the rules") to make the hybrid's stakes land immediately.
- **Facts I verified rather than assumed.** Confirmed the local plugin install flow against the Claude Code docs (marketplace file required, `name@marketplace` install syntax, `enabledPlugins` key, `${CLAUDE_PLUGIN_ROOT}` vs. `CLAUDE_PROJECT_DIR`) before writing, so the steps actually work. Confirmed the packaged test count (49) and the five state subdirs (`tasks`, `verdicts`, `disputes`, `notes`, `log`) from the tree itself.
- **The marketplace.json variant.** The shipped package only has `plugin.json`, so a true local install needs a marketplace file. I documented the user creating a one-plugin `marketplace.json` with `source: "./"` (the marketplace and plugin share the folder) rather than inventing a second artifact, since my scope was the README only.
- **Humanizer.** Ran the skill and revised in place: dissolved three repetitions of the "agents, skills, and hooks" triad, converted `code — description` bullets to full sentences (matching the top README and avoiding spaced em dashes), and thinned prose em dashes to a single tasteful pair. Kept Title Case headings and one unspaced chained em dash per the task's and the user's stated preferences.

## Scope notes

- Wrote nothing outside `dist/plugin/`. C-8 check (`git diff --quiet HEAD -- .claude .agent-guild/hooks .agent-guild/scripts .agent-guild/templates`) passes.
- The ` M .gitignore` in the working tree (adding `dist/`) is a prior task's C-9 work, not mine — I never touched it. The README is correctly ignored by the repo's own `.gitignore` (`dist/` at line 25).
- No protected-passages manifest in this job (constitution: "none"), so no `check-protected.py` run applies.

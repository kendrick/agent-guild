---
task: T-001
tier: sonnet
retry: 0
checker: checker-judgment
verdict: PASS
checked_at: 2026-07-14T20:05:30Z
---

## Per-clause results

| clause | method | evidence (command output / quoted artifact / fetched page) | expected | actual | result |
| ------ | ------ | ---------------------------------------------------------- | -------- | ------ | ------ |
| C-1 | judgment: imports, exec bit, --help, manifest props | Imports (build-plugin.py:24-32) are all stdlib: argparse, filecmp, json, os, re, shutil, subprocess, sys, tempfile — no third-party. `ls -l` → mode `755` (executable). `--help` prints usage documenting build, `--out DIR`, and `--check`. Manifest source `scripts/plugin-src/plugin.json`: `"name": "agent-guild"` (kebab), `"author": { "name": "Kendrick Arnett" }` (object), `"hooks": "./hooks/hooks.json"`, `"version": "0.1.0"`. Manifest lives in the source dir and is copied through (write_plugin_manifest, build-plugin.py:295-303), not embedded in the script. | stdlib-only, executable, --help covers all three modes, manifest carries the four named properties | all satisfied | PASS |
| C-2 | .agent-guild/scripts/check-build.sh (exact C-2 command) | `check-build.sh: exit 0` — six agents byte-identical (cmp), five skill dirs present, six hook files byte-identical, hooks.json + project-template CLAUDE.md/scripts/templates + .claude-plugin/plugin.json present, no hydrate/update-working-memory skills, no hydrator/working-memory agents. Deviation ruled below: project-template scripts/ and templates/ ship every git-tracked source file (diff exit 0 both), filtering only untracked debris (`git ls-files` returns nothing for __pycache__/node_modules/package-lock.json). | exit 0 | exit 0 | PASS |
| C-3 | .agent-guild/scripts/check-build.sh (exact C-3 command) | `hooks.json ok` / `exit 0` — commands all carry CLAUDE_PLUGIN_ROOT, zero CLAUDE_PROJECT_DIR, every referenced script present in built hooks/, all four gates (stop-gate, subagent-return, dispatch-guard, orchestrator-write-guard) registered. | exit 0 | exit 0 | PASS |
| C-4 | judgment: build to temp, grep + overreach/underreach diff | Packaged `project-template/.agent-guild/CLAUDE.md` invokes `/agent-guild:constitution` (L37), `/agent-guild:decompose` (L41), `/agent-guild:retrospective` (L45); source `.agent-guild/CLAUDE.md` stays bare (`/constitution`, `/decompose`, `/retrospective`). Overreach scan `[A-Za-z0-9_./-]/agent-guild:<name>` → no matches (no mangled paths). Underreach scan for surviving bare invocations in packaged CLAUDE.md + 5 SKILL.md bodies → none. `diff` of source vs packaged job/SKILL.md and constitution/SKILL.md shows only invocation→namespaced rewrites; paths like `.agent-guild/state/spec.md` and `skills/retrospective/` untouched, no headings mangled. Repo sources unchanged (see C-7). | namespaced in packaged prose, paths/headings intact, sources unchanged | all satisfied | PASS |
| C-5 | .agent-guild/scripts/check-build.sh (exact C-5 command) + judgment supplement | `exit 0` — match→exit0 (validator ✔), corrupted _lib.py→"content differs: hooks/_lib.py" nonzero, absent plugin/→"does not exist -- run a build first" nonzero, rebuild→exit0. plugin/ removed afterward (git status shows only the two new artifacts). Judgment supplement: run_check (build-plugin.py:374-381) hard-fails when `shutil.which("claude") is None`, writing "the `claude` CLI is not on PATH ... a skipped validation must never report success" and `return 1` — never a silent skip. | exit 0 and CLI-missing hard-fails | exit 0; CLI-missing branch returns 1 | PASS |
| C-6 | .agent-guild/scripts/check-build.sh (exact C-6 command) | `✔ Validation passed` / `exit 0` — `claude plugin validate --strict` on a fresh build succeeds. | exit 0 | exit 0 | PASS |
| C-7 | .agent-guild/scripts/check-build.sh (exact C-7 command) | `exit 0` — `git diff --quiet HEAD` clean across .claude/settings.json, .claude/agents, .claude/skills, .agent-guild/hooks, .agent-guild/scripts, .agent-guild/templates after a build. `git status` shows only untracked `scripts/build-plugin.py` and `scripts/plugin-src/` — exactly the declared additions. | clean diff; only declared additions | exit 0 | PASS |
| C-8 | judgment: read alongside check-provenance.py / new-task.py | Module docstring states what+why and documents modes/exit codes (L1-23). Why-comments at the non-obvious spots: include-when-present (L49-52), isolation guard reasoning (L94-104), build-byproduct filtering (L66-70), NAMESPACE_MAP path-vs-prose distinction (L75-79, L249-254). Small functions (copy_agents, copy_skills, copy_hooks, generate_hooks_json, assemble_project_template, apply_namespacing, write_plugin_manifest, build, diff_trees, run_check) rather than a monolithic main. Errors name the subject (BuildError with concrete paths; diff_trees names each differing file). Matches new-task.py's docstring+why-comment+named-error idiom. | house style: docstring, targeted why-comments, small functions, subject-bearing errors | all satisfied | PASS |

---
id: T-001
title: Build and stage the committed plugin tree, retire dist
spec: .agent-guild/state/spec.md
clauses: [C-1, C-2, C-3, C-4, C-5]
executor: worker-bulk
executor_model: haiku
checker: checker-deterministic
check_method: >-
  All five clauses are deterministic. Run the exact check commands from
  .agent-guild/state/constitution.md clauses C-1, C-2, C-3, C-4, C-5, exactly
  as written there, and transcribe exit codes and output.
status: complete
retries: 0
max_retries: 2
deps: []
escalations: []
artifacts:
  - plugin/
  - scripts/plugin-src/plugin.json
  - .gitignore
---

## Spec excerpt

Mechanical, zero-judgment steps, in this order. Do not edit any file except
the two named below; everything under `plugin/` comes from the build script,
never from your hands.

1. Edit `scripts/plugin-src/plugin.json`: change `"version": "0.1.0"` to
   `"version": "0.2.0"`. Change nothing else in the file.
2. Edit `.gitignore`: remove the `dist/` entry line AND its accompanying
   `# Build artifacts` comment line (they sit together near the bottom).
   Change nothing else in the file.
3. Delete the local staging tree: `rm -rf dist`.
4. Run the build for real, to its default committed location:
   `python3 scripts/build-plugin.py` (no --out). This creates `plugin/` at the
   repo root.
5. Verify your work by running the exact five check commands from
   `.agent-guild/state/constitution.md` (C-1 through C-5). All five must exit
   0 before you set needs-check. If C-1's `--check` fails, rebuild rather than
   hand-editing anything under `plugin/`.

Do NOT run `git add` or commit anything — the orchestrator commits after the
checker's verdict. Do not create, modify, or delete any other file anywhere in
the repo; the footprint check is a repo-wide porcelain assertion whose only
allowed paths are `plugin/`, `scripts/plugin-src/plugin.json`, and
`.gitignore`.

## Rework diagnosis

<!-- ORCHESTRATOR appends here on each FAIL, copied verbatim from the checker's
verdict Diagnosis. Newest at the bottom, headed with the attempt it addresses
(e.g. "### sonnet r1"). Empty until the first failure. -->

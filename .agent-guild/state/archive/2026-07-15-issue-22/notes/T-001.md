# T-001 self-report

## What I did

Wrote `.claude/skills/init/SKILL.md` from scratch — nothing else touched. Structure:

- Frontmatter: `name: init`, a description quoting both trigger phrases from the
  spec ("finish the guild install here" / "set up this project for the guild"),
  `disable-model-invocation: true`.
- Intro paragraph carries the plugin-can't-ship-CLAUDE.md rationale from
  `docs/plugin-publish-plan.md`, plus the governing property (idempotent,
  never silently destructive) and an explicit "running it twice is safe"
  statement.
- Step 0 (unnumbered as one of the five, but first): resolves
  `${CLAUDE_PLUGIN_ROOT}/project-template/`, hard-stops on both failure modes
  (literal unsubstituted string vs. missing path), names the problem, points
  at `/plugin marketplace add` + `/plugin install`, states no-guessing /
  nothing-written-on-failure.
- Steps 1-5: contract, import line, state dirs, gitignore, payload — each with
  an explicit exists→skip-and-report and (where applicable) differs→ask
  path. Step 5 gives a concrete shell loop that skips existing files so local
  patches survive re-init.
- Unnumbered "Never touch `.claude/settings.json`" section stating the
  double-fire rationale.
- Closing "Summary and hand-off" section: what to report, then bare `/job`
  and `/constitution` next-steps (per the hard rule to write these
  unnamespaced so the build transform rewrites them), plus a closing
  second-run-is-safe restatement.
- Self-references to the skill's own packaged identity are written as
  `/agent-guild:init` directly in source (the stated exception to the
  bare-invocation rule), since this skill only does anything useful when
  running from the packaged plugin.

## Verification run

Ran both constitution check commands directly (not just eyeballing):

- C-4: `python3 scripts/build-plugin.py --out <tmp>` then asserted
  `skills/init` exists in the build, the built SKILL.md contains
  `agent-guild:constitution` or `agent-guild:job`, no bare `/job`,
  `/constitution`, or `/init` survive in the built copy, and
  `scripts/build-plugin.py`/`scripts/plugin-src` are untouched. Exit 0.
- C-5: `.claude/skills/init` exists and `git status --porcelain` outside that
  directory is empty. Exit 0 (only untracked output is
  `.claude/skills/init/`).

Also inspected the built copy directly: bare `/job` → `/agent-guild:job`,
bare `/constitution` → `/agent-guild:constitution`, and every
`/agent-guild:init` self-reference in source passed through unchanged (the
namespacing regex requires the slash immediately precede the bare keyword,
so `:init` after `agent-guild` never matches).

## Judgment calls / caveats

- `OPTIONAL_SKILLS = ["init"]` was already wired into
  `scripts/build-plugin.py` (a prior task), so I made zero build-script
  changes — confirmed by the C-4 check's own `git diff --quiet` clause.
- The task file's frontmatter had `status: assigned`; I set it to
  `needs-check` and filled `artifacts` per the guild contract, since the spec
  excerpt only gave me the SKILL.md content to write.
- No other files were created or modified — confirmed via `git status
  --porcelain` before finishing.

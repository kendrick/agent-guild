# T-001 self-report

Built `.agent-guild/hooks/session-nudge.py` and added five fixtures to
`.agent-guild/hooks/test_hooks.py` (one more than the spec's minimum of
three, since I split "state dirs missing" into two checks: the "init"
substring and the one-stdout-line invariant).

## Implementation notes

- Predicate: no `.agent-guild/` → silent. Otherwise checks the five state
  subdirs (via `_lib.state_path()`, not hand-rolled paths) plus root
  `CLAUDE.md` presence/import-line, collects whatever's missing into a list,
  and either stays silent (empty list) or prints exactly one line naming the
  missing pieces and pointing at `/agent-guild:init`.
- Reused `_lib.project_dir()` and `_lib.state_path()` per the spec; the only
  path math I wrote myself is `.agent-guild/` and root `CLAUDE.md`, which
  have no existing `_lib` helper.
- Wrapped in `_lib.run("session-nudge", main)` like every other hook, so
  PAUSED and fail-loud both apply.
- Docstring + a dedicated paragraph on the zero-evidence-silence asymmetry
  (why a user-scope install must stay silent on unrelated repos even though
  a nudge would be informative), matching the style of `stop-gate.py` and
  `orchestrator-write-guard.py`.
- Did NOT touch `.claude/settings.json` — this repo is fully initialized, so
  registering the hook here would be permanent no-op overhead, and C-5's
  footprint check would fail it anyway.

## Verification run before finishing

- `python3 .agent-guild/hooks/test_hooks.py` → 60 passed, 0 failed (55
  baseline + 5 new).
- C-2's exact check command (behavioral: zero-evidence silent, state-dirs-
  missing nudges with one line containing "init", CLAUDE.md-missing nudges,
  fully-initialized silent) → exit 0.
- C-4's exact check command (unmodified `scripts/build-plugin.py` packages
  the file and `hooks.json` gets a `SessionStart` entry with matcher
  `startup` and a `${CLAUDE_PLUGIN_ROOT}`-based command, plus
  `scripts/build-plugin.py` / `scripts/plugin-src` show no diff) → exit 0.
- `git status --porcelain` with the two allowed excludes → empty (footprint
  clean for C-5).

## Judgment calls

- For the "CLAUDE.md missing" fixture I reused the suite's existing
  `fresh_proj()` helper as-is (it creates all five state subdirs but no root
  CLAUDE.md), rather than writing a new near-duplicate helper — it already is
  exactly the "state complete, import line missing" fixture the spec asks
  for.
- Message wording follows the spec's example almost verbatim, substituting
  the actual comma-joined list of missing pieces (e.g. `state/tasks,
  state/verdicts, CLAUDE.md @.agent-guild/CLAUDE.md import line`) so the line
  names the real gap rather than being generic.

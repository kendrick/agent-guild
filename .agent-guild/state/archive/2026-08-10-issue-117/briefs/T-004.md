# Brief: T-004

**Task:** T-004 — Backfill 18 ledger rows in the skills repo

## Constitution clauses

### C-4: The backfill is attributed from evidence, validated, and changes nothing else
- **text**: Every one of the 18 rows in `~/repos/skills/.agent-guild/state/archive/2026-08-08-issue-27/log/vendor-calls.jsonl` carries the right `job`: rows 0-6 `kendrick/skills#17`, rows 7-10 `kendrick/skills#32`, rows 11-17 `kendrick/skills#27`. Row order is preserved, no other field on any row is altered, and every row validates against the amended schema in THIS repo.
- **check**: checker-judgment: re-derive the attribution before trusting the table, using the three discriminators that actually separate these rows — rows byte-identical (but for the added key) to the #32 archive's own `log/vendor-calls.jsonl` are #32's; `judgment`-tier stems for T-005 through T-007 exist only in the #17 archive, and the contiguous ascending run around them predates #32's intake at `2026-08-08T14:47:08Z`; and the remaining rows postdate #27's intake at `2026-08-08T19:28:16Z` while every earlier archive's own ledger already books its own crossings, so a row not booked elsewhere and stamped after that intake is #27's. (An earlier draft of this clause said `sonnet` and `opus` stems exist only in the #27 archive. T-004's checker showed that is false — `T-001-sonnet-r0-codex.json` and `T-002-opus-r0-codex.json` also sit under `2026-08-07/`, whose ledger books them at `04:18:46Z` and `04:33:26Z` — which is the same defect this clause's own failing example calls out for the tier segment.) Attribute the single row with an empty `artifacts` array by append position, a JSONL log being chronological in write time even where a stamped timestamp is wrong. Then diff the file against its git baseline and confirm the only per-line change is an added `job` key. Validate by importing `load_schema()` and `schema_violation()` from THIS repo's `.agent-guild/scripts/ledger-append.py` and walking every row — never the skills repo's frozen copy, whose schema has no `job` and would fail all 18.
- **severity**: blocker
- **failing example**: rows attributed by tier segment alone, which cannot separate rows 0-6 from rows 7-10 because `T-001-judgment-r0-codex.json` through `T-004-judgment-r0-codex.json` exist in both the #17 and #32 archives — 8 of 18 ambiguous, and exactly the ones the backfill exists to distinguish. Also failing: the artifacts-less row left unattributed, the four duplicated #32 rows deleted, or a wrong `started_at` "corrected" while adding `job`.

## Spec excerpt

One file, in a different repository: `~/repos/skills/.agent-guild/state/archive/2026-08-08-issue-27/log/vendor-calls.jsonl`. It holds 18 vendor-call rows from **three** different guild runs, because an archive step swept a live ledger that still carried two earlier jobs' rows. Add a `job` key to each row. Change nothing else: not row order, not a single other field, not the row count.

The values, and how each is established. Do not attribute by timestamp — two rows carry stamps that contradict their own job, one of them stamped local time with a `Z` suffix.

| rows | job | how you know |
| --- | --- | --- |
| 0–6 | `kendrick/skills#17` | their artifacts name `judgment`-tier verdict stems; T-005, T-006, and T-007 at that tier exist only in the `2026-08-08-issue-17` archive, and rows 0–6 are one contiguous ascending run around them |
| 7–10 | `kendrick/skills#32` | byte-identical to the four rows in `2026-08-08-issue-32/log/vendor-calls.jsonl`, that job's own archived ledger |
| 11–17 | `kendrick/skills#27` | their artifacts name `sonnet`- and `opus`-tier stems, which exist only in the `2026-08-08-issue-27` archive |

Two rows need care. Row 15 is stamped `2026-08-08T00:00:00Z`, inside #17's window, but its artifact is `T-006-sonnet-r0-codex.json` — a stem only #27's archive holds, so it is #27's. Row 17 is a timeout (`exit_code: 143`) with an **empty** `artifacts` array, so there is no stem to key on; it is #27's by append position, since a JSONL log is chronological in write time even where a stamped timestamp is wrong.

The tier segment alone will not do this job. `T-001-judgment-r0-codex.json` through `T-004-judgment-r0-codex.json` exist in **both** the #17 and #32 archives, so 8 of the 18 rows are ambiguous on tier — exactly the ones the backfill exists to separate. That is why rows 7–10 are established by the byte-identical match instead.

Validate when done. That project's own `.agent-guild/` payload is frozen at an older install and its schema copy has no `job`, so validating there fails all 18 rows for the wrong reason. Use this repo's:

```python
import importlib.util, json
spec = importlib.util.spec_from_file_location(
    "la", "/Users/k.arnett/repos/agent-guild/.agent-guild/scripts/ledger-append.py")
m = importlib.util.module_from_spec(spec); spec.loader.exec_module(m)
schema = m.load_schema()
# then m.schema_violation(row, schema) per row — None means conforming
```

Out of scope, deliberately. The four #32 rows also living in the #32 archive stay where they are; deleting a run's record is not this job's call. Ten rows record absolute artifact paths under a `/Users/karnett/` home that does not exist on this machine, and one row's `started_at` carries fractional seconds — both get written down by another task, neither gets repaired here. Do not "correct" a wrong timestamp while you are in the file.

**Do not commit** in the skills repo. Leave the file dirty; the checker confirms it is uncommitted before diffing it against its baseline, and a commit would remove the evidence it needs.

## Verdict contract

Return the canonical verdict object. Echo these four fields exactly as given:

  task_id  T-004
  checker  checker-courier
  vendor   openai
  model    gpt-5.6-terra

Set duration_ms and cost_usd to null; call metrics are recorded on this side. A fail carries at least one finding with concrete evidence.

severity is the impact of a defect: blocker, major, minor, or info. Use info for a finding that records a clause being satisfied. A clause's own severity in the text above is the cost of violating it, not the label for every finding about it. A pass carries only info and minor findings.

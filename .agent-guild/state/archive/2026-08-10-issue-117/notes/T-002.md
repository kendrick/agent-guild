# T-002 notes

Added the six labelled `job:` cases to `.agent-guild/scripts/test_ledger_append.py`, appended after the existing "two sequential appends" block and before the final pass/fail summary. Verified with both `python3 .agent-guild/scripts/test_ledger_append.py` directly (43 passed, 0 failed) and the exact check-build.sh command from the task's check_method (grep found all six `ok   job: <label>` lines).

## What each case actually asserts

- **derived from provenance ref** / **flag overrides provenance** / **absent when no spec exists**: three temp-dir fixtures, each run with the script's cwd pinned to that dir via a new `cwd=` kwarg I added to the existing `run()` helper (backward-compatible default `None`, so all prior call sites are unaffected). The "absent" case's temp dir never gets a `.agent-guild/state/spec.md` written into it — I did not lean on the repo root's real spec.md, which is exactly the trap the task spec called out. Each case guards against a script crash: it only reads the ledger and asserts `record.get("job")` when `rc == 0` and the ledger file exists, so a schema regression that makes `job` required (which would flip exit code to 1) fails the check() cleanly instead of throwing an unhandled exception that would abort the rest of the suite.

- **legacy row without the key still validates**: this one departs from the file's "no imports" convention. I load `ledger-append.py` via `importlib.util` and call `load_schema()` + `schema_violation()` directly against a hand-built dict with every required field except `job`. I chose this because the task's own wording names those two functions explicitly, and the constitution's C-4 check text uses the identical import for the identical reason (validating against THIS repo's schema_violation rather than a frozen copy). The subprocess-only alternative I considered (rerun the "absent" fixture and just call it done) would have been redundant with the "absent when no spec exists" case rather than a distinct test of schema leniency, so I judged the direct unit-style check to be the stronger, less-redundant one. Flagging this as a deliberate deviation from the general "rather than importing internals" guidance for the orchestrator's awareness — everything else in the file stays subprocess-based.

- **two jobs are distinguishable**: two runs share `--task-id T-007` and the *same* `--started-at` (both draw from unmodified `BASE_ARGS`), differing only in `--job`, appending to one ledger. Asserts both rows share `task_id`, share `started_at`, and differ only in `job` — so the distinguishing signal can't be coming from a timestamp, per the spec's explicit instruction.

- **schema keeps the field optional**: reads `.agent-guild/schemas/vendor-call.schema.json` straight off disk (no script import), asserts `job` sits under `properties` typed `"string"` and is absent from `required`.

## Judgment calls

- Added a `cwd` kwarg to the shared `run()` helper — the only change to a function used by pre-existing cases, and it's additive/opt-in (`cwd=None` preserves prior behavior exactly).
- Used realistic `owner/repo#N`-shaped job values in fixtures (`kendrick/agent-guild#117`, `override/repo#9`, `org/repo#1`/`#2`) to match the format shown in the schema's own `job` description and ledger-append.py's docstring example.
- Did not touch `ledger-append.py` or the schema, per the task's explicit prohibition. Did not commit anything.

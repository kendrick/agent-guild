# T-004 self-report

## What I did

Added a `job` key to all 18 rows of
`~/repos/skills/.agent-guild/state/archive/2026-08-08-issue-27/log/vendor-calls.jsonl`,
touching nothing else.

Attribution used (0-indexed rows, matching the task table):

- Rows 0-6 -> `kendrick/skills#17`
- Rows 7-10 -> `kendrick/skills#32`
- Rows 11-17 -> `kendrick/skills#27`

I re-derived this myself rather than trusting the table at face value:

- Rows 7-10 (file lines 8-11): diffed byte-for-byte against
  `~/repos/skills/.agent-guild/state/archive/2026-08-08-issue-32/log/vendor-calls.jsonl`
  (all 4 lines there). Every field matched exactly (task_id, started_at,
  duration_ms, tokens, artifacts paths, etc.) -> #32.
- Rows 0-6 (file lines 1-7): a contiguous ascending run (T-001, T-002, T-005,
  T-006, T-003, T-004, T-007) with `judgment`-tier stems, distinct from the
  rows 7-10 block despite sharing the same tier segment on T-001-T-004 -> #17,
  per the task's discriminator about tier segment alone being insufficient.
- Rows 11-17 (file lines 12-18): `sonnet`/`opus` tier stems -> #27, including
  the two flagged edge cases:
  - Row 15 (line 16, T-006): stamped `2026-08-08T00:00:00Z` (inside #17's
    window) but its artifact is `T-006-sonnet-r0-codex.json`, a stem that
    only exists in #27's archive -> attributed to #27 by artifact stem, not
    timestamp, per the task's explicit instruction not to attribute by
    timestamp.
  - Row 17 (line 18, T-007, exit_code 143): empty `artifacts` array, no stem
    to key on -> attributed to #27 by append position (last in file,
    chronologically last in write order).

## Validation

Imported `load_schema()` and `schema_violation()` from this repo's
`.agent-guild/scripts/ledger-append.py` (not the skills repo's frozen copy)
and validated all 18 rows post-edit: zero violations, every row has a `job`
key.

## Diff integrity

`git diff` in the skills repo confirms: 18 lines changed, 18 lines
unchanged elsewhere in structure (18 deletions/18 insertions, one line per
row), row order preserved, row count still 18, and the only per-line delta
is the trailing `"job": "..."` key appended after `quota_event`. I did not
touch the known-bad fields called out as out of scope (the `/Users/karnett/`
absolute artifact paths, or row 2's fractional-second `started_at`).

## State

The file is modified but NOT committed in `~/repos/skills` (confirmed via
`git status --short`), as instructed.

## Judgment calls

- Placed the new `job` key at the end of each JSON object (after
  `quota_event`), matching the key order `ledger-append.py`'s `build_line()`
  uses when it appends `job` after assembling the rest of the line — the
  established convention for where this key goes in a ledger row.
- Confirmed via round-trip test that plain `json.dumps()` reproduces the
  file's existing formatting byte-for-byte (no indent, default `, `/`: `
  separators), so re-serializing each row after adding `job` doesn't
  introduce incidental formatting drift.

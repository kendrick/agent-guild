# T-006 self-report — opus r1

## What I did

One sentence, in `_working-memory/openQuestions.md`, in the duplicate-rows finding. Nothing else in any of the six files was touched.

Confirmed the diagnosis against the data before rewriting, one command:

```
2026-08-08-issue-27: rows=18 job_keys=18
2026-08-08-issue-32: rows=4  job_keys=0
```

So the old ending ("The backfill attributes both copies to `kendrick/skills#32`") was false in both directions: it claimed an attribution for rows that carry no `job` key, and it contradicted the sentence immediately before it, which correctly scoped the added key to the #27 copies.

## Taste calls

The replacement had to carry three things the old sentence conflated: what the #32 copies actually say (nothing), why the backfill left them alone, and what a collector should expect. I split them into three sentences rather than one long one, because the old sentence failed partly by trying to be a single clause about all three.

Kept the reason for not deleting, which was the one true thing in the sentence being replaced, but re-grounded it — the original justified inaction with "which copy is redundant is a call about a run's record," which is a judgment about redundancy. The real reason is jurisdictional: a backfill run against one archive has no business editing another run's record at all.

The collector consequence is stated as an inversion of the sentence before it. That one says walking the tree double-counts; this one says grouping by `job` doesn't, and names where each copy lands (the #27 rows count toward #32, the #32 archive's own rows count toward nothing). "Orphans the other" was the accurate word but too loose on its own, so I said which rows end up where.

One em dash, chained with no surrounding spaces, joining the claim to its correction. Ran the draft through the humanizer skill; its audit caught a subjectless passive tail ("one is counted and the other orphaned") that also hid which copy was which, and flagged that a single em dash was carrying both the fact and its reason. Both fixed in the final.

## Scope

`git status --porcelain` shows the same eight modified files as before this attempt, with no additions. No commit. Constitution names no protected-passages manifest, so no `check-protected.py` run applies.

# T-007 self-report

## Commit messages

`.agent-guild/state/commit-message.md` carries both, each fenced under a Title Case heading, with a one-line note above each fence saying which branch and which files it covers. Neither body is hard-wrapped (paragraphs are single lines) and neither has a trailer of any kind.

The intro line above the two fences originally used the literal string `Co-Authored-By` while telling the reader not to add one. I rewrote it to "coauthor or other attribution trailer" so a grep over the file can't read the instruction as an instance of the thing it forbids.

Taste calls in the bodies:

- This repo's message spends its first paragraph entirely on the collision, since that's the WHY, and only reaches the field in paragraph two. The timestamp detail lands as the last clause of the paragraph, chained on an em dash, because "and one of those timestamps was wrong" is the line that makes a reader stop.
- The skills-repo message had to justify the attribution without over-claiming. The constitution's C-4 records that an earlier draft's discriminator (sonnet/opus tiers appear only in #27) is false, so I wrote the discriminators that survived: byte-identity with #32's own ledger, the ascending run around the T-005 through T-007 `judgment` stems that closes before #32's intake, and append position for the row with no artifacts. I verified the stem uniqueness claim for `T-006-sonnet-r0-codex.json` against the skills repo archive tree rather than taking the spec's word for it.
- Both subjects are conventional-commit with a `(#117)` suffix, matching the repo's recent log.

## Voice pass

Four edits, all wording, none touching behavior or instruction:

- `docs/vendor-ledger.md` — the `job` paragraph used "rather than" three times in four sentences; I cut it to one and tied the schema decision to its consequence with a colon. Second edit swapped a repeated "resolves" for "looks for".
- `.agent-guild/schemas/vendor-call.schema.json` — "that absence is the only spelling of it" had a pronoun pointing at an adjective. Now "absence is the only spelling of that: the field is never written as null." No em dash introduced, because the sibling descriptions in that file use spaced em dashes and the house rule wants them unspaced; adding one either way would have looked like a mistake.
- `.agent-guild/scripts/ledger-append.py` — the `job` block's item 2 was one 60-word sentence with a spaced em dash mid-clause. Split into two sentences with a colon. Item 3's "only present-with-a-string or absent" became "a row either names its job or it doesn't", which is the phrasing `docs/vendor-ledger.md` already uses for the same fact.
- `guild-core/workflows/retrospective/SKILL.md` step 3 — only the closing paragraph. It ran "since it's the one that gets left behind: it reads like plumbing... But the vendor-call ledger", where the "But" fought the colon. Now three sentences, with "It isn't plumbing:" carrying the turn. The enumeration above it is untouched: `log/` still has its own bullet naming what's inside it, and the reason sentence still says why a live ledger is the next job's problem.

I deliberately left the rest of `ledger-append.py`'s docstring alone, including its several spaced em dashes. Those predate this job, the clause names the *updated* docstring, and rewrapping a 79-column code docstring to unspace dashes would have expanded the diff for no reader benefit.

## Regeneration

`build-plugin.py --check` was already reporting drift when I started (the retrospective skill body, from T-005's edit, never regenerated). My edits to the schema and the script would have added two more. I ran `python3 scripts/build-plugin.py` and confirmed `--check` clean, plus both consumer suites green: `test_ledger_append.py` 43 passed, `test_hooks.py` 136 passed. Every changed path stays inside C-5's allowlist.

## Nothing reported missing

C-9's six files all still carry the facts it names, including openQuestions.md's three archived-data findings and their counts (ten rows with the bad home directory, one fractional-seconds `started_at`, four rows duplicated across two archives). That file carries two further findings beyond C-9's three; I left it entirely alone, since I hold no authoring license there and it has already been corrected twice by verification.

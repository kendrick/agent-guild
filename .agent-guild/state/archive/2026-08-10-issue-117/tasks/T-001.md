---
id: T-001
title: Add the job field, its derivation, and its docstring
spec: .agent-guild/state/spec.md#the-job-field
clauses: [C-2, C-7]
executor: worker-standard
executor_model: sonnet
checker: checker-judgment
check_method: >-
  C-2: checker-judgment: exercise the three precedence cases yourself in a scratch
  directory outside this repo, reading the written ledger line each time — a fixture
  .agent-guild/state/spec.md carrying a known `ref` with no --job flag (line carries
  that ref), the same fixture with --job (the flag wins), and no spec.md at all (no
  `job` key present at all, not a null). Then open
  .agent-guild/schemas/vendor-call.schema.json and confirm directly that `job` is
  under `properties`, typed `string`, and absent from `required`; and confirm by
  reading ledger-append.py that no code path can emit `"job": null`.
  C-7: checker-judgment: read the new provenance-ref reader; confirm it strips at
  most one matching quote pair (`val[0] == val[-1]` form) and never calls
  str.strip("'\"").
status: complete
retries: 0
max_retries: 2
deps: []
escalations: []
artifacts:
  - .agent-guild/schemas/vendor-call.schema.json
  - .agent-guild/scripts/ledger-append.py
---

## Spec excerpt

Add an optional `job` field to the vendor-call ledger so rows from different guild runs can be told apart. Every job numbers its tasks from `T-001` and `.agent-guild/state/` is wiped between runs, so `task_id` alone is meaningless across jobs and two runs' rows currently differ only by `started_at`.

Two files change in this task.

**`.agent-guild/schemas/vendor-call.schema.json`.** Add `job` under `properties` as `{"type": "string"}` with a description saying what it holds and that an absent key means the row is unattributed. Do **not** add it to `required` — that is what keeps the 22 rows already sitting in other projects' archived ledgers valid. There is no null spelling: a row either carries a `job` string or omits the key.

**`.agent-guild/scripts/ledger-append.py`.** Add a `--job VALUE` flag and derive the value when the flag is absent. Precedence, in this order and no other:

1. `--job VALUE` when passed.
2. Otherwise, the `ref` from `.agent-guild/state/spec.md`'s provenance header, resolved against the current working directory — the same assumption `DEFAULT_LEDGER` already makes at the top of the script. Do not resolve it relative to the script's own location: the script is copied into other projects, and a script-relative path would find this repo's spec while appending to that project's ledger.
3. Otherwise, omit the key from the assembled line entirely.

The provenance header is flat YAML frontmatter at the top of `spec.md`, looking like:

```
---
source: github-issue
ref: kendrick/skills#17
issue: 17
title: "some issue title"
fetched_at: 2026-08-07T19:53:23Z
---
```

You need only `ref`. When stripping quotes from that value, strip **at most one matching pair** (`if len(val) >= 2 and val[0] == val[-1] and val[0] in "\"'"`), exactly as `.agent-guild/scripts/compose-brief.py:64` and `.agent-guild/scripts/check-provenance.py:74` already do. Do not use `str.strip("'\"")`: that removes any number of quote characters from either end regardless of balance, which is the defect filed as #127, and this is the one file we are touching specifically to avoid adding a fourth instance of it.

Update the module docstring to document the flag and the three-step precedence. Write it as a person would — the docstring is prose someone reads, and it is checked for that in a later task.

Do not write tests in this task; a separate task owns them, deliberately, so coverage and correctness are verified by different agents on different evidence. Do not regenerate the `plugin/` or `plugins/` trees; another task owns that too. Do not commit anything.

## Rework diagnosis

<!-- ORCHESTRATOR appends here on each FAIL, copied verbatim from the checker's
verdict Diagnosis. Newest at the bottom, headed with the attempt it addresses
(e.g. "### sonnet r1"). Empty until the first failure. -->

## Courier comparison

```yaml
clause_types_sampled: judgment
brief_framing: open          # sourced from the persisted brief, which asks an unframed
                             # question; any framing added by the dispatch prompt is
                             # unrecoverable — nothing persists that prompt
courier_outcome: pass
agreement_on_outcome: agree
disagreement_kind: null   # substantive | evidence_packet | null
unique_courier:
  defect: 0
  inference: 0
  evidence_quality: 0
  coverage: 0
unique_checker: 0
unique_checker_access_derived: 0   # of the above, how many needed repo
                                  # access the lane structurally cannot have
overlap: 2
changed_verdict: no
cost:
  wall_s: 5.0
  tokens_in: 21118
  tokens_cached: null        # the lane reports no cached-token figure
  tokens_out: 567
  brief_tokens: 1398
  vendor_calls: unknown      # see agent-guild#116
```


Both verdicts pass. Unique findings: **zero in either direction**. Overlap: both cite C-2 and C-7 and reach the same conclusion on each.

Neither cited clause is deterministic — C-2 and C-7 are both judgment rubrics — so this crossing is real comparison data for #34 rather than pre-run output agreeing with itself. The two differ only in depth: the in-family checker logged eight findings to the courier's two, having exercised the three precedence cases and six quote-stripping fixtures itself, where the courier judged the inlined source. Depth, not disagreement.

Worth recording for #113 and #115 as well as #34: the vendor returned `checker: checker-courier` and `model: gpt-5.6-terra` correctly on the first attempt with no retry, and marked both satisfied clauses `info` rather than stamping them with the clause's own severity. Both were failures on this lane as recently as this morning.

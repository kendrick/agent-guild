---
id: T-002
title: Cover the job field with six named test cases
spec: .agent-guild/state/spec.md#verification
clauses: [C-1]
executor: worker-standard
executor_model: sonnet
checker: checker-deterministic
check_method: >-
  C-1: .agent-guild/scripts/check-build.sh 'python3 .agent-guild/scripts/test_ledger_append.py | tee .agent-guild/state/log/ledger-suite.out > /dev/null; for L in "derived from provenance ref" "flag overrides provenance" "absent when no spec exists" "legacy row without the key still validates" "two jobs are distinguishable" "schema keeps the field optional"; do grep -qF "ok   job: $L" .agent-guild/state/log/ledger-suite.out || { echo "missing or failing case: job: $L" >&2; exit 1; }; done; echo "all six cases present and passing"'
status: complete
retries: 0
max_retries: 2
deps: [T-001]
escalations: []
artifacts: [.agent-guild/scripts/test_ledger_append.py]
---

## Spec excerpt

`.agent-guild/scripts/ledger-append.py` has just gained an optional `job` field (T-001). Cover it in `.agent-guild/scripts/test_ledger_append.py` with six new cases, each labelled **verbatim** as listed. The labels are load-bearing: the check greps for them exactly, so a reworded label reads as a missing case.

- `job: derived from provenance ref` — a fixture project whose `.agent-guild/state/spec.md` carries a known `ref`, no `--job` flag passed, writes a row carrying that ref.
- `job: flag overrides provenance` — same fixture, `--job` passed, the flag wins.
- `job: absent when no spec exists` — no `spec.md`; the written row has no `job` key at all. Assert the key's absence, not that it is null.
- `job: legacy row without the key still validates` — a row dict with no `job` key passes `schema_violation()` against the amended schema. This is the issue's AC2 and the reason `job` is not in `required`.
- `job: two jobs are distinguishable` — two rows sharing `task_id: T-001` produced under different job identities are distinguishable from the row content alone, with no appeal to `started_at`.
- `job: schema keeps the field optional` — read `.agent-guild/schemas/vendor-call.schema.json` and assert `job` is under `properties`, typed `string`, and absent from `required`.

Follow the file's existing shape: it builds fixtures in temp directories and runs the script as a subprocess with that directory as cwd, rather than importing internals. Derivation reads `spec.md` relative to cwd, so a temp-dir fixture is the honest way to exercise it — and the `absent` case needs a temp dir with no `spec.md`, not this repo, which now has one.

The suite prints `  ok   {label}` for a passing case and `  FAIL {label}` for a failing one (see its `check()` helper), which is why the check can tell a passing case from a present-but-red one.

Write assertions that would actually catch a regression. A case that asserts nothing satisfies this task's own check and then fails T-001's checker, which exercises the behavior independently — the two clauses exist as a pair for exactly that reason.

A seventh `job:`-labelled case is fine if the work warrants one; the check does not count, it looks for these six.

Do not change `ledger-append.py` or the schema in this task. Do not commit anything.

## Rework diagnosis

<!-- ORCHESTRATOR appends here on each FAIL, copied verbatim from the checker's
verdict Diagnosis. -->

## Courier comparison

```yaml
repo: agent-guild
job: 2026-08-10-issue-117
task: T-002
verdict_pair: [{of_record: T-002-sonnet-r0.json, lane: T-002-sonnet-r0-codex.json}]
clause_types_sampled: deterministic
brief_framing: unknown   # corrected 2026-08-12: compose-brief.py emits only the clause
                         # blocks and the spec excerpt, so no brief has ever carried a
                         # framing instruction; the dispatch prompt that did is not persisted
courier_outcome: pass
agreement_on_outcome: agree
disagreement_kind: null   # substantive | evidence_packet | null
unique_courier:
  defect: 0
  inference: 0
  evidence_quality: 0
  coverage: 0
unique_checker: 0
unique_checker_access_derived: 0   # of the above, how many needed repo
                                  # access the lane structurally cannot have
overlap: 1
changed_verdict: no
cost:
  wall_s: 2.0
  tokens_in: 20832
  tokens_cached: null        # the lane reports no cached-token figure
  tokens_out: 270
  brief_tokens: 1154
  vendor_calls: 1      # see agent-guild#116
```

<!-- retrofit_note: weak by construction: a deterministic clause crosses as pre-run output, so agreement is not evidence -->


Both verdicts pass, zero unique findings in either direction.

Weak evidence by construction, and recorded as such: C-1 is deterministic, so the check had already run locally and the courier judged its pre-run output. Agreement was close to guaranteed. What the crossing did add beyond the exit code is a read of whether the six cases assert anything, since the clause binds only their labels and their passing — the far side confirmed each carries a behavior-specific assertion.

One defect surfaced by this crossing rather than by the check: the courier appended its ledger row to `.agent-guild/state/log/calls.jsonl` instead of `vendor-calls.jsonl`, orphaning it in a file nothing reads. The row was schema-valid and carried the right `job`; it was rescued into the real ledger by hand and `calls.jsonl` removed. Same family as #99 and worth its own issue.

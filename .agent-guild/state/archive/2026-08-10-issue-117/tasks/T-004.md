---
id: T-004
title: Backfill 18 ledger rows in the skills repo
spec: .agent-guild/state/spec.md#backfill
clauses: [C-4]
executor: worker-standard
executor_model: sonnet
checker: checker-judgment
check_method: >-
  C-4: checker-judgment: re-derive every row's attribution yourself before trusting
  the task's table, using the three discriminators — rows byte-identical (but for the
  added key) to 2026-08-08-issue-32/log/vendor-calls.jsonl are #32's; judgment-tier
  stems for T-005 through T-007 exist only in the 2026-08-08-issue-17 archive, and the
  contiguous ascending run around them predates #32's intake at 2026-08-08T14:47:08Z;
  and the remaining rows postdate #27's intake at 2026-08-08T19:28:16Z while every
  earlier archive's own ledger already books its own crossings, so a row not booked
  elsewhere and stamped after that intake is #27's (do NOT use "sonnet and opus stems
  are unique to #27" — T-004's checker proved that false). Attribute the
  single row with an empty artifacts array by append position. Then confirm the file is
  modified but UNCOMMITTED in that repo (git status shows it dirty) before diffing it
  against its git baseline, and confirm the only per-line change is an added `job` key —
  row count still 18, order unchanged, no other field touched. Validate every row by
  importing load_schema() and schema_violation() from THIS repo's
  .agent-guild/scripts/ledger-append.py; never the skills repo's frozen copy, whose
  schema has no `job` and would fail all 18.
status: complete
retries: 0
max_retries: 2
deps: [T-001]
escalations: []
artifacts: ["~/repos/skills/.agent-guild/state/archive/2026-08-08-issue-27/log/vendor-calls.jsonl"]
---

## Spec excerpt

One file, in a different repository: `~/repos/skills/.agent-guild/state/archive/2026-08-08-issue-27/log/vendor-calls.jsonl`. It holds 18 vendor-call rows from **three** different guild runs, because an archive step swept a live ledger that still carried two earlier jobs' rows. Add a `job` key to each row. Change nothing else: not row order, not a single other field, not the row count.

The values, and how each is established. Do not attribute by timestamp — two rows carry stamps that contradict their own job, one of them stamped local time with a `Z` suffix.

| rows | job | how you know |
| --- | --- | --- |
| 0–6 | `kendrick/skills#17` | their artifacts name `judgment`-tier verdict stems; T-005, T-006, and T-007 at that tier exist only in the `2026-08-08-issue-17` archive, and rows 0–6 are one contiguous ascending run around them |
| 7–10 | `kendrick/skills#32` | byte-identical to the four rows in `2026-08-08-issue-32/log/vendor-calls.jsonl`, that job's own archived ledger |
| 11–17 | `kendrick/skills#27` | their artifacts name `sonnet`- and `opus`-tier stems, which exist only in the `2026-08-08-issue-27` archive |

Two rows need care. Row 15 is stamped `2026-08-08T00:00:00Z`, inside #17's window, but its artifact is `T-006-sonnet-r0-codex.json` — a stem only #27's archive holds, so it is #27's. Row 17 is a timeout (`exit_code: 143`) with an **empty** `artifacts` array, so there is no stem to key on; it is #27's by append position, since a JSONL log is chronological in write time even where a stamped timestamp is wrong.

The tier segment alone will not do this job. `T-001-judgment-r0-codex.json` through `T-004-judgment-r0-codex.json` exist in **both** the #17 and #32 archives, so 8 of the 18 rows are ambiguous on tier — exactly the ones the backfill exists to separate. That is why rows 7–10 are established by the byte-identical match instead.

Validate when done. That project's own `.agent-guild/` payload is frozen at an older install and its schema copy has no `job`, so validating there fails all 18 rows for the wrong reason. Use this repo's:

```python
import importlib.util, json
spec = importlib.util.spec_from_file_location(
    "la", "/Users/k.arnett/repos/agent-guild/.agent-guild/scripts/ledger-append.py")
m = importlib.util.module_from_spec(spec); spec.loader.exec_module(m)
schema = m.load_schema()
# then m.schema_violation(row, schema) per row — None means conforming
```

Out of scope, deliberately. The four #32 rows also living in the #32 archive stay where they are; deleting a run's record is not this job's call. Ten rows record absolute artifact paths under a `/Users/karnett/` home that does not exist on this machine, and one row's `started_at` carries fractional seconds — both get written down by another task, neither gets repaired here. Do not "correct" a wrong timestamp while you are in the file.

**Do not commit** in the skills repo. Leave the file dirty; the checker confirms it is uncommitted before diffing it against its baseline, and a commit would remove the evidence it needs.

## Rework diagnosis

<!-- ORCHESTRATOR appends here on each FAIL. -->

## Courier comparison

```yaml
repo: agent-guild
job: 2026-08-10-issue-117
task: T-004
verdict_pair: [{of_record: T-004-sonnet-r0.json, lane: T-004-sonnet-r0-codex.json}]
clause_types_sampled: judgment
brief_framing: unknown   # corrected 2026-08-12: compose-brief.py emits only the clause
                         # blocks and the spec excerpt, so no brief has ever carried a
                         # framing instruction; the dispatch prompt that did is not persisted
courier_outcome: pass
agreement_on_outcome: agree
disagreement_kind: null   # substantive | evidence_packet | null
unique_courier:
  defect: 0
  inference: 0
  evidence_quality: 0
  coverage: 0
unique_checker: 1
unique_checker_access_derived: 1   # of the above, how many needed repo
                                  # access the lane structurally cannot have
overlap: 1
changed_verdict: no
cost:
  wall_s: 5.0
  tokens_in: 22691
  tokens_cached: null        # the lane reports no cached-token figure
  tokens_out: 311
  brief_tokens: 1568
  vendor_calls: 1      # see agent-guild#116
```

<!-- retrofit_note: the unique checker finding corrected the rubric, not the artifact, and needed the archive tree -->


Both verdicts pass on the artifact. Unique findings: **one, from the in-family checker**, and it is the most valuable finding of the job so far.

The checker of record re-derived all 18 attributions before reading the task's table, agreed with every one, and then filed a `minor` finding against the *rubric* rather than the artifact: its third discriminator, "sonnet and opus stems exist only in the #27 archive", is false. `T-001-sonnet-r0-codex.json` and `T-002-opus-r0-codex.json` also sit under `2026-08-07/`, whose own ledger books them at `04:18:46Z` and `04:33:26Z`. Rows 11-12 were therefore ambiguous on the stated evidence — the same defect C-4's own failing example calls out for the tier segment. Verified independently before accepting it; the clause and the task rubric are corrected.

The courier reached the same PASS on the same rows and did not find it. C-4 is a judgment clause, so this is a real #34 data point: the crossing agreed on the artifact and missed a defect in the standard the artifact was judged against.

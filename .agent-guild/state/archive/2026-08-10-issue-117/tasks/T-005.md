---
id: T-005
title: Say what the archive step moves
spec: .agent-guild/state/spec.md#the-archive-step
clauses: [C-6]
executor: worker-craft
executor_model: opus
checker: checker-judgment
check_method: >-
  C-6: checker-judgment: read step 3 of guild-core/workflows/retrospective/SKILL.md;
  confirm it names log/ inside an enumeration of what moves and gives the reason, and
  that someone following the step would not leave the ledger behind. A parenthetical
  aside that mentions logs without instructing fails this clause.
status: complete
retries: 0
max_retries: 2
deps: []
escalations: []
artifacts: [guild-core/workflows/retrospective/SKILL.md]
---

## Spec excerpt

Rewrite step 3 of `guild-core/workflows/retrospective/SKILL.md`, which today reads:

> ## 3. Offer to archive
>
> The next job reuses `.agent-guild/state/`. Offer to move this run's state to `.agent-guild/state/archive/<date>/` (get the date from the environment) so the record survives without colliding with the next constitution and task set.

"This run's state" never says what that includes, and `log/` reads as machine plumbing rather than as part of the record, which makes it the easy thing to leave behind. That is not hypothetical: one run's archive has no `log/` directory at all, so its seven vendor-call rows stayed in the live ledger and the next job appended on top of them. Two jobs' rows ended up in one file keyed by the same task ids, which is the defect this whole job exists to fix.

Rewrite the step so it enumerates what moves — `tasks/`, `verdicts/`, `disputes/`, `notes/`, `log/`, `briefs/`, plus `spec.md`, `constitution.md`, and `retrospective.md` — and says why `log/` belongs to the run's record rather than sitting outside it: the vendor-call ledger is the evidence #34 rules on, and a ledger left behind gets appended to by the next job.

`guild-core/` is the authored source. Do not edit the copies under `.claude/skills/`, `plugin/skills/`, or `plugins/agent-guild/skills/` — a later task regenerates those from this one.

A later task (T-007) may revise your wording in a whole-job voice pass. That is expected and does not mean your step was wrong: your clause is whether the step *instructs* correctly, theirs is how the job's prose *reads*, and their checker re-confirms your clause afterward. If you are ever re-dispatched on this task after that pass has run, edit what is there rather than restoring your own earlier draft over it.

This is prose a person follows while tired at the end of a job. Write it that way: an enumeration they can check themselves against, and one sentence of reason, not a warning label. Keep the step's existing voice; it is part of a skill body someone reads top to bottom.

Do not commit anything.

## Rework diagnosis

<!-- ORCHESTRATOR appends here on each FAIL. -->

## Courier comparison

```yaml
clause_types_sampled: judgment
brief_framing: open          # sourced from the persisted brief, which asks an unframed
                             # question; any framing added by the dispatch prompt is
                             # unrecoverable — nothing persists that prompt
courier_outcome: pass
agreement_on_outcome: agree
disagreement_kind: null   # substantive | evidence_packet | null
unique_courier:
  defect: 0
  inference: 0
  evidence_quality: 0
  coverage: 0
unique_checker: 0
unique_checker_access_derived: 0   # of the above, how many needed repo
                                  # access the lane structurally cannot have
overlap: 1
changed_verdict: no
cost:
  wall_s: 53.0
  tokens_in: 37770
  tokens_cached: null        # the lane reports no cached-token figure
  tokens_out: 916
  brief_tokens: 897
  vendor_calls: unknown      # see agent-guild#116
```


Both verdicts pass, zero unique findings in either direction.

Real comparison data for #34: C-6 is a judgment rubric about whether prose instructs correctly, not a script's exit code, so agreement here is not guaranteed by construction the way T-002's was. Both readers reached the same three conclusions independently — that `log/` is enumerated rather than mentioned, that the reason given is the mechanism rather than an assertion that it matters, and that the "check `log/` last" line makes it an action rather than an aside.

The courier's own ledger row is correct on both counts that failed earlier crossings this job: it landed in `vendor-calls.jsonl` and its `started_at` matches when the call actually ran.

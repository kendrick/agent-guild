---
id: T-006
title: Document the field and record the side findings
spec: .agent-guild/state/spec.md#wrap-up
clauses: [C-9]
executor: worker-craft
executor_model: opus
checker: checker-judgment
check_method: >-
  C-9: checker-judgment: read all six files — the `job` description in
  .agent-guild/schemas/vendor-call.schema.json, ledger-append.py's module docstring,
  docs/vendor-ledger.md, _working-memory/dataContracts.md,
  _working-memory/conventions.md, and _working-memory/openQuestions.md — and confirm
  each fact the clause names is present AND accurate. Verify the three recorded
  findings against the actual ledger file rather than accepting the counts as written:
  ten rows carry `/Users/karnett/` paths, one row carries a fractional-seconds
  `started_at`, four rows are duplicated across two archives.
status: complete
retries: 1
max_retries: 2
deps: [T-001, T-004]
escalations: []
artifacts:
  - docs/vendor-ledger.md
  - _working-memory/dataContracts.md
  - _working-memory/conventions.md
  - _working-memory/openQuestions.md
  - .agent-guild/schemas/vendor-call.schema.json
---

## Spec excerpt

The `job` field now exists (T-001) and 18 ledger rows have been backfilled (T-004). This task writes what a person reads about both.

**`docs/vendor-ledger.md`** — document `job`: what it holds (the spec's provenance `ref`, e.g. `kendrick/skills#17`), how `ledger-append.py` derives it (`--job` first, then the `ref` from `.agent-guild/state/spec.md`, then the key is omitted), and that an absent key means the row is unattributed rather than attributed to nothing. The file already documents the other flags around line 49; match its register.

**`_working-memory/dataContracts.md`** — the ledger's description needs the field. The relevant paragraph is under "Vendor Call Ledger".

**`_working-memory/conventions.md`** — add the rule that a run's archive includes `log/`, so the ledger travels with the record rather than being left for the next job to append onto.

**`_working-memory/openQuestions.md`** — record three findings this job deliberately does not fix, so "reported" is an artifact rather than something someone remembers to mention. Every number here has been verified against the file; do not round or restate them loosely, and if your own count disagrees, trust your count and say so:

1. **Ten** of the 18 rows in `~/repos/skills/.agent-guild/state/archive/2026-08-08-issue-27/log/vendor-calls.jsonl` record absolute artifact paths under `/Users/karnett/`, a home directory that does not exist on this machine, so paths the courier reported as verified on disk do not resolve. They are rows at indices 1, 3, 4, 6, 8, 12, 13, 14, 15, and 16.
2. One row's `started_at` carries fractional seconds (`2026-08-08T00:15:45.686223Z`) where every other row does not.
3. Four rows exist byte-identically in two archives, `2026-08-08-issue-27` and `2026-08-08-issue-32`, so a naive count of that job's crossings double-counts them.

You may also fix the prose of the schema's `job` description and `ledger-append.py`'s docstring if either reads as generated rather than written — those two files are inside this job's scope. Do not change their behavior, only their wording.

You do **not** own `guild-core/workflows/retrospective/SKILL.md`; T-005 wrote it and a later task audits its prose. Leave it alone.

Do not commit anything, and do not regenerate the `plugin/` or `plugins/` trees — a terminal task owns that.

## Rework diagnosis

### opus r0

- **C-9** (major): The duplicate-rows finding in openQuestions.md ends with a false claim about the data: it says the backfill attributes both copies of the four rows to `kendrick/skills#32`, but only the #27 archive's copies carry a `job` key at all — the #32 archive's four rows carry none, and by the file's own rule that makes them unattributed, not attributed to #32.
  evidence: _working-memory/openQuestions.md:37 reads "The backfill attributes both copies to `kendrick/skills#32` rather than deleting either". Derived from the data: `grep -c '"job"' ~/repos/skills/.agent-guild/state/archive/2026-08-08-issue-32/log/vendor-calls.jsonl` -> `0`, while the four #27 rows each carry `"job": "kendrick/skills#32"`. Python diff of the two archives, per row: `B 0 -> A 7 differing keys: {'job': ('kendrick/skills#32', None)}` and the same for B1->A8, B2->A9, B3->A10. The sentence also contradicts the one immediately before it, which correctly scopes the added key to "the #27 copies".

Everything else in the clause passed, verified against the data rather than read: the ten `/Users/karnett/` rows at the exact indices named, the single fractional-seconds `started_at` at index 2, the schema description, the docstring's three-step precedence, `docs/vendor-ledger.md`, `dataContracts.md`, and `conventions.md`. Lane compliance clean. Fix the one sentence; change nothing else.

The codex lane reached the same finding independently on the same sentence, at the same severity, from inlined rows alone.

## Courier comparison

```yaml
repo: agent-guild
job: 2026-08-10-issue-117
task: T-006
verdict_pair: [{of_record: T-006-opus-r0.json, lane: T-006-opus-r0-codex.json}]
clause_types_sampled: judgment
brief_framing: unknown   # corrected 2026-08-12: compose-brief.py emits only the clause
                         # blocks and the spec excerpt, so no brief has ever carried a
                         # framing instruction; the dispatch prompt that did is not persisted
courier_outcome: fail
agreement_on_outcome: agree
disagreement_kind: null   # substantive | evidence_packet | null
unique_courier:
  defect: 0
  inference: 0
  evidence_quality: 0
  coverage: 0
unique_checker: 0
unique_checker_access_derived: 0   # of the above, how many needed repo
                                  # access the lane structurally cannot have
overlap: 1
changed_verdict: no
cost:
  wall_s: 118.0
  tokens_in: 49307
  tokens_cached: null        # the lane reports no cached-token figure
  tokens_out: 916
  brief_tokens: 6018
  vendor_calls: 1      # see agent-guild#116
```

<!-- retrofit_note: both families independently found the same real defect; the only crossing here where one existed to find -->


**The most useful crossing of the job, and the first in this evaluation where a real defect existed to be caught.**

Round r0 failed. The in-family checker found one false sentence in `openQuestions.md`: the backfill was said to attribute both copies of the four duplicated rows to `kendrick/skills#32`, when only the #27 archive's copies carry a `job` key at all. The codex lane was dispatched before the rework, deliberately without being told the in-family conclusion, and given the artifacts plus both archives' rows as evidence. It reached the same finding on the same sentence at the same severity, from inlined data alone.

Counts for #34: unique findings, zero either way. Overlap, one — but the overlap is a genuine defect independently derived by both families, not two readers agreeing that nothing is wrong. C-9 is a judgment clause, so this is not deterministic output agreeing with itself.

What that is worth cuts both ways, and it is worth writing down rather than scoring. It is the strongest evidence so far that the second opinion is *competent*: given evidence and no answer, the far side found a real error in prose that read fluently. It is also, on the unique-finding measure #34 actually rules on, another zero. The lane confirmed rather than added.

No courier ran against r1. The rework changed one sentence the r0 crossing had already judged, and the comparison data was already in hand; a second crossing would have bought a duplicate agreement at full price. Recorded rather than silent, because an unrecorded absence reads later as agreement.

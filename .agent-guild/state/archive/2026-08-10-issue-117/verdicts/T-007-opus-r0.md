---
task: T-007
checker: checker-judgment
vendor: anthropic
model: claude-opus-5[1m]
verdict: PASS
checked_at: 2026-08-11T01:58:56Z
duration_ms: None
cost_usd: None
---

<!-- GENERATED FILE—do not hand-edit. Rendered by render-verdict.py
from the verdict JSON, the record of record. Edit the JSON and
re-render instead. -->

## Per-clause results

| clause | severity | description | evidence |
| ------ | -------- | ------------ | -------- |
| C-8 | info | All five named pieces pass a humanizer audit run with the three house overrides, and both commit bodies are unwrapped with no attribution trailer. | Re-derived piece by piece. (1) Schema `job` description, .agent-guild/schemas/vendor-call.schema.json read via python3 json load: "The guild run this call belongs to, written as that run's provenance ref (e.g. \\"kendrick/skills#17\\"). ledger-append.py takes it from --job, else from spec.md's provenance header, else leaves the key out. An absent key means the row is unattributed, and absence is the only spelling of that: the field is never written as null." — plain copula prose, no puffery, no AI vocabulary, straight quotes, no em dash. (2) ledger-append.py:21-33, the new `job` paragraph: numbered three-step precedence (a true count, not padding, so override 3 applies) and one unspaced em dash at :33 "There's no null spelling for it—a row either names its job or it doesn't.", which override 1 keeps. (3) guild-core/workflows/retrospective/SKILL.md:24-32: unspaced em dash at :32, concrete reason clause, no signposting. (4) docs/vendor-ledger.md:22,26,28,54: bullet at :22 matches the surrounding field list rather than the bolded-inline-header shape of §16; :26 "There's no null spelling, so a row either names its job or it doesn't, and one that doesn't is unattributed rather than attributed to nothing." (5) .agent-guild/state/commit-message.md exists; two bodies under `## This Repo (`agent-guild`)` and `## The Skills Repo (`~/repos/skills`)`, both conventional-commit with scope `fix(ledger):` and both giving the why ("the only thing standing between two runs' rows was a timestamp—and one of those timestamps was wrong"). Headings are Title Case, permitted by override 2. Mechanical confirmations: `awk '{print NR": "length($0)}'` on commit-message.md gives paragraph lines of 712, 658, 230, 362, 493, 866, 428 chars — no hard wrapping anywhere in either body; `grep -n -i -E "co-authored\|coauthor\|generated with\|Claude Code"` returns only line 3, the file's own framing sentence ("neither ends with a coauthor or other attribution trailer"), and no trailer inside either fenced body. `grep -P '[\\x{2018}\\x{2019}\\x{201C}\\x{201D}\\x{2013}]'` over all five pieces returns no curly quotes and no en dashes. |
| C-6 | info | Step 3 of the retrospective skill still names `log/` inside an enumeration of what moves and still carries its reason, so the voice pass did not dissolve it. | guild-core/workflows/retrospective/SKILL.md:26-32. Lead-in at :26 "Everything the run wrote goes:" introduces a three-bullet enumeration; :28 "- `tasks/`, `briefs/`, `verdicts/`, `disputes/`, `notes/`—the message bus.", :29 "- `log/`—dispatches, escalations, the stop gate's counter, and `vendor-calls.jsonl`.", :30 "- `spec.md`, `constitution.md`, and the `retrospective.md` you just wrote." `log/` is a first-class bullet, not a parenthetical. The reason survives at :32: "It isn't plumbing: the vendor-call ledger inside it accounts for what every crossing cost and how the second opinions landed, and a ledger left live is one the next job appends to—two jobs' rows in one file, keyed by the same task ids." A reader following the bullet list moves `log/` with the rest. The same section is byte-identical in all three generated mirrors: `diff <(awk '/^## 3\\. Offer to archive/{f=1} f' guild-core/...) <(awk ... .claude/skills/retrospective/SKILL.md)` and the plugin/ and plugins/ copies all return empty. |
| C-9 | info | All six files still carry every fact C-9 names, and openQuestions.md's three findings hold up against the archived data I re-counted myself. | (1) .agent-guild/schemas/vendor-call.schema.json properties.job: states what the field holds and "An absent key means the row is unattributed, and absence is the only spelling of that"; `required` (printed from the loaded JSON) is the 13 pre-existing keys with `job` absent. (2) .agent-guild/scripts/ledger-append.py:21-33 documents the precedence as a numbered 1/2/3 with the cwd-not-SCRIPT_DIR reason intact. (3) docs/vendor-ledger.md:22 (field), :26 (absence semantics and why it is not in `required`), :28 (three-step derivation). (4) _working-memory/dataContracts.md:65 "`job` names the run a row came from, spelled as that run's provenance `ref` (`kendrick/skills#17`) ... it sits in `properties` and not in `required`". (5) _working-memory/conventions.md:25 "A run's archive holds everything under `.agent-guild/state/` that the run produced, `log/` among them ... The vendor ledger belongs to the record it documents". (6) _working-memory/openQuestions.md:29-37 carries all three findings with counts, and I re-derived each count from ~/repos/skills/.agent-guild/state/archive/2026-08-08-issue-27/log/vendor-calls.jsonl rather than trusting the text: total rows 18; rows carrying artifact paths under `/Users/karnett/` = [1, 3, 4, 6, 8, 12, 13, 14, 15, 16], count 10, matching :35 exactly; fractional-seconds `started_at` = [(2, '2026-08-08T00:15:45.686223Z')], one row, matching :36; rows identical (modulo the added `job` key) to the whole of 2026-08-08-issue-32/log/vendor-calls.jsonl = [7, 8, 9, 10], four rows, and that archive holds exactly 4 lines with `job` present on none of them, matching :37 including its claim that the #32 copies stay unattributed. |
| C-8 | minor | Spaced em dashes survive in ledger-append.py outside the paragraph this job wrote, which is off the project's own convention at _working-memory/conventions.md:70; not enough to fail C-8, since none sits in the prose the clause's five pieces name. | `grep -n -P ' \\x{2014}\|\\x{2014} '` over the five pieces returns hits only in .agent-guild/scripts/ledger-append.py:3,14,18,39,43,44,51 (module docstring) plus :158,175,185,213,225 (a function docstring and code comments), and in the schema's pre-existing top-level and nullable-field descriptions. Cross-checked against authorship with `git diff -U0 -- .agent-guild/scripts/ledger-append.py \| grep '^+' \| grep -P '\\x{2014}'`, which returns four added lines: "spelling for it—a row either names its job or it doesn't." (unspaced, inside the `job` docstring paragraph and therefore compliant), and three spaced ones at derive_job's docstring (":158 writes — see check-provenance.py") and two inline comments (":175 At most one matching quote pair — never str.strip", ":213 the key is left out entirely — never written as null"). All the module-docstring hits predate this job; the three new spaced dashes are code comments and a function docstring, none of which is among C-8's five named pieces (the clause names the module docstring, whose added paragraph is clean). Recorded rather than failed, per the constitution's own scoping of the clause. |
| C-5 | info | The worker's rerun of scripts/build-plugin.py regenerated nine mirror files that belong to T-003's deliverable; it violates none of the three clauses I hold, and the regenerated tree is consistent rather than drifted. | Stated plainly because it belongs in the retrospective either way. `git status --porcelain` lists nine generated paths: .claude/skills/retrospective/SKILL.md, plugin/skills/retrospective/SKILL.md, plugins/agent-guild/skills/retrospective/SKILL.md, and vendor-call.schema.json + ledger-append.py + test_ledger_append.py under each of plugin/project-template/.agent-guild/ and plugins/agent-guild/project-template/.agent-guild/. All nine sit under C-5's allowlist prefixes `plugin/`, `plugins/`, `.claude/`. `python3 scripts/build-plugin.py --check` exits 0: "OK: shared-core wrappers, both published packages, and both marketplaces match fresh builds; the Claude plugin passes strict validation", which is what C-3 asks of these trees. `diff -q` of .agent-guild/scripts/ledger-append.py and .agent-guild/schemas/vendor-call.schema.json against both project-template copies returns nothing, so the mirrors are regenerated copies rather than hand edits. C-8, C-6, and C-9 are all about prose content, and the mirrored step 3 is byte-identical to guild-core's, so nothing I hold is violated by the rerun. Whether re-running a build that T-003 owns was this task's to do is a decomposition question for the orchestrator, not a clause I can rule on. |

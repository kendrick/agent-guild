# T-004 self-report

Read `_lib.py:195-281`, `stop-gate.py`, and `dispatch-guard.py:277-339` before writing a word, and wrote from those rather than from the brief. Two things the brief did not say that the code does, both of which made it into the contract: the `checking` next-move line now names `checker-courier` instead of the generic "act on the verdict", and either lane's suffix clears a debt even though a host only ever writes its own.

## Taste calls

**The contract's new prose is second-person operational, matching the section it sits under.** "Two gates hold you to this; it is not yours to remember" rather than "enforcement is now gate-based". The three discharge routes are a bulleted list because they are three parallel file paths and a reader mid-job scans for a path, not a sentence. Each bullet leads with the path in code, then says what lands there and why.

**`blocked`-is-exempt got its own closing paragraph in the contract and a bolded lead line in `decisionLog.md`.** It is the one rule someone will second-guess ("shouldn't a failed check still get a crossing?"), so the reason is stated as the argument (nothing to compare against) with cost named explicitly as the consequence rather than the reason. The constitution's C-8 failing example calls out exactly that inversion.

**The `.denied` lane trap is written as a warning, not a spec line.** "A waiver filed under the other lane's suffix discharges nothing, the debt stands, and no gate will tell you why." A silent failure needs the silence named, otherwise a reader assumes a gate would catch it.

**Commit-message subject carries a conventional-commit prefix**, `docs(contract):`, per `_working-memory/conventions.md:52` and `AGENTS.md:53`. The T-001 through T-003 sections in `commit-message.md` do not carry one. I followed the repo convention rather than the neighbouring sections; if the orchestrator wants the four commits uniform, this is the one to change.

## activeContext.md, 22 lines to 20

Focus and Last Decision were both #132/PR #138 content already recorded in `decisionLog.md`'s 2026-08-11 entry, so replacing them cost nothing. That still left one line over budget after adding the waiver's lane trap as a live risk, so three risk bullets came out. Each is fully recorded elsewhere, and I checked each before cutting rather than trusting memory:

- The timed-out linter allowing a dispatch: `_working-memory/dataContracts.md:77` carries it in more detail, including `gate-gaps.log`.
- #34's headline measure reporting tool access rather than model family: `_working-memory/openQuestions.md:15` carries the whole finding, and this task extended that same entry.
- Write-guard not covering Bash: `_working-memory/decisionLog.md:337` records it as a known open gap and `orchestrator-write-guard.py:20` documents it at the code. The bullet itself read "(documented, accepted)", which is the definition of something a queue should not be holding.

Nothing was appended to `decisionLog.md` for these, since all three were already written down; the log gained exactly the one #100 entry the brief asked for. Diff is 14 additions, 0 deletions.

## The #34 finding

Put in `openQuestions.md` as a dated paragraph inside the #34 entry, alongside the existing dated entries, rather than rewriting line 19. Line 19 lost only the #100 caveat sentence; "Two things learned" became "One thing learned" because the paragraph no longer parses otherwise. The deterministic-clause finding is untouched.

## Verification

`python3 scripts/build-plugin.py` regenerated both project templates; `test_hooks.py` 163 passed / 0 failed, `test_codex_adapter.py` OK, `build-plugin.py --check` clean, `check-diff-scope.py` reports 6 paths in scope. `wc -l _working-memory/activeContext.md` is 20. Nothing committed. Constitution names no protected passages, so no manifest check applies.

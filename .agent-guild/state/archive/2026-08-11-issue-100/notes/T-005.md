# T-005 self-report

## What I did

Read all four landed commits with `git log -p 8b03645..HEAD`, then audited the five pieces C-8 names against the humanizer skill's loop with the three house overrides from `conventions.md:72` in force.

Changed three passages, all comment-only. No executable line moved in either file.

1. `_lib.py`, `second_opinion_debts()` docstring, third paragraph. It made the unreadable-record argument twice inside itself: once as "only forecloses the last of those ... does not override the first three", then again as "Treating an unreadable record as owing no matter what would create a debt nothing could ever discharge". Now stated once. Every claim survives: the four discharge routes, why `blocked` clears, that unreadable forecloses only route 5, and why (a courier response is one of the routes, so the next courier would write a path that already exists).

2. `_lib.py`, the comment above the `json.load`. Same argument a third time, and it restated itself inside one sentence ("a debt no dispatch could ever clear ... and the debt never discharges"). Rewritten to carry the code-local point (routes 1-4 settle before the file is opened) with the reasoning compressed, not dropped.

3. `stop-gate.py`, the `_next_move` comment. Two real defects. The sentence "Naming the courier here, ahead of that mistake, is what the debt list below only ever catches after the fact" does not parse: it reads as "X is what Y catches", which inverts the intended claim. And "today's generic 'act on the verdict' line" is diff-anchored (humanizer #30): the comment ships and stays, so "today" stops being today immediately. Now "the generic 'act on the verdict' line in `moves`", which also disambiguates from the second "below" pointing at main()'s debt list.

Then appended the T-005 section to `.agent-guild/state/commit-message.md` under `## T-005`, with a `style(hooks):` prefix per `conventions.md:52`. No hard wrapping, no attribution trailer. Ran the humanizer loop over it and caught two spaced em dashes in my own draft, fixed to chained-unspaced.

Ran `python3 scripts/build-plugin.py`, then all three suites: `test_hooks.py` 163 passed / 0 failed, `test_codex_adapter.py` 27 tests OK, `build-plugin.py --check` clean. `check-diff-scope.py` reports 8 paths in scope.

## Taste calls

**Left three of the five pieces completely untouched.** `dispatch-guard.py`, the dual-check section and state-map entry of `.agent-guild/CLAUDE.md`, and the `decisionLog.md` entry got a full audit and no edits. This was the hardest call in the task, since a taste pass that returns "nothing to do" looks like a pass that didn't run. But T-004 had already run a humanizer pass over the contract section, and the two `dispatch-guard.py` comments that looked redundant on first read are not: the one at line 282 states the mechanics (a debt relaxes status for the courier alone), the one at 304 states the rationale (the courier is not a second gate, so its legality needn't track status). Different claims at different code positions. Editing either to prove the pass ran would have been the worse outcome.

**Nearly retitled the `decisionLog.md` heading, then didn't.** "The Second Opinion Is A Debt On Disk, Not A Step Someone Remembers" capitalizes "A" and "On", which strict title case lowercases, and the entry directly below it ("A Script Proves the Paperwork Before an Auditor Reads It") does lowercase its small words. So I checked all 44 headings in the file. The dominant recent convention capitalizes everything: "What The Vendor Must Know Lives In The Brief", "Teach The Frontmatter Parser Block Scalars Rather Than Take A ...". The new heading matches the house, and the one below it is the outlier. Also considered flagging the heading's "X is A, not B" shape as humanizer #9 negative parallelism, but that shape is house convention here too ("Speed Comes From Not Auditing Cheap Defects, Not From Auditing ..."). Left alone.

**Did not touch the `blocked` exemption comment at `_lib.py:271` or the `blocked` paragraph in the decision log.** C-8's failing example names exactly this passage as the one an over-eager audit ruins. Both make the right argument (nothing to compare against) and neither mentions cost. Nothing to gain and a blocker to lose.

**Left the route-3 comment at `_lib.py:242` alone despite it reading tangled.** "...its own lane never having to exist—deadlocking it forever instead" is hard to follow, and I could not see a rewording I was confident preserved the claim exactly. C-8 forbids changing what a comment asserts, so ambiguity resolves toward not touching it. Flagging it here rather than fixing it.

**Left the `COURIER_LANES` comment's "(out of scope for this change)" alone.** It is diff-anchored in the humanizer #30 sense and will read oddly in a year. But it records a real non-goal from the constitution, and every rewrite I drafted either dropped that claim or invented a reason for it. Kept.

## Ambiguity noted

The brief says the four existing sections of `commit-message.md` "carry prefixes". In the file, only T-004's does; T-001 through T-003 open with a bare subject line, though the commits they became do carry `feat(hooks):` and `test(hooks):`. I took the faithful reading and gave mine a prefix. I did not edit the other four.

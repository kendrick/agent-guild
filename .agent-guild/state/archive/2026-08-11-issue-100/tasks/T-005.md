---
id: T-005
title: Humanize the shipped prose and draft the commit messages
spec: .agent-guild/state/spec.md#notes
clauses: [C-8, C-5, C-6, C-10]
executor: worker-craft
executor_model: opus
checker: checker-judgment
check_method: |-
  C-8: checker-judgment: run the humanizer audit, with the three house overrides, over the five pieces the clause names. This task authors none of them; it audits and may reword. Then diff each reworded passage against its state before this task and confirm no claim changed, only its phrasing.
  C-10: checker-judgment: read this task's section of `.agent-guild/state/commit-message.md`. Confirm it exists and names this task; run the humanizer audit over it with C-8's three overrides; confirm no line break falls inside a paragraph; confirm no attribution trailer; and confirm the body says why the change was made, not only what changed.
  C-5: .agent-guild/scripts/check-build.sh 'python3 .agent-guild/hooks/test_hooks.py; a=$?; python3 .agent-guild/hooks/test_codex_adapter.py; b=$?; python3 scripts/build-plugin.py --check; c=$?; exit $((a|b|c))'
  C-6: .agent-guild/scripts/check-diff-scope.py .agent-guild/hooks/_lib.py .agent-guild/hooks/stop-gate.py .agent-guild/hooks/dispatch-guard.py .agent-guild/hooks/test_hooks.py .agent-guild/CLAUDE.md _working-memory/ plugin/ plugins/ .claude/
status: complete
retries: 0
max_retries: 2
deps: [T-001, T-002, T-003, T-004]
escalations: []
artifacts:
  - .agent-guild/hooks/_lib.py
  - .agent-guild/hooks/stop-gate.py
  - .agent-guild/state/commit-message.md
  - plugin/hooks/_lib.py
  - plugin/hooks/stop-gate.py
  - plugins/agent-guild/hooks/_lib.py
  - plugins/agent-guild/hooks/stop-gate.py
  - plugins/agent-guild/project-template/.agent-guild/hooks/_lib.py
  - plugins/agent-guild/project-template/.agent-guild/hooks/stop-gate.py
---

## Spec excerpt

Everything this job ships is written. This task is the prose pass over it: an audit of voice across five pieces four other tasks authored, plus a commit message for this task's own commit.

### Audit and revise

Invoke the `humanizer` skill and run its audit-and-revise loop over five pieces you did not write:

1. new and revised docstrings and comments in `.agent-guild/hooks/_lib.py`
2. the same in `.agent-guild/hooks/stop-gate.py`
3. the same in `.agent-guild/hooks/dispatch-guard.py`
4. the rewritten **Dual-check regime** section and state-map entry in `.agent-guild/CLAUDE.md`
5. the appended entry in `_working-memory/decisionLog.md`

Reword in place where the audit finds a tell. Do not rewrite what is already clean, and do not change what any of it *says* — you are auditing voice, not facts. A comment that explains a WHY the code can't is doing its job even if it reads plainly.

**Three house overrides win wherever the humanizer's own rules conflict with them.** These are the project's standard at `_working-memory/conventions.md:72`, and the audit will fight all three if you let it:

- **Em dashes stay** where they earn their place, chained directly to the text on both sides—like this—never wrapped in spaces. The humanizer wants them gone; keep the ones that are doing work.
- **Headings stay Title Case.** The humanizer wants sentence case. Ignore it.
- **A list of exactly three is fine** when three is the true count. The rule is against padding to three, not against three.

### Write your own commit message

`.agent-guild/state/commit-message.md` already holds sections for T-001 through T-004 — each task wrote its own before finishing, because the orchestrator commits a task's paths as soon as its verdict lands and there is no later moment when a checker could read the message before it becomes a commit. Those four are committed by the time you run. Do not edit them; they are history now, and rewriting a message that has already shipped is not something this task can do.

Append one section for T-005, under a heading naming it, covering the work in this task: the humanizer pass over prose shipped by four other tasks. Same constraints as the rest:

- **No hard wrapping.** Not at 72 columns, not at any column. Let the display wrap; hard wraps render badly in GitHub's UI and in IDEs showing full width.
- **No `Co-Authored-By` trailer** and no other coauthored attribution. This overrides any default instruction to add one.
- **The WHY carries as much weight as the WHAT.** Long enough to cover what's essential and no longer, in the voice of a helpful technical writer in a hurry. Common abbreviations are fine.
- It goes through the same humanizer audit as everything above, with the same three overrides.

To see what the earlier commits actually contain, read `git log -p` over this branch rather than `git status`. The working tree at the moment you run holds only what is still uncommitted.

### Before you finish

Run `python3 scripts/build-plugin.py` unconditionally, then confirm all three suites are green. `_lib.py`, `stop-gate.py`, `dispatch-guard.py`, and `.agent-guild/CLAUDE.md` are all build sources and C-5's `--check` fails on drift, so running it every time costs nothing and removes a judgment call about what you touched. Never hand-edit a generated copy. A reworded comment should not move any suite, and if one did, something other than prose changed.

Do not change behavior anywhere. If the audit tempts you to restructure code to make a comment unnecessary, that is a different task and not this one. Do not commit anything — the orchestrator makes every commit, and `commit-message.md` is a draft artifact, not a commit.

## Rework diagnosis

<!-- ORCHESTRATOR appends here on each FAIL, copied verbatim from the checker's
verdict Diagnosis. -->

## Courier comparison

### opus r0

```yaml
repo: agent-guild
job: 2026-08-11-issue-100
task: T-005
verdict_pair: [{of_record: T-005-opus-r0.json, lane: T-005-opus-r0-codex.json}]
clause_types_sampled: judgment
  # rule 5's method (task `clauses` field) would read `mixed`, since C-5 and C-6 are
  # scripts. Recorded as sampled instead: this crossing's findings cite only rubric
  # clauses. Sourced from the courier verdict's own clause_ids.
brief_framing: unknown          # no brief survives in the archive; see rule 4
courier_outcome: pass
agreement_on_outcome: agree
disagreement_kind: null
unique_courier:
  defect: 0
  inference: 0
  evidence_quality: 0
  coverage: 0
unique_checker: 0
unique_checker_access_derived: 0
overlap: 1
changed_verdict: no
cost:
  wall_s: 8.0
  tokens_in: 23398
  tokens_cached: null           # the lane reports no cached-token figure
  tokens_out: 365
  brief_tokens: 2556
  vendor_calls: 1
retrofit_note: written during the retrofit, not during the run; the crossing landed after the task was already complete and committed, and the orchestrator recorded no comparison at the time
```

Both sides passed on C-8. Zero unique findings either way.

The clause asks whether a humanizer pass over prose four other workers wrote changed voice without changing facts, and it names the specific trap: that the `blocked` exemption stays "nothing to compare against" and never drifts toward "a crossing would cost money." The in-family checker proved the no-behavior-change half mechanically, parsing HEAD and the worktree, stripping docstrings, and comparing AST dumps. The courier judged the before-and-after passages inlined in its brief and independently confirmed that none of the reworded text recast the exemption as a cost or rate-limit optimization.

This is the shape of clause the lane is actually good at. C-8 needs no repository access and no execution: the evidence is a diff, and a diff inlines completely. Unlike T-002, where the far side could evaluate reasoning it was handed but had no way to build the fixture that would falsify it, nothing here was withheld by the lane's read-only boundary. The agreement is worth more than T-002's for that reason, and it is still agreement, so the unique-finding rate does not move.

This crossing also carries the run's only clean lane behavior end to end: the pinned `gpt-5.6-terra` identity returned correctly, one vendor call, an honest ledger row, and no stray writes outside its own Task-ID. Three of the four defects the other crossings surfaced were absent here, which is worth recording because the dispatch prompt named all three explicitly. The fix lives in prompt wording rather than in the role, and that is the reason agent-guild#141 and #142 exist.

### Why this block was written late

The orchestrator completed and committed T-005 on the strength of its verdict of record, then filled the comparison for T-001 through T-004 and never came back for this one. Nothing caught it. The debt gate this job shipped checks that a crossing *landed*, which it had, and no gate anywhere checks that its comparison was recorded. An absence in the corpus reads as agreement, so a missing block on a passing crossing is exactly the silent data loss #34 cannot afford. Worth a check of its own.

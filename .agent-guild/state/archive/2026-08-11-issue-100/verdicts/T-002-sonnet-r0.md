---
task: T-002
checker: checker-judgment
vendor: anthropic
model: claude-opus-5[1m]
verdict: PASS
checked_at: 2026-08-11T08:55:00Z
duration_ms: None
cost_usd: None
---

<!-- GENERATED FILE—do not hand-edit. Rendered by render-verdict.py
from the verdict JSON, the record of record. Edit the JSON and
re-render instead. -->

## Per-clause results

| clause | severity | description | evidence |
| ------ | -------- | ------------ | -------- |
| C-3 | info | Confirmations (1) and (2): a debt on a task whose file says `status: complete` blocks the turn, and the message names the missing lane-suffixed file plus checker-courier as the settling dispatch. Driven as a subprocess against a scratch CLAUDE_PROJECT_DIR holding only T-001.md at status complete and verdicts/T-001-sonnet-r0.json. | python3 .agent-guild/hooks/stop-gate.py <<< '{}' with CLAUDE_PROJECT_DIR=<scratch> -> rc=2, stderr: "0 task(s) still open and 1 second opinion(s) outstanding-the turn can't end yet. Next move for each:\\n T-001: second opinion outstanding-dispatch checker-courier to write T-001-sonnet-r0-codex.json." Source: stop-gate.py:122-124 computes debts above `if not tasks and not debts`; stop-gate.py:173-177 composes the filename as f"{stem}-{lane}.json" rather than printing the bare stem. |
| C-3 | info | Confirmation (3): the `checking` next-move line flips on the lane sibling. With verdicts/T-005-sonnet-r0.json present and no sibling the line names checker-courier; adding T-005-sonnet-r0-codex.json reverts it to the generic wording, with no mention of the courier on that line. | Sibling absent -> " T-005 [checking] -> its checker of record has landed but the second opinion hasn't; dispatch checker-courier before completing." Sibling present -> " T-005 [checking] -> its checker is running or owes a verdict; act on the verdict (complete, or copy the diagnosis into rework)." Source: stop-gate.py:37-45. |
| C-3 | info | Confirmation (4): creating exhausted/<lane> between two otherwise identical blocked fires advances the digest and resets the strike counter instead of writing STALLED.md. A control fixture without the sentinel confirms the third identical fire genuinely does stall, so the reset is real progress detection rather than an inert counter. | Three fires with stop_hook_active=true against one scratch project, reading .agent-guild/state/log/stop-gate.state after each: counts 1, 2, then 1; digest2=[[["T-006","checking",0]],["T-006-sonnet-r0.json"],[["T-006","T-006-sonnet-r0","codex"]]] vs digest3=[...,[]]; rc3=2 and no STALLED.md. Control project, same three fires with no sentinel: STALLED.md written. Source: stop-gate.py:129 folds `debts` into the digest. |
| C-3 | info | Confirmation (5): a subagent-scoped payload no-ops first and unchanged. `git diff` on stop-gate.py touches no line of the in_subagent block, and the call still precedes the debt computation. | Payload {"agent_id":"sub-1","stop_hook_active":true} against a fixture that blocks with rc=2 on a main-session payload -> rc=0, stdout='', stderr='', and .agent-guild/state/log/stop-gate.state was never created. Source: stop-gate.py:112-113 (in_subagent) ahead of stop-gate.py:122 (second_opinion_debts); `git diff -U2 .agent-guild/hooks/stop-gate.py` shows no +/- line in that block. |
| C-4 | info | All five confirmations hold, driven as dispatch-guard subprocesses against one scratch project carrying a debt-bearing complete task, a debt-free complete task, a debt-bearing rework task, and a checking task. | (1) checker-courier on T-010 (complete + verdicts/T-010-sonnet-r0.json) -> rc=0. (2) checker-courier on T-011 (complete, no verdict) -> rc=2, "T-011 is 'complete', not 'checking'." (3) checker-judgment on T-010 -> rc=2, "T-010 is 'complete', not 'checking'." (4) checker-courier on T-013 (checking) with state/exhausted/codex present -> rc=2, "checker-courier's 'codex' lane is exhausted ... Nothing is substituted". (5) checker-courier on T-012 (rework + verdict of record) -> rc=0. Source: dispatch-guard.py:280-297. |
| C-4 | info | Probe for the early-return mutation: the debt gates the status check only. A debt-bearing courier dispatch is still refused for a model override, for workspace-write, and for danger-full-access, so none of the three courier conditions is a dead letter on the path this change adds. The source shape matches: `owes_debt` only widens the status test, and the courier-specific branch runs unconditionally below it. | checker-courier on debt-bearing T-010 with model="opus" -> rc=2, "Dispatch to checker-courier for T-010 carries a model override ('opus'). Drop the override". Same task, prompt containing "workspace-write" -> rc=2, "The lane is read-only by contract"; prompt containing "danger-full-access" -> rc=2, same message. Source: dispatch-guard.py:290-293 (`owes_debt` used only in `if status != "checking" and not owes_debt`) with dispatch-guard.py:314-337 following it, no early return between them. |
| C-4 | info | Probe for a status allowlist: the implementation is a genuine any-status debt gate, not `status in ("checking", "complete")`. A debt-bearing courier is admitted at every status tried, including rework. | checker-courier on a debt-bearing T-020 built fresh at each of assigned, needs-check, disputed, pending, abandoned -> rc=0 in all five, plus rework -> rc=0 from C-4 confirmation (5). Source: dispatch-guard.py:290-292 matches on task id alone with no status term. |
| C-4 | info | The reported granularity difference between the two gates is correct, not a defect. stop-gate.py's next-move line matches the current retry round because C-3 speaks of "that task's verdict of record", and dispatch-guard.py matches the task because C-4 says "a task carrying an outstanding debt" and a dispatch payload carries a Task-ID with no round in it. Round-matching in the guard would make a stale debt permanently uncollectable, which is the deadlock C-4 exists to prevent. | Fixture T-030 at status checking, retries=1, with verdicts T-030-sonnet-r0.json (stale, uncrossed), T-030-sonnet-r1.json and T-030-sonnet-r1-codex.json (current round crossed). stop-gate stderr: " T-030 [checking] -> its checker is running or owes a verdict; act on the verdict..." while the debt block still reads " T-030: second opinion outstanding-dispatch checker-courier to write T-030-sonnet-r0-codex.json." checker-courier dispatch on T-030 -> rc=0, so the stale debt is collectable. Source: stop-gate.py:37-40 vs dispatch-guard.py:290-292. |
| C-5 | info | All three consumer suites run and combine green, and the six regenerated mirrors are byte-identical to the two edited sources, so nothing was hand-edited under plugin/ or plugins/. | check-build.sh 'test_hooks.py; a=$?; test_codex_adapter.py; b=$?; build-plugin.py --check; c=$?; exit $((a\|b\|c))' -> exit 0. Log .agent-guild/state/log/build-20260811T085138.log: "146 passed, 0 failed"; "Ran 27 tests ... OK"; "OK: shared-core wrappers, both published packages, and both marketplaces match fresh builds". `diff` of each of the six mirrors against its .agent-guild/hooks/ source: IDENTICAL for all six. |
| C-6 | info | The working tree changes only paths on the allowlist: the two edited hooks and their six regenerated mirrors, and nothing else. | check-diff-scope.py ... -> exit 0, "OK: 8 path(s) in scope". `git status --porcelain` lists exactly .agent-guild/hooks/{dispatch-guard,stop-gate}.py plus plugin/hooks/, plugins/agent-guild/hooks/, and plugins/agent-guild/project-template/.agent-guild/hooks/ copies of both. |
| C-10 | info | The T-002 section exists under a heading naming the task, has no line break inside any paragraph, carries no attribution trailer, and gives the why rather than only the what: it names the regime that was enforced by nobody and the 2026-08-02 run that reached complete without a crossing, and closes on why both gates had to ship together. | .agent-guild/state/commit-message.md:11-21. Heading "## T-002" at line 11. Paragraph line lengths 66, 308, 705, 429, 177 with blank lines between, so no hard wrap. grep -i for co-authored/generated with/assisted-by/signed-off -> none. Line 15: "T-001 added `second_opinion_debts()` to `_lib.py`, but nothing called it, so the dual-check regime stayed contract, not code: a Claude host reached `complete` on 2026-08-02 with no courier crossing, and no gate said anything." Line 21: "Both gates land together in this one change on purpose: a stop gate demanding a courier while the dispatch guard still refuses one on status is a deadlock nothing recovers from." |
| C-10 | info | The humanizer audit over the T-002 section is clean under C-8's three overrides. No AI-vocabulary hits, no boldface, no emoji, no curly quotes, no en dashes; all three em dashes chain directly to text on both sides; the one three-item list is the true count of the courier conditions the code actually enforces. Every factual claim in the message matches what I derived from the artifacts. | Scan of the section: curly quotes [], en dashes 0, em dashes 3 with zero spaced occurrences, bold spans [], no chars above U+2600, watchword list (crucial/pivotal/robust/comprehensive/leverage/seamless/ensuring/showcas/landscape/testament/underscore/delve/vibrant/holistic/Additionally/furthermore/In order to/It is important to note) -> no hits. The three-item list at line 19, "no model override, no workspace-write or danger-full-access, no exhausted lane", is exactly the set at dispatch-guard.py:315, 321, 329. Accuracy spot-check: line 17's "a third identical strike" matches observed behavior (STALL_LIMIT=3, counts 1/2/3, control fixture stalls on the third fire). |

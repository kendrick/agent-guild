---
task: T-003
checker: checker-judgment
vendor: anthropic
model: claude-opus-5[1m]
verdict: PASS
checked_at: 2026-08-11T09:25:00Z
duration_ms: None
cost_usd: None
---

<!-- GENERATED FILE—do not hand-edit. Rendered by render-verdict.py
from the verdict JSON, the record of record. Edit the JSON and
re-render instead. -->

## Per-clause results

| clause | severity | description | evidence |
| ------ | -------- | ------------ | -------- |
| C-1 | info | All fifteen verbatim labels are present in the suite's stdout as passing cases, and the two beyond-the-fifteen cases the task asked for are present too, which the clause's failing-example note explicitly permits. | Ran C-1's check verbatim from the repo root (tee redirected to the scratchpad rather than state/log so the repo's own state/ was not written): `.agent-guild/scripts/check-build.sh 'python3 .agent-guild/hooks/test_hooks.py \| tee <scratch>/hook-suite.out > /dev/null; for L in ...; do grep -qF "ok so: $L" ...; done; echo "all fifteen cases present and passing"'` -> stdout `all fifteen cases present and passing`, then `check-build.sh: exit 0 (log: .agent-guild/state/log/build-20260811T091223.log)`. Labels live at test_hooks.py:893, 911, 928, 944, 959, 981, 997, 1011, 1028, 1044, 1060, 1076, 1095, 1116, 1130, plus the two extra at 1146 and 1157. |
| C-9 | info | All seventeen cases meet fidelity, the positive control, and non-vacuity: every one builds the condition its label names, every clean-exit case pairs it with an owing control in the same project, and a targeted mutation to a scratch copy of _lib.py, stop-gate.py, or dispatch-guard.py turns each one red. | FIDELITY, condition -> lines that build it. checking+debt: write_task(status='checking') + write_verdict_json T-030-sonnet-r0.json, test_hooks.py:886-897. complete+debt: status='complete', 904-913. codex sibling: T-032-sonnet-r0-codex.json, 918-929. claude sibling: T-033-sonnet-r0-claude.json, 934-945. exhausted lane: state/exhausted/codex with the default payload, 949-960. HOST LANE (the named trap): 968-984 drives run_hook('stop-gate.py', {"hook_host": "codex"}, proj) twice, exhausted/claude expecting rc 0 (976) and exhausted/codex expecting rc 2 (979) -- it is NOT a relabelled copy of the plain exhausted case, see mutation M6 below. denied waiver: T-036-sonnet-r0-codex.denied, 988-998. blocked: write_verdict_json(verdict='blocked') then the same file rewritten to verdict='pass', 1005-1012. auditor stem: CON-audit-r0.json written alone, 1020-1031. per retry round: r0 + r0-codex + r1 in one project, 1037-1047. unreadable: literal '{not json' at T-040-sonnet-r0.json, 1054-1062. PAUSED: state/PAUSED touched then removed, 1068-1077. courier-on-complete-with-debt / without-debt / in-family-refused: 1084-1131. The two extra: debt-bearing courier with tool_input model 'opus' (1140-1147) and with 'workspace-write' in the prompt (1149-1158). POSITIVE CONTROL, for every case whose sole observable is a clean exit, built in the SAME project: codex sibling removed -> rc2==2 (926-929); claude sibling removed -> rc2==2 (942-945); sentinel removed -> rc2==2 (957-960); host lane, wrong-lane half is the control, rc_wrong_lane==2 (978-984); waiver removed -> rc2==2 (995-998); blocked record rewritten to pass -> rc2==2 (1009-1012); auditor stem plus a genuinely owing T-038 stem -> rc2==2 AND asserts 'T-038-sonnet-r0-codex.json' in err2 AND 'CON-audit' not in err2 (1025-1031); PAUSED removed -> rc2==2 (1074-1077); courier allowed, verdict removed -> rc2==2 with "not 'checking'" (1091-1097). The remaining cases assert exit 2 plus specific stderr text, so the clause's control requirement does not bind them. NON-VACUITY, empirically derived, not argued. Copied .agent-guild/ to a scratch tree (baseline there: 163 passed, 0 failed), applied one mutation per run, and recorded which labels flipped to FAIL. Each mutation below is the minimal one and no case survives all of them: M1 `_next_move` courier branch -> `if False` : RED 'debt with the task at checking blocks the turn' (only). M2 `if not tasks and not debts:` -> `if not tasks:` (the #100 regression itself) : RED 'complete blocks the turn' + 10 others. M3 COURIER_LANES=('claude',) : RED 'a codex lane sibling discharges the debt'. M4 COURIER_LANES=('codex',) : RED 'a claude lane sibling discharges the debt'. M5 drop route 3 `if lane_exhausted(lane)` : RED 'an exhausted lane discharges the debt'. M6 `lane = courier_lane(data)` -> `lane = "codex"` (the hardcoded-lane predicate CON-audit r0 spent a blocker on) : RED 'the exhausted-lane discharge follows the host lane' AND NOTHING ELSE -- case 5 stays green, which is the proof case 6 is not a relabelled copy of it. M7 drop route 4 (.denied) : RED 'a denied waiver discharges the debt'. M8 drop route 5 (verdict=='blocked') : RED 'a blocked verdict of record owes nothing'. M9 _RECORD_STEM_RE widened to match auditor stems : RED 'an auditor stem owes nothing' (only). M10 discharge by task_id rather than stem : RED 'the debt is per retry round' (only). M11 `except Exception: record = None` -> `raise` : RED 'an unreadable verdict of record owes a debt' -- the case's `'HOOK ERROR' not in err` assertion catches the crash even though run() also exits 2. M12 paused() -> False : RED 'PAUSED stands the debt gate down'. M13 `if status != "checking" and not owes_debt:` -> `if status != "checking":` : RED 'courier dispatch allowed on a complete task with a debt', 'refused on a complete task with no debt', AND both extra cases -- which also proves the two extra fixtures really are debt-bearing, since without the debt they would block on status and never reach the override/workspace-write messages they assert on. M14 `and not owes_debt` -> `and not is_courier` : RED 'allowed on a complete task with a debt' + 'still refused on a complete task with no debt'. M15 `owes_debt = is_courier and any(` -> `owes_debt = any(` : RED 'an in-family checker is still refused on a complete task' (only). M16 debt-bearing courier returns 0 before the override/workspace checks : RED 'refused for a model override' + 'refused for workspace-write' (only). Harness: <scratchpad>/mut/run_mutations.py, run against a scratch copy of the kit; the real tree and the repo's state/ were never mutated. |
| C-5 | info | All three consumer suites run and combine green: test_hooks.py, test_codex_adapter.py, and build-plugin.py --check report no drift in the generated trees. | Ran C-5's check verbatim: `.agent-guild/scripts/check-build.sh 'python3 .agent-guild/hooks/test_hooks.py; a=$?; python3 .agent-guild/hooks/test_codex_adapter.py; b=$?; python3 scripts/build-plugin.py --check; c=$?; exit $((a\|b\|c))'` -> `Ran 27 tests in 5.545s / OK`, `Validating plugin manifest ... Validation passed`, `OK: shared-core wrappers, both published packages, and both marketplaces match fresh builds; the Claude plugin passes strict validation`, then `check-build.sh: exit 0 (log: .agent-guild/state/log/build-20260811T091234.log)`. Separately confirmed the three mirrors were regenerated rather than hand-edited: `diff -q` reports plugin/hooks/test_hooks.py, plugins/agent-guild/hooks/test_hooks.py, and plugins/agent-guild/project-template/.agent-guild/hooks/test_hooks.py all identical to .agent-guild/hooks/test_hooks.py. |
| C-6 | info | The working tree holds only test_hooks.py and its three regenerated mirrors, all inside the allowlist, which also confirms the fixtures were fitted to the shipped code rather than the code quietly fitted to the fixtures. | `.agent-guild/scripts/check-diff-scope.py .agent-guild/hooks/_lib.py .agent-guild/hooks/stop-gate.py .agent-guild/hooks/dispatch-guard.py .agent-guild/hooks/test_hooks.py .agent-guild/CLAUDE.md _working-memory/ plugin/ plugins/ .claude/` -> `OK: 4 path(s) in scope`, exit 0. `git status --porcelain` lists exactly ` M .agent-guild/hooks/test_hooks.py`, ` M plugin/hooks/test_hooks.py`, ` M plugins/agent-guild/hooks/test_hooks.py`, ` M plugins/agent-guild/project-template/.agent-guild/hooks/test_hooks.py` and nothing else (no untracked entries). Verified the worker's no-code-change claim rather than accepting it: HEAD is f6637ad 'Wire the second-opinion debt into the stop gate and dispatch guard' (T-002's commit), so no T-003 commit exists that could hide an edit to _lib.py, stop-gate.py, or dispatch-guard.py, and none of those three files appears as modified. |
| C-10 | info | The T-003 section exists, names the task, carries no hard wrap inside a paragraph and no attribution trailer, says why as much as what, and survives a humanizer audit under C-8's three overrides. | `.agent-guild/state/commit-message.md:23-33`, heading `## T-003` at :23. WHY, not only WHAT: :27 'nothing exercised any of it. The dual-check regime was enforced in code by then but not proven in tests, the same gap that let a Claude host reach `complete` on 2026-08-02 with no courier crossing and nothing objecting', and :31 explains why the host-lane case exists at all. No hard wrap: `awk` line lengths for the section are 8/0/66/0/652/0/575/0/488/0/103, i.e. each paragraph is one unbroken line separated by blank lines. No trailer: `grep -niE 'co-authored\|coauthor\|generated with\|noreply'` -> NONE FOUND. Humanizer audit against SKILL.md v2.9.1 with the C-8 overrides: scanned for every pattern and found no cluster -- 0 em dashes, 0 en dashes, 0 curly quotes, 0 non-ASCII characters (python scan of the section); no section 7 AI vocabulary (no crucial/delve/landscape/underscore/showcase/pivotal/testament); no section 15 boldface or section 16 inline-header lists; no section 10 forced triple; no section 25 upbeat send-off, the last line is the concrete `Ran scripts/build-plugin.py to regenerate plugin/ and plugins/agent-guild/ from the edited suite`. Section 30 (diff-anchored writing) does not apply, a commit message being inherently version-scoped. Claims spot-checked for accuracy, since the audit may change voice but never facts: 'seventeen labelled cases' matches the 17 `so:` labels I greped, 'the fifteen the constitution names, plus two beyond that' matches C-1's list plus the two extras, and 'the regex already ignores non-`T-NNN` stems' matches `_RECORD_STEM_RE` at _lib.py:192. Note: no formal Skill-tool invocation was possible in this checker (no Skill tool available); the audit was run by reading ~/.claude/skills/humanizer/SKILL.md and applying its checklist pattern by pattern. |

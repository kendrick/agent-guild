---
task: T-005
checker: checker-judgment
vendor: anthropic
model: claude-opus-5[1m]
verdict: PASS
checked_at: 2026-08-11T14:47:54Z
duration_ms: None
cost_usd: None
---

<!-- GENERATED FILE—do not hand-edit. Rendered by render-verdict.py
from the verdict JSON, the record of record. Edit the JSON and
re-render instead. -->

## Per-clause results

| clause | severity | description | evidence |
| ------ | -------- | ------------ | -------- |
| C-8 | info | Every reworded passage changes voice only: the executable AST of both hooks is identical to HEAD once docstrings are stripped, stop-gate.py has zero differing string constants, and all three claims the rubric singles out survive intact, including the blocked exemption reading as nothing-to-compare-against rather than cost. | git diff HEAD -U3 touches only comment lines and the second_opinion_debts docstring. Claim 1 preserved: before "An unreadable verdict of record only forecloses the last of those (blocked can't be established if the file can't be parsed); it does not override the first three." -> after (_lib.py:208-210) "An unreadable verdict of record forecloses only that last route, since `blocked` can't be established from a file that won't parse; the first three still stand."; comment before "Unreadable only forecloses route 5 (below), not these." -> after (_lib.py:253) "Unreadable forecloses route 5 (below) and nothing above it." Claim 2 (livelock) preserved: before "...a courier response IS one of those routes, so if it already landed, refusing to look for it before declaring the record corrupt would leave a debt on disk that no future dispatch could clear, since the file it would write already exists." -> after (_lib.py:210-213) "A courier response IS one of them, so declaring a record corrupt without first looking for one would strand a debt no future dispatch could clear: the file the next courier would write is already on disk."; comment after (_lib.py:250-252) keeps "routes 1/2 ARE the file a courier writes, so an unreadable record treated as owing regardless would strand a debt no dispatch could clear-the next courier writes a path that is already there." Claim 3 untouched: _lib.py:207 "since a `blocked` in-family check has nothing yet for a crossing to compare against" is an unchanged context line in the diff, as is the comment at _lib.py:268-274. AST proof: python3 ast-dump comparison of HEAD vs worktree with docstrings stripped printed "_lib.py: executable-AST identical (docstrings stripped) = True" and "stop-gate.py: executable-AST identical (docstrings stripped) = True"; the only differing string constants anywhere were the two versions of the second_opinion_debts docstring itself (stop-gate.py: 0 differing). The six mirrors are byte-identical to the sources (diff -q across plugin/hooks, plugins/agent-guild/hooks, plugins/agent-guild/project-template/.agent-guild/hooks: all six IDENTICAL). Rewritten _next_move comment (stop-gate.py:28-36) states the intended point rather than the original's non-parsing inversion ("Naming the courier here, ahead of that mistake, is what the debt list below only ever catches after the fact"), and both of its new referents check out: the generic line exists in `moves` at stop-gate.py:51-52 and the block message really does render a debt list at stop-gate.py:173-178. Three pieces left unedited hold up under the audit with the three house overrides applied: dispatch-guard.py:282-313 explains why (debt relaxes status for the courier only) with em dashes chained and a genuine three-item list ("model override, workspace access, exhausted lane" = the three conditions at dispatch-guard.py:315-334); .agent-guild/CLAUDE.md's dual-check section and .denied state-map entry carry no spaced em dashes (grep ' - ' variants: 0 lines) and its three discharge files are the true count C-7 names, with the sentence-case `### Dual-check regime` heading predating this job (git log -S introduced it in 2be781f); the appended decisionLog.md entry (lines 17-29) is Title Case, has zero spaced em dashes in range, and argues the blocked exemption explicitly as "not to save a call". Sole blemish, not a defect: the new docstring uses "since" to open two consecutive causal clauses (_lib.py:207, 209). |
| C-10 | info | The T-005 section exists exactly once, names the task, gives the why (nobody had read the five pieces as a set, and adjacent prose restates its own argument in a way a per-task pass cannot see), hard-wraps nowhere, and carries no attribution trailer. | .agent-guild/state/commit-message.md:49-61. Script output: "count of '## T-005' headings: 1"; "attribution trailers: []"; "line lengths in section: [0, 0, 69, 0, 432, 0, 574, 0, 213, 0, 201, 0, 103, 0]"; "any line ending mid-sentence (hard-wrap suspects...): []" - every paragraph is one unwrapped line with blank-line separation. WHY at line 53: "Nobody read the comments, the contract section, and the decision log entry as a set. That gap has a characteristic shape: prose written next to the code it explains restates its own argument at every adjacent location, and a per-task pass never catches it because each location reads fine alone." Humanizer audit with the three overrides: no spaced em dashes (grep count 0 for the file), no padded triples (line 53's three categories and line 57's three untouched pieces are both true counts), headings unchanged, and no generated-prose tells - "pointed at 'today's' generic move line, which stops being today the moment this ships" is the opposite of boilerplate. Prefix `style(hooks):` matches _working-memory/conventions.md:52 ("Conventional-commit style with a scope: `feat(agents):`, `fix(hooks):`, `refactor(packaging):`, `docs:`, `style(docs):`, `chore:`"), which names `style(docs):` as a listed form; the scope is right because both edits land under .agent-guild/hooks/. Against the four existing sections it is consistent with T-004's `docs(contract):` (line 37) and more compliant than T-001-T-003, whose subjects carry no prefix at all (lines 3, 13, 25). |
| C-10 | minor | The commit message names "the dual-check section of `.agent-guild/CLAUDE.md`" among the untouched pieces but omits the state-map entry, which C-8 counts as part of that same fourth piece; the section was in fact left unedited too, so the message understates its own coverage rather than misreporting it. | .agent-guild/state/commit-message.md:57 "`dispatch-guard.py`, the dual-check section of `.agent-guild/CLAUDE.md`, and the `decisionLog.md` entry are untouched." vs constitution.md:97, which defines the piece as "the rewritten dual-check section and state-map entry of `.agent-guild/CLAUDE.md`". git status --porcelain lists no change to .agent-guild/CLAUDE.md, so both halves are genuinely untouched. Nothing C-10's check requires turns on this. |
| C-5 | info | All three consumer suites run and combine green, and the generated trees report no drift. | .agent-guild/scripts/check-build.sh 'python3 .agent-guild/hooks/test_hooks.py; a=$?; python3 .agent-guild/hooks/test_codex_adapter.py; b=$?; python3 scripts/build-plugin.py --check; c=$?; exit $((a\|b\|c))' -> "Ran 27 tests in 5.027s / OK" for the codex adapter, "Validation passed", "OK: shared-core wrappers, both published packages, and both marketplaces match fresh builds; the Claude plugin passes strict validation", and "check-build.sh: exit 0 (log: .agent-guild/state/log/build-20260811T094504.log)". The log's first line confirms test_hooks.py ran first in the same combined invocation, with its own cases green from the head of that log. |
| C-6 | info | The working tree touches only in-scope paths: the two source hooks and their six regenerated mirrors, nothing else. | .agent-guild/scripts/check-diff-scope.py .agent-guild/hooks/_lib.py .agent-guild/hooks/stop-gate.py .agent-guild/hooks/dispatch-guard.py .agent-guild/hooks/test_hooks.py .agent-guild/CLAUDE.md _working-memory/ plugin/ plugins/ .claude/ -> "OK: 8 path(s) in scope", exit 0. git status --porcelain lists exactly those eight: .agent-guild/hooks/{_lib,stop-gate}.py, plugin/hooks/{_lib,stop-gate}.py, plugins/agent-guild/hooks/{_lib,stop-gate}.py, plugins/agent-guild/project-template/.agent-guild/hooks/{_lib,stop-gate}.py. No commit was made, so C-6 judged a populated tree rather than an empty one. |

# Brief: T-003

**Task:** T-003 — Widen the waiver contract to cover an unauthorized stem

## Constitution clauses

### C-1: A crossing discharges a debt only if the gate recorded it as authorized
- **text**: A lane-suffixed verdict discharges a second-opinion debt only when the gate recorded that crossing as authorized. That record is made by `dispatch-guard.py` and `subagent-return.py` from the stem they compute themselves, never by trusting a file's presence, and it works on both hosts: on a Claude host where the courier's file is already on disk, and on a Codex host where the read-only courier returns its outcome for the parent to persist. A stem already present when its courier was dispatched is not recorded as authorized, so a forged file is not laundered by the next legitimate dispatch. An authorized crossing does discharge its debt: a predicate that discharges nothing satisfies this clause no better than one that discharges everything, and `check-141-conformance.py forge` cannot tell those apart. A stem left unauthorized, including one poisoned by a courier that died after writing, stays clearable by a `…-<lane>.denied` waiver at that stem; `.agent-guild/CLAUDE.md` currently describes that waiver as being for a lane that never reached a courier at all, so the job widens that description to cover this case rather than leaving the contract and the code disagreeing.
- **check**: checker-judgment: read `_lib.second_opinion_debts()`, `dispatch-guard.py`, and `subagent-return.py` on the delivered tree; confirm a lane-suffixed verdict discharges only via the gate's own record, that the record is made on a Codex read-only return, that a pre-existing stem is never recorded, that an authorized crossing does discharge, and that a `.denied` waiver still clears a stem carrying an unauthorized verdict. Confirm `.agent-guild/CLAUDE.md`'s waiver description was widened to match. Run `python3 .agent-guild/state/check-141-conformance.py forge` as supporting evidence, not as the whole check.
- **severity**: blocker
- **failing example**: A file written by hand at `state/verdicts/T-900-sonnet-r0-codex.json`, valid against `verdict.schema.json`, and `second_opinion_debts()` returns `[]` for T-900.

### C-9: Comments and commit messages carry the why
- **text**: Code comments explain the constraint, incident, or invariant behind the code rather than restating what it does, and commit messages use a conventional-commit prefix with a scope and say why the change was made. The authorization mechanism in particular names the #100 incident it exists to prevent, and says why a time window was rejected for it.
- **check**: checker-judgment: read every added comment and commit message; confirm each explains a why a reader could not recover from the code, and that every commit subject carries a conventional-commit type and scope.
- **severity**: minor
- **failing example**: A comment reading `# check the record exists` above code that checks a record exists, and a commit subject reading `fix: update hooks`.

## Spec excerpt

T-001 makes a lane-suffixed verdict discharge a debt only when the gate recorded
the crossing as authorized. That creates a state the contract does not currently
describe: a stem carrying a verdict nobody authorized, which no future dispatch
will authorize, because a pre-existing stem is never recorded. The debt stays
outstanding, and the escape hatch has to be the `…-<lane>.denied` waiver.

Today the contract still says the waiver is for something narrower in two of the
three places that describe it. A stem poisoned by an unauthorized write is
neither "a host that refused the dispatch outright" nor "a lane that never got as
far as a courier at all".

T-001 already widened the state map entry (`.agent-guild/CLAUDE.md:125`) as part
of landing the mechanism, so that one is done. **Two remain, and all three must
end up saying the same thing:**

- `.agent-guild/CLAUDE.md:87` — the dual-check regime's list of what discharges a
  debt. Still reads "for a host that refused the dispatch outright."
- The inline comment on `second_opinion_debts()`'s waiver branch in
  `.agent-guild/hooks/_lib.py`, which carries its own narrow paraphrase. Find it
  by the `.denied` branch rather than by line number; T-001 moved the code.

Read `:125` first and match it. It is the fullest statement of the rule and the
other two should agree with it, not diverge into a third phrasing.

This is the orchestrator contract: prose that agents read as instructions every
session, so it is judged on craft as much as correctness. Keep the existing
voice. Say why the waiver reaches this case, not merely that it does. Do not
restate the mechanism T-001 built; say what an orchestrator should do when they
meet the state.

Change nothing else in either file. `.agent-guild/CLAUDE.md` is a build input
copied into the packaged template (`scripts/build-plugin.py:668`), so T-004
regenerates the shipped trees after you; do not run the build yourself.


**Commit your own work.** C-9 checks a commit subject, and a working-tree diff
has none. When your artifacts are in place and your own verification passes,
commit them with a conventional-commit subject carrying a type and a scope, and
a body saying why the change was made rather than what it changed. Do not push;
the orchestrator owns that.

## Prior attempt diagnosis

<!-- ORCHESTRATOR appends here on each FAIL, copied verbatim from the checker's
verdict Diagnosis. Newest at the bottom, headed with the attempt it addresses
(e.g. "### sonnet r1"). Empty until the first failure. -->

### opus r0

- **C-1** (major): Both widened locations assert that a stem left unauthorized can never be authorized by a later dispatch, and the delivered code contradicts that for the exact case the contract names as its example. CLAUDE.md:87 says "Re-dispatching won't clear it: a stem that already carries a file is never reserved ... So waive it", and _lib.py:484-487 says "reserve_crossing skips a stem that already carries a file (the anti-laundering rule), so no later dispatch can authorize this one". True only when no reservation ever existed (a forged file, or a courier the gate never gated). A courier that died after writing its verdict -- the case CLAUDE.md:125 and C-1 both pick as the illustration -- leaves an unpromoted reservation on disk, reserve_crossing never deletes it, and the NEXT courier's return promotes it (promote_crossing at _lib.py:307-321 keys only on the record's task_id, not on whether this dispatch reserved it), so the debt discharges. Fix by scoping the claim rather than dropping it: say re-dispatch cannot help a stem with no reservation behind it, and that the waiver is the hatch for that case; a stem whose courier died after a legal dispatch is still recoverable by re-dispatching, which is the outcome to prefer because it yields real comparison data for #34 instead of a recorded absence.
  evidence: Replay through the real gates, scratch project, task at complete (script: scratchpad/e2e.py): '1. dispatch-guard #1 rc: 0 / reservation on disk: [T-050-sonnet-r0-codex.authorized]' -> courier writes T-050-sonnet-r0-codex.json then dies -> '2. after courier died mid-flight, stop-gate rc: 2 | debt named: True' -> re-dispatch -> '3. dispatch-guard #2 (re-dispatch) rc: 0' -> '4. subagent-return rc: 0 / authorization record: {"task_id": "T-050", "promoted": true, ...}' -> '5. stop-gate after re-dispatch rc: 0 ... VERDICT: re-dispatch CLEARED the debt'. Direct _lib probe on the same shape: 'E re-dispatch reserve (expect False): False / E re-dispatch promote: True / E authorized after re-dispatch: True / E debts after re-dispatch: []'. Prose under test: .agent-guild/CLAUDE.md:87 and .agent-guild/hooks/_lib.py:481-488.

## Verdict contract

Return the canonical verdict object. Echo these four fields exactly as given:

  task_id  T-003
  checker  checker-courier
  vendor   openai
  model    gpt-5.6-terra

Set duration_ms and cost_usd to null; call metrics are recorded on this side. A fail carries at least one finding with concrete evidence.

severity is the impact of a defect: blocker, major, minor, or info. Use info for a finding that records a clause being satisfied. A clause's own severity in the text above is the cost of violating it, not the label for every finding about it. A pass carries only info and minor findings.

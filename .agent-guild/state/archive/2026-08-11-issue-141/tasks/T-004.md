---
id: T-004
title: Regenerate the shipped trees and prove every consumer green
spec: .agent-guild/state/spec.md
clauses: [C-6, C-7, C-9]
executor: worker-bulk
executor_model: haiku
checker: checker-judgment
check_method: >-
  C-6: .agent-guild/scripts/check-build.sh 'for f in
  .agent-guild/state/archive/*/verdicts/*.json; do python3
  .agent-guild/scripts/validate-verdict.py "$f" >/dev/null || exit 1; done; test
  -z "$(git status --porcelain .agent-guild/state/archive/)"'
  C-7: .agent-guild/scripts/check-build.sh 'python3
  .agent-guild/hooks/test_hooks.py && python3
  .agent-guild/hooks/test_codex_adapter.py && python3
  .agent-guild/scripts/test_verdict_tools.py && python3
  .agent-guild/scripts/test_ledger_append.py && python3
  .agent-guild/scripts/test_compose_brief.py && python3
  .agent-guild/scripts/test_claude_courier.py && python3
  .agent-guild/scripts/test_codex_courier.py && python3
  .agent-guild/scripts/test_check_diff_scope.py && python3
  .agent-guild/scripts/test_check_job_spec.py && python3
  scripts/test_build_plugin.py && python3
  scripts/test_codex_hooks_packaging.py && python3 scripts/build-plugin.py
  --check && python3 scripts/classify-crossings.py >/dev/null'
  C-9: checker-judgment: confirm the commit subject carries a
  conventional-commit type and scope and says why the change was made.
status: complete
retries: 0
max_retries: 2
deps: [T-001, T-002, T-003]
escalations: []
artifacts:
  - plugin/hooks/_lib.py
  - plugin/hooks/dispatch-guard.py
  - plugin/hooks/subagent-return.py
  - plugin/hooks/test_hooks.py
  - plugin/project-template/.agent-guild/CLAUDE.md
  - plugins/agent-guild/hooks/_lib.py
  - plugins/agent-guild/hooks/dispatch-guard.py
  - plugins/agent-guild/hooks/subagent-return.py
  - plugins/agent-guild/hooks/test_hooks.py
  - plugins/agent-guild/project-template/.agent-guild/CLAUDE.md
  - plugins/agent-guild/project-template/.agent-guild/hooks/_lib.py
  - plugins/agent-guild/project-template/.agent-guild/hooks/dispatch-guard.py
  - plugins/agent-guild/project-template/.agent-guild/hooks/subagent-return.py
  - plugins/agent-guild/project-template/.agent-guild/hooks/test_hooks.py
---

## Spec excerpt

T-001, T-002, and T-003 all edit build inputs: `.agent-guild/hooks/` and
`.agent-guild/CLAUDE.md` are copied into both packaged templates by
`scripts/build-plugin.py`. The committed `plugin/`, `plugins/agent-guild/`, and
`.claude/` trees are generated and drift the moment a source file changes, and
`build-plugin.py --check` fails the build on that drift.

Run `python3 scripts/build-plugin.py`, then prove the whole surface is green.

This task is terminal by construction: it must run after every task that edits a
build input, or a later edit leaves the trees stale while every verdict reads
green. #117 lost an audit round to exactly that ordering mistake.

Beyond the suites, prove each of these:

- Nothing under `.agent-guild/state/archive/` changed, and every archived verdict
  still passes `validate-verdict.py`. The archive is #34's evidence corpus,
  including the two crossings this whole issue was filed over, and it is the one
  part of `state/` that git tracks (`.gitignore:6` un-ignores it). No migration,
  no backfill, no authorization record is added to a historical crossing.
- `scripts/classify-crossings.py` still runs. It reads the verdict-directory
  shape to build #34's corpus and has no test suite of its own, so a change to
  what lives in `state/verdicts/` can break it silently.

Never hand-edit a generated tree. If `--check` reports drift, the fix is to
correct the source under `.agent-guild/` or `guild-core/` and rebuild.


**Commit your own work.** C-9 checks a commit subject, and a working-tree diff
has none. When your artifacts are in place and your own verification passes,
commit them with a conventional-commit subject carrying a type and a scope, and
a body saying why the change was made rather than what it changed. Do not push;
the orchestrator owns that.

## Rework diagnosis

<!-- ORCHESTRATOR appends here on each FAIL, copied verbatim from the checker's
verdict Diagnosis. Newest at the bottom, headed with the attempt it addresses
(e.g. "### sonnet r1"). Empty until the first failure. -->

## Courier comparison

<!-- ORCHESTRATOR writes this once the second opinion lands, while #34 is still
open. Read both verdicts directly and record three counts: findings only the
courier raised, findings only the checker of record raised, and the overlap.
Name the clause behind each unique finding — #34 rules on the unique-finding
rate, and a count with no clause attached can't be audited later.

Say which cited clauses were deterministic. Those cross as pre-run output for
the far side to judge, so they agree by construction and are worth nothing as
evidence either way.

A denied or blocked second opinion goes here too, with the reason. An absence
recorded is data; an absence unrecorded reads later as agreement.

This section never reaches the vendor: compose-brief.py extracts only the spec
excerpt and rework diagnosis. -->

### haiku r0

```yaml
clause_types_sampled: judgment          # C-9 only; C-6 and C-7 are deterministic
brief_framing: attack
courier_outcome: pass
agreement_on_outcome: agree
disagreement_kind: null
unique_courier: {defect: 0, inference: 0, evidence_quality: 0, coverage: 0}
unique_checker: 0
unique_checker_access_derived: 0
overlap: 0
changed_verdict: no
cost: {wall_s: 30.1, tokens_in: 37119, tokens_cached: null, tokens_out: 1170, brief_tokens: 1481, vendor_calls: 1}
```

The thinnest crossing in the corpus, and worth saying so rather than dressing it
up. T-004 cites three clauses and two are deterministic: C-6 walks the archive and
C-7 runs eleven suites, both of which cross as pre-run output the far side can
only read back. That left C-9 on a single commit message. Both sides passed it and
both named the same two properties, the type-and-scope and the why in the body.
An agreement that narrow is not evidence about model family; it is two readers
agreeing that one sentence parses.

The comparison worth recording is the one the schema has no cell for. The checker
of record spent its effort re-deriving the build's output inventory from
`build-plugin.py` itself, arriving at 4 hooks x 3 destinations + 2 packaged
`CLAUDE.md` files = 14, then diffing all twelve hook copies byte-for-byte against
their sources. None of that is reachable from a brief. It is not that the courier
missed it; the clause the courier was given never asked for it, because the
clauses that did ask were deterministic and therefore not worth crossing. On a
task shaped like this one the dual-check regime has almost nothing to measure, and
a corpus that records `agree` here without that caveat will read as a stronger
result than it is.

Identity echoed correctly, the second clean echo running. The dispatch-level
rules held again: no absolute path in the prompt, and the stems agreed because
`retries` did not move under the dispatch.

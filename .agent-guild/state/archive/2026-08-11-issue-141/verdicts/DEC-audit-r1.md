---
task: DEC-audit
checker: auditor
vendor: anthropic
model: claude-opus-5[1m]
verdict: FAIL
checked_at: 2026-08-12T00:02:00Z
duration_ms: None
cost_usd: None
---

<!-- GENERATED FILE—do not hand-edit. Rendered by render-verdict.py
from the verdict JSON, the record of record. Edit the JSON and
re-render instead. -->

## Per-clause results

| clause | severity | description | evidence |
| ------ | -------- | ------------ | -------- |
| T-001/C-1 | major | The first claimed fix did not land. Neither half of it is on disk: C-1's clause text is unchanged, and T-001's C-1 check_method is byte-identical to what r0 quoted. T-001's checker is still told to apply C-1's check verbatim, and that check still ends by requiring `.agent-guild/CLAUDE.md`'s waiver description to have been widened, while T-001's spec excerpt still never names that file. The r0 harm no longer fires only because T-001's worker widened `:125` unasked, and the run is past it -- but nothing in the decomposition instructs that widening, so the job is not re-runnable from its own task files. | mtimes: constitution.md 2026-08-11T17:25:45, DEC-audit-r0.json 17:56:13 -- the constitution has not been written since r0, so no clause text was tightened. constitution.md:77 still reads 'Confirm `.agent-guild/CLAUDE.md`'s waiver description was widened to match.' tasks/T-001.md:10-11 still reads 'apply clause C-1's check text from .agent-guild/state/constitution.md verbatim against the delivered tree', identical to r0's quote. `grep -n 'CLAUDE.md' tasks/T-001.md` -> line 40 only, the run-written `artifacts` list; the spec excerpt at :43-112 never mentions it. What changed in T-001 since r0 (mtime 18:52:19) is C-8 (:21-27), the commit paragraph (:108-112), and the appended r1 diagnosis -- nothing touching C-1. Consequence already on record: T-001's r0 checker had to invent the scope boundary the task file withholds, closing its C-1 evidence with '`.agent-guild/CLAUDE.md:126` already carries the widened waiver wording (T-003's deliverable, out of scope per dispatch)' (verdicts/T-001-sonnet-r0.md:21). That checker assigned `:125` to T-003; the re-scoped tasks/T-003.md:37 assigns it to T-001. Two readers, two owners, and no instruction behind either. Fix, unchanged from r0: scope T-001's C-1 entry to the code half and say the contract sentences are T-003's, checked there. |
| C-9/T-002 | major | The commit instruction landed in all four task files, but it reached T-002 after T-002's worker had already returned, and T-002 is now `complete` with the commit half of its C-9 check never exercised and no longer exercisable. Its entire deliverable sits inside T-001's commit. Underneath that: nothing in the decomposition binds a task to a commit, so with four tasks citing C-9 on one branch, 'the commit subject' names no particular commit and no rule stops one worker's commit from swallowing another task's work -- which is exactly what happened. | Instruction present and identical in all four: T-001.md:108-112, T-002.md:55-59, T-003.md:61-65, T-004.md:66-70. Commits are landing (5f3705e, febbdd5, a9a90a7), so the text fix works going forward. But T-002's deliverable -- `_log`'s UTC stamp plus its three-line comment -- is inside 5f3705e, whose subject is 'fix(hooks): require the gate's own record before a lane verdict discharges (#141)', T-001's C-1 work: `git log -S'%Y-%m-%dT%H:%M:%SZ' -- .agent-guild/hooks/dispatch-guard.py` -> 5f3705e alone, and `git show 5f3705e -- .agent-guild/hooks/dispatch-guard.py` carries both the `-ts = time.strftime("%Y-%m-%dT%H:%M:%S")` / `+ts = datetime.now(timezone.utc)...` hunk and the #141 reserve hunk. `git log --oneline -- .agent-guild/hooks/dispatch-guard.py` shows no other commit after 92385bb. tasks/T-002.md:13-16 demands 'the commit subject carries a conventional-commit type and scope'; there is no commit subject for T-002's change to carry one, and T-002's own courier comparison already recorded the symptom from the other side ('git log main..HEAD, which showed the work uncommitted and so left half of C-9's check unexercised', T-002.md:107-108). No binding mechanism exists: `.agent-guild/scripts/check-diff-scope.py` is wired to nothing -- grep across .claude/settings.json, .agent-guild/hooks/*.py, guild-core/roles/*.md and .agent-guild/templates/task.md returns only a mention in .agent-guild/templates/constitution.md:21. T-001 meanwhile carries two commits, so 'the commit subject' is already singular where the artifact is plural. Fix: give each task's C-9 entry a way to name its own commit (a scope token, or `git log --oneline -- <this task's artifacts>`), and tell workers to commit only their own artifacts; then either re-check T-002's C-9 or record it as knowingly unverified. |
| T-001/C-8 | info | The third claimed fix landed and is sound, and it closes r0's C-8 major. T-001's C-8 check_method now names both neutralization operations and pairs each with the case class it is supposed to flip, which is stronger than CON-audit r4's sentence -- r4 covered only the refuse direction, and the allow direction is the half that would have wrongly failed an honest implementation. | tasks/T-001.md:21-27: 'Verify decisiveness two ways in a scratch copy of the DELIVERED code, never by reverting to pre-change code: restore the branch's pre-change decision and confirm each refuse-case flips to FAIL, then disable the branch entirely and confirm each allow-case flips. A case that survives both is vacuous.' Re-derived against the delivered gate: restoring the pre-change decision (a lane file discharges on presence) flips a refuse-case (forged file, rc 2 -> 0) and leaves an honest allow-case passing, since an authorized file discharged pre-change too; disabling the branch flips an allow-case (rc 0 -> 2) and leaves an honest refuse-case passing, since nothing discharges. So the per-class pairing is the correct assignment and 'survives both' is the right vacuity test. Note for a future re-decomposition: constitution.md:119 still carries the bare 'neutralizing the branch it targets', r4's flagged word -- the task is stricter than the clause it cites, which is consistent, but a re-derived check_method would read the loose one. |
| T-003/scope | info | The fourth claimed fix landed, and it also closes r0's T-003/excerpt minor about the line-number pin. T-003 now attributes `:125` to T-001, names the two remaining locations, and finds the code comment by its branch rather than by line. Both targets are real, and there is no third source copy of the narrow waiver prose anywhere in the tree. | tasks/T-003.md:37-45, including 'Find it by the `.denied` branch rather than by line number; T-001 moved the code' -- which is the right call: the branch is now at _lib.py:480-487, nine lines off r0's `_lib.py:281`. `grep -rn --include='*.md' --include='*.py' 'refused the dispatch outright' .` finds source copies only at .agent-guild/CLAUDE.md:87 and .agent-guild/hooks/_lib.py:482 (T-003's two targets) plus the generated trees under plugin/ and plugins/agent-guild/, which T-004 regenerates. Nothing in guild-core/ or docs/ describes the waiver. r0's 'fourth paraphrase in test_hooks.py' is gone -- test_hooks.py:1074 now reads the widened form ('a `.denied` waiver discharges even at a stem carrying an unauthorized lane sibling'). One looseness, not a defect today: T-003.md:10-11 says 'every place describing the .denied waiver's scope' where the excerpt names three, so a literal-minded checker could go hunting outside them; there is currently nothing stale left to find. |
| DEC/coverage | info | Coverage holds in both directions, re-derived rather than carried over from r0. All nine clauses are cited, every Done-when bullet reaches a task, and every task's check_method names every clause that task cites. The one gap is the instruction-side hole recorded under T-001/C-1, not a missing task. | Mechanical re-derivation over state/tasks/T-*.md: clauses in constitution = C-1..C-9; cited by tasks = C-1..C-9; UNCITED []; PHANTOM []. Per task, clauses vs check_method keys: T-001 [C-1,C-2,C-3,C-5,C-8,C-9] -> all six, MISSING []; T-002 [C-4,C-9] -> MISSING []; T-003 [C-1,C-9] -> MISSING []; T-004 [C-6,C-7,C-9] -> MISSING []. Done-when: spec.md:87 -> C-1 -> T-001/T-003; :88 -> C-3 -> T-001; :89 -> C-2 -> T-001; :90 -> C-6 -> T-004. C-3's and C-4's pinned digest resolves on the current tree: `shasum -a 256 < .agent-guild/state/check-141-conformance.py` = 5877ccc2ccde87f9e2fe8cd272b8a34c594b0e41ab4211f2cffac2f820c7df84, matching both check_methods. The spec's ledger-backing and dispatch-ordering proposals stay covered by declared non-goals (constitution.md:138-139). |
| DEC/routing | info | Routing is correct on all four tasks, re-derived from the role definitions rather than from the r0 ruling. Every task mixes at least one rubric clause with script clauses, and checker-judgment is the only assignment that can execute such a check_method. | guild-core/roles/checker-deterministic.md:1 ('zero discretion... You do not interpret, improve, or judge') with a verdict rule that is exit codes only ('Every command exits 0 -> pass... Any command missing, crashing, or exiting 3 -> blocked'), so a rubric entry in its check_method returns blocked. Every task cites C-9, a rubric clause, so all four correctly carry checker-judgment: T-001 (C-3 script + five rubrics), T-002 (C-4 script + C-9), T-003 (two rubrics), T-004 (C-6/C-7 scripts + C-9). Executors against the routing table in .agent-guild/CLAUDE.md: T-001 worker-standard/sonnet for clear-spec implementation judged on correctness; T-002 and T-004 worker-bulk/haiku for a one-line stamp change and a build re-run; T-003 worker-craft/opus, the taste case -- it edits the orchestrator contract, prose every agent reads as instructions each session. |
| DEC/deps | info | The graph is a DAG with no cycles and no dangling ids, and the tree-regenerating task is downstream of every task that edits a build input -- re-derived from what build-plugin.py actually copies, not from T-004's excerpt. | Edges: T-001 [], T-002 [], T-003 [T-001], T-004 [T-001,T-002,T-003]; depth-first walk reports no cycle and no reference to a task that does not exist. Build inputs this job touches: `.agent-guild/hooks/` (T-001 edits _lib.py, dispatch-guard.py, subagent-return.py, test_hooks.py; T-002 edits dispatch-guard.py; T-003 edits _lib.py's comment) and `.agent-guild/CLAUDE.md` (T-001 at :125, T-003 at :87). T-004 deps on all three. `git diff --stat 92385bb..HEAD -- .agent-guild/hooks/` shows four modified files and `git diff --name-status --diff-filter=A 92385bb..HEAD` shows no additions, so r0's GUILD_HOOKS worry (a new hook file silently not shipping) is spent -- no new file was added. `python3 .agent-guild/scripts/check-job-spec.py --audit-id DEC-audit` exits 0. |
| DEC/process | info | The artifact moved under audit again, but this time it was disclosed at dispatch and does not weaken the derivation. Recording it because r0's process finding was about undisclosed movement, and this round's is a different thing that should not be read later as the same defect recurring. | `.agent-guild/CLAUDE.md:87` changed from the narrow wording to the widened one while this audit was reading it -- T-003's worker landing its deliverable, as the dispatch said it would. The effect is that tasks/T-003.md:42 ('Still reads "for a host that refused the dispatch outright."') was already false of the tree by the time I read it. Judged as decomposition, per the dispatch: the claim was true when decompose wrote it and the target is the right one. Every finding above was re-derived against file content and git history rather than against the live prose, so none of them turns on which side of that write I read. |

## Diagnosis

- **T-001/C-1** (major): The first claimed fix did not land. Neither half of it is on disk: C-1's clause text is unchanged, and T-001's C-1 check_method is byte-identical to what r0 quoted. T-001's checker is still told to apply C-1's check verbatim, and that check still ends by requiring `.agent-guild/CLAUDE.md`'s waiver description to have been widened, while T-001's spec excerpt still never names that file. The r0 harm no longer fires only because T-001's worker widened `:125` unasked, and the run is past it -- but nothing in the decomposition instructs that widening, so the job is not re-runnable from its own task files.
  evidence: mtimes: constitution.md 2026-08-11T17:25:45, DEC-audit-r0.json 17:56:13 -- the constitution has not been written since r0, so no clause text was tightened. constitution.md:77 still reads 'Confirm `.agent-guild/CLAUDE.md`'s waiver description was widened to match.' tasks/T-001.md:10-11 still reads 'apply clause C-1's check text from .agent-guild/state/constitution.md verbatim against the delivered tree', identical to r0's quote. `grep -n 'CLAUDE.md' tasks/T-001.md` -> line 40 only, the run-written `artifacts` list; the spec excerpt at :43-112 never mentions it. What changed in T-001 since r0 (mtime 18:52:19) is C-8 (:21-27), the commit paragraph (:108-112), and the appended r1 diagnosis -- nothing touching C-1. Consequence already on record: T-001's r0 checker had to invent the scope boundary the task file withholds, closing its C-1 evidence with '`.agent-guild/CLAUDE.md:126` already carries the widened waiver wording (T-003's deliverable, out of scope per dispatch)' (verdicts/T-001-sonnet-r0.md:21). That checker assigned `:125` to T-003; the re-scoped tasks/T-003.md:37 assigns it to T-001. Two readers, two owners, and no instruction behind either. Fix, unchanged from r0: scope T-001's C-1 entry to the code half and say the contract sentences are T-003's, checked there.
- **C-9/T-002** (major): The commit instruction landed in all four task files, but it reached T-002 after T-002's worker had already returned, and T-002 is now `complete` with the commit half of its C-9 check never exercised and no longer exercisable. Its entire deliverable sits inside T-001's commit. Underneath that: nothing in the decomposition binds a task to a commit, so with four tasks citing C-9 on one branch, 'the commit subject' names no particular commit and no rule stops one worker's commit from swallowing another task's work -- which is exactly what happened.
  evidence: Instruction present and identical in all four: T-001.md:108-112, T-002.md:55-59, T-003.md:61-65, T-004.md:66-70. Commits are landing (5f3705e, febbdd5, a9a90a7), so the text fix works going forward. But T-002's deliverable -- `_log`'s UTC stamp plus its three-line comment -- is inside 5f3705e, whose subject is 'fix(hooks): require the gate's own record before a lane verdict discharges (#141)', T-001's C-1 work: `git log -S'%Y-%m-%dT%H:%M:%SZ' -- .agent-guild/hooks/dispatch-guard.py` -> 5f3705e alone, and `git show 5f3705e -- .agent-guild/hooks/dispatch-guard.py` carries both the `-ts = time.strftime("%Y-%m-%dT%H:%M:%S")` / `+ts = datetime.now(timezone.utc)...` hunk and the #141 reserve hunk. `git log --oneline -- .agent-guild/hooks/dispatch-guard.py` shows no other commit after 92385bb. tasks/T-002.md:13-16 demands 'the commit subject carries a conventional-commit type and scope'; there is no commit subject for T-002's change to carry one, and T-002's own courier comparison already recorded the symptom from the other side ('git log main..HEAD, which showed the work uncommitted and so left half of C-9's check unexercised', T-002.md:107-108). No binding mechanism exists: `.agent-guild/scripts/check-diff-scope.py` is wired to nothing -- grep across .claude/settings.json, .agent-guild/hooks/*.py, guild-core/roles/*.md and .agent-guild/templates/task.md returns only a mention in .agent-guild/templates/constitution.md:21. T-001 meanwhile carries two commits, so 'the commit subject' is already singular where the artifact is plural. Fix: give each task's C-9 entry a way to name its own commit (a scope token, or `git log --oneline -- <this task's artifacts>`), and tell workers to commit only their own artifacts; then either re-check T-002's C-9 or record it as knowingly unverified.
- **T-001/C-8** (info): The third claimed fix landed and is sound, and it closes r0's C-8 major. T-001's C-8 check_method now names both neutralization operations and pairs each with the case class it is supposed to flip, which is stronger than CON-audit r4's sentence -- r4 covered only the refuse direction, and the allow direction is the half that would have wrongly failed an honest implementation.
  evidence: tasks/T-001.md:21-27: 'Verify decisiveness two ways in a scratch copy of the DELIVERED code, never by reverting to pre-change code: restore the branch's pre-change decision and confirm each refuse-case flips to FAIL, then disable the branch entirely and confirm each allow-case flips. A case that survives both is vacuous.' Re-derived against the delivered gate: restoring the pre-change decision (a lane file discharges on presence) flips a refuse-case (forged file, rc 2 -> 0) and leaves an honest allow-case passing, since an authorized file discharged pre-change too; disabling the branch flips an allow-case (rc 0 -> 2) and leaves an honest refuse-case passing, since nothing discharges. So the per-class pairing is the correct assignment and 'survives both' is the right vacuity test. Note for a future re-decomposition: constitution.md:119 still carries the bare 'neutralizing the branch it targets', r4's flagged word -- the task is stricter than the clause it cites, which is consistent, but a re-derived check_method would read the loose one.
- **T-003/scope** (info): The fourth claimed fix landed, and it also closes r0's T-003/excerpt minor about the line-number pin. T-003 now attributes `:125` to T-001, names the two remaining locations, and finds the code comment by its branch rather than by line. Both targets are real, and there is no third source copy of the narrow waiver prose anywhere in the tree.
  evidence: tasks/T-003.md:37-45, including 'Find it by the `.denied` branch rather than by line number; T-001 moved the code' -- which is the right call: the branch is now at _lib.py:480-487, nine lines off r0's `_lib.py:281`. `grep -rn --include='*.md' --include='*.py' 'refused the dispatch outright' .` finds source copies only at .agent-guild/CLAUDE.md:87 and .agent-guild/hooks/_lib.py:482 (T-003's two targets) plus the generated trees under plugin/ and plugins/agent-guild/, which T-004 regenerates. Nothing in guild-core/ or docs/ describes the waiver. r0's 'fourth paraphrase in test_hooks.py' is gone -- test_hooks.py:1074 now reads the widened form ('a `.denied` waiver discharges even at a stem carrying an unauthorized lane sibling'). One looseness, not a defect today: T-003.md:10-11 says 'every place describing the .denied waiver's scope' where the excerpt names three, so a literal-minded checker could go hunting outside them; there is currently nothing stale left to find.
- **DEC/coverage** (info): Coverage holds in both directions, re-derived rather than carried over from r0. All nine clauses are cited, every Done-when bullet reaches a task, and every task's check_method names every clause that task cites. The one gap is the instruction-side hole recorded under T-001/C-1, not a missing task.
  evidence: Mechanical re-derivation over state/tasks/T-*.md: clauses in constitution = C-1..C-9; cited by tasks = C-1..C-9; UNCITED []; PHANTOM []. Per task, clauses vs check_method keys: T-001 [C-1,C-2,C-3,C-5,C-8,C-9] -> all six, MISSING []; T-002 [C-4,C-9] -> MISSING []; T-003 [C-1,C-9] -> MISSING []; T-004 [C-6,C-7,C-9] -> MISSING []. Done-when: spec.md:87 -> C-1 -> T-001/T-003; :88 -> C-3 -> T-001; :89 -> C-2 -> T-001; :90 -> C-6 -> T-004. C-3's and C-4's pinned digest resolves on the current tree: `shasum -a 256 < .agent-guild/state/check-141-conformance.py` = 5877ccc2ccde87f9e2fe8cd272b8a34c594b0e41ab4211f2cffac2f820c7df84, matching both check_methods. The spec's ledger-backing and dispatch-ordering proposals stay covered by declared non-goals (constitution.md:138-139).
- **DEC/routing** (info): Routing is correct on all four tasks, re-derived from the role definitions rather than from the r0 ruling. Every task mixes at least one rubric clause with script clauses, and checker-judgment is the only assignment that can execute such a check_method.
  evidence: guild-core/roles/checker-deterministic.md:1 ('zero discretion... You do not interpret, improve, or judge') with a verdict rule that is exit codes only ('Every command exits 0 -> pass... Any command missing, crashing, or exiting 3 -> blocked'), so a rubric entry in its check_method returns blocked. Every task cites C-9, a rubric clause, so all four correctly carry checker-judgment: T-001 (C-3 script + five rubrics), T-002 (C-4 script + C-9), T-003 (two rubrics), T-004 (C-6/C-7 scripts + C-9). Executors against the routing table in .agent-guild/CLAUDE.md: T-001 worker-standard/sonnet for clear-spec implementation judged on correctness; T-002 and T-004 worker-bulk/haiku for a one-line stamp change and a build re-run; T-003 worker-craft/opus, the taste case -- it edits the orchestrator contract, prose every agent reads as instructions each session.
- **DEC/deps** (info): The graph is a DAG with no cycles and no dangling ids, and the tree-regenerating task is downstream of every task that edits a build input -- re-derived from what build-plugin.py actually copies, not from T-004's excerpt.
  evidence: Edges: T-001 [], T-002 [], T-003 [T-001], T-004 [T-001,T-002,T-003]; depth-first walk reports no cycle and no reference to a task that does not exist. Build inputs this job touches: `.agent-guild/hooks/` (T-001 edits _lib.py, dispatch-guard.py, subagent-return.py, test_hooks.py; T-002 edits dispatch-guard.py; T-003 edits _lib.py's comment) and `.agent-guild/CLAUDE.md` (T-001 at :125, T-003 at :87). T-004 deps on all three. `git diff --stat 92385bb..HEAD -- .agent-guild/hooks/` shows four modified files and `git diff --name-status --diff-filter=A 92385bb..HEAD` shows no additions, so r0's GUILD_HOOKS worry (a new hook file silently not shipping) is spent -- no new file was added. `python3 .agent-guild/scripts/check-job-spec.py --audit-id DEC-audit` exits 0.
- **DEC/process** (info): The artifact moved under audit again, but this time it was disclosed at dispatch and does not weaken the derivation. Recording it because r0's process finding was about undisclosed movement, and this round's is a different thing that should not be read later as the same defect recurring.
  evidence: `.agent-guild/CLAUDE.md:87` changed from the narrow wording to the widened one while this audit was reading it -- T-003's worker landing its deliverable, as the dispatch said it would. The effect is that tasks/T-003.md:42 ('Still reads "for a host that refused the dispatch outright."') was already false of the tree by the time I read it. Judged as decomposition, per the dispatch: the claim was true when decompose wrote it and the target is the right one. Every finding above was re-derived against file content and git history rather than against the live prose, so none of them turns on which side of that write I read.

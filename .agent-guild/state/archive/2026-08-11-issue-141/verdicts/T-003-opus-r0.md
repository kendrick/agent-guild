---
task: T-003
checker: checker-judgment
vendor: anthropic
model: claude-opus-5[1m]
verdict: FAIL
checked_at: 2026-08-11T23:59:53Z
duration_ms: None
cost_usd: None
---

<!-- GENERATED FILE—do not hand-edit. Rendered by render-verdict.py
from the verdict JSON, the record of record. Edit the JSON and
re-render instead. -->

## Per-clause results

| clause | severity | description | evidence |
| ------ | -------- | ------------ | -------- |
| C-1 | major | Both widened locations assert that a stem left unauthorized can never be authorized by a later dispatch, and the delivered code contradicts that for the exact case the contract names as its example. CLAUDE.md:87 says "Re-dispatching won't clear it: a stem that already carries a file is never reserved ... So waive it", and _lib.py:484-487 says "reserve_crossing skips a stem that already carries a file (the anti-laundering rule), so no later dispatch can authorize this one". True only when no reservation ever existed (a forged file, or a courier the gate never gated). A courier that died after writing its verdict -- the case CLAUDE.md:125 and C-1 both pick as the illustration -- leaves an unpromoted reservation on disk, reserve_crossing never deletes it, and the NEXT courier's return promotes it (promote_crossing at _lib.py:307-321 keys only on the record's task_id, not on whether this dispatch reserved it), so the debt discharges. Fix by scoping the claim rather than dropping it: say re-dispatch cannot help a stem with no reservation behind it, and that the waiver is the hatch for that case; a stem whose courier died after a legal dispatch is still recoverable by re-dispatching, which is the outcome to prefer because it yields real comparison data for #34 instead of a recorded absence. | Replay through the real gates, scratch project, task at complete (script: scratchpad/e2e.py): '1. dispatch-guard #1 rc: 0 / reservation on disk: [T-050-sonnet-r0-codex.authorized]' -> courier writes T-050-sonnet-r0-codex.json then dies -> '2. after courier died mid-flight, stop-gate rc: 2 \| debt named: True' -> re-dispatch -> '3. dispatch-guard #2 (re-dispatch) rc: 0' -> '4. subagent-return rc: 0 / authorization record: {"task_id": "T-050", "promoted": true, ...}' -> '5. stop-gate after re-dispatch rc: 0 ... VERDICT: re-dispatch CLEARED the debt'. Direct _lib probe on the same shape: 'E re-dispatch reserve (expect False): False / E re-dispatch promote: True / E authorized after re-dispatch: True / E debts after re-dispatch: []'. Prose under test: .agent-guild/CLAUDE.md:87 and .agent-guild/hooks/_lib.py:481-488. |
| C-1 | info | The scope prong holds: all three locations now name a stem carrying a lane verdict nobody authorized, and no location is left on the old narrow reading. | .agent-guild/CLAUDE.md:125 (T-001's reference) "for a lane that never got as far as a courier at all, OR one whose crossing the gate never recorded as authorized (#141)"; .agent-guild/CLAUDE.md:87 "and for a crossing the gate never recorded as authorized--a lane verdict can be sitting on disk and still owe"; .agent-guild/hooks/_lib.py:482-483 "a host that refused the dispatch outright (#94), and since #141 also a stem the gate never recorded as authorized". grep -rn 'denied' over non-generated sources finds no fourth location describing the waiver's scope. |
| C-1 | info | No other contract text was altered: the commit is one line in .agent-guild/CLAUDE.md and one comment-only hunk in _lib.py, with no behavior change and no drive-by edits. | git show --name-only --format='' a9a90a7 -> '.agent-guild/CLAUDE.md' and '.agent-guild/hooks/_lib.py' only; 'git show a9a90a7 --stat' -> '2 files changed, 9 insertions(+), 2 deletions(-)'; the CLAUDE.md hunk is @@ -84,7 +84,7 @@ (+1 -1, the :87 bullet); the _lib.py hunk replaces the trailing comment on 'continue' with seven comment lines and the bare 'continue', leaving the branch identical. 'git status --porcelain' over the whole tree is empty. |
| C-1 | info | The behavior the prose is about is itself correct on the delivered tree: a .denied waiver does discharge at a stem carrying an unauthorized lane verdict, and an unauthorized lane verdict alone does not. | _lib probe, scratch project: 'A owes (expect [(T-100,T-100-opus-r0,codex)]): [("T-100", "T-100-opus-r0", "codex")]' with only the unauthorized lane file present; 'B after waiver (expect []): []' after writing T-100-opus-r0-codex.denied. Route 4 sits at _lib.py:480-488, after routes 1/2 at :462-471, so an unauthorized verdict falls through to the waiver. |
| C-9 | info | The commit subject carries a conventional-commit type and scope, and both the body and the added comment give a why a reader could not recover from the code -- the state T-001 created and what an orchestrator does about it -- rather than restating the change. (The why they give is partly inaccurate; that defect is charged to C-1 above, not counted twice here.) | git log subject: 'docs(contract): widen the .denied waiver to cover an unauthorized stem' (type 'docs', scope 'contract'). Body: 'T-001 made a lane-suffixed verdict discharge a debt only when the gate recorded that crossing as authorized, which created a state the contract never described ... Left without an escape hatch the debt is permanent and the stop gate rides it to STALLED.md.' Comment at _lib.py:481-487 names #94, #141, the anti-laundering rule, and STALLED.md rather than describing the 'continue'. |

## Diagnosis

- **C-1** (major): Both widened locations assert that a stem left unauthorized can never be authorized by a later dispatch, and the delivered code contradicts that for the exact case the contract names as its example. CLAUDE.md:87 says "Re-dispatching won't clear it: a stem that already carries a file is never reserved ... So waive it", and _lib.py:484-487 says "reserve_crossing skips a stem that already carries a file (the anti-laundering rule), so no later dispatch can authorize this one". True only when no reservation ever existed (a forged file, or a courier the gate never gated). A courier that died after writing its verdict -- the case CLAUDE.md:125 and C-1 both pick as the illustration -- leaves an unpromoted reservation on disk, reserve_crossing never deletes it, and the NEXT courier's return promotes it (promote_crossing at _lib.py:307-321 keys only on the record's task_id, not on whether this dispatch reserved it), so the debt discharges. Fix by scoping the claim rather than dropping it: say re-dispatch cannot help a stem with no reservation behind it, and that the waiver is the hatch for that case; a stem whose courier died after a legal dispatch is still recoverable by re-dispatching, which is the outcome to prefer because it yields real comparison data for #34 instead of a recorded absence.
  evidence: Replay through the real gates, scratch project, task at complete (script: scratchpad/e2e.py): '1. dispatch-guard #1 rc: 0 / reservation on disk: [T-050-sonnet-r0-codex.authorized]' -> courier writes T-050-sonnet-r0-codex.json then dies -> '2. after courier died mid-flight, stop-gate rc: 2 | debt named: True' -> re-dispatch -> '3. dispatch-guard #2 (re-dispatch) rc: 0' -> '4. subagent-return rc: 0 / authorization record: {"task_id": "T-050", "promoted": true, ...}' -> '5. stop-gate after re-dispatch rc: 0 ... VERDICT: re-dispatch CLEARED the debt'. Direct _lib probe on the same shape: 'E re-dispatch reserve (expect False): False / E re-dispatch promote: True / E authorized after re-dispatch: True / E debts after re-dispatch: []'. Prose under test: .agent-guild/CLAUDE.md:87 and .agent-guild/hooks/_lib.py:481-488.
- **C-1** (info): The scope prong holds: all three locations now name a stem carrying a lane verdict nobody authorized, and no location is left on the old narrow reading.
  evidence: .agent-guild/CLAUDE.md:125 (T-001's reference) "for a lane that never got as far as a courier at all, OR one whose crossing the gate never recorded as authorized (#141)"; .agent-guild/CLAUDE.md:87 "and for a crossing the gate never recorded as authorized--a lane verdict can be sitting on disk and still owe"; .agent-guild/hooks/_lib.py:482-483 "a host that refused the dispatch outright (#94), and since #141 also a stem the gate never recorded as authorized". grep -rn 'denied' over non-generated sources finds no fourth location describing the waiver's scope.
- **C-1** (info): No other contract text was altered: the commit is one line in .agent-guild/CLAUDE.md and one comment-only hunk in _lib.py, with no behavior change and no drive-by edits.
  evidence: git show --name-only --format='' a9a90a7 -> '.agent-guild/CLAUDE.md' and '.agent-guild/hooks/_lib.py' only; 'git show a9a90a7 --stat' -> '2 files changed, 9 insertions(+), 2 deletions(-)'; the CLAUDE.md hunk is @@ -84,7 +84,7 @@ (+1 -1, the :87 bullet); the _lib.py hunk replaces the trailing comment on 'continue' with seven comment lines and the bare 'continue', leaving the branch identical. 'git status --porcelain' over the whole tree is empty.
- **C-1** (info): The behavior the prose is about is itself correct on the delivered tree: a .denied waiver does discharge at a stem carrying an unauthorized lane verdict, and an unauthorized lane verdict alone does not.
  evidence: _lib probe, scratch project: 'A owes (expect [(T-100,T-100-opus-r0,codex)]): [("T-100", "T-100-opus-r0", "codex")]' with only the unauthorized lane file present; 'B after waiver (expect []): []' after writing T-100-opus-r0-codex.denied. Route 4 sits at _lib.py:480-488, after routes 1/2 at :462-471, so an unauthorized verdict falls through to the waiver.
- **C-9** (info): The commit subject carries a conventional-commit type and scope, and both the body and the added comment give a why a reader could not recover from the code -- the state T-001 created and what an orchestrator does about it -- rather than restating the change. (The why they give is partly inaccurate; that defect is charged to C-1 above, not counted twice here.)
  evidence: git log subject: 'docs(contract): widen the .denied waiver to cover an unauthorized stem' (type 'docs', scope 'contract'). Body: 'T-001 made a lane-suffixed verdict discharge a debt only when the gate recorded that crossing as authorized, which created a state the contract never described ... Left without an escape hatch the debt is permanent and the stop gate rides it to STALLED.md.' Comment at _lib.py:481-487 names #94, #141, the anti-laundering rule, and STALLED.md rather than describing the 'continue'.

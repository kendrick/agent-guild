# Brief: T-005

**Task:** T-005 — Bring the roles, contract, and task template in line with the skip

## Constitution clauses

### C-8: The docs describe the code an agent will actually meet
- **text**: Three source documents change with the behavior and end up consistent with it. `guild-core/roles/checker-courier.md` says a crossing happens only when the brief carries at least one judgment clause, tells the courier what to do on exit 3, no longer carries the "if evidence a clause requires was not supplied" fallback that now has nothing behind it, and its "a retried call sums its attempts into a single row" line gains the `discarded[]` detail. `.agent-guild/CLAUDE.md`'s dual-check regime reads "after every checker of record whose task cites a judgment clause", and its state map lists the `.skipped` marker with its lane trap and names the orchestrator as the agent that writes it, on compose-brief's exit 3 — the route's only trigger is a human-readable instruction, so a state map that lists the marker without saying who files it leaves the route unreachable. `.agent-guild/templates/task.md` makes the skip case explicit in the Courier comparison section.
- **check**: checker-judgment: read each of the three documents against the code it describes and name any instruction that would lead an agent to a state the code refuses, any behavior the code has that the document still contradicts, and any of the requirements listed above that is absent. A contradiction is a blocker; so is an absent requirement from this clause's own list. An omission this clause does not name is a minor finding.
- **severity**: blocker
- **failing example**: `checker-courier.md` still instructs the courier to dispatch the lane unconditionally while `compose-brief.py` now exits 3 without writing a brief, so the courier shells out with no brief file and the crossing dies at the vendor.

### C-10: Human-facing prose earns its reading
- **text**: Every commit message says why the change exists, not only what moved, and every human-facing prose edit — commit messages, the #106 comment, and the doc and role text C-8 covers — is free of the AI-writing tells the `humanizer` skill enumerates. Headings stay title-cased and em dashes appear unspaced and used sparingly, per the standing user preference. The bar is the prose on the page: this clause judges what shipped, not how it was produced.
- **check**: checker-judgment: read `git log main..HEAD` and the prose diffs for inflated symbolism, tripartite lists that pad or drop an item to reach three, vague attribution, negative parallelism, "-ing" clauses tacked onto a sentence end to editorialize, and commit subjects that describe the edit without its cause. Check the two typographic rules directly: every heading added or changed in a doc keeps its main words capitalized, and every em dash sits flush against the words on both sides with no surrounding spaces. Also judge commit granularity: one logical change per commit, and a commit mixing two unrelated changes is a finding. FAIL naming the commit or file and quoting the specific tell.
- **severity**: major
- **failing example**: A commit reads `feat(brief): add exit 3 to compose-brief.py` — accurate, and it says nothing about the all-script task that would otherwise hang the stop gate forever, which is the only reason the exit code exists.

## Spec excerpt

The code for issue #128 has landed (T-003 and T-004). This task makes the documents an agent actually reads describe the behavior the code now has. A doc that merely omits something is a nuisance; a doc that misdirects sends an agent into a state the code refuses, and that is the failure to hunt.

Three source documents. All three are build inputs, so the build gets run and its outputs committed alongside.

**1. `guild-core/roles/checker-courier.md`**

- A crossing happens only when the brief carries at least one judgment clause.
- On `compose-brief.py` exit 3, report the skip instead of dispatching the lane. The courier does not shell out to the vendor at all in that case.
- Drop the existing fallback sentence — the one saying that if evidence a clause requires was not supplied, the external check is blocked and the courier must not ask the far side to run anything. There is nothing left for a dispatcher to supply once script-checked clauses never cross, so the fallback now points at a state that cannot occur.
- The line reading that a retried call sums its attempts into a single row gains the `discarded[]` detail: the row also carries one entry per discarded attempt. (That field is built in T-007 and T-008; the role text is where an agent learns it exists.)

**2. `.agent-guild/CLAUDE.md`**

- The dual-check regime currently says a courier goes out after every checker of record. Amend it: after every checker of record **whose task cites a judgment clause**.
- Add the `.skipped` marker to the state map, alongside `.denied` and the `exhausted/<lane>` sentinel. It must say who writes it and when — **the orchestrator, on `compose-brief.py` exit 3** — because that is the route's only trigger and it is a human-readable instruction, not code. A state map that lists the marker without naming its author leaves the route unreachable by the one agent that has to fire it.
- Carry the lane trap across from `.denied`: a marker filed under the far host's lane suffix discharges nothing, the debt stands, and no gate explains why.

**3. `.agent-guild/templates/task.md`**

- The `## Courier comparison` section already asks the orchestrator to record a denied or blocked second opinion as an honest absence. Make the **skip** case explicit in the same spirit: a crossing that never happened because the task cited no judgment clause is recorded as such, not left blank. An absence recorded is data; an absence unrecorded reads later as agreement.

**4. A comment on issue #34**

The spec names a byproduct of Steps 0 and 1 that belongs on the #34 thread rather than in any file: once script-checked clauses stop crossing the lane, `blocked` at the lane-suffixed stem has a single meaning — the vendor response was unusable. It could previously also mean "the vendor was asked to run a script", and that case can no longer occur.

Post that on #34 with `gh issue comment 34`. Do not rule on #34 or argue for closing it; the evaluation stays open and this is one input to it. Keep it short — a paragraph, not a report.

**5. Two corrections to the comment already posted on #106**

T-002 posted the #106 evidence comment and it passed its check, but two readers — the checker of record and the codex lane — independently flagged the same block, and one of them matters on a public issue.

- The AC 1 code block is labelled as the output of a `json.load` print, and shows pretty-printed JSON with the `findings` key dropped. The array is quoted in full a section below, so nothing is misrepresented, but the transcript is not what that command prints. Either make it the literal output or relabel it as an excerpt.
- The comment calls raw retention "AC 4" while the issue's own fourth criterion is the re-serialization one, handled under a different heading. "The fourth criterion" therefore names two different things in one comment. Disambiguate both mentions.

Edit the existing comment with `gh issue comment 106 --edit-last` rather than posting a second one. Change nothing else about it — the evidence stands, and this is a correction to how it is labelled.

**Read the code before writing about it.** The behavior is on this branch already: the exit-3 path and the partition in `.agent-guild/scripts/compose-brief.py`, the discharge route in `.agent-guild/hooks/_lib.py`, and the dispatch denial in `.agent-guild/hooks/dispatch-guard.py`. Describe what is there, not what the plan said would be there.

**Prose.** All of this is human-facing. Invoke the `humanizer` skill and work its audit-and-revise loop over every passage you write, including the commit message — applying its principles from memory consistently misses tells. Match the surrounding voice; these documents are written in a specific register and a paragraph in a different one reads as pasted in. Keep headings title-cased. Em dashes go unspaced, chained directly to the words on both sides, and used sparingly.

**Commit.** Run `python3 scripts/build-plugin.py` and commit the regenerated packages in the same commit. Note that a `guild-core/roles/` edit also regenerates `.claude/agents/` — that is expected and belongs in the same commit. Never hand-edit `plugin/`, `plugins/agent-guild/`, or `.claude/agents/`.

## Verdict contract

Return the canonical verdict object. Echo these four fields exactly as given:

  task_id  T-005
  checker  checker-courier
  vendor   openai
  model    gpt-5.6-terra

Set duration_ms and cost_usd to null; call metrics are recorded on this side. A fail carries at least one finding with concrete evidence.

severity is the impact of a defect: blocker, major, minor, or info. Use info for a finding that records a clause being satisfied. A clause's own severity in the text above is the cost of violating it, not the label for every finding about it. A pass carries only info and minor findings.

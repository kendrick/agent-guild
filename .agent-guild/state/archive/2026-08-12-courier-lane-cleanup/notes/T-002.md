# T-002 self-report

**Posted comment:** https://github.com/kendrick/agent-guild/issues/106#issuecomment-5262456691
**Commit:** 6720a5a `test(courier): replay #106's embedded-quote payload, not just NOT_JSON`
**Issue #106 state:** OPEN (not closed, per spec)

## What I did

No production change. The gate #84 shipped holds against #106's payload, so this closed an evidence gap, not a code gap.

Added a `malformed_quotes` fixture mode to `test_codex_courier.py` plus a four-assertion case driving it. The fixture builds a real verdict object with `json.dumps`, then substitutes a placeholder for `the brief says "C-1 must pass" at line 2`, which yields #106's exact shape: valid JSON until a string value carries raw quotes. It parses 242 characters and dies at `Expecting ',' delimiter: line 1 column 243`.

Ran the replay two ways. The suite case, and a separate driven run against a preserved project directory so I could read the artifacts off disk rather than trusting the runner's stdout. Both are in the comment.

## Taste calls

**Why a new fixture instead of reusing `malformed`.** The existing one emits `NOT_JSON`, which fails at character one. The whole risk #106 describes is the partway parse, where a permissive decoder keeps the prefix. Different failure, different code path, worth its own case.

**Four assertions, one per acceptance criterion.** Tempting to write one fat assertion; kept them split so a checker re-running this sees which criterion moved. Assertion 2 reads `len(calls) == 2` from the fake CLI's own counter file, not `outcome["attempts"]`, so the retry count comes from the vendor side rather than the runner's self-report. Both are asserted.

**Did not add a `malformed_quotes_once` variant.** The suite already covers retry-succeeds via `malformed_once`, and the spec asked for "a case" driving the embedded-quote shape. Duplicating the once-path with a new payload would have been scope creep.

**Mutation testing (C-5) I ran myself.** In a `cp -a` copy under the scratchpad, three separate backouts of `codex-courier.py`:
- `range(2)` to `range(1)` on the retry loop: all four assertions fail
- `_should_keep_raw` forced False: retention assertion fails
- decoder taught to salvage the parsed prefix (#106's bug reintroduced): first assertion fails

Note for whoever checks this: the full suite crashes under mutation 1 at the pre-existing `malformed_once` case (line ~503 does `outcome["persisted"]["raw_path"]` where `outcome` is None), so it never reaches my block. I isolated my case into a trimmed runner inside the scripts dir to get a clean red/green. Baseline green in isolation, confirmed before mutating.

The third mutation surfaced something I put in the comment: with the decoder made permissive, the truncation still doesn't land, because the salvaged object claims `verdict: fail` with empty `findings` and the semantic validator rejects it independently. Two layers, not one. That was an observation from the run, not an assumption.

**On the amended criterion.** The spec said say so explicitly, so it has its own section naming #142/#144, the local `model` stamp, the `info` finding, and raw retention. Framed as intent-holds/letter-changed rather than buried.

**Live crossing.** Verified the orchestrator's tip before using it. `T-001-haiku-r0-codex.jsonl` has two attempts, both well-formed JSON with a wrong `checker` field (`checker-second-opinion`, then `second-opinion-attacker`), and the stem holds a `blocked` verdict with 8554 bytes of evidence. Real, and it does exercise ACs 1 to 4. Presented as corroboration only, since it failed on identity rather than embedded quotes, which is exactly why the driven replay still had to happen.

## Prose

Invoked the `humanizer` skill and worked its loop. The audit caught: spaced em dashes throughout (user rule is unspaced or none), two manufactured punchlines ("A passing test proves nothing until it can fail", "The gate is two deep, not one"), a subjectless closing fragment, and a misattribution where I credited the CI workflow line to #128. Checked git log: it came from T-001's commit 9985472, so the comment now just says "the same branch" without naming an issue. Headings left title-cased per standing preference.

## Verification

All seven suites exit 0 (46 in `test_codex_courier.py`, up from 42). `build-plugin.py --check` clean at every commit on the branch. C-9 and C-11 guards exit 0, tree clean outside `state/`, no upstream ref, nothing pushed.

# T-004 self-report

## What I did

`compose-brief.py`:
- Added `CHECK_LINE_RE`/`JUDGMENT_PREFIX`, `extract_check_value()` (pulls the
  `- **check**:` bullet's value out of a verbatim clause block), and
  `is_judgment_clause()` (anchored `str.startswith("checker-judgment:")`,
  not a substring test).
- `compose()` now partitions `clause_blocks` into `judgment_blocks` right
  after all cited clause ids are resolved against the constitution. If
  `judgment_blocks` is empty, it returns the exact stderr line
  `compose-brief: nothing to cross: <task-id> cites only script-checked
  clauses` plus a new third return element (`nothing_to_cross: bool`) that
  `main()` uses to pick exit 3 over exit 1. The brief body itself is built
  from `judgment_blocks`, so a dropped clause never appears anywhere in it.
- `main()` threads the third tuple element through; exit 3 happens before
  any file write, so nothing is ever written to the output path on that
  branch (verified: no file, no `briefs/` dir at all in that case).
- Updated the module docstring's exit-code list and `compose()`'s own
  docstring for the 3-tuple.

`test_compose_brief.py`:
- Fixture constitution's `check` lines for C-1/C-2 gained the
  `checker-judgment:` prefix (per the task's own diagnosis: the suite would
  otherwise go red under the new filter with every clause becoming
  script-checked). Golden constants (`EXPECTED_NO_DIAG`, `EXPECTED_WITH_DIAG`)
  updated to match — `EXPECTED_WITH_CONTRACT` derives from
  `EXPECTED_NO_DIAG` so it updated automatically.
- Added a third fixture clause, C-3, whose check is
  `.agent-guild/scripts/check-foo.sh --mode checker-judgment:x` — script-
  checked, but containing the literal substring `checker-judgment` without
  starting with it. This is deliberately the anchored-prefix negative case
  from the spec, folded into the mixed-clause fixture rather than kept as a
  separate case, since it's exactly what the mixed test needs to prove
  anchoring rather than substring matching.
- Added the three required shapes as a new "partition" section:
  - **all-judgment**: reruns the existing all-judgment task (T-100, both
    clauses judgment) and asserts the brief is byte-identical to
    `EXPECTED_NO_DIAG`, labeled explicitly so it isn't just a side effect of
    the pre-existing golden-file tests.
  - **mixed**: new task T-104 citing [C-2, C-3]. Asserts C-3's heading and
    check line are both absent, and C-2's block survives byte-for-byte.
  - **all-script**: new task T-105 citing [C-3] only. Asserts exit 3, the
    exact stderr string, no brief at the default output path, and that
    `briefs/` is never even created.

## Mutation check I ran (not just claimed)

Temporarily replaced `compose-brief.py` with the pre-change version recovered
via `git show 8899b42:.agent-guild/scripts/compose-brief.py` (merge-base with
main) and reran the suite: 6 checks went red — exactly the mixed and
all-script assertions (script clause not absent, wrong exit code, no exact
stderr, brief written where it shouldn't be). The all-judgment case stayed
green under the revert, which I believe is correct and not a defect: with
two judgment-only clauses cited, the partition is a no-op regardless of
whether the filter code exists at all, so no revert of the filter itself can
turn that specific case red. I read it as a legitimate byte-identity
regression guard rather than a feature-detection test, and it's explicitly
permitted by the task text ("you are free to rewrite those constants"). I
flag this as a judgment call in case a checker disagrees on whether C-5's
mutation requirement applies to it — I don't see a way to make an
all-judgment-only case distinguish "filter present" from "filter absent"
without changing what makes it "all-judgment" in the first place. Restored
the real file and confirmed `git diff --stat` showed only my actual changes
before committing.

## Other verification run

All seven suites named in C-1 pass individually after the build:
test_compose_brief.py, test_ledger_append.py, test_codex_courier.py,
test_claude_courier.py, test_hooks.py, test_codex_adapter.py,
test_build_plugin.py. Ran `python3 scripts/build-plugin.py` and
`--check` (both clean), plus the C-2 per-commit clone-and-check walk by hand
(all commits on the branch pass `--check`). Confirmed no upstream tracking
ref, a clean tree outside `state/`, nothing changed under
`.agent-guild/state/archive/`, and the whole diff sits inside the paths C-9
names.

## Scope note

C-7 names four sites; three (`_lib.py`'s `second_opinion_debts`,
`dispatch-guard.py`, and `test_hooks.py`'s three `.skipped` cases) are
T-003's delivered code, which the task explicitly forbids me to touch. I
didn't touch them and didn't need to — my only C-7 site is
`compose-brief.py`'s exit-3 behavior, which I implemented and verified
(exact stderr byte-for-byte, no brief written).

## Commit

`32d2418` — `fix(courier): drop script-checked clauses before they cross to
a courier`. Ran the commit message through the `humanizer` skill (embedded
mode) before committing; one em dash in my first draft was cut per the
style pass. Six files staged and committed together: the two source files
plus their four regenerated build outputs under `plugin/` and
`plugins/agent-guild/`.

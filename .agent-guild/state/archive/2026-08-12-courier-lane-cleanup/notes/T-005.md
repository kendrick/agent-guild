# T-005 self-report

Commit `81baa04`, `docs(courier): describe the crossing #128 stopped making`. Plus two GitHub comments, which change no file.

## What I did

Read the code first, on this branch: `compose-brief.py`'s `is_judgment_clause` prefix test and the exit-3 return in `compose()` (the error tuple's third element is what picks 3 over 1, and no brief is written on that path), route 5 in `_lib.second_opinion_debts`, and the `.skipped` denial in `dispatch-guard.py` (its `stem` is computed above both denials so the check has something to key on before `reserve_crossing` runs).

**`guild-core/roles/checker-courier.md`.** Four changes: the intro gains the "a crossing happens only when the brief carries at least one judgment clause" rule; step 2 gains a second paragraph on exit 3 quoting the stderr line verbatim and saying to run no lane command; the "if evidence a clause requires was not supplied" fallback is gone from What You Read; step 6's single-row line gains `attempts` and `discarded[]`.

**`.agent-guild/CLAUDE.md`.** The dual-check regime and the model-routing table row both now read "after every checker of record whose task cites a judgment clause". The debt list went from three files to four. The state map gained the `.skipped` entry.

**`.agent-guild/templates/task.md`.** The skip is now one of three named reasons a second opinion never landed. The header line also allows for a comparison written when nothing is coming.

**#34.** One comment, two paragraphs plus a line saying it is an input and not a ruling.

**#106.** Edited with `--edit-last`. The AC 1 block is now an excerpt labelled as one, with the validator transcript split into its own block, and the AC numbering follows the issue's own list.

Build run and committed with the sources. All seven suites green.

## Taste calls

**The model-routing table row was out of scope as written, and I amended it anyway.** C-8 names only the dual-check regime paragraph. But the table said "after every checker of record returns", which is the exact instruction `dispatch-guard` now refuses on a skipped stem, and C-8's check also fails "any behavior the code has that the document still contradicts". Leaving the correct rule in one paragraph and the stale one in a table three screens up seemed worse than the one-line edit.

**Same reasoning for two others.** "Three files discharge a debt" is now four: `_lib.py` has five discharge routes and `.skipped` is one of them, so a state map entry with no matching bullet in the debt list would have left the two halves of the same doc disagreeing. And the template's "Those cross as pre-run output for the far side to judge" was a live contradiction of the composer, not merely a gap, so it is rewritten rather than left beside the new skip sentence.

**The state map entry leads with the author, not the path.** Everything else in that list describes a file; this one had to describe an instruction, because exit 3 is the only trigger and no hook fires on it. I put "the orchestrator's record" in the first clause and "you write it by hand, on `compose-brief.py`'s exit 3 and on nothing else" in the second, then said explicitly that no hook files it, since the surrounding entries are all about things gates do for you and the reader's default assumption would be wrong here.

**The #106 relabelling is a relabelling.** I did not re-run the fixture or touch a number. The `json.load` command line is gone because it was the thing that was false; the JSON below it stays exactly as posted, now introduced as an excerpt with the elision explained. For the numbering I aligned to the issue's own list rather than renumbering the comment's later blocks: retention is the tail of criterion 3, so it is labelled "AC 3, second half", and the comment now has no AC 4 block at all, which is the honest shape given criterion 4 is the amended one with its own section. "Criteria 1 through 4 firing against a live vendor" became "the first three criteria", which covers the same ground under the issue's numbering. The section heading names re-serialization so "the fourth criterion" can only mean one thing on the page.

**Register.** These documents run long sentences with an unspaced em dash for the turn, and they explain the incident behind a rule rather than stating the rule alone (#128, #141, #94 all appear that way). I matched that: the new state map entry carries its issue number and the reason a script-checked clause is worthless as a second opinion, because a bare "write this file on exit 3" would have read as pasted in from a different document.

## Ambiguity noted

The spec's #106 item offers "either make it the literal output or relabel it as an excerpt". I took the relabel. Reproducing literal output would have meant re-running a fixture crossing to regenerate a file the original run left in a temp tree, and the spec's own framing is that the evidence stands and only the labelling is wrong.

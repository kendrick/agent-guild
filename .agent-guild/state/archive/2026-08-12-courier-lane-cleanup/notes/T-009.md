# T-009 worker notes

Four commits on `chore/courier-lane-cleanup`, in this order (each rebuilds `plugin/`
and `plugins/agent-guild/` and commits the refresh):

1. `65dea37` — item 6, its own commit, exempt from C-5: guarded the second
   unguarded `outcome["identity"]` dereference in `test_codex_courier.py`
   (T-008 only guarded `outcome["persisted"]`). Confirmed the suite still
   passes (53/53) and that this is pure instrument repair, no behavior
   change.
2. `dcf6b04` — items 1+2 together, as the spec requires: `_courier_lib.ledger_record`
   grows an optional `discarded` param (omitted from the dict when unset, so
   `codex-courier.py`'s existing calls are unaffected — it never passes it);
   `subagent-return.py`'s `_codex_courier_outcome_ok` widens its `ledger`
   key-set check to admit `discarded` as optional while still rejecting any
   other unknown key, and validates the shape of each discarded entry
   (reason/duration_ms/exit_code/tokens_in/tokens_out) rather than only
   checking the key is present. That last part (validating `discarded`'s
   contents) is beyond what item 2's text literally asks for, but it's a
   direct reading of "the point is to admit that one, not to stop checking" —
   flagging in case the checker reads that phrase more narrowly.
3. `4373ab7` — item 3 + item 5 together (implementation with its own test
   coverage, matching this repo's own convention, e.g. f32a490): claude-courier.py
   now times each attempt on its own clock and holds each attempt's usage
   aside before folding into cumulative totals, builds one `discarded` entry
   per non-final attempt, and never invents one on the quota path (verified
   this holds even when a prior attempt was itself discarded, via a new
   `malformed_then_quota` fake-CLI mode).
4. `c80d541` — item 4, its own commit since it's a different suite
   (`test_codex_adapter.py`) testing the item 1/2 widening against
   claude-courier.py's real output rather than the hand-built `courier_outcome()`
   fixture.

Genuineness (C-5) self-check before handing off, since I could run the same
mutation-and-rerun a checker would: reverted item 3's two `discarded=...`
call sites in a `cp -a` scratch copy — the new "discarded attempt's own
figures" case in `test_claude_courier.py` went red as expected, the two
quota-no-discarded cases stayed green (they don't depend on item 3 alone).
Separately, made the quota path pass `discarded=_discarded_entries(...)` (the
plausible regression the two quota tests exist to catch) — the new
`malformed_then_quota` case went red, confirming it's not a vacuous
assertion. Reverted item 2's key-set widening — the new
`test_codex_adapter.py` case went red with exactly the "ledger fields must be
exactly [...]" rejection the task describes.

All seven suites named in C-1 pass (`test_compose_brief.py`,
`test_ledger_append.py`, `test_codex_courier.py`, `test_claude_courier.py`,
`test_hooks.py`, `test_codex_adapter.py`, `scripts/test_build_plugin.py`).
C-2's per-commit walk (`build-plugin.py --check` at every commit from
`origin/main` through HEAD in a disposable clone) is clean. C-3 (archive),
C-9 (path scope), C-11 (branch, no upstream, clean tree outside `state/`),
and C-12 (CI line already present from an earlier task) all check out from
the command lines in their `check:` fields.

`codex-courier.py`, `vendor-call.schema.json`, and `ledger-append.py` are
untouched by this task (verified via `git diff 3059c2f..HEAD`), per the
non-goal and item 1's instruction that T-008 left the codex lane alone on
purpose.

Judgment call: item 2's ledger key-set widening also validates the shape of
`discarded` entries (not just their presence as a key), which is a bit more
than the literal ask. I did this because leaving it unchecked felt like it
would violate "widen, do not weaken" in spirit even though nothing currently
sends a malformed `discarded` value through this path — flagging so the
checker can judge whether that's in scope for C-15/C-1 or over-build.

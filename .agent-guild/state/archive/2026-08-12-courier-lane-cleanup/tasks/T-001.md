---
id: T-001
title: Cut the job branch and put test_codex_courier.py in CI
spec: .agent-guild/state/spec.md
clauses: [C-1, C-2, C-9, C-11, C-12]
executor: worker-bulk
executor_model: haiku
checker: checker-deterministic
check_method: >-
  C-1: .agent-guild/scripts/check-build.sh with the command that clause C-1 names in
  .agent-guild/state/constitution.md, run verbatim.
  C-2: .agent-guild/scripts/check-build.sh with the command that clause C-2 names in
  .agent-guild/state/constitution.md, run verbatim.
  C-9: .agent-guild/scripts/check-build.sh with the command that clause C-9 names in
  .agent-guild/state/constitution.md, run verbatim.
  C-11: .agent-guild/scripts/check-build.sh with the command that clause C-11 names in
  .agent-guild/state/constitution.md, run verbatim.
  C-12: .agent-guild/scripts/check-build.sh with the command that clause C-12 names in
  .agent-guild/state/constitution.md, run verbatim.
status: complete
retries: 0
max_retries: 2
deps: []
escalations: []
artifacts: [".github/workflows/plugin-build.yml"]
---

## Spec excerpt

Two things, in this order.

**1. Cut the branch.** Every task in this job commits to one branch. Create it off `main` and stay on it:

```sh
git checkout -b chore/courier-lane-cleanup main
```

Never push it, and never set an upstream tracking ref. The user pushes; you do not.

**2. Add the missing suite to CI.** `.agent-guild/scripts/test_codex_courier.py` is the only test of `codex-courier.py`, which is where issue #106's whole guarantee lives — validate before persisting, retry once on malformed vendor output, emit a `blocked` verdict on the second failure. That test file appears in no workflow in this repo, so every assertion it holds runs locally and nowhere else.

Add it to the suite list in `.github/workflows/plugin-build.yml`. The list is a plain `run:` block of `python3 <path>` lines under a `Run Test Suites` step, indented ten spaces like its neighbours. The `.agent-guild/scripts/` entries in it are in alphabetical order, so the new line belongs directly after `python3 .agent-guild/scripts/test_claude_courier.py`.

The line must be live: indented into the `run:` block, not commented out, and nothing else on it.

**Commit it.** One commit, message explaining why the gap mattered rather than restating the diff — the file under test carries a guarantee that was going unverified in CI. Nothing else changes in this commit.

**Do not** edit `plugin/` or `plugins/agent-guild/` by hand. Those are build outputs. A workflow file is not a build input, so this task needs no `build-plugin.py` run — but if you find `python3 scripts/build-plugin.py --check` already dirty on a clean checkout, stop and report it rather than committing over it.

## Rework diagnosis

<!-- ORCHESTRATOR appends here on each FAIL, copied verbatim from the checker's
verdict Diagnosis. Newest at the bottom, headed with the attempt it addresses
(e.g. "### sonnet r1"). Empty until the first failure. -->

## Courier comparison

```yaml
clause_types_sampled: deterministic
brief_framing: attack
courier_outcome: blocked
agreement_on_outcome: null
disagreement_kind: null   # substantive | evidence_packet | null
unique_courier: {defect: 0, inference: 0, evidence_quality: 0, coverage: 0}
unique_checker: 0
unique_checker_access_derived: 0   # of the above, how many needed repo
                                  # access the lane structurally cannot have
overlap: 0
changed_verdict: no
cost: {wall_s: 61.8, tokens_in: 40906, tokens_cached: null, tokens_out: 2797, brief_tokens: 2508, vendor_calls: 2}
retrofit_note: unique_checker was 5 in the original block, counting the checker's info-level clause confirmations; those are not findings and the field is 0. vendor_calls 2 is sourced: courier-raw retained two attempts.
```

The crossing produced no admissible second opinion, and the reason is not that the vendor had nothing to say. Codex returned structured output that failed identity validation twice running, so the runner emitted a conforming `blocked` verdict and retained both attempts at `.agent-guild/state/log/courier-raw/T-001-haiku-r0-codex.jsonl` — the #106 path working as designed, observed live rather than in a fixture.

What the identity check threw away was substantive. Both discarded attempts attacked the checker of record on evidence quality rather than on computation: that C-1's summary claims 379 tests where the per-suite totals sum to 393, that C-2's transcript shows final state without demonstrating the per-commit walk actually visited every commit, and that C-12's `grep` establishes the string exists somewhere in the workflow rather than that the line is live in a `run:` block. That last one is a point the auditor also made at CON-audit r2 and I judged a minor. None of it reached a verdict, because the envelope was rejected before the contents were read.

The rejection cause is the finding to carry forward. The verdict contract pins `checker` to `checker-courier`; attempt one returned `checker-second-opinion` and attempt two returned `second-opinion-attacker`. Neither name came from nowhere — this crossing carried `attack` framing, and the vendor read the framing as a persona to adopt and then wrote that persona into the identity field #113 exists to pin. Two for two on one crossing is a pattern worth watching, not a conclusion. If it reproduces, `attack` framing and the echo contract are in tension: the framing that makes a second opinion worth having is the framing most likely to make the vendor rename itself, and the guild then discards the opinion it paid for. Both attempts also echoed `model: gpt-5` against the pinned `gpt-5.6-terra`, which is the divergence #142/#144 already stamps locally and records as `info`.

Every cited clause here is deterministic — five `check-build.sh` invocations, run locally with their output inlined as evidence. Even had the vendor answered, it would have been judging pre-run output that agrees by construction, so this crossing was near-worthless as #34 evidence before it ever went out. That is the waste #128 exists to stop, and T-004 is the task that stops it. Recorded as an absence, not as agreement.

`tokens_cached` is null because the lane does not persist it; #150 documented that gap deliberately and it remains unfilled.

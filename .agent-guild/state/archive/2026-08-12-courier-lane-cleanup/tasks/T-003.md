---
id: T-003
title: Discharge a skipped crossing in the hooks
spec: .agent-guild/state/spec.md
clauses: [C-1, C-2, C-5, C-7, C-9, C-10, C-11]
executor: worker-standard
executor_model: sonnet
checker: checker-judgment
check_method: >-
  C-1: .agent-guild/scripts/check-build.sh with the command that clause C-1 names in
  .agent-guild/state/constitution.md, run verbatim.
  C-2: .agent-guild/scripts/check-build.sh with the command that clause C-2 names in
  .agent-guild/state/constitution.md, run verbatim.
  C-5: checker-judgment: apply the check text of clause C-5 from
  .agent-guild/state/constitution.md verbatim against the delivered tree,
  mutating only inside a cp -a copy under the session scratchpad. The three
  new test_hooks.py cases are the ones to mutate.
  C-7: checker-judgment: apply every operation of the check text of clause C-7
  except the compose-brief exit-3 site, which does not exist until T-004 and is
  not judged here. Build the fixture project and observe the discharge, refile the
  marker under the far lane and observe the debt stand, read dispatch-guard.py for
  where its stem is computed relative to the denial, and read test_hooks.py for the
  three named cases. Stage every fixture outside the live .agent-guild/state/
  directory, using CLAUDE_PROJECT_DIR for the hook-side reads.
  C-9: .agent-guild/scripts/check-build.sh with the command that clause C-9 names in
  .agent-guild/state/constitution.md, run verbatim.
  C-10: checker-judgment: apply the check text of clause C-10 verbatim to the
  commit messages this task added, scoped with git log 6720a5a..HEAD rather than
  the whole branch. An earlier task commit is not this worker to rework.
  C-11: .agent-guild/scripts/check-build.sh with the command that clause C-11 names in
  .agent-guild/state/constitution.md, run verbatim.
status: complete
retries: 0
max_retries: 2
deps: [T-002]
escalations: []
artifacts: [.agent-guild/hooks/_lib.py, .agent-guild/hooks/dispatch-guard.py, .agent-guild/hooks/test_hooks.py, plugin/hooks/_lib.py, plugin/hooks/dispatch-guard.py, plugin/hooks/test_hooks.py, plugins/agent-guild/hooks/_lib.py, plugins/agent-guild/hooks/dispatch-guard.py, plugins/agent-guild/hooks/test_hooks.py, plugins/agent-guild/project-template/.agent-guild/hooks/_lib.py, plugins/agent-guild/project-template/.agent-guild/hooks/dispatch-guard.py, plugins/agent-guild/project-template/.agent-guild/hooks/test_hooks.py]
---

## Spec excerpt

Issue #128 stops script-checked clauses crossing the courier lane. A task citing **only** script-checked clauses therefore has nothing to cross, and the composer will refuse to write a brief for it (that is T-004's half). This task builds the discharge route that refusal needs, because without one the stop gate holds the orchestrator's turn open forever: no verdict, no sentinel, and no waiver would ever land for that stem.

The new marker is `.agent-guild/state/verdicts/T-NNN-<tier>-r<N>-<lane>.skipped`, one line naming the reason, written by the orchestrator. It sits under `state/`, so the write-guard allows it.

**Three code changes.**

1. **`.agent-guild/hooks/_lib.py`** — in `second_opinion_debts`, add a `.skipped` discharge route alongside the existing `.denied` one. Read the `.denied` route first and mirror it exactly, including the lane pinning: the function computes `lane = courier_lane(data)` once at the top, and every route keys off that single lane rather than trying both. A marker filed under the far host's lane must discharge nothing. The function's docstring enumerates the discharge routes in prose; update it so the list matches the code.

2. **`.agent-guild/hooks/dispatch-guard.py`** — deny a `checker-courier` dispatch on a stem that already carries a `.skipped` marker, mirroring the existing exhausted-lane denial in shape and in message quality. A skip that was recorded should not then be crossable. Note the ordering problem before you start: the exhausted-lane denial sits above the point where `stem` is computed, so a `.skipped` check keyed on `stem` needs that computation hoisted above the denial, or done inline. The denial must come **before** the crossing is reserved — reserving a crossing you are about to refuse leaves an authorization record for a dispatch that never happened.

3. **`.agent-guild/hooks/test_hooks.py`** — three cases:
   - a debt discharged by a `.skipped` marker;
   - a `checker-courier` dispatch denied on a skipped stem;
   - the wrong-lane trap: a marker filed under the other host's lane discharges nothing, the same trap `.denied` already carries.

   This suite has no `def test_*` functions; each case is a `check("<label>", <condition>, <detail>)` call under a numbered comment. Follow that idiom and give each case a label a reader can find. Note the existing helper that performs a real dispatch-guard reserve plus subagent-return promote round trip rather than hand-writing a lane file — use it where a case needs an authorized crossing, deliberately, because hand-writing that file is the forgery the #141 work exists to catch.

**What this task does not do.** `compose-brief.py`'s exit 3 is T-004. Do not touch it. Do not write the docs or role text either; that is T-005.

**Tests must be genuine.** Each new case has to go red when the production change it covers is backed out. A case that passes against both the old and new `_lib.py` covers nothing and will fail this task's check.

**Commit.** `.agent-guild/hooks/` is a build input: run `python3 scripts/build-plugin.py` and commit the regenerated packages in the same commit as the source change. Never hand-edit `plugin/` or `plugins/agent-guild/`. One logical change per commit; the commit message says why a recorded skip needs a discharge route, not merely which functions moved. Run the `humanizer` skill over the commit message before committing.

## Rework diagnosis

<!-- ORCHESTRATOR appends here on each FAIL, copied verbatim from the checker's
verdict Diagnosis. -->

## Courier comparison

```yaml
clause_types_sampled: mixed
brief_framing: confirm
courier_outcome: fail
agreement_on_outcome: disagree
disagreement_kind: evidence_packet   # substantive | evidence_packet | null
unique_courier: {defect: 0, inference: 0, evidence_quality: 0, coverage: 2}
unique_checker: 0
unique_checker_access_derived: 0   # of the above, how many needed repo
                                  # access the lane structurally cannot have
overlap: 1
changed_verdict: no
cost: {wall_s: 40.3, tokens_in: 62876, tokens_cached: null, tokens_out: 1815, brief_tokens: 4202, vendor_calls: unknown}
retrofit_note: vendor_calls was 1 by inference; not positively recorded.
```

The framing was `confirm` on purpose, against `attack` on T-001 and T-002, so the record can attribute outcome differences to framing rather than drift. It still returned `fail`, which is the first thing worth recording: the framing did not soften the outcome.

**Overlap, one.** Both readers caught the stale inline route numbering at `_lib.py:523-524`, and both graded it minor. The docstring the spec explicitly required updating is correct; only a comment ten lines below it still counts routes the old way.

**Unique to the courier, two, both coverage claims, and both artifacts of the brief rather than judgments about the work.** It called C-7 unestablished because the record leaves the compose-brief exit-3 site unjudged, and it called C-10 unestablished because nothing shows the #106 comment being read. Neither belongs to this task: C-7's site 1 is T-004's code and does not exist yet, and the #106 comment is T-002's deliverable, already judged there. The task's `check_method` says both things explicitly.

The vendor never saw that. `compose-brief.py` assembles the clause blocks and the `## Spec excerpt` and nothing else — the task's `check_method` is not in the brief. So the far side reads a clause that names four sites and the #106 comment, has no way to learn that this task's checker was scoped to three sites and to its own commits, and correctly reports that the clause as written was not fully established. Both findings are sound against the clause text and wrong against the task, and the vendor had no material with which to tell the difference.

That is the most useful thing this crossing produced, and it is a finding about the lane rather than about T-003: **a brief that carries clause text without the task's scoping will manufacture coverage findings on any task whose checker is legitimately scoped.** It will recur on T-004, which cites C-7 across all four sites while forbidden to touch three of them. Worth filing against `compose-brief.py` after this job — it is adjacent to #128's work but outside what the spec asks for, so it is not being folded into T-004.

**Cited clauses that were deterministic:** C-1, C-2, C-9, C-11. The judgment clauses are C-5, C-7, and C-10. Note the shape: the lane agreed with the checker on C-5, where the evidence was a recorded mutation with observable red-then-green results, and disagreed on the two clauses where it had to reason about scope. It is at its best judging results it can see and at its worst inferring what a check should have covered.

`tokens_cached` is null; #150 documented that gap and it remains unfilled.

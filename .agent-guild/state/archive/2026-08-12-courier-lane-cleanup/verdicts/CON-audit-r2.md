---
task: CON-audit
checker: auditor
vendor: anthropic
model: claude-opus-5
verdict: FAIL
checked_at: 2026-08-11T23:20:00Z
duration_ms: None
cost_usd: None
---

## Per-clause results

| clause | severity | description | evidence |
| ------ | -------- | ------------ | -------- |
| C-1 | info | PASS, re-verified on a fresh clone at the job branch. | `check-build.sh '<the seven suites>'` → `43 passed, 0 failed` … `check-build.sh: exit 0` on probe clone `scratchpad/p2`. Confirmed it still fails when it should: with an uncommitted source edit, `test_build_plugin.py` returned `42 passed, 1 failed` (`FAIL the explicit Claude build reproduces the published plugin exactly`). |
| C-2 | major | The check is sound and its r1 minor is fixed — but its enumeration of build outputs is wrong, and that error is what makes the C-9 blocker below reachable. See the blocker row. | constitution.md:61 says "`plugin/` and `plugins/agent-guild/` are outputs of `python3 scripts/build-plugin.py`". `scripts/build-plugin.py:1197` also calls `sync_dogfood()`, which writes `.claude/agents/*` and `.claude/skills/*` from `guild-core/` (`:428-447`), and `--check` verifies them (`dogfood_diffs(CLAUDE_DIR)`, `:1066`). Probed: editing `guild-core/roles/checker-courier.md` and running the build modifies `.claude/agents/checker-courier.md`, a tracked file. |
| C-2 | info | The r1 minor is closed. The trap fires and the clone walk still catches the staleness it exists for. | Probe: commit 1 edits `.agent-guild/scripts/ledger-append.py` with no rebuild, commit 2 regenerates. Tip `--check` exit 0; full C-2 → `stale generated packages at commit e041fee…`, exit 1. Swept `/var/folders/*/*/T/tmp.*` afterward for any directory containing `.git`: none. One `--check` costs 1.7s wall, so the O(commits) walk is a few tens of seconds at this job's size. |
| C-2 | minor | One sentence of the text is unreachable by its check, by construction. | constitution.md:61: "Editing a generated copy by hand … fails this clause even when `--check` is momentarily quiet." A hand-edit that is byte-identical to the build output leaves no artifact `--check` can see. Practical blast radius is zero (an imperfect hand-edit is exactly what `--check` catches), but it is a claim no checker can act on. |
| C-3 | info | PASS, unchanged from r1 and still correct. | constitution.md:68. The branch precondition plus merge-base diff plus `git status --porcelain` sweep covers committed, unstaged, and untracked changes under `state/archive/`; r1 probed all three and the text is unchanged this round. |
| C-4 | info | FIXED. The r1 minor is closed: `attempts` is declared `integer` and `discarded` an `array` of objects, and the five member types match the live top level exactly. | constitution.md:73. Read the schema directly: `duration_ms` `integer`, `exit_code` `integer`, `tokens_in`/`tokens_out` `["number","null"]` — the same types C-4 names for the `discarded` members. `wc -l` on the three named ledgers: 7 + 8 + 6 = 21, as stated. |
| C-4 | minor | One runtime claim in the text that no check reaches. | constitution.md:73: "A vendor that reported no token figure for a discarded attempt gets `null`, never a fabricated `0`." C-4's check reads the schema and validates archived rows; C-15's check reads `reason`, `duration_ms`, and the cumulative totals. Neither observes what a courier writes when usage is absent, so a courier that fabricates `0` passes both while the schema permits `null`. |
| C-5 | info | FIXED, and fixed rather than relocated. I reproduced the r1 probe and the new venue's behaviour, then confirmed the copy is a working check instrument, not just a working copy. | constitution.md:80. With `# NEW TEST CASE (uncommitted)` in `test_ledger_append.py`, `# PRODUCTION EDIT (uncommitted)` in `ledger-append.py`, and an untracked `test_new_untracked.py`: `cp -a` copy greps 1, 1, and carries the untracked file; `git clone --local --no-hardlinks` of the same tree greps 0. The copy is self-contained — appending `raise SystemExit("MUTATED")` to the copy's `ledger-append.py` turned the copy's suite red while the origin's stayed `43 passed, 0 failed`, so a mutation there is both visible and isolated. Repo is 14M, so the copy is cheap. |
| C-5 | minor | The venue is right; the sentence justifying it now describes a tree state C-11 forbids. | constitution.md:80: "a task's new tests are usually still on disk unstaged when the checker arrives." constitution.md:115 makes exactly that state a blocker FAIL. Both are satisfiable at once (`cp -a` is still the right venue — it is the only one that carries untracked files), so this is not a contradiction of requirements, but the document tells a checker two different things about the tree it should expect. |
| C-6 | info | PASS, unchanged. Still the model the newer clauses were built from. | constitution.md:85-86. Four criteria, the amendment note, and "asserted rather than demonstrated" as an explicit FAIL condition. |
| C-6 | info | Its dependency on C-5's new backstop resolves in fact, which is what keeps C-11 out of trouble on the #106 task. Checked the fixture. | The existing `malformed` mode writes the literal string `NOT_JSON` (`test_codex_courier.py:178-182`), not #106's embedded-quote payload, so spec.md:26's conditional ("if the existing malformed fixture doesn't cover embedded-quote truncation specifically") resolves to *add a case*. C-5's backstop naming C-6 therefore demands a test that the spec independently demands. |
| C-7 | info | FIXED on both r1 findings. Site 4 is now checked, and "no brief written" is in the text and in the check. Both fixture routes are named and both work. | constitution.md:91-92. `check(label, cond, detail="")` is the real idiom in `test_hooks.py:63` (163 call sites), so "name the three cases by their `check(...)` label" is executable. Probed the venues: `compose-brief.py` invoked by absolute path from a fixture cwd reads the fixture's state dir (`compose-brief.py:250`), and `second_opinion_debts` under `CLAUDE_PROJECT_DIR=<fixture>` returned `[('T-900','T-900-sonnet-r0','codex')]`, and still returned it after a `…-r0-codex.skipped` file was added — the behaviour the job adds is genuinely absent today, so site 2 is falsifiable now. The brief's output path is deterministic (`state/briefs/T-NNN.md` or `--out`, `:256`), so "confirm no file exists" is decidable. |
| C-7 | minor | The accounting is complete but the count is wrong: six operations, not five. | constitution.md:92 says "five operations across the four sites", then enumerates site 1 twice (stderr, no-brief), site 2 twice (discharge, far-lane), site 3 once, site 4 once = 6. Every site named in the text now carries at least one operation, so nothing is hidden — but the number was added for auditability by counting, and a checker who counts gets a different answer than the one it is given. |
| C-7 | info | Site 3 is the one operation that observes nothing executable, and it is backstopped rather than load-bearing. | "Read `dispatch-guard.py` and say where its `stem` is computed relative to the denial" (constitution.md:92) cannot fail on a denial that exists but is wrong. Site 4's required `test_hooks.py` case ("courier dispatch denied on a skipped stem") plus C-1 running the suite is what actually pins that behaviour. Acceptable as written. |
| C-8 | info | PASS, unchanged from r1's FIXED. | constitution.md:97-98. The `discarded[]` detail on `checker-courier.md`'s retry line and "names the orchestrator as the agent that writes it" are both in the clause's own list, and an absent item from that list is a blocker. |
| C-9 | blocker | FAIL. The allowlist omits a build output this job is required to produce, so C-9 and C-2 cannot both be satisfied by the spec's own Step 1 deliverable. Probed end to end. | constitution.md:103-104. Spec.md:46 requires editing `guild-core/roles/checker-courier.md`; C-2 requires the rebuild in the same commit. Probe: append one sentence to that role file, run `python3 scripts/build-plugin.py` → four files change, one of which is `.claude/agents/checker-courier.md`. C-9's check on that tree prints `.claude/agents/checker-courier.md` and exits 1 — the path matches no allowlist prefix. The escape is closed: `git checkout -- .claude/agents/` and then `python3 scripts/build-plugin.py --check` → `dogfooded Claude wrappers diverge from the shared core (1 difference(s)): - content differs: agents/checker-courier.md`, exit 1, which is C-2's first arm. |
| C-9 | info | Everything else about the guard is sound and unchanged. Only the allowlist's contents are wrong, not its shape. | The branch precondition, the merge-base diff, and the porcelain sweep all behaved in r1's probes and the check text is unchanged. Verified the other five source classes this job touches produce only allowlisted outputs: edits to `.agent-guild/{scripts,hooks,schemas,templates}` and `.agent-guild/CLAUDE.md` each rewrite only `plugin/…` and `plugins/agent-guild/…`. |
| C-10 | info | FIXED. The r1 vacuity is closed from the outside: C-11 now guarantees `git log main..HEAD` has something to read. | constitution.md:109-110 unchanged; constitution.md:115-116 supplies the commit. Probed: with one commit past the merge base, C-11 exits 0 and `git log main..HEAD` is non-empty. |
| C-10 | minor | Two requirements in the text are missing from the check's tell enumeration, and one of them is a standing user rule. | constitution.md:109 requires "Headings stay title-cased and em dashes appear unspaced and used sparingly". The check (:110) enumerates inflated symbolism, padded tripartite lists, vague attribution, negative parallelism, trailing "-ing" clauses, and cause-free commit subjects — neither heading case nor em-dash spacing. A doc shipping ` — ` spaced em dashes violates the text and passes the check as enumerated. |
| C-11 | major | The commit requirement works exactly as claimed in all four states, and it introduces one legitimate state it fails: a task that produces no commit at all. The worker has no remedy. | constitution.md:115-116. Probed on `scratchpad/p2`: no commits → exit 1 `no commits on chore/courier-lane-cleanup yet…`; one commit, clean → exit 0; uncommitted source edit → exit 1 naming the file; dirty only under `state/` → exit 0. Also correct on an untracked source file and on a filename with a space. The defect is scope, not mechanics: the third arm is branch-level ("the branch carries at least one commit"), but the clause is a blocker evaluated per task, so the first task checked on a fresh branch fails it if its deliverable is a GitHub comment rather than a file. Committing is not that worker's to do. |
| C-11 | info | The second state you asked about — a checker arriving between a worker's edit and its commit — is a legitimate FAIL, not a defect. The worker's remedy is to commit, and the requirement is discoverable in the clause the task cites. | The only residue is C-5's stale rationale sentence (row above). Worth knowing: because C-11 forces commit-before-check, every rework round adds a fixup commit, and C-10's granularity rubric ("a commit mixing two unrelated changes is a finding") does not fail those, so the branch will legitimately carry them. |
| C-11 | info | The r1 minor is closed — each of the four halves prints its own diagnostic. | Probed all four messages verbatim; each names which arm tripped. |
| C-12 | info | FIXED. The r1 minor is closed: the anchored regex rejects the commented-out line. | Probed four states on the real workflow file. Today → exit 1. Line inserted at CI indentation (`          python3 …`) → exit 0. Same line commented (`          # python3 …`) → exit 1. Line with a trailing `  # flaky` → exit 1. |
| C-12 | minor | One residual text/check divergence, far-fetched but real. | The same line placed in a second job guarded by `if: false` → exit 0. The text says "runs … as a live line in its suite list"; `grep -E` cannot tell which job a `run:` block belongs to. No plausible worker reaches this state. |
| C-13 | info | FIXED on both r1 minors, and the recovery route it now names works. | constitution.md:127-128. `git show "$(git merge-base main HEAD):.agent-guild/scripts/compose-brief.py"` returns the file on the probe branch. The fixture venue is named with the same two mechanisms C-7 uses, and both were probed working. The `test_compose_brief.py` read closes the "three shapes" half; `check(label, …)` is that file's idiom too (`test_compose_brief.py:19`). |
| C-14 | info | The r1 minor on existence-gating is FIXED, and the divergence from the courier helper is now stated as deliberate. | constitution.md:133: "The rewrite is not existence-gated … That is a deliberate divergence from `_courier_lib.repo_relative`". `_courier_lib.py:177` confirms the helper's shape and `codex-courier.py:665` its only call site. |
| C-14 | minor | The clause now says the courier pre-pass stays; nothing checks that it does. Verified the gap empirically. | constitution.md:133-134. The check runs three ledger rows and an ordering assertion; it never reads `_courier_lib.py`. Probed: replacing `repo_relative` with a pass-through stub leaves `test_codex_courier.py` at `42 passed, 0 failed` and `test_claude_courier.py` at `30 passed, 0 failed`, so C-1 does not backstop it either. r1 raised this as "no clause"; it is now "clause text, unchecked", which is a smaller gap and still a gap. |
| C-15 | info | PASS, unchanged, and every anchor it names is real. | constitution.md:139-140. `malformed_once` at `test_codex_courier.py:178`, `run_courier(… persist=True)` at `:219` and `:465`, `ledger_lines` at `:275`, and the 108/26 sum pinned at `:480-481` exactly as the clause claims. |
| coverage | info | The r1 blocker is closed, both ways it offered. Every spec deliverable now maps to a clause that fails when it is skipped. | spec.md:43's three `test_hooks.py` cases → C-7 site 4, by `check(...)` label. spec.md:37's three `test_compose_brief.py` shapes → C-13's file read. C-6 and C-7 are both on C-5's existence backstop (constitution.md:80). Re-walked the whole spec: Step 0 → C-6, C-12; Step 1 → C-13, C-7, C-8, C-2; Step 2 → C-14, C-4, C-15, C-8; verification block → C-1, C-2; repo mechanics → C-11, C-10. |
| coverage | info | The r1 major is closed. "No brief written" is in C-7's text and is its second site-1 operation. | constitution.md:91-92, against spec.md:37. |
| coverage | minor | spec.md:58's and spec.md:66's named test cases are covered only by C-5's "names no test" backstop, so partial test coverage passes where zero fails. Flagging for symmetry, not as a new bar. | For #128, C-7 and C-13 name the required cases and read the files for them. For #47 and #116, C-5's backstop fires only when the artifact list names *no* test for a C-14/C-15 behaviour, so a worker who ships one of spec.md:58's three cases satisfies every clause. The behaviours themselves are verified — C-14 and C-15 both run their own repro — so what is missing is CI regression coverage, not verification. r1 accepted the backstop as sufficient here and I am not moving that goalpost; noting it in case you want the two clauses shaped alike. |
| coverage | info | For DEC-audit, not for this document: C-7, C-13, and C-15 are whole-step clauses whose checks only pass once the entire step lands. | C-7 exercises four sites across `compose-brief.py`, `_lib.py`, `dispatch-guard.py`, and `test_hooks.py`. If the decomposition splits #128 across tasks, only the task that completes the step can cite C-7 without a guaranteed FAIL. Same shape for C-13 and C-15. |
| overreach | info | No clause claims work the spec excludes. The three intake decisions and C-10's documented reduction are still carried correctly. | Branch (constitution.md:115 vs spec.md:18), #106's close left to the user (:153 vs spec.md:29), per-commit rebuild cadence (:61), C-10's output standard in place of the process claim (:109, recorded at :40-41). C-15's non-goal at :152 still matches the code: `claude-courier.py` never shells out to `ledger-append.py`. |
| mechanics | info | The paperwork is clean and the routing split the header claims holds. | `python3 .agent-guild/scripts/check-job-spec.py --audit-id CON-audit` → `OK: .agent-guild/state passes check-job-spec (CON-audit)`, exit 0. C-1, C-2, C-3, C-9, C-11, C-12 are `check-build.sh` invocations; C-4 through C-8, C-10, and C-13 through C-15 carry the `checker-judgment:` prefix C-13 makes load-bearing. Protected content is an honest "none". |
| mechanics | info | No two clauses contradict except the C-2/C-9 pair in the blocker row. Re-checked the overlaps. | C-3's archive ban is a stricter overlay on C-9's `.agent-guild/state/` allowance. C-11's clean-tree rule exempts `state/`, which C-3 then re-forbids for `state/archive/` — stricter, not conflicting. C-11's "no upstream" is about the job branch, not the `origin` C-2's clone walks. |

## Diagnosis

- **C-9 with C-2** (blocker): the allowlist omits a build output the spec requires this job to produce, so two clauses fail each other on Step 1's own deliverable.
  spec.md:46 requires editing `guild-core/roles/checker-courier.md`. C-2 (constitution.md:61) requires the rebuild to land in the same commit. `scripts/build-plugin.py` does not write only `plugin/` and `plugins/agent-guild/`: `main()` calls `sync_dogfood()` at `:1197`, which overlays `.claude/agents/*` and `.claude/skills/*` from `guild-core/` (`:428-447`), and `--check` verifies those wrappers through `dogfood_diffs(CLAUDE_DIR)` at `:1066`. Probed on `scratchpad/p2`:
  - Append one sentence to `guild-core/roles/checker-courier.md`, run `python3 scripts/build-plugin.py`. Four files change: the role source, `plugin/agents/checker-courier.md`, `plugins/agent-guild/project-template/.codex/agents/checker-courier.toml`, and **`.claude/agents/checker-courier.md`** — a tracked file matching no prefix in C-9's allowlist (constitution.md:104).
  - C-9's check on that tree prints `.claude/agents/checker-courier.md` and exits 1.
  - The obvious escape is closed. `git checkout -- .claude/agents/` and then `python3 scripts/build-plugin.py --check` → `dogfooded Claude wrappers diverge from the shared core (1 difference(s)): - content differs: agents/checker-courier.md`, exit 1, which is C-2's first arm and a blocker.
  So a worker who does exactly what the spec asks fails C-9 if they rebuild and C-2 if they don't. This is mine to own: r0 recorded "C-9's allowlist blocks nothing this repo requires", and I checked `CHANGELOG.md` and `scripts/plugin-src/` but never ran a role edit through the build. Fix by adding `.claude/agents/` to C-9's allowlist (and `.claude/skills/` if you want the source list to be complete rather than merely sufficient), and by correcting C-2's output enumeration at constitution.md:61 — `.claude/agents/` and `.claude/skills/` are generated too, which is the fact both clauses got wrong.

- **C-11** (major): the commit arm is branch-scoped in its text and per-task in its application, and there is a legitimate task it fails with no worker-side remedy.
  Mechanically it is correct — I probed all four states you named plus untracked files and a filename with a space, and each behaves, with a distinct message per arm. The problem is the one you asked about: `test "$(git rev-list --count "$b"..HEAD)" -gt 0` is a fact about the branch, but the clause is a blocker evaluated on every task, so the first task checked on a fresh branch fails it if that task's deliverable is a GitHub comment rather than a file. The worker cannot fix that; the only way out is a dispute against a blocker, or a commit that isn't theirs to make.
  How reachable it is, precisely: for *this* spec it is narrow, because the #106 task cannot in fact be commit-less. The existing `malformed` fixture writes the literal string `NOT_JSON` (`test_codex_courier.py:178-182`), not #106's embedded-quote payload, so spec.md:26's conditional resolves to "add a case", and C-5's backstop (which now names C-6) independently demands one. It is still not something the constitution should be leaning on the decomposition to avoid. Fix in one sentence of text: say the commit arm applies to a task that changed a tracked file, and that a task whose only deliverable is off-repo (a posted comment) satisfies it vacuously — or move the arm into a job-completion clause that runs once at the end, since what it exists to protect is C-2's walk and C-10's log, neither of which needs it on every task.
  Your second question — a checker arriving between a worker's edit and its commit — is not a defect. That FAIL is legitimate and the worker's remedy is to commit. The only residue is that C-5's check still calls that state "usual" (constitution.md:80) while C-11 makes it a blocker; one sentence in C-5's rationale, and the `cp -a` venue itself stays exactly as it is, since it is the only venue that carries untracked files.

- **C-7's count** (minor): six operations, not five.
  constitution.md:92 says "five operations across the four sites" and then enumerates site 1 twice, site 2 twice, site 3 once, site 4 once. The coverage is complete — every site the text names now carries an operation, and `test_hooks.py` is genuinely checked, which was the r1 finding — so nothing is hidden behind the wrong number. But the number was added so coverage could be audited by counting, and it fails that test on its own terms. Say six.

- **Minors worth folding in while the document is open** (none of these alone would fail it): C-10's check enumerates six tells and omits the two its own text names — title-cased headings and unspaced em dashes — so ` — ` ships past it. C-14 says `_courier_lib.repo_relative` "stays exactly as it is" and nothing checks it; probed, a pass-through stub leaves both courier suites green, so C-1 is not a backstop — one clause of the check ("confirm the helper is unchanged") closes it. C-4's "never a fabricated `0`" is a runtime claim neither C-4 nor C-15 observes. C-2's "editing a generated copy by hand fails this clause even when `--check` is momentarily quiet" is unreachable by construction, which is harmless but uncheckable. C-12 still passes on a line placed in a job guarded by `if: false` — implausible, and the plausible case (commented out) is now correctly caught. For symmetry only: spec.md:58's and spec.md:66's named test cases are held up by C-5's zero-tests backstop alone, where #128's equivalents are named case by case in C-7 and C-13; r1 accepted the backstop there and I am not changing that bar.

---
task: CON-audit
checker: auditor
vendor: anthropic
model: claude-opus-5
verdict: PASS
checked_at: 2026-08-12T04:30:08Z
duration_ms: None
cost_usd: None
---

## Per-clause results

| clause | severity | description | evidence |
| ------ | -------- | ------------ | -------- |
| C-1 | info | PASS, re-verified on a fresh probe copy at the job branch. All seven suites green. | `check-build.sh '<the seven suites>'` on probe copy `scratchpad/p3` (branch `chore/courier-lane-cleanup` off `main` @ `8899b42`) → `43 passed, 0 failed`, `check-build.sh: exit 0`. |
| C-2 | info | The r2 blocker is closed on this clause's side. The `.claude/` pair is named in the text and the deadlock with C-9 is gone for every source this job actually edits. | constitution.md:74. Probe: append a sentence to `guild-core/roles/checker-courier.md`, `python3 scripts/build-plugin.py` → four files change, including `.claude/agents/checker-courier.md`; `--check` exit 0 and C-9 exit 0 on the same tree. |
| C-2 | info | Mapped every source class the spec requires this job to edit against its build outputs. All of them land inside C-9's allowlist. | Edited one file in each of `.agent-guild/scripts/`, `.agent-guild/hooks/`, `.agent-guild/schemas/`, `.agent-guild/templates/`, `.agent-guild/CLAUDE.md`, `docs/plugin-readme.md`, rebuilding after each: outputs land only under `plugin/…` and `plugins/agent-guild/…`. `guild-core/roles/` adds `.claude/agents/…` and `plugins/agent-guild/project-template/.codex/agents/…`. |
| C-2 | minor | The output enumeration is still incomplete. The build writes six output locations; the clause names four. | constitution.md:74 says the outputs "are `plugin/`, `plugins/agent-guild/`, and … `.claude/agents/` and `.claude/skills/`". `main()` also calls `write_marketplaces()` (`scripts/build-plugin.py:1199`), which writes `.claude-plugin/marketplace.json` (`:57-59`) and `.agents/plugins/marketplace.json` (`:60-62`), and `run_check()` verifies both (`:1086-1098`). Measured the write set directly by mtime after a build: `plugin/`, `plugins/agent-guild/`, `.claude/agents/`, `.claude/skills/`, `.claude-plugin/marketplace.json`, `.agents/plugins/marketplace.json`. |
| C-2 | info | The clause's own check is not fooled by that omission — `--check` verifies the marketplaces whatever the text enumerates. The gap bites through C-9's allowlist, not through C-2's check. | `run_check()` marketplace arm at `scripts/build-plugin.py:1086-1098`. Probed: revert a regenerated `.claude-plugin/marketplace.json` → `--check` prints `generated marketplaces are stale relative to release metadata (1 difference(s))`, exit 1. |
| C-2 | minor | Carried from r2, unchanged and still harmless: "Editing a generated copy by hand … fails this clause even when `--check` is momentarily quiet" is unreachable by construction. | constitution.md:74. A hand-edit byte-identical to the build output leaves nothing `--check` can observe; an imperfect one is exactly what `--check` catches. |
| C-3 | info | PASS, text and check unchanged since r1. Branch precondition, merge-base diff, and porcelain sweep still cover committed, unstaged, and untracked archive edits. | constitution.md:80-81. |
| C-4 | info | PASS. Types re-read against the live schema; the 21-row count is right. | constitution.md:86-87. `vendor-call.schema.json` top level: `duration_ms` `integer`, `exit_code` `integer`, `tokens_in`/`tokens_out` `["number","null"]` — the types C-4 requires of the `discarded` members. |
| C-4 | info | The r2 minor is closed, by C-15 rather than by C-4. "Never a fabricated `0`" is now observed. | constitution.md:153: "Read the code that builds a discarded entry and confirm an attempt whose usage the vendor never reported writes `null` … rather than `0`". The claim is reached by the document, which is the bar; a checker holding C-4 alone still does not see it. |
| C-5 | info | PASS. The r2 rationale minor is closed — the sentence that described a tree state C-11 forbids is gone, and the replacement explains the venue without contradicting C-11. | constitution.md:93: "C-11 requires the tree to be clean outside `state/`, so in the ordinary case the copy and a clone would agree; the copy is specified because it is correct in both states and a clone is correct in only one." |
| C-6 | info | PASS, unchanged. Four criteria, the amendment note, and "asserted rather than demonstrated" as an explicit FAIL condition. | constitution.md:98-99. |
| C-7 | info | FIXED. The count is right now: site 1 twice, site 2 twice, site 3 once, site 4 once = six. | constitution.md:104-105, recounted against the enumeration. |
| C-7 | info | Anchors still resolve. `check(label, cond, detail="")` is the real idiom in both files the clause tells a checker to read by label. | `.agent-guild/hooks/test_hooks.py:63`, `.agent-guild/scripts/test_compose_brief.py:19`. `second_opinion_debts` is live at `_lib.py:419` and its discharge routes at `:480`. |
| C-7 | info | Site 3 remains the one operation that cannot fail on a wrong-but-present denial. Unchanged from r2, still backstopped by site 4's required `test_hooks.py` case plus C-1. | constitution.md:105. |
| C-8 | info | PASS, unchanged. Every requirement in the clause's own list is a blocker when absent. | constitution.md:110-111. |
| C-9 | info | FIXED — the r2 blocker is closed, probed end to end, and the guard still rejects what it is for. | constitution.md:116-117. Role edit + rebuild → C-9 exit 0. Same tree plus an untracked `docs/oops.md` → prints `docs/oops.md`, exit 1. `--check` exit 0 on the same tree, so C-2 and C-9 are now jointly satisfiable on Step 1's own deliverable. |
| C-9 | minor | The two `.claude/` entries are wider than the build outputs they were added for, and `--check` does not close the gap for names it does not know. | `dogfood_diffs` iterates the fixed `GUILD_AGENTS` list (`scripts/build-plugin.py:923`) and the fixed skill lists (`:931`); a file under `.claude/agents/` or a directory under `.claude/skills/` outside those names is compared against nothing. Probed: an untracked `.claude/agents/rogue.md` passes C-9 (exit 0) and `--check` (exit 0). Committed, it also passes C-11. `plugin/` and `plugins/agent-guild/` do not have this property — `diff_trees` (`:888-914`) reports extras in both directions. |
| C-9 | minor | The allowlist permits a source (`scripts/plugin-src/`) whose build output it forbids, which reopens the r2 deadlock shape on work the spec does not require. | Probe: edit `scripts/plugin-src/plugin.json`'s `description`, rebuild → `.claude-plugin/marketplace.json` changes; C-9 prints it and exits 1. Revert that file and `--check` exits 1 with `generated marketplaces are stale`. Unlike r2's case the worker has a correct remedy (revert the plugin-src edit — no task requires one), so it fails safe rather than deadlocking. |
| C-10 | info | FIXED. The r2 minor is closed: both typographic rules the text carries are now in the check. | constitution.md:123: "every heading added or changed in a doc keeps its main words capitalized, and every em dash sits flush against the words on both sides with no surrounding spaces". |
| C-10 | info | Still non-vacuous under the revised C-11: `git log main..HEAD` has content whenever any work is on disk, and the #106 comment is prose C-10 names directly, so a commit-less task is still judged. | constitution.md:122-123 against constitution.md:128-129. |
| C-11 | info | FIXED, and the removal is correct. All four states probed on `scratchpad/p3`; the scope defect r2 raised is gone with the commit-count arm. | constitution.md:128-129. Rebuild left uncommitted → exit 1 naming all four files; committed → exit 0; clean branch with zero commits → exit 0; dirty only under `state/` → exit 0. |
| C-11 | info | Your subsumption argument holds, and I checked the escape routes rather than taking it on the reasoning. The clean-tree arm sees every path this job can write. | No deliverable path is gitignored (`git check-ignore` on `.claude/agents/…`, `.claude/skills`, `plugin`, `plugins/agent-guild`, `.agent-guild/{scripts,hooks,templates}/…`, `guild-core/roles/…`: none ignored). `.gitignore` covers `.agent-guild/state/*` only, which C-11 exempts anyway. So at every check point, work outside `state/` is either committed (visible to C-2's walk and C-10's log) or a FAIL. |
| C-12 | info | PASS, unchanged. The anchored regex still rejects the commented-out line, which was r1's finding. | constitution.md:134-135. |
| C-12 | minor | Carried from r2: a line placed in a second job guarded by `if: false` passes. No plausible worker reaches it. | `grep -E` cannot tell which `run:` block a line belongs to. |
| C-13 | info | PASS, unchanged. Fixture venue, pre-change-composer recovery, the anchored-prefix negative case, and the three-shapes read are all in place. | constitution.md:140-141. |
| C-14 | info | FIXED. The r2 gap is closed and the probe the clause now specifies does discriminate. | constitution.md:147. Ran it: `repo_relative(<abs path under root, no file>, root)` → `None`; with an existing file → `.agent-guild/state/spec.md`. A pass-through stub returns the relpath instead, so the check catches it where both courier suites stay green. |
| C-14 | minor | "Stays exactly as it is" is now partly checked rather than checked. One shape of edit to the helper still slides past. | constitution.md:146-147. The probe pins existence-gating only. Probed: `repo_relative('/etc/hosts', root)` returns `../../../../etc/hosts`, so the helper's out-of-root behaviour is the very thing C-14's failing example forbids in the script — a worker who "harmonizes" the helper by adding an out-of-root guard changes it and still returns `None` for the probed case. `git diff` on `_courier_lib.py` being empty would close it outright. |
| C-15 | info | PASS. The fabricated-zero read is a real addition and lands on code no fixture exercises. | constitution.md:153. Every other anchor re-confirmed in r2 (`malformed_once` at `test_codex_courier.py:178`, the 108/26 sum at `:480-481`). |
| C-15 | minor | The null convention is required of `codex-courier.py`'s discarded entries and not of `claude-courier.py`'s returned payload, which the same clause covers. | constitution.md:153 asks only that "the per-attempt figures are present in it". Narrow, because the Codex-host parent that writes the row is a declared non-goal (constitution.md:165). |
| coverage | info | Re-walked the spec section by section. Every deliverable maps to a clause that fails when it is skipped. | Step 0 → C-6, C-12; Step 1 → C-13, C-7, C-8, C-2; Step 2 → C-14, C-4, C-15, C-8; verification block → C-1, C-2; repo mechanics → C-11, C-10, C-9, C-3. |
| coverage | minor | Carried from r1 and r2 unchanged, recorded for symmetry only: spec.md:58's and spec.md:66's named test cases rest on C-5's zero-tests backstop, so partial test coverage passes where zero fails. | For #128 the cases are named one by one (C-7 site 4, C-13's three shapes). For #47/#116 they are not. The behaviours themselves are verified by C-14's and C-15's own repros, so what is thin is regression coverage, not verification. |
| coverage | minor | Not a constitution defect, but it will mislead a worker: the spec's line references into `_lib.py` are stale. | spec.md:39 cites `_lib.py:230` and spec.md:41 cites `_lib.py:281`; `second_opinion_debts` is at `_lib.py:419` and the `.denied` route at `:480` on `main` @ `8899b42`. No clause cites those numbers, so nothing fails — but the decomposition will copy them into a task. |
| overreach | info | No clause claims work the spec excludes, and the five non-goals still match the code. | `claude-courier.py` still never shells out to `ledger-append.py`; #106's close is still left to the user (constitution.md:166). |
| mechanics | info | Paperwork clean, routing split intact. | `python3 .agent-guild/scripts/check-job-spec.py --audit-id CON-audit` → `OK`, exit 0. C-1, C-2, C-3, C-9, C-11, C-12 are `check-build.sh` invocations; C-4 through C-8, C-10, C-13 through C-15 carry the `checker-judgment:` prefix C-13 makes load-bearing. Protected content is an honest "none". |
| mechanics | info | No two clauses contradict. Re-checked every overlap the r3 edits touch. | C-9's new `.claude/` entries vs C-2's `--check`: jointly satisfiable, probed. C-11's clean-tree rule vs C-5's copy venue: consistent after the r3 rewording. C-3's archive ban is still a stricter overlay on C-9's `state/` allowance. |

## Findings

PASS. The r2 blocker is closed and nothing of that severity replaced it. Six minors below, all with a one-line fix if you want them; none blocks the build, and I would not hold the document for any of them.

1. **C-2's output enumeration is still incomplete** (constitution.md:74). The build writes six locations, not four: `write_marketplaces()` at `scripts/build-plugin.py:1199` also produces `.claude-plugin/marketplace.json` and `.agents/plugins/marketplace.json`, and `--check` verifies both. I measured the write set by mtime rather than inferring it, so this list is complete: `plugin/`, `plugins/agent-guild/`, `.claude/agents/`, `.claude/skills/`, `.claude-plugin/marketplace.json`, `.agents/plugins/marketplace.json`.

   Why it is a minor and not a repeat of r2: those two files change only when `scripts/plugin-src/{plugin.json,marketplace.json,adapters/codex.json}` changes, and no step of the spec requires such an edit. I probed every source class the spec *does* require and all their outputs land inside C-9's allowlist. If a worker edits plugin-src anyway, C-9 (major) fires with a remedy the worker can actually take — revert it — rather than the r2 shape where reverting broke C-2. Fix, if you want it: name the two marketplace files in C-2's text, and either allowlist them in C-9 or drop `scripts/plugin-src/` from C-9's allowlist, since nothing in this job needs to edit it.

2. **C-9's `.claude/` entries are wider than the outputs they were added for.** `--check` only compares names it knows: `dogfood_diffs` walks the fixed `GUILD_AGENTS` list (`build-plugin.py:923`) and the fixed skill lists (`:931`). A file under `.claude/agents/` or a directory under `.claude/skills/` outside those names is compared against nothing. Probed: an untracked `.claude/agents/rogue.md` passes C-9 and `--check`; committed, it passes C-11 too. `plugin/` and `plugins/agent-guild/` do not have this hole — `diff_trees` reports extras in both directions. Tightening the two entries to the generated wrapper names, or adding one sentence to C-9's text that only `sync_dogfood`'s own outputs are permitted there, closes it.

3. **C-14's "stays exactly as it is" is partly checked, not checked.** The new probe pins existence-gating and nothing else. `repo_relative('/etc/hosts', root)` returns `../../../../etc/hosts` today — the helper's out-of-root behaviour is exactly what C-14's failing example forbids in `ledger-append.py`, which makes "harmonize the helper" a plausible worker move that the probe would not catch. Requiring `git diff` on `_courier_lib.py` to be empty would close it outright.

4. **C-15's null convention is asymmetric.** `codex-courier.py`'s discarded entries must write `null` rather than a fabricated `0`; `claude-courier.py`'s returned payload is only required to carry the per-attempt figures. Narrow, since the Codex-host parent that writes the row is a declared non-goal.

5. **Two carried-forward minors, unchanged and still not worth an edit.** C-2's "editing a generated copy by hand fails this clause even when `--check` is momentarily quiet" remains unreachable by construction (the only residual case is a hand-edit byte-identical to the build output). C-12 still passes on a line placed inside a job guarded by `if: false`.

6. **For the decomposition, not for this document.** spec.md:39 and spec.md:41 cite `_lib.py:230` and `_lib.py:281`; `second_opinion_debts` now lives at `_lib.py:419` and its `.denied` route at `:480`. No clause depends on those numbers, but a task file will copy them.

## Answers to the three questions

**1. Is the r2 blocker closed without opening another?** Yes, on the work the spec requires. Probed the whole path on a copy of the repo at `chore/courier-lane-cleanup`: role edit plus `python3 scripts/build-plugin.py` changes four files, C-9 exits 0 on that tree, `--check` exits 0 on the same tree, and adding a stray `docs/oops.md` still exits 1. Then I checked the general case instead of trusting the one probe: editing one file in each of `.agent-guild/{scripts,hooks,schemas,templates}`, `.agent-guild/CLAUDE.md`, and `docs/plugin-readme.md` and rebuilding after each produces outputs only under `plugin/` and `plugins/agent-guild/`. Allowing `.claude/agents/` and `.claude/skills/` does let one thing through that nothing else catches — a file outside `sync_dogfood`'s known name lists (finding 2). C-2's enumeration is *not* complete: two marketplace files are missing from it (finding 1), which is where the same deadlock shape survives, on a source the spec never asks anyone to touch. Reading `sync_dogfood()` rather than allowing all of `.claude/` was the right call; `.claude/settings.json` stays correctly out of scope.

**2. Does removing C-11's commit arm reopen the r1 finding?** No. Your reasoning is right, and I checked the escape routes rather than accepting it on the argument. The r1 finding was that C-10's `git log main..HEAD` and C-2's commit walk both go vacuous on an uncommitted tree. For that to happen now, work would have to exist on disk outside `.agent-guild/state/` while `git status --porcelain` stays quiet — which needs a gitignored deliverable path, and there isn't one (`.gitignore` covers `.agent-guild/state/*`, `node_modules`, `__pycache__`, `/dist/`, and a working-memory file; every path this job writes is tracked). So at every check point, on-disk work is either committed and visible to both clauses or it is a C-11 FAIL. The zero-commit clean branch that now passes is the genuinely empty case, where C-2's first arm still runs and C-10 still has the #106 comment to judge. Dropping the arm was also the right call on scope: the r2 defect was that a branch-level fact was being enforced per task.

**3. Anything still shippable-badly with no clause failing?** Nothing I would call major. The two coverage claims still outrunning their checks are C-14's "stays exactly as it is" (finding 3) and C-9's `.claude/` breadth (finding 2). C-7's site 3 stays the weakest operation in the document — a read that cannot fail on a wrong-but-present denial — but it was accepted in r2 on the strength of site 4's required `test_hooks.py` case plus C-1, and nothing has changed that. C-4's "never a fabricated `0`" is now reached, by C-15's check rather than C-4's own, which is fine at document level.

---
task: DEC-audit
checker: auditor
vendor: anthropic
model: claude-opus-5
verdict: FAIL
checked_at: 2026-08-12T05:17:24Z
duration_ms: None
cost_usd: None
---

## Per-task results

| task | severity | description | evidence |
| ---- | -------- | ------------ | -------- |
| T-008/T-009 | info | The r0 blocker is closed, and it is closed rather than relocated. I re-derived the seam from the source instead of taking the split on its word: the exact-key-set validator runs only on the Codex host and only against a payload whose `ledger.vendor` is `"claude"`, so the codex courier's own outcome never meets it. T-008 can add a top-level key to `_outcome`'s return without touching the comparison that broke the r0 scoping. | `subagent-return.py:456` `if agent == "checker-courier"`, `:459` `if data.get("hook_host") == "codex"` gates `_codex_courier_outcome_ok`; `:225-227` `expected_ledger` pins `vendor: "claude"` and `CLAUDE_COURIER_MODEL`. The Claude-host branch at `:479-483` validates the written verdict file, not a key set. |
| T-008/T-009 | info | The lane isolation holds at the file level too. `_append_ledger` is defined once, in `codex-courier.py`, and `claude-courier.py` has no ledger subprocess at all — so T-008's argv work cannot reach the other lane by any route. The single shared surface is `_courier_lib.ledger_record`, and T-008 is told in as many words not to touch it. | `grep -rn ledger_record` over `.agent-guild/` and `guild-core/` returns exactly two call sites: `codex-courier.py:545`, `claude-courier.py:317`. `_append_ledger` at `codex-courier.py:643`, called once at `:628`; no equivalent in `claude-courier.py`. `tasks/T-008.md:49`. |
| T-008/T-009 | info | The ordering opens no inconsistent window. T-008 edits no file the claude lane reads, and T-009's widening is additive and optional, so the codex `ledger` dict's shape does not move under it. T-008's new `test_codex_courier.py` assertions are inside C-1's block, which every later task re-runs, so a T-009 regression of the codex row has a live check standing on it. | `tasks/T-008.md:38, 49`; `tasks/T-009.md:52, 54`; constitution.md:69 (C-1 names `test_codex_courier.py`). |
| T-009 | major | The excerpt never names the keys or the shape the per-attempt figures take in the returned payload, and neither does the clause, so the worker invents them and the checker has nothing to judge them against. "Widen `ledger_record` to carry the per-attempt figures as optional keys" (`:52`) and "the same shape as the codex one" (`:56`) both point at a shape T-008 deliberately kept *out* of `ledger` — T-008's figures ride a separate `_append_ledger` argument, so there is no codex `ledger` shape to mirror. Meanwhile `attempts` already exists as a top-level outcome key the validator pins to `1 or 2`, so a worker who adds `attempts` inside `ledger` duplicates it and one who adds only `discarded` is equally defensible. | `tasks/T-009.md:52`, `:56`; `tasks/T-008.md:49`; `subagent-return.py:196-198` (`attempts` already top-level, `in {1, 2}`); constitution.md:152 says only "the per-attempt figures are present in it"; C-4's five-key `discarded` entry shape (constitution.md:86) is pinned for the schema, not for this payload. |
| T-009 | major | Item 4 asks for an assertion against a value that is not exported. `ledger_fields` is a local inside `_codex_courier_outcome_ok`, so "asserts the accepted key set matches what `claude-courier.py` actually emits" cannot be written as a set comparison without first hoisting it to module scope — a change to the hook the task does not authorize and no clause covers. The check has to be behavioral: build the payload, feed it to the validator, assert acceptance. T-009's own `check_method` already says exactly that for the checker; the excerpt does not say it for the worker. | `subagent-return.py:206-218` (`ledger_fields = {` inside the function); `tasks/T-009.md:58` vs `tasks/T-009.md:25-28`. |
| T-009 | minor | The venue for item 4 is the harder of the two available, and the excerpt does not acknowledge it. `test_codex_adapter.py` is a `unittest` suite that runs the adapter as a subprocess; it has no fake-vendor harness and no importer for a hyphenated script. `test_claude_courier.py` has both. Putting the case where the stale fixture lives is defensible, but the worker should be told it has to reach across directories and import by path. | `test_codex_adapter.py:11-23` (imports, `KIT_ROOT`, no importlib); `test_claude_courier.py:19-25` and its `FAKE_CLAUDE` harness. |
| T-009 | minor | Nothing requires `_courier_lib.py` and `subagent-return.py` to land in one commit. Split across two, the branch carries a commit where the helper emits keys the validator still refuses — the r0 blocker, present in the history rather than at the tip. C-2 walks every commit but runs only `build-plugin.py --check`; no clause runs a suite per commit, so nothing catches it. | constitution.md:75 (C-2's per-commit loop is `build-plugin.py --check` only); `tasks/T-009.md:52`, `:54`. |
| T-009 | minor | The spec asks `test_claude_courier.py` for a quota case as well as a retried-crossing case; T-009's item 5 asks only for the retried crossing. C-15's quota operation reads on the codex harness, so T-008 covers its own side and nobody covers this one. | spec.md:66 "…and that a quota abandonment stays distinguishable (`quota_event` unchanged)" names both suites; `tasks/T-009.md:60`; constitution.md:153 ("Drive a quota abandonment") is scoped to `test_codex_courier.py`'s harness. |
| T-009 | info | Sizing and routing hold. Five sites, each named, none large: a helper signature, a key-set comparison, a loop capture mirroring one T-008 just built, and two test cases. That is `worker-standard` work and `checker-judgment` follows from C-5 and C-15. It is the widest task remaining, and the only reason it does not need splitting again is that four of the five edits are a handful of lines each. | `tasks/T-009.md:6-8`, `:52-60`. |
| T-008 | major | The excerpt's own instruction routes a new key into the courier's published stdout, and the excerpt warns about the wrong artifact. `_persist` receives `outcome` and calls `_append_ledger(outcome["ledger"], …)`, so per-attempt data reaching that call has to ride in `outcome`; `main()` then publishes every outcome key except `raw_attempts` to stdout as the courier's return contract. The task warns carefully about `raw_attempts` being persisted and says nothing about the stdout shape the alternative changes — which is the route it is steering the worker onto. | `codex-courier.py:628` `_append_ledger(outcome["ledger"], artifacts, project_root, brief)`; `:741` `published = {k: v for k, v in outcome.items() if k != "raw_attempts"}`; `tasks/T-008.md:47` (the `raw_attempts` warning), `:49` (the instruction). |
| T-008 | major | The pre-existing crash at `test_codex_courier.py:503` needs an owner and T-008 is the only candidate. `outcome["persisted"]["raw_path"]` is unguarded where its two neighbours guard (`:461` `if outcome else {}`, `:485` `outcome is not None`), so a run that exits non-zero raises `TypeError` inside a bare `try/finally` and the suite dies with a traceback. That is precisely the state C-5's mutation arm induces, and T-008's new retried-crossing cases go in that block. | `test_codex_courier.py:503`, `:505` against `:461`, `:485`; `:477-478` `finally: temp.cleanup()` with no `except`; constitution.md:93 ("run that suite, and confirm the case fails … report … the observed failure text for each case"). |
| T-008 | info | Clause set and tier are right for the narrowed scope. Two files, one source and one suite, with every edit point named by symbol — the same shape as T-004, T-006, and T-007. The `raw_attempts` warning and the `ledger_record` prohibition both landed and both read correctly against the code. | `tasks/T-008.md:38-58`; `_courier_lib.py:118-135`; `codex-courier.py:590-593`. |
| T-003 | info | The C-7 scoping is coherent and every operation it keeps is runnable against T-003's own tree. C-7 carries six operations across four sites; site 1 is compose-brief's exit 3 and is the only one T-003 excludes. Site 2 (twice) calls `second_opinion_debts` against a fixture project, site 3 reads `dispatch-guard.py`, site 4 reads `test_hooks.py` — all three are T-003 deliverables and all three exist at check time. Four of six operations, and the two dropped are the two that cannot exist yet. | constitution.md:105 (the six operations); `tasks/T-003.md:18-24`; `tasks/T-003.md:45-52` builds exactly sites 2, 3, and 4. |
| T-003 | info | The scoping closes the r0 finding at the step that creates it. Both defects r0 named — a `.skipped` denial placed after `reserve_crossing`, and a far-lane case asserting the wrong direction — are now read at T-003 by site 3 and site 2's refile respectively, rather than one task later. | constitution.md:105 site 3 ("say where its `stem` is computed relative to the denial") and site 2 ("refile the same marker under the far host's lane suffix"); `tasks/T-003.md:20-22`. |
| T-004 | info | T-004's full C-7 citation still makes sense after the split, and not only as ceremony. It is the first tree in which all four sites coexist, so it is where site 1's stderr and no-brief behavior get read at all; and re-reading sites 2 through 4 is the only check standing between T-004 and a semantic regression of the discharge route, which C-1 and C-9 would both pass through silently. | constitution.md:104-105; `tasks/T-004.md:17-19`. |
| T-004 | minor | A C-7 failure at T-004 on sites 2, 3, or 4 names code T-004's worker is forbidden to touch. The excerpt says "do not touch `_lib.py` or `dispatch-guard.py`", and the retry ladder copies the diagnosis into T-004's `## Rework diagnosis` and re-dispatches T-004's worker against it. One line in T-004's `check_method` — a site-2/3/4 failure is a T-003 reopen, not T-004 rework — costs nothing and removes the trap. | `tasks/T-004.md:68`; `.agent-guild/CLAUDE.md` retry ladder step 1. |
| T-004 | info | The fixture-collision fix landed and reads correctly. The excerpt now states that the suite goes red first, why (`- **check**: some check.` is unprefixed, so every existing task fixture becomes all-script), that the fix is the fixture rather than the filter, that loosening the prefix is C-13's own failing example, and that the byte-identical comparison is old-composer-against-new on the same fixture. All four r0 asks, all four present. | `tasks/T-004.md:60-64` against `test_compose_brief.py:55-78`; constitution.md:141, :143. |
| T-002 | info | The C-5 mutation fix landed with the stronger form, and the checker applied it rather than accepting the vacuous arm. | `tasks/T-002.md` `check_method` C-5; verdict at `state/verdicts/T-002-opus-r0.json`. |
| T-005 | info | The unowned spec.md:50 note has an owner and the right one. It is a short comment posted with `gh issue comment 34`, alongside three prose deliverables already going through the humanizer loop on `worker-craft`, and the excerpt tells the worker not to rule on #34. | spec.md:50; `tasks/T-005.md:57-61`. |
| T-005 | minor | The `discarded[]` forward reference got longer, not shorter. T-005 runs at position 5 and now documents a field that lands across T-007, T-008, and T-009; its parenthetical still says "built in T-007 and T-008". C-8 asks its checker to name "any behavior the code has that the document still contradicts", and the parenthetical is the only thing standing between that and a finding. | `tasks/T-005.md:45`; constitution.md:111; deps chain T-005 → T-006 → T-007 → T-008 → T-009. |
| T-007 | minor | Its negative boundary is stale. "Per-attempt timing and usage capture in `codex-courier.py` and `claude-courier.py` is T-008" now names the wrong task for half of it. Harmless in effect — both couriers are still forbidden — but it is the one seam statement the reshuffle left pointing at the old shape. | `tasks/T-007.md:71`; `tasks/T-009.md:2`. |
| coverage | blocker | **C-10 now judges nobody's commits on six of the nine tasks.** Scoping C-10 per task closed the misattribution, and closed it by dropping coverage rather than moving it. C-10 is cited on T-002 and T-005 only, and both are now scoped to their own commits — T-002's explicitly to `git log T-001s-commit..HEAD`. T-003, T-004, T-006, T-007, T-008, and T-009 each instruct the worker to run the humanizer over a commit message and to say why the change exists, and none of them cites C-10 or any other clause that reads a commit message. T-001's commit, which r0 flagged as judged against the wrong worker, is now judged against nobody at all. | `tasks/T-002.md:25-28` ("scoped with `git log T-001s-commit..HEAD`"); `tasks/T-005.md:20-23`; `clauses:` lines of T-003, T-004, T-006, T-007, T-008, T-009 — no C-10 on any; `tasks/T-003.md:60`, `T-004.md:70`, `T-006.md:65`, `T-007.md:73`, `T-008.md:64`, `T-009.md:68` all require the humanizer loop. spec.md:18 requires it of every step. C-10 also carries the commit-granularity rule (constitution.md:123), unenforced on the same six. |
| coverage | info | Every other spec section still has exactly one owner and the seams still state themselves negatively on both sides. Step 0 → T-001, T-002. Step 1 → T-003, T-004, T-005. Step 2 → T-006 (#47), T-007 (#116 contract), T-008 (codex lane), T-009 (claude lane and the parent validator). The new T-008/T-009 boundary is stated from both directions. | spec.md:22-66; `tasks/T-008.md:38, 49`; `tasks/T-009.md:52`. |
| coverage | minor | The branch deviation is still unrecorded, and it is still minor. C-11 pins one branch by name, so the choice is visible to anyone reading the constitution beside the spec; what is missing is the reason. | spec.md:18; constitution.md:128. |
| coverage | info | The spec.md line-number call is right and I would not reopen it. `spec.md` carries a `source: file` / `ref:` provenance header, and no task or clause reads `_lib.py:230` or `:281` any more — every excerpt names the symbol instead, and I verified in r0 that each one resolves. A verbatim-copy guarantee is worth more than a pointer nobody follows. | spec.md:1-5, :39, :41; `tasks/T-003.md:45` names `second_opinion_debts` and `courier_lane(data)` by symbol. |
| routing | info | Routing holds across all nine, in both directions. T-009 at `worker-standard`/`checker-judgment` is correct: clear-spec implementation judged on correctness, with C-5 and C-15 both rubrics. Nothing mechanical sits on opus and nothing judgment-bearing on haiku. | `tasks/T-00{1..9}.md` frontmatter against CLAUDE.md's routing table. |
| mechanics | info | `deps` remain a valid DAG: the single chain T-001 → … → T-009, no cycles, every referenced task present. The serialization is forced by C-2, C-11, and one worktree, as before; the split adds a link and no branch. | `deps` across all nine. |
| mechanics | info | Paperwork clean. `check-job-spec.py --audit-id DEC-audit` exits 0, and `DEC-audit-r0.md` is the only prior round, so this is r1. | `python3 .agent-guild/scripts/check-job-spec.py --audit-id DEC-audit` → `OK`, exit 0; `ls state/verdicts/ | grep DEC` → `DEC-audit-r0.md`. |

## Diagnosis

- **coverage** (blocker): six of nine tasks now ship commit prose that no clause reads.

  The r0 fix worked on the symptom I named and not on the one I should have named. I offered scoping C-10 per task as a one-line edit to T-002's and T-005's `check_method`, and that was incomplete advice: per-task scoping is only equivalent to branch-wide scoping if *every* task that produces a commit cites C-10. Six do not.

  ```
  T-003 clauses: [C-1, C-2, C-5, C-7, C-9, C-11]           T-003.md:60  "Run the `humanizer` skill over the commit message"
  T-004 clauses: [C-1, C-2, C-5, C-7, C-9, C-11, C-13]     T-004.md:70  same
  T-006 clauses: [C-1, C-2, C-3, C-5, C-9, C-11, C-14]     T-006.md:65  same
  T-007 clauses: [C-1, C-2, C-3, C-4, C-5, C-9, C-11]      T-007.md:73  same
  T-008 clauses: [C-1, C-2, C-3, C-5, C-9, C-11, C-15]     T-008.md:64  same
  T-009 clauses: [C-1, C-2, C-3, C-5, C-9, C-11, C-15]     T-009.md:68  same
  ```

  Each of those six asks the worker for a commit message that says why the change exists and that has been through the humanizer loop. C-10 is the clause that judges exactly that, it is the only clause in the document that reads a commit message, and it is cited on neither. C-10 also carries the granularity rule — "one logical change per commit, and a commit mixing two unrelated changes is a finding" (constitution.md:123) — which is now unenforced across the same six, on a job whose every task commits source plus regenerated packages together.

  This is a spec requirement with no owner, which is the thing DEC-audit exists to catch. spec.md:18 states it for every step: "Commit messages and any human-facing doc prose go through the `humanizer` skill before commit."

  Fix: add C-10 to the `clauses` list of T-003, T-004, T-006, T-007, T-008, and T-009, each with the scoped `check_method` T-002 and T-005 already carry — `apply the check text of clause C-10 verbatim to the commit message this task produced, scoping the commit-log half to this task's own commits rather than the whole branch`. Every one of the six already routes to `checker-judgment`, so no routing moves and no tier changes. This is six frontmatter lines.

  T-001 is the one loss you cannot recover cheaply: it is `complete`, its checker was `checker-deterministic`, and T-002's C-10 now explicitly excludes its commit. I would not reopen it. Either accept its message as unjudged and say so in the retrospective, or hand a final sweep of `git log main..HEAD` to T-005's C-10 as a second, branch-wide arm — but if you do the latter, say in T-005's `check_method` that a tell in an earlier task's commit is reported and not reworked, or you have rebuilt the exact misattribution r0 objected to.

- **T-009** (major): the payload's shape is pinned by nothing, so the worker invents it and the checker cannot judge it.

  Three sentences point at three different shapes. The excerpt says to widen `ledger_record` "to carry the per-attempt figures as optional keys" (`:52`) and to capture them in claude-courier "the same shape as the codex one" (`:56`) — but T-008 deliberately keeps the codex figures *out* of `ledger`, passing them to `_append_ledger` as a separate argument (`T-008.md:49`), so there is no codex `ledger` shape to mirror. And C-15 asks its checker only to "confirm the per-attempt figures are present in it" (constitution.md:152), which any of the candidate shapes satisfies.

  The candidates are not equivalent. `attempts` already exists as a top-level outcome key the validator pins to `1 or 2` (`subagent-return.py:196-198`), so adding `attempts` inside `ledger` duplicates it; adding only `discarded` is equally defensible; and nothing says whether a `discarded` entry here carries C-4's five keys (`reason`, `duration_ms`, `exit_code`, `tokens_in`, `tokens_out`) or something the worker finds convenient. Since this payload's whole purpose is to become `ledger-append.py` flags in a later job, the two lanes emitting different discarded-entry shapes is a defect that will not surface until that job runs.

  Fix: name the shape in the excerpt. The `ledger` payload gains `discarded` only — `attempts` is already carried at the outcome's top level — and a `discarded` entry carries the same five keys with the same types C-4 pins for the schema, so both lanes emit one shape. Then add that to T-009's C-15 `check_method` so the checker has something to fail against.

  Two smaller things in the same excerpt. Item 4 asks the worker to assert "the accepted key set matches what `claude-courier.py` actually emits", but `ledger_fields` is a local inside `_codex_courier_outcome_ok` (`subagent-return.py:206`), so there is no key set to compare against without hoisting it — say the assertion is behavioral, build the payload and feed it to the validator, which is what T-009's `check_method` already tells the checker to do. And item 4's venue, `test_codex_adapter.py`, is a `unittest` suite with no fake-vendor harness and no path importer; the worker has to reach across directories and import a hyphenated script. That is the right venue — the stale fixture is the reason the gap was invisible — but it should be told, not discovered.

- **T-008** (major): the excerpt's own instruction changes the courier's stdout contract, and the warning it gives is about the other artifact.

  "Pass the per-attempt data to `_append_ledger` as its own argument" (`T-008.md:49`) has one call site: `_persist` at `codex-courier.py:628`, which receives `outcome` and nothing else. So the data has to ride in the outcome dict — and `main()` publishes every outcome key except `raw_attempts` to stdout:

  ```
  codex-courier.py:741   published = {k: v for k, v in outcome.items() if k != "raw_attempts"}
  ```

  That stdout JSON is the courier's return contract, the thing `checker-courier.md` tells the courier to relay. The task warns carefully that `raw_attempts` is serialized into `courier-raw/` and says nothing about the shape the alternative changes, which is the shape its own instruction steers the worker toward.

  Fix: one sentence. A new top-level outcome key is published to stdout unless it is stripped alongside `raw_attempts` at `:741` — decide which, deliberately. Either answer is fine; discovering it after the fact is not.

- **T-008** (major): the crash at `test_codex_courier.py:503` needs an owner, and T-008 is the only one.

  You asked directly, so: yes, give it to T-008.

  ```
  test_codex_courier.py:503        outcome["persisted"]["raw_path"] is not None
  test_codex_courier.py:461        verdict = outcome["verdict"] if outcome else {}
  test_codex_courier.py:485        outcome is not None and outcome["status"] == "verdict"
  ```

  Its two neighbours in the same block guard for a `None` outcome and it does not, and it sits inside a bare `try/finally` (`:477-478`), so a run whose courier exits non-zero raises `TypeError` and takes the whole suite down with a traceback rather than a `FAIL` line.

  That is not a hypothetical state — it is the state C-5's mutation arm manufactures. C-5 tells the checker to back out the production edit and "confirm the case fails … report the copy's path, the mutation applied, and the observed failure text for each case" (constitution.md:93). Back out the per-attempt capture while `_append_ledger` still passes `--attempts`, and the courier raises, `outcome` is `None`, and the suite dies at `:503` — which is the block T-008's own new cases go into. The checker gets a traceback instead of a demonstration that the new case discriminates, and every case after `:503` never runs at all.

  It is not a defect in the delivered tree: on a green run `outcome` is populated and C-1 passes, which is why nobody has owned it. It is a defect in the instrument T-008's own check depends on, in a file T-008 already has open, and it is a two-line guard.

  One thing to say in the task if you assign it: the guard is not a behavior test, so C-5's mutation arm does not apply to it — there is no production change behind it to back out. Without that sentence you have recreated the vacuous-mutation problem you just fixed on T-002, in the task that can least afford a wasted rework cycle.

## Minor findings

Six, none of which would hold the decomposition on its own.

1. **T-009's two edits should land in one commit and nothing says so.** `_courier_lib.ledger_record` and `subagent-return.py`'s key set are the two halves of one contract change. Split across two commits, the branch carries a commit where the helper emits keys the validator refuses — the r0 blocker, sitting in the history instead of at the tip. C-2 walks every commit but runs only `build-plugin.py --check` (constitution.md:75); no clause runs a suite per commit, so nothing catches it. One sentence in the excerpt.

2. **The claude-side quota case has no owner.** spec.md:66 asks both courier suites to assert that "a quota abandonment stays distinguishable (`quota_event` unchanged)". C-15's quota operation reads on `test_codex_courier.py`'s harness, so T-008 covers its side; T-009's item 5 asks only for the retried-crossing case (`T-009.md:60`). Add it to item 5 or say why the claude lane does not need it — it never writes a ledger row, so there is a real argument that the outcome's `quota` status already carries this, but the argument should be in the file.

3. **A C-7 failure at T-004 lands on a worker forbidden to fix it.** T-004 re-runs all four of C-7's sites; three of them are T-003's code, and T-004's excerpt says "do not touch `_lib.py` or `dispatch-guard.py`" (`:68`). The retry ladder would copy that diagnosis into T-004's rework section and re-dispatch T-004's worker against it. One line in T-004's `check_method`: a failure at site 2, 3, or 4 is a T-003 reopen, not T-004 rework.

4. **T-007's negative boundary points at the old shape.** "Per-attempt timing and usage capture in `codex-courier.py` and `claude-courier.py` is T-008" (`T-007.md:71`) now names the wrong task for the claude half. Both couriers stay forbidden either way, so nothing breaks; it is the one seam sentence the reshuffle left behind.

5. **T-005's `discarded[]` forward reference got longer.** The field now lands across T-007, T-008, and T-009, and the parenthetical still reads "built in T-007 and T-008" (`T-005.md:45`). C-8's checker is asked to name "any behavior the code has that the document still contradicts", and that parenthetical is the whole mitigation. Update it or move the sentence.

6. **The branch deviation, and my answer to your question.** Record it, and not in the constitution's clauses. spec.md:18 says one branch per step; C-11 pins one branch by name, so the *choice* is already visible to anyone reading the two documents together — only the reason is missing. Two venues cost nothing: a sentence in the constitution's existing HTML-comment preamble, which touches no clause and moves no check, or one line in the Phase 3 retrospective. I would not amend a clause for it. What I would not do is leave it in neither, because the #106 close is the same kind of deviation and it reads cleanly precisely because constitution.md:166 says why.

## Answers to the questions asked

**1. Does the T-008/T-009 split close the blocker or relocate it?** It closes it, and I re-derived the seam rather than accepting the split's own account. Two facts do the work. First, the exact-key-set validator is Codex-host-only and additionally pins `ledger.vendor == "claude"` (`subagent-return.py:459`, `:225-227`), so the codex courier's outcome never reaches it — T-008 can add a top-level key safely, which is the freedom the r0 scoping did not have. Second, the lanes share exactly one function: `ledger_record` has two call sites and `_append_ledger` exists only in `codex-courier.py`, so T-008's argv work has no route into the claude lane at all. T-008 avoiding `ledger_record` therefore does keep `--attempts`/`--discarded` out of the shared payload, and it does so structurally rather than by the worker's care.

The ordering opens no window. T-008 edits no file the claude lane reads; T-009's widening is additive and optional, so the codex `ledger` dict does not change shape under it; and T-008's new assertions live in `test_codex_courier.py`, which C-1 re-runs at T-009, so a regression of the codex row has a live check on it. The one seam worth naming is inside T-009, not between the two: its helper edit and its validator edit have to land together, or the branch carries a commit with the original bug in it (minor 1).

**2. Is T-009 sized, routed, and self-containable?** Sized and routed yes — five small named edits, `worker-standard` with `checker-judgment`, and it is only the widest remaining task because four of the five are a handful of lines each. Self-containable no, on three counts, and the first is the one that matters: dispatched on this file I would have to invent the payload's key names and the shape of a `discarded` entry, with the excerpt pointing me at a "same shape as the codex one" that T-008 deliberately does not have, and with C-15 too loose to correct me. I would also reach for a `ledger_fields` constant that does not exist, and I would discover on my own that `test_codex_adapter.py` has no harness for calling the courier. The first is a major; the other two are cheap sentences.

**3. Is T-003's C-7 scoping coherent?** Yes, and it is exactly scoped. C-7 carries six operations across four sites; T-003 excludes site 1's two and keeps four, and all four run against T-003's own tree — site 2 calls `second_opinion_debts` against a fixture project, site 3 reads `dispatch-guard.py`, site 4 reads `test_hooks.py`, all three T-003 deliverables. Both defects r0 named are now read at the step that creates them. T-004's full citation still makes sense and is not ceremony: it is the first tree where all four sites coexist, so site 1 is judged there for the first time, and re-reading sites 2 through 4 is the only thing standing between T-004 and a semantic regression of the discharge route that C-1 and C-9 would both pass. The one thing to add is where a T-004 C-7 failure gets routed (minor 3).

**4. What the reshuffle introduced, and what is still uncovered.** The reshuffle itself introduced one stale seam sentence in T-007 and nothing else; the split is clean on both sides. The gap that matters was introduced by a different fix: scoping C-10 per task dropped commit-message coverage on six of nine tasks and on T-001, and that is the blocker above. Beyond it, the coverage holes are the claude-side quota case (minor 2) and the crash at `test_codex_courier.py:503`, which is not spec coverage but check-instrument coverage.

**5. Does the crash need an owner?** Yes, T-008. It is not a defect in the delivered tree — on a green run `outcome` is populated and C-1 is quiet, which is why it has survived — but it is a defect in the instrument T-008's own C-5 arm runs on, in a file T-008 already has open, and it is a two-line guard. Give it to T-008 with an explicit note that the guard is exempt from C-5's mutation arm, since there is no production change behind it to back out.

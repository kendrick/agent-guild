---
task: DEC-audit
checker: auditor
vendor: anthropic
model: claude-opus-5
verdict: PASS
checked_at: 2026-08-12T05:27:53Z
duration_ms: None
cost_usd: None
---

## Per-task results

| task | severity | description | evidence |
| ---- | -------- | ------------ | -------- |
| coverage | info | **The C-10 blocker is closed.** Every commit-producing task cites it: T-002 through T-009, eight of nine, and T-001 is the only task on the branch without it. I read the `clauses:` line of all nine rather than taking the count on report. The six that gained it were already `checker-judgment`, so no routing moved and no tier changed, exactly as the fix predicted. | `clauses:` at `tasks/T-002.md:5`, `T-003.md:5`, `T-004.md:5`, `T-005.md:5`, `T-006.md:5`, `T-007.md:5`, `T-008.md:5`, `T-009.md:5`, each carrying C-10; `tasks/T-001.md:5` without it. `checker: checker-judgment` on all eight. |
| coverage | info | The scoping is runnable and no task is double-billed. Seven of the eight carry the same exclusion in as many words — "An earlier task commit is not this worker to rework" — so even a checker who over-reads `git log` cannot land a predecessor's tell in this worker's rework section. T-005 states the scoping without the exclusion sentence, which is harmless because the scoping alone already bounds it. | `tasks/T-003.md:27-30`, `T-004.md:22-25`, `T-006.md:21-24`, `T-007.md:27-30`, `T-008.md:24-27`, `T-009.md:22-25`, `T-002.md:25-28`; `tasks/T-005.md:20-23` (scoping, no exclusion sentence). |
| coverage | minor | The boundary is named on one task and left to inference on the rest. T-002 anchors concretely — "scoped with `git log T-001s-commit..HEAD`" — while T-003 through T-009 say "scoped with `git log` to the commits of this task", and nothing in the task file records the SHA that was HEAD when the task was dispatched. A checker can recover it (the chain is serial and each task's commits touch its own artifacts), and the failure mode is under-scoping rather than misattribution, so this reads a false pass at worst and never a false fail. | `tasks/T-002.md:26` against `tasks/T-006.md:22-23`; `deps` chain across all nine; no base-SHA field in any task's frontmatter. |
| T-001 | info | The unjudged commit is a smaller loss than r1 priced it at. I read the message rather than assuming it: it gives the cause — the sole test of #106's guarantee was running locally and nowhere else — rather than restating the diff, and its one em dash is flush on both sides. It would have passed C-10 had C-10 been cited. It should still go in the retrospective as unjudged, because the record is the point, not the outcome. | `git log main..HEAD`, commit `9985472` "ci: run test_codex_courier.py in the CI suite"; constitution.md:130-131, :133. |
| T-009 | info | **Pinning the payload to `discarded` alone is right, and the reason given is the correct one.** `attempts` is already a top-level outcome key that the parent validator pins to `1` or `2`, so a second one inside `ledger` is a duplicate of a field the contract carries and could disagree with it. Reusing C-4's five members with C-4's types is the other half: it is what keeps the two lanes emitting one shape when the flags are eventually threaded through. | `subagent-return.py:181-193` (`set(outcome)` requires `attempts`), `:197-199` (`attempts not in {1, 2}` → refused); constitution.md:94 (C-4's five members and their types); `tasks/T-009.md:62`. |
| T-009 | info | **Nothing downstream of `ledger_record` breaks when only that key is added**, and I traced every consumer rather than reasoning from the seam. There are exactly two call sites and one validator. `_append_ledger` builds its argv from named keys and never iterates the dict, so an eleventh key on the codex lane is inert. The codex outcome never meets the exact-key-set validator, which is Codex-host-only and additionally pins `ledger.vendor == "claude"`. And `main()`'s published stdout carries `ledger` as a nested object, so its shape moves but its key set at the top level does not. | `grep -rn ledger_record` over `.agent-guild/scripts`, `.agent-guild/hooks`, `guild-core`, `scripts/plugin-src` → `_courier_lib.py:118`, `claude-courier.py:317`, `codex-courier.py:545`. `codex-courier.py:643-660` (argv from named keys), `:628` (single call site), `:741`. `subagent-return.py:456-459`, `:225-227`. |
| T-009 | info | The mirror-image failure — widening the validator so the new key becomes *required* and every un-retried claude crossing is refused at the door — is the one shape that would reintroduce the r0 blocker, and it is already caught. `test_codex_adapter.py`'s `courier_outcome` fixture builds a ten-key ledger with `attempts: 1`, drives it through the real `subagent-return` gate, and asserts exit 0. An exact eleven-key comparison turns that into exit 2, and C-1 runs that suite on this task and every later one. | `test_codex_adapter.py:234`, `:249-261` (ten-key ledger, no `discarded`), `:866-867` (`run_adapter("subagent-return", payload)`, `assertEqual(result.returncode, 0)`); constitution.md:77 names `test_codex_adapter.py`. |
| T-009 | minor | Items 1 and 2 still read plural against item 3's singular. "Widen `ledger_record` to carry the per-attempt figures as optional **keys**" and "the point is to admit **these two**" both survive from the pre-pin draft, while item 3 says "Add exactly one key, `discarded`" and forbids `attempts` with its reason. Item 3 is explicit enough to govern, so the likely damage is a validator that admits a key no courier emits — laxer than intended, not broken. | `tasks/T-009.md:56`, `:58` against `:62`. |
| T-009 | minor | Nothing still requires `_courier_lib.py` and `subagent-return.py` to land in one commit (r1 minor 1, still open). Split across two, the branch carries a commit where the helper emits a key the validator refuses — the r0 blocker sitting in history rather than at the tip. C-2 walks every commit but runs only `build-plugin.py --check`, so no suite runs per commit and nothing reads for it. | `tasks/T-009.md:56`, `:58`, `:74`; constitution.md:83. |
| T-009 | minor | The claude-side quota case is still unowned (r1 minor 2, still open). spec.md:66 asks both courier suites to assert that a quota abandonment stays distinguishable; C-15's quota operation reads on the codex harness, T-008 covers its side, and T-009's item 5 asks only for the retried crossing. | spec.md:66; `tasks/T-009.md:66`; constitution.md:161; `tasks/T-008.md:67`. |
| T-008 | info | **The crash claim re-derives against the current tree**, which matters because T-002 has since committed to that file. `outcome["persisted"]["raw_path"]` is still unguarded at `:503` and again in the detail string at `:507`, where its neighbour at `:487` guards with `outcome is not None`, inside a bare `try/finally`. The suite is green today — 46 passed, 0 failed — so this bites only in the state C-5's mutation arm manufactures, which is exactly why it has survived this long. | `test_codex_courier.py:503`, `:505`, `:507` against `:487`; `:509-510` `finally: temp.cleanup()` with no `except`; `python3 .agent-guild/scripts/test_codex_courier.py` → `46 passed, 0 failed`. The new retried-crossing cases go into the block at `:480-508`. |
| T-008 | info | **The exemption is tight and I could not find a stretch worth naming.** It is pinned by artifact and by file — "the guard this task adds to the crashing dereference in `test_codex_courier.py`" — not by a category like "harness fixes", so it does not reach the task's own new cases, which are behavior tests with production edits behind them. The reasoning is stated in both venues, and it is the same reasoning C-5 already implies rather than an override of it: the clause's mutation arm asks the checker to "back out the corresponding production edit", and for a guard there is no such edit, so the arm is vacuous rather than waived. | `tasks/T-008.md:18-21` (check_method), `:56` (excerpt); constitution.md:101. |
| T-008 | info | No conflict with C-1 or with C-5's existence backstop. The guard is a no-op on a green run (`outcome` is populated, the assertion is unchanged), so it cannot move C-1; and C-5's backstop lists C-6, C-7, C-13, C-14, C-15, none of which the guard touches. | constitution.md:77, :101. |
| T-008 | minor | The one thing the crash fix does create is a C-10 granularity question nobody answers. C-10 judges commit granularity — "one logical change per commit, and a commit mixing two unrelated changes is a finding" — and a harness guard is not the per-attempt capture. T-008's commit paragraph describes a single message about retries costing nothing measurable, so the default reading is one commit holding both. One sentence settles it. | constitution.md:131; `tasks/T-008.md:73`, `:56`. |
| T-008 | info | The stdout finding landed and reads correctly against the code. The excerpt now names both artifacts its own instruction reaches — `raw_attempts` serializing into `courier-raw/`, and the outcome published to stdout as the return contract — and suggests the parallel structure that leaves both alone. | `tasks/T-008.md:54` against `codex-courier.py:590-593`, `:741`. |
| T-004 | minor | A C-7 failure at sites 2, 3, or 4 still lands on a worker forbidden to fix it (r1 minor 3, still open). T-004 applies all six operations; three of the four sites are T-003's code, and T-004's excerpt says "do not touch `_lib.py` or `dispatch-guard.py`". The retry ladder would copy that diagnosis into T-004's rework section and re-dispatch T-004's worker against it. | `tasks/T-004.md:17-19`, `:72`; constitution.md:112-113; `.agent-guild/CLAUDE.md` retry ladder step 1. |
| T-007 | minor | Its negative boundary still names the old shape (r1 minor 4, still open): "Per-attempt timing and usage capture in `codex-courier.py` and `claude-courier.py` is T-008" is now half wrong. Both couriers stay forbidden either way, so nothing breaks. | `tasks/T-007.md:75` against `tasks/T-009.md:2`. |
| T-005 | info | I withdraw r1 minor 5. The role text's `discarded[]` sentence describes the **ledger row's** field, and that field is built by T-007 (schema and flags) and populated by T-008 (codex lane); T-009 builds the returned *payload*, which is a different artifact. The parenthetical naming T-007 and T-008 is accurate as written. | `tasks/T-005.md:45`; `tasks/T-007.md:45-52`; `tasks/T-008.md:58`; `tasks/T-009.md:56`. |
| constitution | info | The branch deviation is recorded, in the right venue, with the reason C-11 pins without stating — and the audited clause set is genuinely unchanged. I verified that rather than accepting it: every clause reference from r1 lands at exactly +8 lines today (C-4's members 86→94, C-5's check 93→101, C-7's operations 105→113, C-8's check 111→119, C-10 123→131, C-11 128→136, C-15 152→160, the #106 non-goal 166→174), and the inserted preamble paragraph is exactly 8 lines. A uniform shift with no residue is what a comment-only edit looks like, so CON-audit-r3's PASS still covers the document. | constitution.md:58-65 (inside the `<!--` at :3 closed at :71); `grep -c "^### C-"` → 15; the +8 mapping above against DEC-audit-r1's citations. |
| coverage | info | Walked the spec section by section again. Step 0 → T-001 (CI line), T-002 (replay, four ACs, amendment note, comment). Step 1 → T-003 (`_lib.py`, `dispatch-guard.py`, `test_hooks.py`), T-004 (partition, exit 3, three shapes), T-005 (role, contract, template, #34 note). Step 2 → T-006 (#47 and the `repo_relative` pre-pass), T-007 (#116 schema and appender), T-008 (codex lane), T-009 (claude lane and the parent validator). Every seam is still stated negatively from both sides. The one spec line only half-covered is the claude-side quota assertion, recorded as a minor above. | spec.md:22-66 against `tasks/T-00{1..9}.md`; `tasks/T-003.md:60`, `T-004.md:72`, `T-006.md:67`, `T-007.md:75`, `T-008.md:45`, `T-009.md:68`. |
| routing | info | Routing holds across all nine in both directions, and the C-10 additions moved nothing: all six recipients were already `checker-judgment`, since each cites at least one rubric clause. T-001 remains the only all-script task and the only `checker-deterministic`. Nothing mechanical sits on opus, nothing judgment-bearing on haiku. | `tasks/T-00{1..9}.md` frontmatter against CLAUDE.md's routing table. |
| mechanics | info | Paperwork clean. `deps` are the single chain T-001 → … → T-009, no cycles, every referenced task present. `check-job-spec.py --audit-id DEC-audit` exits 0, and r0 and r1 are the only prior rounds, so this is r2. | `deps` across all nine; `python3 .agent-guild/scripts/check-job-spec.py --audit-id DEC-audit` → `OK`, exit 0; `ls state/verdicts/ | grep DEC` → `DEC-audit-r0.md`, `DEC-audit-r1.md`. |

## Minor findings

Six. None holds the decomposition, and five of the six are one sentence each.

1. **The C-10 commit boundary is anchored on one task and inferred on six.** T-002 names it (`git log T-001s-commit..HEAD`); T-003 through T-009 say "the commits of this task" and leave the checker to derive the base. Derivable it is — the chain is serial and each task's commits touch its own artifacts — and the exclusion sentence means a mis-derivation costs a missed tell rather than a misattributed one. If you want it airtight for free, record each task's base SHA in the task file when you move it to `assigned`, or say "everything after the last commit that touches `<dep>`'s artifacts".

2. **T-009 items 1 and 2 still read plural against item 3's pin.** "Optional **keys**" and "admit **these two**" survive from the draft that had not yet chosen; item 3 says "exactly one key, `discarded`" and forbids `attempts` with its reason. Item 3 governs, so the damage is a validator admitting a key nothing emits. Two words.

3. **Nothing requires T-009's helper edit and validator edit to land in one commit** (r1 minor 1, unaddressed). Split, the branch carries a commit where `ledger_record` emits a key `subagent-return.py` refuses. C-2 walks every commit but runs only `build-plugin.py --check`, so no suite runs per commit and nothing reads for it.

4. **The claude-side quota case still has no owner** (r1 minor 2, unaddressed). spec.md:66 names both suites; T-009's item 5 asks only for the retried crossing. Add it, or say in the file why the claude lane does not need it — it writes no ledger row, so the outcome's `quota` status arguably already carries this, but the argument belongs in the task rather than in my head.

5. **A C-7 failure at T-004 still routes to a worker forbidden to fix it** (r1 minor 3, unaddressed). One line in T-004's `check_method`: a failure at site 2, 3, or 4 is a T-003 reopen, not T-004 rework.

6. **T-008's crash guard and its capture work are two logical changes in one commit unless you say otherwise, and C-10 reads for that.** Give the guard its own commit — the message writes itself, since a harness that dies mid-run is why the guard exists. Related, and cheaper: **T-007's negative boundary** (r1 minor 4) still says the claude courier "is T-008".

Worth flagging plainly, since your cover note says every r1 finding is addressed: the blocker and all three majors are, and I re-derived each rather than taking the report. Four of r1's six minors are not (1, 2, 3, 4 above map to r1's minors 1, 2, 3, 4). The fifth I have withdrawn myself as wrong, and the sixth — the branch deviation — is done.

## Answers to the questions asked

**1. Is the C-10 fix complete and correct?** Yes, on all three counts you asked about. Every commit-producing task cites it — eight of nine, T-001 excepted and left `complete`. The scoping is runnable: at any task's check time the branch is serial, the task's own commits are the tip-most ones, and they touch that task's artifacts. And no task is double-billed, because seven of the eight carry the explicit exclusion — an earlier task's commit is not this worker's to rework — so an over-reading checker reports rather than reworks. What I would tighten is the boundary anchor: T-002 names a concrete range and the other six do not, which makes under-scoping the plausible error. That is a missed finding, not a misplaced one, so it is a minor.

On T-001 specifically, I read the commit rather than pricing the loss abstractly, and it is smaller than r1 implied: the message gives the cause instead of the diff and its em dash is flush. It would have passed. Record it as unjudged in the retrospective anyway — the value is the record.

**2. Is pinning T-009's payload to `discarded` alone right, and does anything downstream break?** Right, and nothing breaks. `attempts` already rides at the outcome's top level where the validator pins it to 1 or 2, so a second one inside `ledger` duplicates a field the contract carries and can contradict it; reusing C-4's five members with C-4's types is what keeps both lanes on one shape when someone finally threads the flags. Downstream I traced every consumer: `ledger_record` has exactly two call sites, `_append_ledger` builds argv from named keys and never iterates the dict, the codex outcome never reaches the exact-key-set validator (Codex-host-only, and it pins `ledger.vendor == "claude"`), and the published stdout carries `ledger` as a nested object.

The one shape that would hurt is the mirror image of the r0 blocker — widening the key set so `discarded` becomes required, which would refuse every un-retried claude crossing at the door while the retried one passes. That is already caught, and not by luck: `test_codex_adapter.py`'s existing fixture drives a ten-key ledger through the real gate and asserts exit 0, and C-1 runs that suite on this task. The excerpt also tells the worker both shapes must pass. The only residue is items 1 and 2's stale plural, which points at a validator slightly laxer than the pin intends.

**3. Does giving T-008 the crash fix create a problem?** No. The exemption is pinned to one dereference in one named file, not to a category, so it does not reach the task's own new cases — those have production edits behind them and C-5's mutation arm applies in full. It is also not really an override: C-5 tells the checker to back out "the corresponding production edit", and for a guard there is none, so the arm is vacuous there and the sentence is saying so rather than suspending the clause. No conflict with C-1 either — the guard is a no-op on a green run, which I confirmed by running the suite (46 passed).

The one thing it does create is a C-10 question: the guard and the capture work are two logical changes, and C-10 reads commit granularity. Say the guard commits on its own.

**4. What the reshuffle introduced, and what is still uncovered.** The C-10 reshuffle introduced nothing — six frontmatter lines and six `check_method` blocks, all on tasks already routed to `checker-judgment`, and `check-job-spec` agrees. The only thing that came in with it is the unanchored boundary. On the constitution I checked that the preamble edit really was preamble-only, since a clause edit after CON-audit-r3's PASS would put the whole clause set out of audit: every r1 clause citation lands at exactly +8 today and the inserted paragraph is exactly 8 lines, which is what a comment-only insertion looks like and what a clause edit would not.

Still uncovered: the claude-side quota assertion from spec.md:66, which is the same half-line r1 named and the only spec text without an owner. Everything else in the spec has exactly one, and every seam is stated from both directions.

Three rounds is long, and this document is done. Ship it.

---
task: T-003
checker: checker-judgment
vendor: anthropic
model: claude-opus-5
verdict: PASS
checked_at: 2026-08-12T05:47:18Z
duration_ms: None
cost_usd: None
---

<!-- GENERATED FILE—do not hand-edit. Rendered by render-verdict.py
from the verdict JSON, the record of record. Edit the JSON and
re-render instead. -->

## Per-clause results

| clause | severity | description | evidence |
| ------ | -------- | ------------ | -------- |
| C-1 | info | All seven suites named in the spec's verification block exit 0 after the task's edits. | .agent-guild/scripts/check-build.sh run verbatim with C-1's command; log .agent-guild/state/log/build-20260812T004229.log lines 35/88/137/170/370/448 read '29 passed, 0 failed', '43 passed, 0 failed', '46 passed, 0 failed', '30 passed, 0 failed', '182 passed, 0 failed', '43 passed, 0 failed'; final line 449: 'check-build.sh: exit 0'. The && chain means every one of the seven exited 0. |
| C-2 | info | build-plugin.py --check is clean at the tip and at every commit on the branch walked in a throwaway clone, so the regenerated packages landed in the same commit as the hook sources. | .agent-guild/scripts/check-build.sh run verbatim with C-2's command; tail: 'OK: shared-core wrappers, both published packages, and both marketplaces match fresh builds; the Claude plugin passes strict validation' / 'check-build.sh: exit 0 (log: .agent-guild/state/log/build-20260812T004315.log)'. Independently, `git show --stat e70c3b5` lists the four package copies of _lib.py, dispatch-guard.py and test_hooks.py in the same commit as .agent-guild/hooks/ (12 files, 436 insertions). |
| C-5 | info | Each of the three new test_hooks.py cases goes red when the production change it covers is backed out, and green again when restored; the mutations ran only in a cp -a copy of the working tree under the session scratchpad. | Copy: /private/tmp/claude-501/-Users-k-arnett-repos-agent-guild/7188fa38-b39f-46d7-9b46-9e5e2fe93992/scratchpad/c5copy (cp -a of the working tree; job branch never edited). Baseline there: '182 passed, 0 failed'. MUTATION 1, deleting the route-5 `.skipped` block from second_opinion_debts (_lib.py:494-505): '180 passed, 2 failed' — 'FAIL so: a .skipped marker discharges the debt (#128) rc1=2 ... rc2=2' and 'FAIL so: a .skipped marker filed under the far lane discharges nothing (#128)'. MUTATION 2, deleting the `.skipped` denial from dispatch-guard.py:355-363: '181 passed, 1 failed' — 'FAIL dispatch-guard: checker-courier denied on a skipped stem, and allowed once the marker clears rc1=0 err1=\\'\\' rc2=0 err2=\\'\\''. MUTATION 3, replacing route 5's single-lane test with `any(... for l in COURIER_LANES)`: '181 passed, 1 failed' — only 'FAIL so: a .skipped marker filed under the far lane discharges nothing (#128) claude marker+hook_host=codex → rc=0; codex marker+hook_host=codex → rc=0', with case 21 still green, which is what proves the lane-trap case asserts the pinning rather than merely the presence of a marker. After restoring both files: '182 passed, 0 failed' and `diff` against the repo copies reports both files identical. |
| C-7 | info | Sites 2, 3 and 4 of the .skipped discharge route behave exactly as the clause states; site 1 (compose-brief's exit 3) is out of scope per the task's check_method and was not judged. | Site 2, fixture project at scratchpad/c7fix with CLAUDE_PROJECT_DIR set there, driving _lib.second_opinion_debts directly: verdict of record alone → [('T-901', 'T-901-sonnet-r0', 'codex')]; + T-901-sonnet-r0-codex.skipped (own lane on a Claude host) → []; + T-901-sonnet-r0-claude.skipped (far lane) → [('T-901', 'T-901-sonnet-r0', 'codex')], the debt standing. Reciprocal direction confirmed too: the same claude-suffixed marker with hook_host=codex → [], and the codex-suffixed marker with hook_host=codex → [('T-901', 'T-901-sonnet-r0', 'claude')]. `courier_lane({})` = 'codex', `courier_lane({'hook_host': 'codex'})` = 'claude' (_lib.py:206-215). Site 3, .agent-guild/hooks/dispatch-guard.py:345 computes `stem = _lib.crossing_stem(...)` ABOVE both denials — the lane-exhausted denial at :346 and the new `.skipped` denial at :355-363 — and `_lib.reserve_crossing(tid, stem, lane)` sits at :372, AFTER both. Confirmed empirically at scratchpad/c7dg: with the marker present the dispatch exits 2 with "checker-courier's crossing for T-902-sonnet-r0 on the 'codex' lane was recorded skipped ... a recorded skip is not crossable. Nothing is substituted." and the verdicts dir afterwards holds only T-902-sonnet-r0-codex.skipped — no .authorized file, so no authorization record is left for a dispatch that never happened; with the marker removed the same dispatch exits 0 and T-902-sonnet-r0-codex.authorized appears. Site 4, .agent-guild/hooks/test_hooks.py:1391 'so: a .skipped marker discharges the debt (#128)', :1411 'dispatch-guard: checker-courier denied on a skipped stem, and allowed once the marker clears', :1435 'so: a .skipped marker filed under the far lane discharges nothing (#128)' — all three present. |
| C-7 | minor | The docstring's route list was updated to match the code, but an inline comment further down still uses the old numbering: _lib.py:523-524 reads "means route 5 can't be established, so it owes (routes 1-4 already had their shot above)" when the blocked route is now route 6 and routes 1-5 had their shot, as the corrected comment two lines earlier already says. | .agent-guild/hooks/_lib.py:513 '# Unreadable forecloses route 6 (below) and nothing above it.' against :523-524 '# object—any of those means route 5 can't be established, so it / # owes (routes 1-4 already had their shot above).' |
| C-9 | info | Every file the branch changes relative to its merge base with main sits inside the paths this job names. | .agent-guild/scripts/check-build.sh run verbatim with C-9's command; the grep -v filter left nothing, so `test -z "$out"` held: 'check-build.sh: exit 0 (log: .agent-guild/state/log/build-20260812T004329.log)'. |
| C-10 | info | The single commit in 6720a5a..HEAD says why the change exists, is one logical change, and carries none of the tells the clause enumerates. | `git log --format=%B 6720a5a..HEAD` returns one commit, e70c3b5 'fix(hooks): add the .skipped discharge route for all-script tasks (#128)'. The subject names the cause (all-script tasks), not just the edit, and the body gives the reason directly: 'no verdict, no sentinel, and no waiver would land at that stem, so the stop gate would hold the orchestrator's turn open forever.' That three-item list is the three discharge routes that actually existed before this change, so it is not padded to reach three. No headings are added anywhere in the commit (the diff is .py only). Every em dash in the added prose is flush on both sides — `grep -nE '^\\+.* — \| — '` over the commit returns nothing, while the six added em dashes at _lib.py/dispatch-guard.py/test_hooks.py all read e.g. 'clauses (#128—compose-brief's exit 3'. Granularity: the twelve files are the three hook sources plus their four generated package copies, which C-2 requires in the same commit, so this is one logical change. One observation outside this clause's list, recorded as info only: the commit body is hard-wrapped at ~76 columns, against the standing no-hard-wrap preference; C-10 imports only the heading-case and em-dash rules from that preference, so it does not decide here. |
| C-11 | info | Work is on chore/courier-lane-cleanup with no upstream tracking ref, and nothing outside .agent-guild/state/ is left uncommitted. | .agent-guild/scripts/check-build.sh run verbatim with C-11's command: 'check-build.sh: exit 0 (log: .agent-guild/state/log/build-20260812T004332.log)'. Independently, `git rev-parse --abbrev-ref HEAD` → 'chore/courier-lane-cleanup' and `git status --porcelain` → empty. |

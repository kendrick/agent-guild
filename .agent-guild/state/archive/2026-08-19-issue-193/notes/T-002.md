# T-002 self-report

Added three archive-drawn regression cases to `.agent-guild/scripts/test_check_job_spec.py`,
inserted after the existing P4 block and before the R15 section, so they sit
alongside the other corpus-mutation cases inside the same `fixture_root`
scope.

## M7 — R10's relaxed adjacency

Mutates T-006.md's real "record three findings" sentence (the same one M3
already mutates) to `record four separate findings` — inserting the
adjective "separate" between the count and the plural noun it governs, in
addition to breaking the count. Asserts exit 4, R10 named, no traceback.
This is the case the spec calls for specifically: M3 alone doesn't exercise
the adjacency relaxation, because its mutated noun still sits immediately
after the number. Confirmed via `probe.py suite-coverage`'s
`R10 relaxation: revert to strict adjacency landed` / `... turns the suite
red` pair that reverting `_governs_plural_noun` to match only the token
right after the number turns this exact case red.

## R21 — checker-routing proof

Mutates T-001.md's `checker: checker-judgment` to `checker:
checker-deterministic`. T-001 cites C-2 and C-7, both `checker-judgment:`
rubric clauses in the corpus's own constitution.md, so this is a real
archived task with a one-line flip, not an invented fixture — the spec
notes no archived task already carries this shape, since nothing refused it
before this job. Asserts exit 1 (proof, not 4), R21 named, T-001 named, C-2
named (the first judgment clause in the task's `clauses:` order), no
traceback. Confirmed via the probe's `R21 checker routing: disabling it
turns the suite red`.

## R2-anchor — anchor text in the diagnostic

Reuses M1's exact mutation (T-001.md's `compose-brief.py:64` ->
`compose-brief.py:58`, the same stale-citation shape) in its own fixture,
and adds an assertion beyond "R2 fired": that the substring `val[0] ==
val[-1]` — part of the anchor text `r2_anchor_message` quotes — appears
literally in stderr. M1 itself only checks the rule fired, so it stays
green if the anchor text is stripped out of the message; this case is what
the spec requires to close that gap. Confirmed via the probe's `R2
diagnostic: removing the quoted anchor turns the suite red`.

## Verification performed

- `python3 .agent-guild/scripts/test_check_job_spec.py`: 230 passed, 0
  failed, exit 0.
- `python3 .agent-guild/state/checks/probe.py suite-coverage`: all mutation
  reverts (R10 adjacency disable, R21 disable, R2 anchor-strip, and R10
  strict-adjacency revert) turned the suite red as expected; passed
  overall.
- `git status --porcelain`: only `.agent-guild/scripts/check-job-spec.py`
  (T-001's, untouched by me) and `.agent-guild/scripts/test_check_job_spec.py`
  (mine) are modified. Nothing outside `owns` was touched, and nothing was
  committed.

## Judgment calls

- Picked T-001.md from `2026-08-10-issue-117` as the R21 fixture because it
  cites two judgment clauses and already ships with the correct
  `checker: checker-judgment` routing, so a single-line flip is the whole
  mutation. The rule reports the first judgment clause in citation order
  (C-2); I assert only C-2 is named, not that C-7 is absent, since the spec
  doesn't require proving the "only" half and the constitution's C-3 text
  covers that as the auditor's read of the implementation, not this
  suite's job.
- Chose "record four separate findings" over other adjective choices
  specifically because "separate" doesn't end in "s" (so it can't
  accidentally satisfy the predicate on its own) and "findings" is the
  first later token that does, landing within the relaxation's own
  4-word walk.

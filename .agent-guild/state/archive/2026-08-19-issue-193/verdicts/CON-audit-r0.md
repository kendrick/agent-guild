---
task: CON-audit
checker: auditor
vendor: anthropic
model: claude-opus-5
verdict: FAIL
checked_at: 2026-08-17T22:05:00Z
duration_ms: None
cost_usd: None
---

<!-- GENERATED FILE—do not hand-edit. Rendered by render-verdict.py
from the verdict JSON, the record of record. Edit the JSON and
re-render instead. -->

## Per-clause results

| clause | severity | description | evidence |
| ------ | -------- | ------------ | -------- |
| C-3 | blocker | C-3's text makes the constitution's clause the authority ("citing a clause whose check begins `checker-judgment:`") and requires the message name "both the task id and the clause id", but its probe's fixture cites exactly one clause and interpolates that clause's check text verbatim into check_method, so an implementation that reads the task's own check_method prose instead of the clause passes every one of the eight clauses while naming the wrong clause on a multi-clause task and hard-blocking a correctly-routed one. | Reference impl at /tmp/conaudit-r0-p83XeS/repo (r21 reads ctx.clauses[cid].check_text) and variant at /tmp/conaudit-r0-ZpTg77/repo (r21 reads task.check_method_text). Variant run: 'PASS r10-adjective / PASS r10-corpus / PASS r21-unsafe / PASS r21-safe / PASS r2-anchor / PASS suite+self-test' -- all 8 clauses green. Divergence 1, apparatus/CON-audit-r0/divergence2.py, task 'clauses: [C-1, C-2]' with C-1 script-checked and C-2 the rubric, checker: checker-deterministic: reference emits "...but cites C-2, whose check is a checker-judgment rubric"; variant emits "...but cites C-1, a judgment rubric" -- the wrong clause. 36 of 40 archived tasks cite >1 clause (6 of those routed checker-deterministic), so the single-clause shape the probe tests is the rare one. Divergence 2, /tmp/c3_overfire.py, a correctly-routed 'checker-deterministic' task citing only a script clause whose R4-legal check_method is ".agent-guild/scripts/check-build.sh 'grep -q \\"checker-judgment:\\" README.md'": reference rc=0 err=''; variant rc=1 'R21 checker-routing: tasks/T-002.md (T-002) declares checker: checker-deterministic but cites C-2, a judgment rubric' -- a proof-class refusal of a legal dispatch, and RULE_CLASS/rule_R20 (check-job-spec.py:748-761) refuse a **Lint exception** against a PROOF rule, so nothing short of state/PAUSED clears it. |
| C-7 | major | C-7's text ends "...and no generated tree is hand-edited" and its failing example names only the hand-edit case, but its check allowlists `plugin/` and `plugins/` wholesale and therefore exits 0 on that exact artifact, so the clause's own recorded failing example does not falsify its own check and a checker-deterministic running C-7 files a PASS certifying a property it never tested. | Venue /tmp/conaudit-r0-c7b-EHsYJt/repo. Performed C-7's failing example verbatim -- edited plugins/agent-guild/project-template/.agent-guild/scripts/check-job-spec.py directly, source untouched. C-7 verbatim: 'check-diff-scope: in scope: plugins/agent-guild/project-template/.agent-guild/scripts/check-job-spec.py (dir: plugins/) / OK: 1 path(s) in scope', rc=0. C-6 verbatim in the same venue: rc=1. C-7 does discriminate on its first half: adding a stray line to README.md gives 'check-diff-scope: out of scope: README.md', rc=1. |
| C-5 | major | C-5's text requires the anchor's line be "a different number from the two the message already reports", which is false of a correct implementation whenever the anchor sits on the citation's own line -- a live case, since R2's sentence scoping is exactly what lets the anchor sit either on or off that line -- and the probe never asserts distinctness at all, only that the number appears, so the clause's text and its check specify different things about the same message. | apparatus/CON-audit-r0/c5_sameline.py against the faithful reference /tmp/conaudit-r0-p83XeS/repo, excerpt "Check the guard at `src/fixture.py:12`, namely `if len(val) >= 2 and val[0] == val[-1]:`, before proceeding." on one line: 'R2 citation-anchor: tasks/T-001.md:22 cites src/fixture.py:12 but the quoted code is at src/fixture.py:29; the anchor matched was `if len(val) >= 2 and val[0] == val[-1]:`, taken from tasks/T-001.md:22' -- anchor line 22 equals the citing line 22. probe.py:416-418 asserts only re.search(rf"(?<!\\d){anchor_doc_line}(?!\\d)", line) against a fixture that hard-codes the anchor two lines below the citation (anchor_doc_line=24, src_line=22). The check does discriminate on both halves it actually asserts: variant r2-quote-only (quotes the anchor, omits its line), venue /tmp/conaudit-r0-ZoqcdS, gives 'FAIL diagnostic gives the anchor's own document line', and the untouched tree fails both assertions. The defect is the distinctness the text asserts and the check does not. |
| preamble | minor | The `**Job weight**:` line records "every acceptance check runs an existing command except the archive sweep" as its first reason, but six of the eight clauses run `.agent-guild/state/checks/probe.py`, a 445-line instrument authored for this job, so the exception is six clauses rather than the archive sweep alone and CLAUDE.md's stated discriminator ("does verification require building an instrument, or invoking one that already exists?") points at deep on the reason as written. | constitution.md:3. C-1, C-2, C-3, C-4, C-5 invoke probe.py directly; C-2's r10-corpus is the archive sweep and is one of them, not the exception to them. `wc -l .agent-guild/state/checks/probe.py` = 445. The weight itself survives on the line's second reason (unattended blast radius via dispatch-guard), and the ceiling is not binding -- check-job-spec.py R18 passes at 8 clauses against standard's ceiling of 8 -- so no clause was squeezed out and nothing downstream changes. The recorded reason is what needs correcting, not the weight. |
| C-4 | minor | probe.py:344-348 states C-4 "guards against R21 over-firing", but C-4's fixture carries no `checker-deterministic` task at all and so cannot observe over-firing in the deterministic direction; what actually catches that is C-3's `assert_clean` precondition, whose fixture does carry a correctly-routed checker-deterministic T-002. | Variant r21-any-clause (fires on any checker-deterministic task whenever the constitution holds any judgment clause, ignoring the task's citations), venue /tmp/conaudit-r0-1a2KfX: 'PASS r21-safe' -- C-4 does not see it -- while r21-unsafe fails at its precondition, 'FAIL r21: baseline fixture is clean before mutation rc=1 err=R21 checker-routing: tasks/T-002.md (T-002) declares checker: checker-deterministic but C-1 is a checker-judgment rubric'. C-4 does catch the narrower bug its own failing example names: variant comparing the two fields for equality, venue /tmp/conaudit-r0-c4-1VamsC, 'r21-safe rc=1, FAIL safe direction does not trip R21'. The hole is closed, but by C-3, so a later trim of C-3's assert_clean would open it while C-4 still claims to hold it. |
| C-2 | minor | C-2's text says R10 stays silent on "every constitution and task set under `.agent-guild/state/archive/`", but its probe selects only archive directories carrying both `constitution.md` and a `tasks/` directory and then asserts `len(archives) >= 11`, a floor rather than a census, so a directory that fails the selection is dropped silently while the assertion still holds. | probe.py:265-273. Verified all 11 archive directories currently carry both, so the sweep is complete today: the run named 2026-07-14, 2026-07-14-intake, 2026-07-15-issue-20/21/22/23/27, 2026-08-10-issue-117, 2026-08-11-issue-100, 2026-08-11-issue-141, 2026-08-12-courier-lane-cleanup, each 'R10 silent', and `ls -d */ \| wc -l` = 11. |
| C-1 | info | C-1's probe discriminates in both directions and its controls hold: the laziest change that turns it green is caught by C-2, and a rule that stops firing entirely is caught by C-1's own control. | Reference impl (bounded adjective window) /tmp/conaudit-r0-p83XeS: 'PASS r10-adjective'. Untouched tree: rc=1, 'FAIL adjective between count and noun still fires R10 rc=0 err=""'. Variant `_governs_plural_noun -> return True` /tmp/conaudit-r0-li17UY: r10-adjective PASSES but r10-corpus FAILS on 4 archives, e.g. "R10 count-vs-list: tasks/T-003.md:41 says '30' (30) but the following list has 4 item(s)". Variant R10 neutered to `return None` /tmp/conaudit-r0-neuter-I1UYYT: 'FAIL control: bare count over a 3-item list fires R10' and 'FAIL control: the same call DOES fire on a known-bad fixture'. |
| C-2 | info | C-2's stated reason for calling rule_R10 directly rather than through the CLI holds, and its corpus sweep is the clause that settles the fork C-1's text otherwise leaves open between a bounded adjective window and an unbounded scan to any plural noun later on the line. | run_rules at check-job-spec.py:1985-2005 orders R10 second-to-last, before R12 only, and returns the first violation across all rules, so the CLI would mask it -- confirmed against the #117 archive, where the CLI's first violation is R17 while `check_citation_rules(regions, root, 'R3')` alone returns "R3 citation-shape: tasks/T-007.md:81 cites _working-memory/conventions.md:65 but that line is blank", four steps ahead of R2. Reading B (unbounded), venue /tmp/conaudit-r0-65ZcYl: 'PASS r10-adjective' but r10-corpus FAILS on 2026-08-10-issue-117 and 2026-08-11-issue-100. The synthetic-fixture scope decision therefore survives challenge. |
| C-6 | info | C-6 runs all four commands, propagates the last link's exit code through check-build.sh, and is jointly satisfiable with C-7 -- the rebuild C-6 demands touches exactly the three paths C-7 allowlists, so C-7 is neither too narrow to permit the rebuild nor in conflict with C-6. | Untouched tree: C-6 verbatim exits 0, '324 passed, 0 failed', 'Validation passed', 'OK: shared-core wrappers, both published packages, and both marketplaces match fresh builds'. Venue /tmp/conaudit-r0-c67-ntelq9: source edit alone gives C-6 rc=1 / C-7 rc=0; after `python3 scripts/build-plugin.py`, C-6 rc=0 and C-7 rc=0 over exactly 'M .agent-guild/scripts/check-job-spec.py, M plugin/project-template/.agent-guild/scripts/check-job-spec.py, M plugins/agent-guild/project-template/.agent-guild/scripts/check-job-spec.py'. Scope note: build-plugin.py:1100-1106 hard-fails when the `claude` CLI is absent, so C-6 is host-dependent -- present here, and it fails loud rather than passing silently. |
| C-8 | info | C-8's nested quoting survives the double `bash -c` (check-baselines.py's own, then check-build.sh's) intact, and both legs of the pipeline are live rather than mangled into a no-op. | Venue /tmp/conaudit-r0-c8-Wenyj4: unmodified rc=0; `import regex` at module level rc=1; `from yaml import safe_load` rc=1; `import regex` inside main()'s body rc=1 (ast.walk reaches function-scoped imports). Venue /tmp/conaudit-r0-c8b-xz3UYK isolates leg 1: a module-level `raise RuntimeError` with all imports stdlib gives whole-C8 rc=1 while the leg-2 AST scan alone gives rc=0, proving the `-X importtime` exec_module leg executes. Host python3 is 3.14.7, so sys.stdlib_module_names is present. |
| preamble | info | The baseline sweep, re-derived rather than trusted, matches the constitution's declarations exactly, and every one of the eight clauses was executed -- none blocked, none judgment-only, none unexecuted. | `python3 .agent-guild/scripts/check-baselines.py .agent-guild/state --repo-root .` -> 'check-baselines: ran 8 (3 red, 5 green), skipped 0 (0 judgment, 0 no-baseline)', exit 0. Re-derived by hand: C-1 rc=1, C-3 rc=1, C-5 rc=1 (red, as declared); C-2 rc=0, C-4 rc=0, C-6 rc=0, C-7 rc=0, C-8 rc=0 (green, as declared). No `**Lint exception**:` line is present, so R20's waiver-honesty judgment does not apply. No `**Ceiling overrun**:` line is needed: 8 clauses against standard's ceiling of 8. Working tree verified clean at ae3f757 on feat/193-linter-three-shapes before and after every run; every mutation ran in its own mktemp -d, listed in apparatus/CON-audit-r0/VENUES.txt. |

## Diagnosis

- **C-3** (blocker): C-3's text makes the constitution's clause the authority ("citing a clause whose check begins `checker-judgment:`") and requires the message name "both the task id and the clause id", but its probe's fixture cites exactly one clause and interpolates that clause's check text verbatim into check_method, so an implementation that reads the task's own check_method prose instead of the clause passes every one of the eight clauses while naming the wrong clause on a multi-clause task and hard-blocking a correctly-routed one.
  evidence: Reference impl at /tmp/conaudit-r0-p83XeS/repo (r21 reads ctx.clauses[cid].check_text) and variant at /tmp/conaudit-r0-ZpTg77/repo (r21 reads task.check_method_text). Variant run: 'PASS r10-adjective / PASS r10-corpus / PASS r21-unsafe / PASS r21-safe / PASS r2-anchor / PASS suite+self-test' -- all 8 clauses green. Divergence 1, apparatus/CON-audit-r0/divergence2.py, task 'clauses: [C-1, C-2]' with C-1 script-checked and C-2 the rubric, checker: checker-deterministic: reference emits "...but cites C-2, whose check is a checker-judgment rubric"; variant emits "...but cites C-1, a judgment rubric" -- the wrong clause. 36 of 40 archived tasks cite >1 clause (6 of those routed checker-deterministic), so the single-clause shape the probe tests is the rare one. Divergence 2, /tmp/c3_overfire.py, a correctly-routed 'checker-deterministic' task citing only a script clause whose R4-legal check_method is ".agent-guild/scripts/check-build.sh 'grep -q \"checker-judgment:\" README.md'": reference rc=0 err=''; variant rc=1 'R21 checker-routing: tasks/T-002.md (T-002) declares checker: checker-deterministic but cites C-2, a judgment rubric' -- a proof-class refusal of a legal dispatch, and RULE_CLASS/rule_R20 (check-job-spec.py:748-761) refuse a **Lint exception** against a PROOF rule, so nothing short of state/PAUSED clears it.
- **C-7** (major): C-7's text ends "...and no generated tree is hand-edited" and its failing example names only the hand-edit case, but its check allowlists `plugin/` and `plugins/` wholesale and therefore exits 0 on that exact artifact, so the clause's own recorded failing example does not falsify its own check and a checker-deterministic running C-7 files a PASS certifying a property it never tested.
  evidence: Venue /tmp/conaudit-r0-c7b-EHsYJt/repo. Performed C-7's failing example verbatim -- edited plugins/agent-guild/project-template/.agent-guild/scripts/check-job-spec.py directly, source untouched. C-7 verbatim: 'check-diff-scope: in scope: plugins/agent-guild/project-template/.agent-guild/scripts/check-job-spec.py (dir: plugins/) / OK: 1 path(s) in scope', rc=0. C-6 verbatim in the same venue: rc=1. C-7 does discriminate on its first half: adding a stray line to README.md gives 'check-diff-scope: out of scope: README.md', rc=1.
- **C-5** (major): C-5's text requires the anchor's line be "a different number from the two the message already reports", which is false of a correct implementation whenever the anchor sits on the citation's own line -- a live case, since R2's sentence scoping is exactly what lets the anchor sit either on or off that line -- and the probe never asserts distinctness at all, only that the number appears, so the clause's text and its check specify different things about the same message.
  evidence: apparatus/CON-audit-r0/c5_sameline.py against the faithful reference /tmp/conaudit-r0-p83XeS/repo, excerpt "Check the guard at `src/fixture.py:12`, namely `if len(val) >= 2 and val[0] == val[-1]:`, before proceeding." on one line: 'R2 citation-anchor: tasks/T-001.md:22 cites src/fixture.py:12 but the quoted code is at src/fixture.py:29; the anchor matched was `if len(val) >= 2 and val[0] == val[-1]:`, taken from tasks/T-001.md:22' -- anchor line 22 equals the citing line 22. probe.py:416-418 asserts only re.search(rf"(?<!\d){anchor_doc_line}(?!\d)", line) against a fixture that hard-codes the anchor two lines below the citation (anchor_doc_line=24, src_line=22). The check does discriminate on both halves it actually asserts: variant r2-quote-only (quotes the anchor, omits its line), venue /tmp/conaudit-r0-ZoqcdS, gives 'FAIL diagnostic gives the anchor's own document line', and the untouched tree fails both assertions. The defect is the distinctness the text asserts and the check does not.
- **preamble** (minor): The `**Job weight**:` line records "every acceptance check runs an existing command except the archive sweep" as its first reason, but six of the eight clauses run `.agent-guild/state/checks/probe.py`, a 445-line instrument authored for this job, so the exception is six clauses rather than the archive sweep alone and CLAUDE.md's stated discriminator ("does verification require building an instrument, or invoking one that already exists?") points at deep on the reason as written.
  evidence: constitution.md:3. C-1, C-2, C-3, C-4, C-5 invoke probe.py directly; C-2's r10-corpus is the archive sweep and is one of them, not the exception to them. `wc -l .agent-guild/state/checks/probe.py` = 445. The weight itself survives on the line's second reason (unattended blast radius via dispatch-guard), and the ceiling is not binding -- check-job-spec.py R18 passes at 8 clauses against standard's ceiling of 8 -- so no clause was squeezed out and nothing downstream changes. The recorded reason is what needs correcting, not the weight.
- **C-4** (minor): probe.py:344-348 states C-4 "guards against R21 over-firing", but C-4's fixture carries no `checker-deterministic` task at all and so cannot observe over-firing in the deterministic direction; what actually catches that is C-3's `assert_clean` precondition, whose fixture does carry a correctly-routed checker-deterministic T-002.
  evidence: Variant r21-any-clause (fires on any checker-deterministic task whenever the constitution holds any judgment clause, ignoring the task's citations), venue /tmp/conaudit-r0-1a2KfX: 'PASS r21-safe' -- C-4 does not see it -- while r21-unsafe fails at its precondition, 'FAIL r21: baseline fixture is clean before mutation rc=1 err=R21 checker-routing: tasks/T-002.md (T-002) declares checker: checker-deterministic but C-1 is a checker-judgment rubric'. C-4 does catch the narrower bug its own failing example names: variant comparing the two fields for equality, venue /tmp/conaudit-r0-c4-1VamsC, 'r21-safe rc=1, FAIL safe direction does not trip R21'. The hole is closed, but by C-3, so a later trim of C-3's assert_clean would open it while C-4 still claims to hold it.
- **C-2** (minor): C-2's text says R10 stays silent on "every constitution and task set under `.agent-guild/state/archive/`", but its probe selects only archive directories carrying both `constitution.md` and a `tasks/` directory and then asserts `len(archives) >= 11`, a floor rather than a census, so a directory that fails the selection is dropped silently while the assertion still holds.
  evidence: probe.py:265-273. Verified all 11 archive directories currently carry both, so the sweep is complete today: the run named 2026-07-14, 2026-07-14-intake, 2026-07-15-issue-20/21/22/23/27, 2026-08-10-issue-117, 2026-08-11-issue-100, 2026-08-11-issue-141, 2026-08-12-courier-lane-cleanup, each 'R10 silent', and `ls -d */ | wc -l` = 11.
- **C-1** (info): C-1's probe discriminates in both directions and its controls hold: the laziest change that turns it green is caught by C-2, and a rule that stops firing entirely is caught by C-1's own control.
  evidence: Reference impl (bounded adjective window) /tmp/conaudit-r0-p83XeS: 'PASS r10-adjective'. Untouched tree: rc=1, 'FAIL adjective between count and noun still fires R10 rc=0 err=""'. Variant `_governs_plural_noun -> return True` /tmp/conaudit-r0-li17UY: r10-adjective PASSES but r10-corpus FAILS on 4 archives, e.g. "R10 count-vs-list: tasks/T-003.md:41 says '30' (30) but the following list has 4 item(s)". Variant R10 neutered to `return None` /tmp/conaudit-r0-neuter-I1UYYT: 'FAIL control: bare count over a 3-item list fires R10' and 'FAIL control: the same call DOES fire on a known-bad fixture'.
- **C-2** (info): C-2's stated reason for calling rule_R10 directly rather than through the CLI holds, and its corpus sweep is the clause that settles the fork C-1's text otherwise leaves open between a bounded adjective window and an unbounded scan to any plural noun later on the line.
  evidence: run_rules at check-job-spec.py:1985-2005 orders R10 second-to-last, before R12 only, and returns the first violation across all rules, so the CLI would mask it -- confirmed against the #117 archive, where the CLI's first violation is R17 while `check_citation_rules(regions, root, 'R3')` alone returns "R3 citation-shape: tasks/T-007.md:81 cites _working-memory/conventions.md:65 but that line is blank", four steps ahead of R2. Reading B (unbounded), venue /tmp/conaudit-r0-65ZcYl: 'PASS r10-adjective' but r10-corpus FAILS on 2026-08-10-issue-117 and 2026-08-11-issue-100. The synthetic-fixture scope decision therefore survives challenge.
- **C-6** (info): C-6 runs all four commands, propagates the last link's exit code through check-build.sh, and is jointly satisfiable with C-7 -- the rebuild C-6 demands touches exactly the three paths C-7 allowlists, so C-7 is neither too narrow to permit the rebuild nor in conflict with C-6.
  evidence: Untouched tree: C-6 verbatim exits 0, '324 passed, 0 failed', 'Validation passed', 'OK: shared-core wrappers, both published packages, and both marketplaces match fresh builds'. Venue /tmp/conaudit-r0-c67-ntelq9: source edit alone gives C-6 rc=1 / C-7 rc=0; after `python3 scripts/build-plugin.py`, C-6 rc=0 and C-7 rc=0 over exactly 'M .agent-guild/scripts/check-job-spec.py, M plugin/project-template/.agent-guild/scripts/check-job-spec.py, M plugins/agent-guild/project-template/.agent-guild/scripts/check-job-spec.py'. Scope note: build-plugin.py:1100-1106 hard-fails when the `claude` CLI is absent, so C-6 is host-dependent -- present here, and it fails loud rather than passing silently.
- **C-8** (info): C-8's nested quoting survives the double `bash -c` (check-baselines.py's own, then check-build.sh's) intact, and both legs of the pipeline are live rather than mangled into a no-op.
  evidence: Venue /tmp/conaudit-r0-c8-Wenyj4: unmodified rc=0; `import regex` at module level rc=1; `from yaml import safe_load` rc=1; `import regex` inside main()'s body rc=1 (ast.walk reaches function-scoped imports). Venue /tmp/conaudit-r0-c8b-xz3UYK isolates leg 1: a module-level `raise RuntimeError` with all imports stdlib gives whole-C8 rc=1 while the leg-2 AST scan alone gives rc=0, proving the `-X importtime` exec_module leg executes. Host python3 is 3.14.7, so sys.stdlib_module_names is present.
- **preamble** (info): The baseline sweep, re-derived rather than trusted, matches the constitution's declarations exactly, and every one of the eight clauses was executed -- none blocked, none judgment-only, none unexecuted.
  evidence: `python3 .agent-guild/scripts/check-baselines.py .agent-guild/state --repo-root .` -> 'check-baselines: ran 8 (3 red, 5 green), skipped 0 (0 judgment, 0 no-baseline)', exit 0. Re-derived by hand: C-1 rc=1, C-3 rc=1, C-5 rc=1 (red, as declared); C-2 rc=0, C-4 rc=0, C-6 rc=0, C-7 rc=0, C-8 rc=0 (green, as declared). No `**Lint exception**:` line is present, so R20's waiver-honesty judgment does not apply. No `**Ceiling overrun**:` line is needed: 8 clauses against standard's ceiling of 8. Working tree verified clean at ae3f757 on feat/193-linter-three-shapes before and after every run; every mutation ran in its own mktemp -d, listed in apparatus/CON-audit-r0/VENUES.txt.

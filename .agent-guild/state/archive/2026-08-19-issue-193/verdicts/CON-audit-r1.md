---
task: CON-audit
checker: auditor
vendor: anthropic
model: claude-opus-5
verdict: FAIL
checked_at: 2026-08-17T23:40:00Z
duration_ms: None
cost_usd: None
---

<!-- GENERATED FILE—do not hand-edit. Rendered by render-verdict.py
from the verdict JSON, the record of record. Edit the JSON and
re-render instead. -->

## Per-clause results

| clause | severity | description | evidence |
| ------ | -------- | ------------ | -------- |
| C-5 | major | r0's rewording added a sentence its check cannot falsify: "That line is reported whether or not it differs from the citation's own -- an anchor on the citing line is the ordinary case and must read correctly too." probe_r2_anchor's fixture hard-codes the anchor two lines below the citation and asserts only that the anchor's number appears somewhere in the message, so an implementation that omits the anchor line exactly when it equals the citing line -- the reading r0's own diagnosis ("a different number from the two the message already reports") invites -- passes all eight clauses green. The clause now asserts a property no check in the constitution can refuse. | Variant --r2 suppress-same-line, venue /tmp/conaudit-r1-r2same-een4nA: 'PASS r10-adjective / PASS r10-corpus / PASS r21-unsafe / PASS r21-safe / PASS r2-anchor' -- all eight green. apparatus/CON-audit-r1/c5_sameline.py drives the same-line case (citation and anchor both on document line 22). Faithful reference /tmp/conaudit-r1-ref-7C9nrs: '...but the quoted code is at src/fixture.py:29; the anchor matched was `if len(val) >= 2 and val[0] == val[-1]:`, taken from tasks/T-001.md:22'. Variant, same input: '...the anchor matched was `if len(val) >= 2 and val[0] == val[-1]:`' -- no anchor line at all, and probe.py:466-470 never looks. The two runs C-5 does discriminate: --r2 quote-only fails 'diagnostic gives the anchor's own document line' (venue /tmp/conaudit-r1-r2q-sSYh0O), --r2 line-only fails 'diagnostic quotes the anchor text it matched' (venue /tmp/conaudit-r1-r2l-V8mPpF). |
| C-5 | major | C-5 is the only clause that exercises R2 at all, and it carries no control in the silent direction, so nothing in the constitution catches an R2 that fires where it must stay silent. That falsifies the preamble's universal claim that "Every probe carries a control in the opposite direction" -- r10-adjective, r10-corpus and r21-unsafe each carry one and r2-anchor does not -- and the hole is unclosed rather than closed elsewhere, unlike r0's C-4 finding which C-3's assert_clean covered. R2 is the branch this job edits, and a heuristic firing on correct paperwork blocks every dispatch in every project running the kit at exit 4. | Variant, venue /tmp/conaudit-r1-r2over-8l6F02: the `continue` at check-job-spec.py:996 ("The anchor text is absent from the WHOLE target file... That's evidence this span isn't this citation's anchor at all") replaced by a report. All five probes rc=0, plus test_check_job_spec.py '215 passed', --self-test 'OK (12 fixtures)', test_hooks.py '324 passed' -- every one of the eight clauses green against an R2 that now reports a wrong anchor line for correct citations. dispatch-guard.py:225-238 blocks on exit 4 ('It still blocks'), so the recourse is a per-job `**Lint exception**: R2` line in every consuming project. The suite is not a second net: `grep -c 'R2 ' test_check_job_spec.py` = 2, and its one case (test_check_job_spec.py:622, 'M1: R2 named') asserts R2 fires, never that it stays silent. |
| C-2 | minor | r0's census fix overshot its own clause text. C-2's text scopes the sweep to "every constitution and task set under `.agent-guild/state/archive/`", but the check now asserts that every archive DIRECTORY is a lintable job and returns immediately when one is not -- so a directory holding neither a constitution nor a task set turns a green blocker clause red and skips the sweep entirely, reporting it as an R10 regression when nothing about R10 changed. The archive is static for this job's lifetime, so this does not block today; the text and the check disagree about which artifact is in scope. | Venue /tmp/conaudit-r1-c2census-Odfjcf, faithful reference: r10-corpus rc=0 as found. Added `archive/2026-08-20-abandoned-at-phase0/` holding only a constitution.md (the shape a job abandoned between Phase 0 and Phase 1 leaves): 'probe: r10-corpus FAILED (1): every archive directory is a lintable job, none skipped / FAIL every archive directory is a lintable job, none skipped skipped=[2026-08-20-abandoned-at-phase0]' and probe.py:279-281 returns before a single archive is swept. guild-core/workflows/retrospective/SKILL.md:28 is what creates these directories ('offer to move this run's record into .agent-guild/state/archive/<date>/'), and nothing there guarantees a tasks/ dir. All 11 directories on disk are jobs today, so the clause is green as found. |
| C-3 | minor | R21's position in run_rules is a fork no clause pins down, and two faithful transcriptions of C-3 produce materially different output on the same paperwork. The spec's "Read first" names the decision explicitly ("a routing rule has to be placed against the existing R6, R7, R13, R14, R15, R16 sequence"), and run_rules' own docstring states the house invariant it turns on -- "evaluated in the plan's order, proofs before heuristics" -- but C-3 says only that the violation is "proof-class", which constrains the exit code and not the position. Both placements pass all eight clauses. | Reference /tmp/conaudit-r1-ref-7C9nrs places R21 after rule_R14 and before rule_R4; variant /tmp/conaudit-r1-r21last-v3s7qN moves it after rule_R12. Variant run: all five probes rc=0, suite '215 passed', --self-test rc=0, test_hooks rc=0. apparatus/CON-audit-r1/r21_placement.py, one paperwork set carrying both defects (T-001 checker-deterministic citing the rubric clause, and 'Four rules constrain how:' over three bullets): reference 'rc=1 / [proof] R21 checker-routing: tasks/T-001.md declares checker: checker-deterministic but cites C-2...'; variant 'rc=4 / [heuristic] R10 count-vs-list: constitution.md:19 says Four (4) but the following list has 3 item(s)' -- the author is pointed at the waivable heuristic and told nothing about the unwaivable routing error. Both block, so the cost is the diagnostic rather than a missed refusal; with `**Lint exception**: R10` present the variant reports R21 after all, so no waiver path opens. |
| C-6 | info | C-6's stated reason -- "because `check-job-spec.py` is parsed by `dispatch-guard`" -- describes the wrong relationship. dispatch-guard executes the linter as a subprocess and reads its exit code; it never parses the file. C-8 describes the same relationship correctly one clause later ("hooks run it as a bare `python3` call with no venv"), so the two clauses disagree about the same fact. The conclusion the reason supports ("a change to it has consumers beyond its own suite") is true and the check is unaffected. | dispatch-guard.py:167, verbatim: 'Shells out to check-job-spec.py rather than importing it'; :186 builds the path and :190-196 runs it under subprocess with JOB_SPEC_TIMEOUT_S. Constitution C-6 text at constitution.md:100, C-8 text at constitution.md:118. |
| C-4 | info | C-4's text names two legal routings; its fixture carries three tasks, and the third -- T-003, a `checker-judgment` citing the rubric clause C-2 -- is the one that catches an R21 ignoring the checker field entirely. The clause's discriminating power partly rests on a fixture element its own text never mentions. No disagreement, only an under-description: one clause naming the canonical judgment-plus-rubric routing would close it. | probe.py:398-405 builds T-001, T-002 and T-003; constitution.md:86 names only the first two. Variant --r21 ignore-checker (fires on any task citing a judgment clause, whatever its checker), venue /tmp/conaudit-r1-ignchk-uSIP9b: r21-safe 'FAIL neither legal shape trips R21 rc=1 err=R21 checker-routing: tasks/T-003.md cites C-2, whose check is a judgment rubric' -- T-003 is the task that trips it, not T-001 or T-002. |
| C-3 | info | r0's blocker is closed. The check_method-reading variant that passed all eight clauses on r0 now fails five of C-3's assertions, and the fixture's premise holds under reading: nothing in the linter requires a check_method segment to agree with the clause it names, so the deliberately disagreeing paraphrase is legal paperwork rather than a fixture defect. | Variant --r21 check-method, venue /tmp/conaudit-r1-cm-oy7ZGq: 'FAIL misrouted checker exits nonzero rc=0 err=\\'\\'; FAIL R21 named; FAIL exit 1, a proof rather than an inference; FAIL message names the task; FAIL message names the offending clause C-2'. Variant --r21 name-all (names every cited clause): 'FAIL message does NOT name the innocent clause C-1 err=...while citing C-1, C-2; at least one is a judgment rubric' (venue /tmp/conaudit-r1-nameall-64NcX9). Variant --r21 any-clause: caught twice now, at C-3's assert_clean and at both of C-4's assertions (venue /tmp/conaudit-r1-anyclause-FIVNeP) -- r0 reported C-4 missing it. Every reader of check_method_text is R4 (:1077), R5 (:1111), R6 (:1179), gather_prose_regions (:1488) and R9 (:1593); none compares a segment to the clause's check_text. R6 is satisfied by segment presence alone (:1178-1187). Empirically clean: with R21 absent (venue /tmp/conaudit-r1-nor21-00gz81) the mutated fixture gives rc=0 err='', so no other rule fires on it. |
| C-4 | info | C-4's new grep fixture is R4-legal and passes for the reason it claims, not because a malformed fixture suppresses the rule. The literal `checker-judgment:` survives YAML folding into check_method_text, the segment classifies as a script, R5's `bash -n` accepts the inner command, and a variant that greps check_method for the prefix is caught. | Direct parse: load_task_file gives check_method_text 'C-1: .agent-guild/scripts/check-build.sh \\'grep -q "checker-judgment:" README.md\\'', find_segments yields one C-1 segment with empty preamble, classify_check_text returns ('script', ...), shlex.split gives ['.agent-guild/scripts/check-build.sh', 'grep -q "checker-judgment:" README.md'], `bash -n` rc=0. Variant --r21 substring, venue /tmp/conaudit-r1-substr-Fj5c5k: r21-safe 'FAIL neither legal shape trips R21 rc=1 err=R21 checker-routing: tasks/T-002.md ... its check_method carries a checker-judgment: rubric' -- the rule can see the literal, and C-4 refuses it. |
| C-1 | info | C-1 is green against a faithful implementation and red against three separate wrong ones, and the fork its text leaves open is settled by C-2 rather than by C-1 -- confirmed by rebuilding rather than by inheriting r0's reading. Two of the three readings I built failed C-2 on the real archive, which is the corpus sweep doing exactly the work the preamble claims for it. | Reference /tmp/conaudit-r1-ref-7C9nrs (up to three intervening words, whitespace-separated, no punctuation): 'PASS r10-adjective' and 'PASS r10-corpus'. Reading B, unbounded scan to any plural noun later on the line, venue /tmp/conaudit-r1-r10unb-BbvYbm: r10-adjective passes, r10-corpus fails on two archives ("tasks/T-007.md:81 says 'two' (2) but the following list has 3 item(s)" and "tasks/T-005.md:57 says 'one' (1) but the following list has 4 item(s)"). Reading C, a three-WORD window with a bare endswith('s') test, failed r10-corpus on 2026-08-10-issue-117 because 'plus' ends in s -- the punctuation boundary is what the archive forces, and no clause text says so. R10 absent, venue /tmp/conaudit-r1-r10none-xcfPHM: r10-adjective fails 5 assertions. |
| C-7 | info | r0's C-7 finding is closed and its replacement text is honest: the hand-edit case really is C-6's, not C-7's. I performed C-7's old failing example verbatim and confirmed the split -- C-7 exits 0 on it, C-6's build-plugin leg exits 1 -- and C-7 still discriminates on the property it does claim. | Venue /tmp/conaudit-r1-c67-xgKyoz. Hand-edited plugins/agent-guild/project-template/.agent-guild/scripts/check-job-spec.py with the source untouched: C-7 verbatim 'in scope: plugins/agent-guild/project-template/.agent-guild/scripts/check-job-spec.py (dir: plugins/) / OK: 1 path(s) in scope' rc=0; `build-plugin.py --check` rc=1, 'codex: content differs: project-template/.agent-guild/scripts/check-job-spec.py'. C-7's own failing example (an unrelated `.agent-guild/hooks/dispatch-guard.py` edit): 'out of scope: .agent-guild/hooks/dispatch-guard.py' rc=1. |
| C-6 | info | C-6 and C-7 are jointly satisfiable against a complete reference implementation, and C-6 discriminates: it is the clause that refuses a source change whose generated trees were never rebuilt. | Reference /tmp/conaudit-r1-ref-7C9nrs before the rebuild: C-6 rc=1, 'generated distributions are stale relative to a fresh build (2 difference(s))'. After `python3 scripts/build-plugin.py`: C-6 rc=0 ('324 passed, 0 failed' / 'Validation passed' / 'OK: shared-core wrappers, both published packages, and both marketplaces match fresh builds'), and C-7 rc=0 over exactly the three allowlisted paths. Scope note carried forward from r0 and re-confirmed: build-plugin.py hard-fails without the `claude` CLI, which is present on this host, so C-6 fails loud rather than passing silently where it is absent. |
| C-8 | info | C-8's nested quoting survives check-build.sh's `bash -c` intact and both legs are live, and it refuses the import class its failing example names. | Reference /tmp/conaudit-r1-ref-7C9nrs: rc=0. Venue /tmp/conaudit-r1-c67-xgKyoz with `import regex` added at module level: rc=1. Host python3 is 3.14.7, so sys.stdlib_module_names is present and the AST leg is meaningful. |
| C-2 | info | The constitution's preamble facts re-derived rather than trusted: the 36-of-40 claim about multi-clause archived tasks is exact, R20 is the highest RULE_CLASS entry so R21 is free, and the corpus sweep's control fires. | Counted over `archive/*/tasks/*.md`: 40 task files, 36 citing more than one clause, 6 of those routed checker-deterministic -- matching constitution.md:46-47 and probe.py:322-323 exactly. RULE_CLASS at check-job-spec.py:553-559 ends at R20. r10-corpus's control 'the same call DOES fire on a known-bad fixture' passes in every venue where R10 is live and fails in /tmp/conaudit-r1-r10none-xcfPHM's opposite direction check. |
| C-1 | info | The `**Job weight**:` line's rewritten reason is honest and standard survives, though the ceiling it sets is now binding on this round's own remedy. Both halves of the reason are true: the deliverable extends an existing script and suite, and dispatch-guard runs the linter unattended on every dispatch, which is verbatim a standard signal in CLAUDE.md's table. The tension r0 named is real -- probe.py is 497 lines of instrument written for this job -- but the discriminator asks whether verification needs an instrument built, and the probes drive the shipped linter rather than standing in for it. | constitution.md:3. `wc -l .agent-guild/state/checks/probe.py` = 497. R17/R18 pass: 8 clauses against standard's ceiling of 8, no `**Ceiling overrun**:` line needed and none present; `grep -n 'Lint exception\\\|Ceiling overrun' constitution.md` returns nothing, so R20's waiver-honesty judgment does not apply. Consequence worth stating for the repair: the constitution is AT budget, so closing the two C-5 findings by adding a ninth clause would trip R18 and require an overrun line, while extending probe_r2_anchor's assertions costs nothing. |
| C-1 | info | Scope of this round: every one of the eight clauses was executed against the tree as found, against a full reference implementation of all three changes, and against at least one variant built to violate its own text. None blocked, none judgment-only, none unexecuted. The baseline sweep was re-derived by hand rather than trusted and matches the constitution's declarations exactly. | `python3 .agent-guild/scripts/check-baselines.py .agent-guild/state --repo-root .` -> 'check-baselines: ran 8 (3 red, 5 green), skipped 0 (0 judgment, 0 no-baseline)', exit 0. Re-derived one command at a time on the untouched tree: C-1 rc=1, C-3 rc=1, C-5 rc=1 (red as declared); C-2, C-4, C-6, C-7, C-8 rc=0 (green as declared). Reference implementation at /tmp/conaudit-r1-ref-7C9nrs: all eight clauses rc=0. Working tree verified clean at ae3f757 on feat/193-linter-three-shapes before every run and after the last; the 16 venues are listed in apparatus/CON-audit-r1/VENUES.txt and every mutation ran inside one. |

## Diagnosis

- **C-5** (major): r0's rewording added a sentence its check cannot falsify: "That line is reported whether or not it differs from the citation's own -- an anchor on the citing line is the ordinary case and must read correctly too." probe_r2_anchor's fixture hard-codes the anchor two lines below the citation and asserts only that the anchor's number appears somewhere in the message, so an implementation that omits the anchor line exactly when it equals the citing line -- the reading r0's own diagnosis ("a different number from the two the message already reports") invites -- passes all eight clauses green. The clause now asserts a property no check in the constitution can refuse.
  evidence: Variant --r2 suppress-same-line, venue /tmp/conaudit-r1-r2same-een4nA: 'PASS r10-adjective / PASS r10-corpus / PASS r21-unsafe / PASS r21-safe / PASS r2-anchor' -- all eight green. apparatus/CON-audit-r1/c5_sameline.py drives the same-line case (citation and anchor both on document line 22). Faithful reference /tmp/conaudit-r1-ref-7C9nrs: '...but the quoted code is at src/fixture.py:29; the anchor matched was `if len(val) >= 2 and val[0] == val[-1]:`, taken from tasks/T-001.md:22'. Variant, same input: '...the anchor matched was `if len(val) >= 2 and val[0] == val[-1]:`' -- no anchor line at all, and probe.py:466-470 never looks. The two runs C-5 does discriminate: --r2 quote-only fails 'diagnostic gives the anchor's own document line' (venue /tmp/conaudit-r1-r2q-sSYh0O), --r2 line-only fails 'diagnostic quotes the anchor text it matched' (venue /tmp/conaudit-r1-r2l-V8mPpF).
- **C-5** (major): C-5 is the only clause that exercises R2 at all, and it carries no control in the silent direction, so nothing in the constitution catches an R2 that fires where it must stay silent. That falsifies the preamble's universal claim that "Every probe carries a control in the opposite direction" -- r10-adjective, r10-corpus and r21-unsafe each carry one and r2-anchor does not -- and the hole is unclosed rather than closed elsewhere, unlike r0's C-4 finding which C-3's assert_clean covered. R2 is the branch this job edits, and a heuristic firing on correct paperwork blocks every dispatch in every project running the kit at exit 4.
  evidence: Variant, venue /tmp/conaudit-r1-r2over-8l6F02: the `continue` at check-job-spec.py:996 ("The anchor text is absent from the WHOLE target file... That's evidence this span isn't this citation's anchor at all") replaced by a report. All five probes rc=0, plus test_check_job_spec.py '215 passed', --self-test 'OK (12 fixtures)', test_hooks.py '324 passed' -- every one of the eight clauses green against an R2 that now reports a wrong anchor line for correct citations. dispatch-guard.py:225-238 blocks on exit 4 ('It still blocks'), so the recourse is a per-job `**Lint exception**: R2` line in every consuming project. The suite is not a second net: `grep -c 'R2 ' test_check_job_spec.py` = 2, and its one case (test_check_job_spec.py:622, 'M1: R2 named') asserts R2 fires, never that it stays silent.
- **C-2** (minor): r0's census fix overshot its own clause text. C-2's text scopes the sweep to "every constitution and task set under `.agent-guild/state/archive/`", but the check now asserts that every archive DIRECTORY is a lintable job and returns immediately when one is not -- so a directory holding neither a constitution nor a task set turns a green blocker clause red and skips the sweep entirely, reporting it as an R10 regression when nothing about R10 changed. The archive is static for this job's lifetime, so this does not block today; the text and the check disagree about which artifact is in scope.
  evidence: Venue /tmp/conaudit-r1-c2census-Odfjcf, faithful reference: r10-corpus rc=0 as found. Added `archive/2026-08-20-abandoned-at-phase0/` holding only a constitution.md (the shape a job abandoned between Phase 0 and Phase 1 leaves): 'probe: r10-corpus FAILED (1): every archive directory is a lintable job, none skipped / FAIL every archive directory is a lintable job, none skipped skipped=[2026-08-20-abandoned-at-phase0]' and probe.py:279-281 returns before a single archive is swept. guild-core/workflows/retrospective/SKILL.md:28 is what creates these directories ('offer to move this run's record into .agent-guild/state/archive/<date>/'), and nothing there guarantees a tasks/ dir. All 11 directories on disk are jobs today, so the clause is green as found.
- **C-3** (minor): R21's position in run_rules is a fork no clause pins down, and two faithful transcriptions of C-3 produce materially different output on the same paperwork. The spec's "Read first" names the decision explicitly ("a routing rule has to be placed against the existing R6, R7, R13, R14, R15, R16 sequence"), and run_rules' own docstring states the house invariant it turns on -- "evaluated in the plan's order, proofs before heuristics" -- but C-3 says only that the violation is "proof-class", which constrains the exit code and not the position. Both placements pass all eight clauses.
  evidence: Reference /tmp/conaudit-r1-ref-7C9nrs places R21 after rule_R14 and before rule_R4; variant /tmp/conaudit-r1-r21last-v3s7qN moves it after rule_R12. Variant run: all five probes rc=0, suite '215 passed', --self-test rc=0, test_hooks rc=0. apparatus/CON-audit-r1/r21_placement.py, one paperwork set carrying both defects (T-001 checker-deterministic citing the rubric clause, and 'Four rules constrain how:' over three bullets): reference 'rc=1 / [proof] R21 checker-routing: tasks/T-001.md declares checker: checker-deterministic but cites C-2...'; variant 'rc=4 / [heuristic] R10 count-vs-list: constitution.md:19 says Four (4) but the following list has 3 item(s)' -- the author is pointed at the waivable heuristic and told nothing about the unwaivable routing error. Both block, so the cost is the diagnostic rather than a missed refusal; with `**Lint exception**: R10` present the variant reports R21 after all, so no waiver path opens.
- **C-6** (info): C-6's stated reason -- "because `check-job-spec.py` is parsed by `dispatch-guard`" -- describes the wrong relationship. dispatch-guard executes the linter as a subprocess and reads its exit code; it never parses the file. C-8 describes the same relationship correctly one clause later ("hooks run it as a bare `python3` call with no venv"), so the two clauses disagree about the same fact. The conclusion the reason supports ("a change to it has consumers beyond its own suite") is true and the check is unaffected.
  evidence: dispatch-guard.py:167, verbatim: 'Shells out to check-job-spec.py rather than importing it'; :186 builds the path and :190-196 runs it under subprocess with JOB_SPEC_TIMEOUT_S. Constitution C-6 text at constitution.md:100, C-8 text at constitution.md:118.
- **C-4** (info): C-4's text names two legal routings; its fixture carries three tasks, and the third -- T-003, a `checker-judgment` citing the rubric clause C-2 -- is the one that catches an R21 ignoring the checker field entirely. The clause's discriminating power partly rests on a fixture element its own text never mentions. No disagreement, only an under-description: one clause naming the canonical judgment-plus-rubric routing would close it.
  evidence: probe.py:398-405 builds T-001, T-002 and T-003; constitution.md:86 names only the first two. Variant --r21 ignore-checker (fires on any task citing a judgment clause, whatever its checker), venue /tmp/conaudit-r1-ignchk-uSIP9b: r21-safe 'FAIL neither legal shape trips R21 rc=1 err=R21 checker-routing: tasks/T-003.md cites C-2, whose check is a judgment rubric' -- T-003 is the task that trips it, not T-001 or T-002.
- **C-3** (info): r0's blocker is closed. The check_method-reading variant that passed all eight clauses on r0 now fails five of C-3's assertions, and the fixture's premise holds under reading: nothing in the linter requires a check_method segment to agree with the clause it names, so the deliberately disagreeing paraphrase is legal paperwork rather than a fixture defect.
  evidence: Variant --r21 check-method, venue /tmp/conaudit-r1-cm-oy7ZGq: 'FAIL misrouted checker exits nonzero rc=0 err=\'\'; FAIL R21 named; FAIL exit 1, a proof rather than an inference; FAIL message names the task; FAIL message names the offending clause C-2'. Variant --r21 name-all (names every cited clause): 'FAIL message does NOT name the innocent clause C-1 err=...while citing C-1, C-2; at least one is a judgment rubric' (venue /tmp/conaudit-r1-nameall-64NcX9). Variant --r21 any-clause: caught twice now, at C-3's assert_clean and at both of C-4's assertions (venue /tmp/conaudit-r1-anyclause-FIVNeP) -- r0 reported C-4 missing it. Every reader of check_method_text is R4 (:1077), R5 (:1111), R6 (:1179), gather_prose_regions (:1488) and R9 (:1593); none compares a segment to the clause's check_text. R6 is satisfied by segment presence alone (:1178-1187). Empirically clean: with R21 absent (venue /tmp/conaudit-r1-nor21-00gz81) the mutated fixture gives rc=0 err='', so no other rule fires on it.
- **C-4** (info): C-4's new grep fixture is R4-legal and passes for the reason it claims, not because a malformed fixture suppresses the rule. The literal `checker-judgment:` survives YAML folding into check_method_text, the segment classifies as a script, R5's `bash -n` accepts the inner command, and a variant that greps check_method for the prefix is caught.
  evidence: Direct parse: load_task_file gives check_method_text 'C-1: .agent-guild/scripts/check-build.sh \'grep -q "checker-judgment:" README.md\'', find_segments yields one C-1 segment with empty preamble, classify_check_text returns ('script', ...), shlex.split gives ['.agent-guild/scripts/check-build.sh', 'grep -q "checker-judgment:" README.md'], `bash -n` rc=0. Variant --r21 substring, venue /tmp/conaudit-r1-substr-Fj5c5k: r21-safe 'FAIL neither legal shape trips R21 rc=1 err=R21 checker-routing: tasks/T-002.md ... its check_method carries a checker-judgment: rubric' -- the rule can see the literal, and C-4 refuses it.
- **C-1** (info): C-1 is green against a faithful implementation and red against three separate wrong ones, and the fork its text leaves open is settled by C-2 rather than by C-1 -- confirmed by rebuilding rather than by inheriting r0's reading. Two of the three readings I built failed C-2 on the real archive, which is the corpus sweep doing exactly the work the preamble claims for it.
  evidence: Reference /tmp/conaudit-r1-ref-7C9nrs (up to three intervening words, whitespace-separated, no punctuation): 'PASS r10-adjective' and 'PASS r10-corpus'. Reading B, unbounded scan to any plural noun later on the line, venue /tmp/conaudit-r1-r10unb-BbvYbm: r10-adjective passes, r10-corpus fails on two archives ("tasks/T-007.md:81 says 'two' (2) but the following list has 3 item(s)" and "tasks/T-005.md:57 says 'one' (1) but the following list has 4 item(s)"). Reading C, a three-WORD window with a bare endswith('s') test, failed r10-corpus on 2026-08-10-issue-117 because 'plus' ends in s -- the punctuation boundary is what the archive forces, and no clause text says so. R10 absent, venue /tmp/conaudit-r1-r10none-xcfPHM: r10-adjective fails 5 assertions.
- **C-7** (info): r0's C-7 finding is closed and its replacement text is honest: the hand-edit case really is C-6's, not C-7's. I performed C-7's old failing example verbatim and confirmed the split -- C-7 exits 0 on it, C-6's build-plugin leg exits 1 -- and C-7 still discriminates on the property it does claim.
  evidence: Venue /tmp/conaudit-r1-c67-xgKyoz. Hand-edited plugins/agent-guild/project-template/.agent-guild/scripts/check-job-spec.py with the source untouched: C-7 verbatim 'in scope: plugins/agent-guild/project-template/.agent-guild/scripts/check-job-spec.py (dir: plugins/) / OK: 1 path(s) in scope' rc=0; `build-plugin.py --check` rc=1, 'codex: content differs: project-template/.agent-guild/scripts/check-job-spec.py'. C-7's own failing example (an unrelated `.agent-guild/hooks/dispatch-guard.py` edit): 'out of scope: .agent-guild/hooks/dispatch-guard.py' rc=1.
- **C-6** (info): C-6 and C-7 are jointly satisfiable against a complete reference implementation, and C-6 discriminates: it is the clause that refuses a source change whose generated trees were never rebuilt.
  evidence: Reference /tmp/conaudit-r1-ref-7C9nrs before the rebuild: C-6 rc=1, 'generated distributions are stale relative to a fresh build (2 difference(s))'. After `python3 scripts/build-plugin.py`: C-6 rc=0 ('324 passed, 0 failed' / 'Validation passed' / 'OK: shared-core wrappers, both published packages, and both marketplaces match fresh builds'), and C-7 rc=0 over exactly the three allowlisted paths. Scope note carried forward from r0 and re-confirmed: build-plugin.py hard-fails without the `claude` CLI, which is present on this host, so C-6 fails loud rather than passing silently where it is absent.
- **C-8** (info): C-8's nested quoting survives check-build.sh's `bash -c` intact and both legs are live, and it refuses the import class its failing example names.
  evidence: Reference /tmp/conaudit-r1-ref-7C9nrs: rc=0. Venue /tmp/conaudit-r1-c67-xgKyoz with `import regex` added at module level: rc=1. Host python3 is 3.14.7, so sys.stdlib_module_names is present and the AST leg is meaningful.
- **C-2** (info): The constitution's preamble facts re-derived rather than trusted: the 36-of-40 claim about multi-clause archived tasks is exact, R20 is the highest RULE_CLASS entry so R21 is free, and the corpus sweep's control fires.
  evidence: Counted over `archive/*/tasks/*.md`: 40 task files, 36 citing more than one clause, 6 of those routed checker-deterministic -- matching constitution.md:46-47 and probe.py:322-323 exactly. RULE_CLASS at check-job-spec.py:553-559 ends at R20. r10-corpus's control 'the same call DOES fire on a known-bad fixture' passes in every venue where R10 is live and fails in /tmp/conaudit-r1-r10none-xcfPHM's opposite direction check.
- **C-1** (info): The `**Job weight**:` line's rewritten reason is honest and standard survives, though the ceiling it sets is now binding on this round's own remedy. Both halves of the reason are true: the deliverable extends an existing script and suite, and dispatch-guard runs the linter unattended on every dispatch, which is verbatim a standard signal in CLAUDE.md's table. The tension r0 named is real -- probe.py is 497 lines of instrument written for this job -- but the discriminator asks whether verification needs an instrument built, and the probes drive the shipped linter rather than standing in for it.
  evidence: constitution.md:3. `wc -l .agent-guild/state/checks/probe.py` = 497. R17/R18 pass: 8 clauses against standard's ceiling of 8, no `**Ceiling overrun**:` line needed and none present; `grep -n 'Lint exception\|Ceiling overrun' constitution.md` returns nothing, so R20's waiver-honesty judgment does not apply. Consequence worth stating for the repair: the constitution is AT budget, so closing the two C-5 findings by adding a ninth clause would trip R18 and require an overrun line, while extending probe_r2_anchor's assertions costs nothing.
- **C-1** (info): Scope of this round: every one of the eight clauses was executed against the tree as found, against a full reference implementation of all three changes, and against at least one variant built to violate its own text. None blocked, none judgment-only, none unexecuted. The baseline sweep was re-derived by hand rather than trusted and matches the constitution's declarations exactly.
  evidence: `python3 .agent-guild/scripts/check-baselines.py .agent-guild/state --repo-root .` -> 'check-baselines: ran 8 (3 red, 5 green), skipped 0 (0 judgment, 0 no-baseline)', exit 0. Re-derived one command at a time on the untouched tree: C-1 rc=1, C-3 rc=1, C-5 rc=1 (red as declared); C-2, C-4, C-6, C-7, C-8 rc=0 (green as declared). Reference implementation at /tmp/conaudit-r1-ref-7C9nrs: all eight clauses rc=0. Working tree verified clean at ae3f757 on feat/193-linter-three-shapes before every run and after the last; the 16 venues are listed in apparatus/CON-audit-r1/VENUES.txt and every mutation ran inside one.

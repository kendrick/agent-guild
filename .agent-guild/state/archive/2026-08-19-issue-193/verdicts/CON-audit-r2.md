---
task: CON-audit
checker: auditor
vendor: anthropic
model: claude-opus-5
verdict: FAIL
checked_at: 2026-08-18T03:45:55Z
duration_ms: None
cost_usd: None
---

<!-- GENERATED FILE—do not hand-edit. Rendered by render-verdict.py
from the verdict JSON, the record of record. Edit the JSON and
re-render instead. -->

## Per-clause results

| clause | severity | description | evidence |
| ------ | -------- | ------------ | -------- |
| C-1 | major | C-1's text claims R10 fires "when adjectives separate the number from the plural noun it governs", but every adjective its check ever puts between a count and its noun is `further` -- once alone, once inside `further load-bearing`. The probe's own comment claims the second case proves "the fix isn't a one-word special case", and it does not: both phrases are built on the same word. A two-word allowlist keyed to exactly those two strings passes all eight clauses, and so does an implementation gated on adjectival morphology (er/ly/ing/ed), which `further` and `load-bearing` both satisfy. The general reading and the special case are indistinguishable to this constitution, and the special case misses `Four key rules constrain how:` -- the plain shape the spec's own problem statement is about. | Variant r10-wordlist (_R10_ADJECTIVES = ("further", "load-bearing"), apparatus/CON-audit-r2/patch.py:R10_WORDLIST), venue /tmp/conaudit-r2-var-G4uwJT: C-1 rc=0, C-2 rc=0, C-3 rc=0, C-4 rc=0, C-5 rc=0, C-6 rc=0, C-7 rc=0, C-8 rc=0 -- GREEN on all eight after `python3 scripts/build-plugin.py`. Variant r10-morphology likewise green on C-1 and C-2. apparatus/CON-audit-r2/r10_adjectives.py over eight phrases: reference (/tmp/conaudit-r2-ref-2HXmwy) FIRES on all eight including `Four key rules`, `Four new rules`, `Four separate rules`, `Four such rules`, `Four brief rules`; wordlist variant FIRES on the three the probe uses and is `silent` on the other five. probe.py:220 and :229 are the only two adjective cases and both read `further`. Fix: give probe_r10_adjective at least one adjective sharing neither stem nor suffix with `further` (`Four key rules constrain how:` is enough), and name a second adjective in C-1's failing example so the text stops resting on one word. |
| C-3 | major | C-3's new last sentence -- "The rule runs ahead of the heuristics, so paperwork carrying both a routing defect and an inferred one reports the unwaivable proof rather than the waivable guess" -- is pinned by a check that tests one of the four heuristics. RULE_CLASS marks R2, R9, R10 and R12 as HEURISTIC; probe.py:397-406 builds its dual-defect fixture with an R10 count error only. An implementation placing R21 between rule_R9 and rule_R10 -- behind two heuristics, ahead of one -- passes all eight clauses. R2 is the rule this same job edits, so paperwork carrying an R2 anchor defect alongside a routing defect is the ordinary case rather than a contrivance, and that is exactly the pairing the fix cannot see. | Variant r21-after-r9 (STEP_R21 inserted before `lambda: rule_R10(ctx),`), venue /tmp/conaudit-r2-var-G4uwJT: C-1..C-8 all rc=0, GREEN, including C-3's own `R21 is reported ahead of the heuristic R10` and `and the report is the unwaivable proof exit`. apparatus/CON-audit-r2/r2_vs_r21.py builds one paperwork set carrying both an R21 routing defect (T-001 checker-deterministic citing C-1 and the rubric C-2) and an R2 anchor defect (citation on line 23, anchor on line 25, quoted code at src/fixture.py:29). Reference /tmp/conaudit-r2-ref-2HXmwy: `rc=1 / job-spec: [proof] R21 checker-routing: tasks/T-001.md declares checker: checker-deterministic but cites C-2...`. Variant: `rc=4 / job-spec: [heuristic] R2 citation-anchor: tasks/T-001.md:23 cites src/fixture.py:12...` followed by the advisory `record a **Lint exception**: R2 - <why> line in the constitution's preamble and re-run` -- the author is invited to waive a rule that was right, and told nothing about the unwaivable routing error. run_rules' step order is check-job-spec.py:1985-2005. Fix: the placement fixture has to carry a heuristic that runs BEFORE R2 in file order or an R2 defect specifically; asserting R21 ahead of R2 pins the boundary the clause's text actually names, since R2 is the first heuristic in run_rules. |
| C-6 | major | The spec's sixth acceptance criterion is `python3 .agent-guild/scripts/test_check_job_spec.py` passes, "with a case per change drawn from the archive rather than from invented fixtures, matching that suite's existing convention." C-6 covers the first half and nothing covers the second, and it is not recorded in Non-goals either. I built a complete, faithful implementation of all three changes without adding a single test case and it passed all eight clauses. `.agent-guild/state/` is gitignored in full, so probe.py is not a substitute net: it never enters the tracked suite and nothing runs it after teardown. The job as specified therefore ships three new linter behaviors with zero regression coverage in the repo's own suite, and no clause or non-goal records that as a decision. | spec.md:56. Reference implementation /tmp/conaudit-r2-ref-2HXmwy: `.agent-guild/scripts/test_check_job_spec.py` unmodified (215 passed), and run-clauses.py reports C-1..C-8 all rc=0 after `python3 scripts/build-plugin.py`. `git check-ignore -v .agent-guild/state/apparatus/CON-audit-r2/patch.py` -> `.gitignore:5:.agent-guild/state/*`; `git ls-files --error-unmatch .agent-guild/state/checks/probe.py` -> `did not match any file(s) known to git`. No archived job under state/archive/ carries a checks/ dir, so the archived copy is a record rather than a suite. Fix, whichever the orchestrator means: add a Non-goals bullet recording that the suite-content half of criterion 6 is dropped and why (#141), or add the requirement to C-6 as a judgment leg. Silence is the one option that leaves DEC-audit's coverage pass to either burn a round on it or miss it. |
| C-5 | minor | Third consecutive round with a defect in C-5, and this one is the fork case B settles silently. C-5's text says the anchor's line "is reported whether or not it differs from the citation's own"; probe.py:521-523 settles that as "the numeral appears at least twice in the message". An implementation that writes `the anchor matched was `...`, quoted on that same line` reports where the anchor sits, reads correctly to a human, and fails case B. The two readings turn on the phrase "That line is reported" -- whether it demands the numeral or admits an unambiguous reference to a line already named in the same message. Per the preamble's own standing commitment ("If a third round finds a defect in C-5, cut it and re-derive"), the cut is due. | Variant r2-prose-same, venue /tmp/conaudit-r2-var-G4uwJT: `FAIL B: anchor's line reported even though it equals the citation's anchor_doc_line=22 line='job-spec: [heuristic] R2 citation-anchor: tasks/T-001.md:22 cites src/fixture.py:12 but the quoted code is at src/fixture.py:29; the anchor matched was `if len(val) >= 2 and val[0] == val[-1]:`, quoted on that same line'`. Case A kills the two false-positive routes I could build for the >=2 count: an implementation reporting the CITING line twice gives 22/22 in case B but 22 rather than 24 in case A, and a relative offset gives neither. So the assertion is sound in the direction it guards and over-strict in the other. If the cut is taken, the probe's three cases discriminate correctly (see the r2-suppress-same and r2-overfire rows) and are worth re-deriving the clause text against rather than discarding. |
| C-3 | minor | A worker can add R21 to run_rules without adding it to RULE_CLASS and pass all eight clauses, because rule_class_of falls back to PROOF and C-3 only asserts the exit code. The linter's own --self-test claims to catch exactly this ("Every rule run_rules dispatches needs a RULE_CLASS entry") but checks a hard-coded id list that ends at R20, so that invariant goes quietly false the moment this job lands, and C-6 stays green. The observable cost is R20's diagnostic: a constitution carrying `**Lint exception**: R21` is refused either way, but with the wrong reason. | Variant: reference implementation with `"R21": PROOF` removed from RULE_CLASS, venue /tmp/conaudit-r2-var-G4uwJT after `python3 scripts/build-plugin.py`: C-1..C-8 all rc=0, GREEN, and the C-6 log line 234 reads `OK: self-test passed (12 fixtures)`. check-job-spec.py:2246-2247 is the literal id tuple, ending `"R19", "R20"`. Same fixture constitution with a `**Lint exception**: R21 - ...` preamble line: reference -> `R20 lint-exception: constitution.md waives R21, which proves its defect rather than inferring it; only a heuristic can misread paperwork (waivable: R2, R9, R10, R12)`; no-entry variant -> `R20 lint-exception: constitution.md waives R21, which is not a rule this linter has`, sending the author to hunt a typo for a rule that exists. Fix: C-3 should say the class is declared in RULE_CLASS rather than merely observed at the exit code, and r21-unsafe should assert R20's proof-class refusal message for a `**Lint exception**: R21` line -- one assertion that pins both the table entry and the self-test list. |
| C-2 | minor | r1's census fix landed and its shape is closed, but the census still counts directories rather than constitutions that parse. An archive directory whose constitution.md exists and yields zero clauses is swept, reported `R10 silent`, and counted toward the `>= 11` floor, with R10 never having read a line of it. The asymmetry is the tell: a broken TASK file in an archive fails loudly through load_context, and a broken constitution does not fail at all. That is r0's silent-coverage-loss finding surviving in a new shape. | Venue /tmp/conaudit-r2-c2-vnlIbN (reference implementation). archive/2026-08-20-corrupt/ carrying `not a constitution any more` as constitution.md plus one real task: `ok 2026-08-20-corrupt: R10 silent` / `probe: r10-corpus passed` / exit 0. Direct measurement through load_context: `2026-08-20-corrupt: err=None clauses=0 tasks=1 regions=2 R10=None` against `2026-08-11-issue-100: err=None clauses=10 tasks=5 regions=22`. Contrast archive/2026-08-22-brokentask/ (valid constitution, a task with no frontmatter): `FAIL 2026-08-22-brokentask: loads .../tasks/T-001.md: no YAML frontmatter header found`, exit 1. r1's minor is confirmed closed: archive/2026-08-21-phase0-only/ (constitution, no tasks/) gives `ok 2026-08-21-phase0-only: R10 silent`, and a tasks-only directory is noted (`note archive dirs carrying no constitution, not swept: ['2026-08-23-tasks-only']`) rather than failing. Fix: after load_context, fail the archive by name when ctx.clauses is empty. |
| C-5 | minor | C-5's text and the spec's fifth acceptance criterion now disagree about the same artifact, and nothing records the override. spec.md:55 requires the anchor's line be "distinct from the two line numbers it already reports"; C-5 says the opposite for the same-line case, correctly, after r0's finding. The constitution won that argument and the spec was never amended, so a DEC task's `## Spec excerpt` will carry the false version into the worker's brief -- which is precisely the shape #193 exists to catch, a written claim about the repo that isn't true. | spec.md:55 verbatim: "R2's diagnostic quotes the anchor text it matched and gives that anchor's line, distinct from the two line numbers it already reports." constitution.md:121 verbatim: "That line is reported whether or not it differs from the citation's own - an anchor on the citing line is the ordinary case and must read correctly too." Measured: in probe case B the anchor's document line IS one of the two already reported (anchor_doc_line=22 = the citing line), so an implementation obeying spec.md:55 literally would have to suppress it and would fail C-5. Fix: amend spec.md:55, or record the divergence in the constitution preamble so the decompose step does not excerpt the stale sentence. |
| C-6 | minor | r1 filed this as info and it was not corrected. C-6's stated reason -- "because `check-job-spec.py` is parsed by `dispatch-guard`" -- names the wrong relationship. dispatch-guard runs the linter as a subprocess and reads its exit code; it never parses the file. C-8 states the same relationship correctly one clause later. The conclusion the reason supports is true and the check is unaffected, but the clause's own stated reason is a false claim about the repo sitting inside the constitution for a job about false claims a linter should catch. | dispatch-guard.py:167 verbatim: `Shells out to check-job-spec.py rather than importing it, same reasoning as subagent-return.py's validate-verdict.py call: the CLI is the documented contract`; :186 builds the path and :190-196 runs it under subprocess with JOB_SPEC_TIMEOUT_S. constitution.md:128 (C-6) against constitution.md:146 (C-8, `hooks run it as a bare python3 call with no venv`). Fix: one word -- `run by dispatch-guard`. |
| C-3 | minor | probe.py:401 calls `tempfile.mkdtemp()` inside the placement assertion and never removes the directory, so every run of r21-unsafe leaks one state tree into $TMPDIR -- on a checker dispatch, on every audit round, and on every worker retry. The same call is the sole argument to ok() with no `detail` passed, so if that control ever fails it prints nothing to diagnose it with. | Measured: one r21-unsafe run against the untouched tree leaves exactly one new directory behind (`/var/folders/zz/.../T/tmpit92zgvh` containing `['state']`); $TMPDIR held 94 such directories at the time of measurement. probe.py:400-403, the `ok("both defects present: R10 fires on this fixture alone", rule_hit(run_linter(build_state(tempfile.mkdtemp(), ...))))` call. Fix: wrap it in a `with tempfile.TemporaryDirectory() as d2:` like every other fixture in the file, and pass a detail string. |
| C-5 | info | Both of r1's majors are genuinely closed, and case C is a live control rather than a vacuous one -- the reason it stays silent is the guard, not the rule failing to look. ABSENT_ANCHOR is 52 characters with spaces, so it clears anchor_spans' >=24-plus-a-space filter; SENTENCE_SPLIT_RE needs whitespace after the period, so `lambda r: r.stamp` does not close the sentence early; and the over-firing variant proves R2 reached the guard by naming that very span in its message. | Variant r2-suppress-same (omit the anchor line iff it equals the citing line -- r1's variant): `FAIL B: anchor's line reported even though it equals the citation's`. Variant r2-overfire (the `continue` at check-job-spec.py:996 replaced by a report -- r1's other variant): `FAIL C: an anchor absent from the target file stays silent rc=4 err="...R2 citation-anchor: tasks/T-001.md:22 cites src/fixture.py:12 but the quoted code is nowhere in src/fixture.py; the anchor matched was `for record in sorted(pending, key=lambda r: r.stamp):`, quoted at tasks/T-001.md:22"` plus `FAIL C: and the fixture is otherwise clean`. The variant naming ABSENT_ANCHOR is the proof the rule looked. Variants r2-quote-only and r2-line-only each fail their own half of case A and case B. |
| C-4 | info | C-4 discriminates in the direction it claims, and the innocent-clause assertion in C-3 does too. Every prior round's attack on the routing rule dies now, each at a clause I can name. | Variant r21-checkmethod (r0's blocker, reading the task's own check_method): C-3 rc=1, seven assertions failing, `FAIL misrouted checker exits nonzero rc=0 err=''`. Variant r21-ignore-checker (fires on any task citing a rubric): dies twice, at C-3's `r21: baseline fixture is clean before mutation rc=1 err='...tasks/T-002.md cites C-2...'` and at both of C-4's assertions via T-003. Variant r21-last (after rule_R12): C-3 `FAIL R21 is reported ahead of the heuristic R10` and `FAIL and the report is the unwaivable proof exit`. Variant r21-none: C-3 fails seven assertions. |
| C-1 | info | Every prior round's attack on R10 dies, and C-2 is what kills three of the four. The archive sweep is doing real work rather than passing by default. | Variant r10-unbounded: C-1 rc=0 but C-2 rc=1 -- `2026-08-10-issue-117: R10 silent msg="...tasks/T-007.md:81 says 'two' (2) but the following list has 3 item(s)"` and `2026-08-11-issue-100 ... T-005.md:57 says 'one' (1) but the following list has 4 item(s)`. Variant r10-lazy (`return True`): C-2 rc=1 on four archives. Variant r10-neutered (`return False`): C-1 rc=1 with the bare-count control failing, and C-2's own control `the same call DOES fire on a known-bad fixture` failing. Variant r10-none (the shipped adjacency): C-1 rc=1, five assertions. |
| C-8 | info | C-8 refuses both import shapes, including the one its exec leg cannot see. The AST leg is load-bearing rather than decorative. | Venue /tmp/conaudit-r2-var-G4uwJT on the reference implementation: module-level `import regex` -> C-8 rc=1. A lazy `import yaml` inside rule_R10's body, which module-level execution never runs -> C-8 rc=1, caught by the ast.walk leg. Host python3 is 3.14.x, so sys.stdlib_module_names exists and the leg is meaningful. |
| C-7 | info | C-6 and C-7 split the way C-7's revised text claims, re-derived rather than inherited from r1. C-7 refuses an unrelated file and stays silent on a hand-edited generated tree; C-6 is what catches the hand-edit. | Venue /tmp/conaudit-r2-var-G4uwJT. Hand-edited plugins/agent-guild/project-template/.agent-guild/scripts/check-job-spec.py with the source untouched: C-6 rc=1 `generated distributions are stale relative to a fresh build (1 difference(s)): - codex: content differs`, C-7 rc=0 `OK: 3 path(s) in scope`. An unauthorized append to .agent-guild/hooks/dispatch-guard.py: C-7 rc=1 `check-diff-scope: out of scope: .agent-guild/hooks/dispatch-guard.py`. Source patched with the generated trees not yet rebuilt: C-6 rc=1 naming both published trees. |
| C-4 | info | The preamble's universal claim `Every probe carries a control in the opposite direction` is still not true of all five probes -- r21-safe carries none of its own -- but unlike r1's C-5 finding, this hole is closed elsewhere and does not need a fix beyond the sentence. C-3 is a blocker with baseline red, so an R21 that never fires cannot pass the constitution however green r21-safe stays. | probe.py:409-433 asserts only that neither legal shape trips R21; there is no case in it that requires R21 to fire. The preamble at constitution.md:23-27 enumerates r10-adjective, r10-corpus and r21-unsafe as carrying controls, three of five, while claiming all of them do. Measured: variant r21-none leaves r21-safe green (C-4 rc=0) and C-3 red (rc=1), so the coverage exists. Fix is the sentence, not the probe. |
| C-2 | info | Baselines re-derived by hand rather than trusted, and they match the constitution's declarations exactly. Weight, clause count and the preamble rules re-derived too. | `python3 .agent-guild/scripts/check-baselines.py .agent-guild/state --repo-root .` -> `check-baselines: ran 8 (3 red, 5 green), skipped 0 (0 judgment, 0 no-baseline)`, exit 0. Independently, each check run verbatim on the untouched tree at ae3f757: C-1 rc=1, C-3 rc=1, C-5 rc=1; C-2, C-4, C-6, C-7, C-8 rc=0. Parsed baselines: C-1 red, C-2 green, C-3 red, C-4 green, C-5 red, C-6 green, C-7 green, C-8 green. 8 clause headings, weight standard, CLAUSE_CEILINGS['standard'] = 8, so at budget with no overrun line needed and none present; rule_R17, rule_R18, rule_R19 and rule_R20 all return None against this constitution, and no `**Lint exception**:` line exists, so no waiver honesty judgment applies. |
| C-6 | info | Scope of this round: all eight clauses executed against the untouched tree, against a reference implementation of all three changes built from clause text alone, and against at least one variant built to violate each clause's own text. None blocked, none judgment-only, none unexecuted. The working tree was clean at ae3f757 on feat/193-linter-three-shapes before the first run and clean after the last; every mutation ran inside a venue outside the repo. | Venues, absolute: /tmp/conaudit-r2-ref-2HXmwy (reference), /tmp/conaudit-r2-var-G4uwJT (variant venue, restored from a pristine copy per variant), /tmp/conaudit-r2-c2-vnlIbN (C-2 archive shapes), /tmp/conaudit-r2-keep (the pristine linter, held outside every venue so it cannot dirty one). Apparatus at .agent-guild/state/apparatus/CON-audit-r2/: patch.py (reference plus 15 named variants), run-clauses.py (runs each `- **check**:` string verbatim under bash -c with cwd set to a venue), r2_vs_r21.py, r10_adjectives.py, VENUES.txt. `git status --porcelain` empty before and after. The reference implementation is green on all eight clauses after `python3 scripts/build-plugin.py`, which is the rebuild C-6 exists to require. |

## Diagnosis

- **C-1** (major): C-1's text claims R10 fires "when adjectives separate the number from the plural noun it governs", but every adjective its check ever puts between a count and its noun is `further` -- once alone, once inside `further load-bearing`. The probe's own comment claims the second case proves "the fix isn't a one-word special case", and it does not: both phrases are built on the same word. A two-word allowlist keyed to exactly those two strings passes all eight clauses, and so does an implementation gated on adjectival morphology (er/ly/ing/ed), which `further` and `load-bearing` both satisfy. The general reading and the special case are indistinguishable to this constitution, and the special case misses `Four key rules constrain how:` -- the plain shape the spec's own problem statement is about.
  evidence: Variant r10-wordlist (_R10_ADJECTIVES = ("further", "load-bearing"), apparatus/CON-audit-r2/patch.py:R10_WORDLIST), venue /tmp/conaudit-r2-var-G4uwJT: C-1 rc=0, C-2 rc=0, C-3 rc=0, C-4 rc=0, C-5 rc=0, C-6 rc=0, C-7 rc=0, C-8 rc=0 -- GREEN on all eight after `python3 scripts/build-plugin.py`. Variant r10-morphology likewise green on C-1 and C-2. apparatus/CON-audit-r2/r10_adjectives.py over eight phrases: reference (/tmp/conaudit-r2-ref-2HXmwy) FIRES on all eight including `Four key rules`, `Four new rules`, `Four separate rules`, `Four such rules`, `Four brief rules`; wordlist variant FIRES on the three the probe uses and is `silent` on the other five. probe.py:220 and :229 are the only two adjective cases and both read `further`. Fix: give probe_r10_adjective at least one adjective sharing neither stem nor suffix with `further` (`Four key rules constrain how:` is enough), and name a second adjective in C-1's failing example so the text stops resting on one word.
- **C-3** (major): C-3's new last sentence -- "The rule runs ahead of the heuristics, so paperwork carrying both a routing defect and an inferred one reports the unwaivable proof rather than the waivable guess" -- is pinned by a check that tests one of the four heuristics. RULE_CLASS marks R2, R9, R10 and R12 as HEURISTIC; probe.py:397-406 builds its dual-defect fixture with an R10 count error only. An implementation placing R21 between rule_R9 and rule_R10 -- behind two heuristics, ahead of one -- passes all eight clauses. R2 is the rule this same job edits, so paperwork carrying an R2 anchor defect alongside a routing defect is the ordinary case rather than a contrivance, and that is exactly the pairing the fix cannot see.
  evidence: Variant r21-after-r9 (STEP_R21 inserted before `lambda: rule_R10(ctx),`), venue /tmp/conaudit-r2-var-G4uwJT: C-1..C-8 all rc=0, GREEN, including C-3's own `R21 is reported ahead of the heuristic R10` and `and the report is the unwaivable proof exit`. apparatus/CON-audit-r2/r2_vs_r21.py builds one paperwork set carrying both an R21 routing defect (T-001 checker-deterministic citing C-1 and the rubric C-2) and an R2 anchor defect (citation on line 23, anchor on line 25, quoted code at src/fixture.py:29). Reference /tmp/conaudit-r2-ref-2HXmwy: `rc=1 / job-spec: [proof] R21 checker-routing: tasks/T-001.md declares checker: checker-deterministic but cites C-2...`. Variant: `rc=4 / job-spec: [heuristic] R2 citation-anchor: tasks/T-001.md:23 cites src/fixture.py:12...` followed by the advisory `record a **Lint exception**: R2 - <why> line in the constitution's preamble and re-run` -- the author is invited to waive a rule that was right, and told nothing about the unwaivable routing error. run_rules' step order is check-job-spec.py:1985-2005. Fix: the placement fixture has to carry a heuristic that runs BEFORE R2 in file order or an R2 defect specifically; asserting R21 ahead of R2 pins the boundary the clause's text actually names, since R2 is the first heuristic in run_rules.
- **C-6** (major): The spec's sixth acceptance criterion is `python3 .agent-guild/scripts/test_check_job_spec.py` passes, "with a case per change drawn from the archive rather than from invented fixtures, matching that suite's existing convention." C-6 covers the first half and nothing covers the second, and it is not recorded in Non-goals either. I built a complete, faithful implementation of all three changes without adding a single test case and it passed all eight clauses. `.agent-guild/state/` is gitignored in full, so probe.py is not a substitute net: it never enters the tracked suite and nothing runs it after teardown. The job as specified therefore ships three new linter behaviors with zero regression coverage in the repo's own suite, and no clause or non-goal records that as a decision.
  evidence: spec.md:56. Reference implementation /tmp/conaudit-r2-ref-2HXmwy: `.agent-guild/scripts/test_check_job_spec.py` unmodified (215 passed), and run-clauses.py reports C-1..C-8 all rc=0 after `python3 scripts/build-plugin.py`. `git check-ignore -v .agent-guild/state/apparatus/CON-audit-r2/patch.py` -> `.gitignore:5:.agent-guild/state/*`; `git ls-files --error-unmatch .agent-guild/state/checks/probe.py` -> `did not match any file(s) known to git`. No archived job under state/archive/ carries a checks/ dir, so the archived copy is a record rather than a suite. Fix, whichever the orchestrator means: add a Non-goals bullet recording that the suite-content half of criterion 6 is dropped and why (#141), or add the requirement to C-6 as a judgment leg. Silence is the one option that leaves DEC-audit's coverage pass to either burn a round on it or miss it.
- **C-5** (minor): Third consecutive round with a defect in C-5, and this one is the fork case B settles silently. C-5's text says the anchor's line "is reported whether or not it differs from the citation's own"; probe.py:521-523 settles that as "the numeral appears at least twice in the message". An implementation that writes `the anchor matched was `...`, quoted on that same line` reports where the anchor sits, reads correctly to a human, and fails case B. The two readings turn on the phrase "That line is reported" -- whether it demands the numeral or admits an unambiguous reference to a line already named in the same message. Per the preamble's own standing commitment ("If a third round finds a defect in C-5, cut it and re-derive"), the cut is due.
  evidence: Variant r2-prose-same, venue /tmp/conaudit-r2-var-G4uwJT: `FAIL B: anchor's line reported even though it equals the citation's  anchor_doc_line=22 line='job-spec: [heuristic] R2 citation-anchor: tasks/T-001.md:22 cites src/fixture.py:12 but the quoted code is at src/fixture.py:29; the anchor matched was `if len(val) >= 2 and val[0] == val[-1]:`, quoted on that same line'`. Case A kills the two false-positive routes I could build for the >=2 count: an implementation reporting the CITING line twice gives 22/22 in case B but 22 rather than 24 in case A, and a relative offset gives neither. So the assertion is sound in the direction it guards and over-strict in the other. If the cut is taken, the probe's three cases discriminate correctly (see the r2-suppress-same and r2-overfire rows) and are worth re-deriving the clause text against rather than discarding.
- **C-3** (minor): A worker can add R21 to run_rules without adding it to RULE_CLASS and pass all eight clauses, because rule_class_of falls back to PROOF and C-3 only asserts the exit code. The linter's own --self-test claims to catch exactly this ("Every rule run_rules dispatches needs a RULE_CLASS entry") but checks a hard-coded id list that ends at R20, so that invariant goes quietly false the moment this job lands, and C-6 stays green. The observable cost is R20's diagnostic: a constitution carrying `**Lint exception**: R21` is refused either way, but with the wrong reason.
  evidence: Variant: reference implementation with `"R21": PROOF` removed from RULE_CLASS, venue /tmp/conaudit-r2-var-G4uwJT after `python3 scripts/build-plugin.py`: C-1..C-8 all rc=0, GREEN, and the C-6 log line 234 reads `OK: self-test passed (12 fixtures)`. check-job-spec.py:2246-2247 is the literal id tuple, ending `"R19", "R20"`. Same fixture constitution with a `**Lint exception**: R21 - ...` preamble line: reference -> `R20 lint-exception: constitution.md waives R21, which proves its defect rather than inferring it; only a heuristic can misread paperwork (waivable: R2, R9, R10, R12)`; no-entry variant -> `R20 lint-exception: constitution.md waives R21, which is not a rule this linter has`, sending the author to hunt a typo for a rule that exists. Fix: C-3 should say the class is declared in RULE_CLASS rather than merely observed at the exit code, and r21-unsafe should assert R20's proof-class refusal message for a `**Lint exception**: R21` line -- one assertion that pins both the table entry and the self-test list.
- **C-2** (minor): r1's census fix landed and its shape is closed, but the census still counts directories rather than constitutions that parse. An archive directory whose constitution.md exists and yields zero clauses is swept, reported `R10 silent`, and counted toward the `>= 11` floor, with R10 never having read a line of it. The asymmetry is the tell: a broken TASK file in an archive fails loudly through load_context, and a broken constitution does not fail at all. That is r0's silent-coverage-loss finding surviving in a new shape.
  evidence: Venue /tmp/conaudit-r2-c2-vnlIbN (reference implementation). archive/2026-08-20-corrupt/ carrying `not a constitution any more` as constitution.md plus one real task: `ok   2026-08-20-corrupt: R10 silent` / `probe: r10-corpus passed` / exit 0. Direct measurement through load_context: `2026-08-20-corrupt: err=None clauses=0 tasks=1 regions=2 R10=None` against `2026-08-11-issue-100: err=None clauses=10 tasks=5 regions=22`. Contrast archive/2026-08-22-brokentask/ (valid constitution, a task with no frontmatter): `FAIL 2026-08-22-brokentask: loads  .../tasks/T-001.md: no YAML frontmatter header found`, exit 1. r1's minor is confirmed closed: archive/2026-08-21-phase0-only/ (constitution, no tasks/) gives `ok   2026-08-21-phase0-only: R10 silent`, and a tasks-only directory is noted (`note archive dirs carrying no constitution, not swept: ['2026-08-23-tasks-only']`) rather than failing. Fix: after load_context, fail the archive by name when ctx.clauses is empty.
- **C-5** (minor): C-5's text and the spec's fifth acceptance criterion now disagree about the same artifact, and nothing records the override. spec.md:55 requires the anchor's line be "distinct from the two line numbers it already reports"; C-5 says the opposite for the same-line case, correctly, after r0's finding. The constitution won that argument and the spec was never amended, so a DEC task's `## Spec excerpt` will carry the false version into the worker's brief -- which is precisely the shape #193 exists to catch, a written claim about the repo that isn't true.
  evidence: spec.md:55 verbatim: "R2's diagnostic quotes the anchor text it matched and gives that anchor's line, distinct from the two line numbers it already reports." constitution.md:121 verbatim: "That line is reported whether or not it differs from the citation's own - an anchor on the citing line is the ordinary case and must read correctly too." Measured: in probe case B the anchor's document line IS one of the two already reported (anchor_doc_line=22 = the citing line), so an implementation obeying spec.md:55 literally would have to suppress it and would fail C-5. Fix: amend spec.md:55, or record the divergence in the constitution preamble so the decompose step does not excerpt the stale sentence.
- **C-6** (minor): r1 filed this as info and it was not corrected. C-6's stated reason -- "because `check-job-spec.py` is parsed by `dispatch-guard`" -- names the wrong relationship. dispatch-guard runs the linter as a subprocess and reads its exit code; it never parses the file. C-8 states the same relationship correctly one clause later. The conclusion the reason supports is true and the check is unaffected, but the clause's own stated reason is a false claim about the repo sitting inside the constitution for a job about false claims a linter should catch.
  evidence: dispatch-guard.py:167 verbatim: `Shells out to check-job-spec.py rather than importing it, same reasoning as subagent-return.py's validate-verdict.py call: the CLI is the documented contract`; :186 builds the path and :190-196 runs it under subprocess with JOB_SPEC_TIMEOUT_S. constitution.md:128 (C-6) against constitution.md:146 (C-8, `hooks run it as a bare python3 call with no venv`). Fix: one word -- `run by dispatch-guard`.
- **C-3** (minor): probe.py:401 calls `tempfile.mkdtemp()` inside the placement assertion and never removes the directory, so every run of r21-unsafe leaks one state tree into $TMPDIR -- on a checker dispatch, on every audit round, and on every worker retry. The same call is the sole argument to ok() with no `detail` passed, so if that control ever fails it prints nothing to diagnose it with.
  evidence: Measured: one r21-unsafe run against the untouched tree leaves exactly one new directory behind (`/var/folders/zz/.../T/tmpit92zgvh` containing `['state']`); $TMPDIR held 94 such directories at the time of measurement. probe.py:400-403, the `ok("both defects present: R10 fires on this fixture alone", rule_hit(run_linter(build_state(tempfile.mkdtemp(), ...))))` call. Fix: wrap it in a `with tempfile.TemporaryDirectory() as d2:` like every other fixture in the file, and pass a detail string.
- **C-5** (info): Both of r1's majors are genuinely closed, and case C is a live control rather than a vacuous one -- the reason it stays silent is the guard, not the rule failing to look. ABSENT_ANCHOR is 52 characters with spaces, so it clears anchor_spans' >=24-plus-a-space filter; SENTENCE_SPLIT_RE needs whitespace after the period, so `lambda r: r.stamp` does not close the sentence early; and the over-firing variant proves R2 reached the guard by naming that very span in its message.
  evidence: Variant r2-suppress-same (omit the anchor line iff it equals the citing line -- r1's variant): `FAIL B: anchor's line reported even though it equals the citation's`. Variant r2-overfire (the `continue` at check-job-spec.py:996 replaced by a report -- r1's other variant): `FAIL C: an anchor absent from the target file stays silent  rc=4 err="...R2 citation-anchor: tasks/T-001.md:22 cites src/fixture.py:12 but the quoted code is nowhere in src/fixture.py; the anchor matched was `for record in sorted(pending, key=lambda r: r.stamp):`, quoted at tasks/T-001.md:22"` plus `FAIL C: and the fixture is otherwise clean`. The variant naming ABSENT_ANCHOR is the proof the rule looked. Variants r2-quote-only and r2-line-only each fail their own half of case A and case B.
- **C-4** (info): C-4 discriminates in the direction it claims, and the innocent-clause assertion in C-3 does too. Every prior round's attack on the routing rule dies now, each at a clause I can name.
  evidence: Variant r21-checkmethod (r0's blocker, reading the task's own check_method): C-3 rc=1, seven assertions failing, `FAIL misrouted checker exits nonzero rc=0 err=''`. Variant r21-ignore-checker (fires on any task citing a rubric): dies twice, at C-3's `r21: baseline fixture is clean before mutation  rc=1 err='...tasks/T-002.md cites C-2...'` and at both of C-4's assertions via T-003. Variant r21-last (after rule_R12): C-3 `FAIL R21 is reported ahead of the heuristic R10` and `FAIL and the report is the unwaivable proof exit`. Variant r21-none: C-3 fails seven assertions.
- **C-1** (info): Every prior round's attack on R10 dies, and C-2 is what kills three of the four. The archive sweep is doing real work rather than passing by default.
  evidence: Variant r10-unbounded: C-1 rc=0 but C-2 rc=1 -- `2026-08-10-issue-117: R10 silent  msg="...tasks/T-007.md:81 says 'two' (2) but the following list has 3 item(s)"` and `2026-08-11-issue-100 ... T-005.md:57 says 'one' (1) but the following list has 4 item(s)`. Variant r10-lazy (`return True`): C-2 rc=1 on four archives. Variant r10-neutered (`return False`): C-1 rc=1 with the bare-count control failing, and C-2's own control `the same call DOES fire on a known-bad fixture` failing. Variant r10-none (the shipped adjacency): C-1 rc=1, five assertions.
- **C-8** (info): C-8 refuses both import shapes, including the one its exec leg cannot see. The AST leg is load-bearing rather than decorative.
  evidence: Venue /tmp/conaudit-r2-var-G4uwJT on the reference implementation: module-level `import regex` -> C-8 rc=1. A lazy `import yaml` inside rule_R10's body, which module-level execution never runs -> C-8 rc=1, caught by the ast.walk leg. Host python3 is 3.14.x, so sys.stdlib_module_names exists and the leg is meaningful.
- **C-7** (info): C-6 and C-7 split the way C-7's revised text claims, re-derived rather than inherited from r1. C-7 refuses an unrelated file and stays silent on a hand-edited generated tree; C-6 is what catches the hand-edit.
  evidence: Venue /tmp/conaudit-r2-var-G4uwJT. Hand-edited plugins/agent-guild/project-template/.agent-guild/scripts/check-job-spec.py with the source untouched: C-6 rc=1 `generated distributions are stale relative to a fresh build (1 difference(s)): - codex: content differs`, C-7 rc=0 `OK: 3 path(s) in scope`. An unauthorized append to .agent-guild/hooks/dispatch-guard.py: C-7 rc=1 `check-diff-scope: out of scope: .agent-guild/hooks/dispatch-guard.py`. Source patched with the generated trees not yet rebuilt: C-6 rc=1 naming both published trees.
- **C-4** (info): The preamble's universal claim `Every probe carries a control in the opposite direction` is still not true of all five probes -- r21-safe carries none of its own -- but unlike r1's C-5 finding, this hole is closed elsewhere and does not need a fix beyond the sentence. C-3 is a blocker with baseline red, so an R21 that never fires cannot pass the constitution however green r21-safe stays.
  evidence: probe.py:409-433 asserts only that neither legal shape trips R21; there is no case in it that requires R21 to fire. The preamble at constitution.md:23-27 enumerates r10-adjective, r10-corpus and r21-unsafe as carrying controls, three of five, while claiming all of them do. Measured: variant r21-none leaves r21-safe green (C-4 rc=0) and C-3 red (rc=1), so the coverage exists. Fix is the sentence, not the probe.
- **C-2** (info): Baselines re-derived by hand rather than trusted, and they match the constitution's declarations exactly. Weight, clause count and the preamble rules re-derived too.
  evidence: `python3 .agent-guild/scripts/check-baselines.py .agent-guild/state --repo-root .` -> `check-baselines: ran 8 (3 red, 5 green), skipped 0 (0 judgment, 0 no-baseline)`, exit 0. Independently, each check run verbatim on the untouched tree at ae3f757: C-1 rc=1, C-3 rc=1, C-5 rc=1; C-2, C-4, C-6, C-7, C-8 rc=0. Parsed baselines: C-1 red, C-2 green, C-3 red, C-4 green, C-5 red, C-6 green, C-7 green, C-8 green. 8 clause headings, weight standard, CLAUSE_CEILINGS['standard'] = 8, so at budget with no overrun line needed and none present; rule_R17, rule_R18, rule_R19 and rule_R20 all return None against this constitution, and no `**Lint exception**:` line exists, so no waiver honesty judgment applies.
- **C-6** (info): Scope of this round: all eight clauses executed against the untouched tree, against a reference implementation of all three changes built from clause text alone, and against at least one variant built to violate each clause's own text. None blocked, none judgment-only, none unexecuted. The working tree was clean at ae3f757 on feat/193-linter-three-shapes before the first run and clean after the last; every mutation ran inside a venue outside the repo.
  evidence: Venues, absolute: /tmp/conaudit-r2-ref-2HXmwy (reference), /tmp/conaudit-r2-var-G4uwJT (variant venue, restored from a pristine copy per variant), /tmp/conaudit-r2-c2-vnlIbN (C-2 archive shapes), /tmp/conaudit-r2-keep (the pristine linter, held outside every venue so it cannot dirty one). Apparatus at .agent-guild/state/apparatus/CON-audit-r2/: patch.py (reference plus 15 named variants), run-clauses.py (runs each `- **check**:` string verbatim under bash -c with cwd set to a venue), r2_vs_r21.py, r10_adjectives.py, VENUES.txt. `git status --porcelain` empty before and after. The reference implementation is green on all eight clauses after `python3 scripts/build-plugin.py`, which is the rebuild C-6 exists to require.

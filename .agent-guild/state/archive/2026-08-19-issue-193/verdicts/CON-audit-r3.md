---
task: CON-audit
checker: auditor
vendor: anthropic
model: claude-opus-5
verdict: FAIL
checked_at: 2026-08-18T04:35:45Z
duration_ms: None
cost_usd: None
---

<!-- GENERATED FILE—do not hand-edit. Rendered by render-verdict.py
from the verdict JSON, the record of record. Edit the JSON and
re-render instead. -->

## Per-clause results

| clause | severity | description | evidence |
| ------ | -------- | ------------ | -------- |
| C-5 | blocker | C-5's re-derived text sells the anchor's position as sayable "by repeating the number or by saying so in words, the wording being the author's choice", and the words branch re-admits the exact implementation r1 caught. probe.py:574 matches `same line\|that line\|the citing line` anywhere on the R2 line, and `that line` is phrasing an R2 message naturally carries about the CITED line in the target file, which is a different line in a different document. I built r1's variant again -- the anchor's own position printed when it differs from the citation's and omitted when it does not -- worded `but the anchor `...` is not on that line; it is at src/fixture.py:29`, and C-5 passes. Case B's assertion, "the anchor's position is stated though it equals the citation's", goes green on a message that states no position at all. This is not a corner: the #117 corpus's own R2 case has its anchor ON the citing line (verified through the suite's M1 mutation), so case B is the ordinary shape, and what ships is a diagnostic that still leaves an author permuting prose in the common case. Fourth consecutive round with a defect in C-5, with the one cut already spent, against conventions.md:53's "when two rounds find a blocker in the same mechanism, cut the mechanism rather than patch it a third time". Fix, and it has to stop settling this in prose: require the anchor's position as a `label:line` pair in both cases -- in case B that is `tasks/T-001.md:22` appearing twice on the line -- and rewrite the text to say the position is reported as a path:line pair, retiring the "wording being the author's choice" sentence. That closes the fork by choosing rather than by admitting both readings. If the reading-well property is what the clause actually wants, the honest form is a `checker-judgment:` rubric, which routes to checker-judgment and stops pretending a regex can tell the two apart. | Variant said-same (apparatus/CON-audit-r3/patch.py:SAIDSAME_R2_MSG), venue /tmp/conaudit-r3-var-said-same-KmgoWZ: C-5 rc=0 PASS. Case A message: 'R2 citation-anchor: tasks/T-001.md:22 cites src/fixture.py:12 but the anchor `if len(val) >= 2 and val[0] == val[-1]:` (found at tasks/T-001.md:24) is not on that line; it is at src/fixture.py:29'. Case B message, anchor at task line 22: 'R2 citation-anchor: tasks/T-001.md:22 cites src/fixture.py:12 but the anchor `...` is not on that line; it is at src/fixture.py:29' -- no anchor position anywhere in it. probe.py:573-577; constitution.md:156. |
| C-9 | blocker | C-9's text says "Disabling any one of the three shipped behaviors ... turns `test_check_job_spec.py` red, so each change has a case in the repo's own suite", and its check cannot see whether the R2 diagnostic -- one of the three -- has one. Stubbing `nearest_anchor` turns the suite red through cases that predate this job: I measured it in the reference tree, and the stub fails M1 and `--self-test`, both of which cover R2 as it already was. So a delivered tree carrying the whole new anchor-naming diagnostic and not one suite case for it passes C-9, C-6 and C-5 together. The clause's own escape hatch is false: "R2 carries the same gap and closes it through C-5 instead, whose three cases a stubbed suite cannot satisfy" -- C-5's three cases live in `.agent-guild/state/checks/probe.py`, which is gitignored and deleted at teardown, which is the premise C-9 exists on. C-5 covers the behavior; nothing covers the behavior surviving the harness that proved it. Net effect: R2's diagnostic ships with no live regression net, which is exactly the hole r2's C-6 major reported and C-9 was written to close. Fix: give the R2 half a revert-shaped mutation the way R10 got one -- restore the shipped message body (`but the quoted code is at {path}:{actual_line}`) inside `check_citation_rules` and require the suite to go red -- or assert the mutation-detection per NEW behavior rather than per function name. | Variant no-r2-case (apparatus/CON-audit-r3/patch.py), venue /tmp/conaudit-r3-var-no-r2-case-m6hjdH: C-5 rc=0 PASS, C-6 rc=0 PASS, C-9 rc=0 PASS with the suite carrying archive-drawn cases for the R10 relaxation and R21 and none for R2. Per-mutation case failures measured against the reference tree: stub nearest_anchor -> 5 failing cases, of which `M1 stale code citation` and `--self-test` predate this job; stub rule_R21 -> 3, all new; revert _governs_plural_noun -> 2, all new. constitution.md:188-194. |
| C-1 | major | C-1's text claims R10 fires "when adjectives separate the number from the plural noun it governs", and r2's remedy -- five adjectives sharing no stem or suffix -- narrows the evasion without closing it, because the five words are readable out of `probe.py` by the worker they gate. A six-word allowlist keyed to exactly the words the probe uses passes C-1, C-2, C-6 and C-9, and is silent on `Four such rules constrain how:` (the phrase r2's own finding used), `Four additional rules`, `Four remaining rules` and `Four unrelated rules`, all four of which the reference implementation fires on. No fixed example set can pin an open word class against an implementation that can read the examples. Fix: generate one nonce adjective per run -- `Four glorptic rules constrain how:` -- alongside the five fixed ones. A correct predicate fires on it because the relaxation is about position, not vocabulary, and C-8 rules out any implementation that would legitimately need a real word (stdlib-only, no POS tagger); an allowlist cannot anticipate a token that does not exist until the probe runs. | Variant allowlist (apparatus/CON-audit-r3/patch.py:ALLOWLIST, `_R10_ADJECTIVES = ("further", "key", "new", "separate", "brief", "load-bearing")`), venue /tmp/conaudit-r3-var-allowlist-I6NhNR: C-1 rc=0, C-2 rc=0, C-6 rc=0, C-9 rc=0. Direct call of find_r10_violation over eight phrases, allowlist vs reference: 'Four such rules' silent/FIRES, 'Four additional rules' silent/FIRES, 'Four remaining rules' silent/FIRES, 'Four unrelated rules' silent/FIRES. probe.py:222-233; constitution.md:128. |
| C-9 | major | C-9 refuses a conforming implementation and misreports why, and the guard that should have caught it can never fail. `revert_adjacency` (probe.py:701-715) returns `"\\n".join(out) != src`, and `src` ends in a newline while the join does not, so it returns True on every input -- including one where it wrote the file back byte for byte. Run it against the untouched tree, where `_governs_plural_noun` is already strict: it reports the revert landed and the file does not change by a single byte. That makes the preamble's "Every probe that mutates its input asserts the mutation landed before it asserts anything else" false of this mutation, which is the `kendrick/skills#27` shape the probe's own docstring cites as its reason. The consequence is a false FAIL. I built the relaxation in the caller, leaving `_governs_plural_noun` identical to its shipped strict form: C-1, C-2 and C-6 pass, the suite carries the archive-drawn relaxation case M7 and M7 is green -- and C-9 fails, saying "the suite has no case for the relaxation itself", which is exactly backwards. A worker handed that diagnosis writes a test that already exists and burns a retry. Fix, two parts: compare against real bytes (`out_text = "\\n".join(out) + "\\n"; return out_text != src`), and say in C-9's text that the relaxation must live in `_governs_plural_noun` -- the clause requires it today and does not say so -- so a no-op revert reports "the predicate is not where the relaxation lives" instead of blaming the suite. | revert_adjacency against the untouched .agent-guild/scripts/check-job-spec.py: returned True, `file actually changed: False`, zero diff lines. Variant strict-elsewhere (apparatus/CON-audit-r3/patch.py:STRICT_ELSEWHERE_HELPER), venue /tmp/conaudit-r3-var-strict-elsewhere-N60NV5: C-1 rc=0, C-2 rc=0, C-6 rc=0, C-9 rc=1 'R10 relaxation: the old strict behavior turns the suite red -- rc=0 the suite has no case for the relaxation itself', while that venue's own suite reports `ok M7 adjective between count and list: exit 4 (heuristic)` and `ok M7: R10 named`. probe.py:701-715, :677-687; constitution.md:188-194. |
| C-9 | major | C-9 is satisfiable with no behavioral case for any of the three changes. Mutation-detection is a proxy for coverage, and any assertion that reads the linter's bytes goes red under all four mutations while testing nothing. I added one line to the suite -- a sha256 of `check-job-spec.py` compared against its shipped digest -- and deleted every other new case: C-6 and C-9 both pass. This is #141's defect recurring inside the clause built to escape it; the preamble's "A check cannot tell a real test from a written one" is still true here, one level up. It takes deliberate gaming rather than ordinary sloppiness, which is why it sits below the two findings above, but the whole reason C-9 exists is that the probe harness vanishes and the repo's suite is what remains. Fix: pin each mutation to a named case rather than to the suite's exit code -- assert that the suite's own failure output names a case id the clause requires (M7/M8/M9-shaped), so a red run has to be red for the behavior that was disabled. A digest assertion turns everything red and names nothing. | Variant hash (apparatus/CON-audit-r3/patch.py:HASH_CASE), venue /tmp/conaudit-r3-var-hash-tJ3yJ8: C-6 rc=0 PASS, C-9 rc=0 PASS with the suite's only new assertion being `check("the shipped linter is intact", _digest == "<sha256>")`. The weaker line-count form does NOT satisfy it (venue /tmp/conaudit-r3-var-linecount-3RPX90, C-9 rc=1), because revert_adjacency replaces a 3-line function with a 3-line function -- so the hole is content-shaped, not size-shaped. probe.py:657-668. |
| C-9 | minor | The second half of the spec's sixth acceptance criterion is still covered by nothing. spec.md:56 asks for the suite to pass "with a case per change drawn from the archive rather than from invented fixtures, matching that suite's existing convention". r2 filed that as a major against C-6 because nothing covered it; C-9 closed the "a case per change" half and left the provenance half open. I built the three new cases as invented fixtures with no corpus mutation anywhere: C-6 and C-9 pass, and that variant's linter is byte-identical to the reference, so the remaining seven clauses hold unchanged. Two honest ways out, and picking either is fine: assert the provenance (require each new case to route through `copy_corpus_state` + `mutate`, which is mechanical and matches how M1-M6 are written), or rule the convention non-binding and record the divergence in the preamble the way the R2 criterion's divergence is already recorded. What is not fine is a criterion that reads as required and is checked by nothing, since Phase 1 will excerpt it into a brief and a checker will have no clause to hold it to. | Variant synthetic (apparatus/CON-audit-r3/patch.py:SYNTHETIC_CASES), venue /tmp/conaudit-r3-var-synthetic-kfGrfr: C-6 rc=0 PASS, C-9 rc=0 PASS; `diff` of that venue's check-job-spec.py against the reference venue's is empty. spec.md:56; constitution.md:188-194. |
| C-6 | minor | Third round on the same false sentence. C-6's text says the four suites matter "because `check-job-spec.py` is parsed by `dispatch-guard` and rebuilt into both published plugin trees". dispatch-guard does not parse it -- it shells out to it and reads the exit code, as its own docstring says in as many words. r1 filed this as info, r2 filed it as minor, and the sentence is unchanged. C-8 states the same relationship correctly two clauses later ("hooks run it as a bare `python3` call"). It costs nothing to fix and it is the kind of stated reason a worker reasons from: someone who believes the file is parsed will worry about its shape rather than its exit code. Fix: "because `check-job-spec.py` is executed by `dispatch-guard` on every dispatch and rebuilt into both published plugin trees". | .agent-guild/hooks/dispatch-guard.py:167 'Shells out to check-job-spec.py rather than importing it', and :186-240 building the subprocess argv and branching on proc.returncode. constitution.md:163. |
| preamble | minor | Two stale numbers in the preamble, both in sentences a later reader will take as fact. (1) "Three fail today and two pass" was written for five probes and five behavioral clauses; there are now six probes and nine clauses, and the observed split is four red and five green. I re-derived it rather than trusting it: check-baselines.py reports `ran 9 (4 red, 5 green), skipped 0 (0 judgment, 0 no-baseline)`, matching every clause's declaration. (2) The weight line calls the harness "a ~500-line probe harness"; `probe.py` is 743 lines. The weight decision itself is sound and I am not disputing it -- deep is right, and I confirm the reason below -- but the retrospective skill quotes this line verbatim as the record of how the weight was derived, so a number half again off is the wrong thing to archive. Fix both numbers. | python3 .agent-guild/scripts/check-baselines.py .agent-guild/state --repo-root . -> 'check-baselines: ran 9 (4 red, 5 green), skipped 0 (0 judgment, 0 no-baseline)', exit 0. wc -l .agent-guild/state/checks/probe.py -> 743. constitution.md:19-20, :3. |
| C-3 | minor | Unchanged from r2, and I re-derived it rather than inheriting it: nothing requires R21 to be declared in `RULE_CLASS`. `rule_class_of` defaults to PROOF, so C-3's exit-1 assertion holds either way and the behavior is correct -- which is why this stays minor. What breaks is the diagnostic one layer out: with no entry, a mistaken `**Lint exception**: R21` is refused by rule_R20 with "which is not a rule this linter has", sending an author looking for a typo in a rule that exists. The linter's own `--self-test` claims to catch a missing entry ("Every rule run_rules dispatches needs a RULE_CLASS entry") and cannot, because it iterates a hard-coded id list ending at R20. Fix, either: add a sentence to C-3 requiring the declaration, or -- cheaper and self-maintaining -- have the self-test derive its id list from `run_rules` instead of hard-coding it, at which point C-6 enforces it for free. | .agent-guild/scripts/check-job-spec.py:578-583 (rule_class_of's PROOF fallback), :747-752 (rule_R20's unknown-id branch), :2242-2247 (the hard-coded id tuple in self_test). My reference implementation added the entry; a variant omitting it still passes C-3, since the probe asserts only rc == 1. |
| preamble | info | Three small inexactnesses in the harness's own prose, none of which changes a check's behavior, all cheap to correct while the file is open. (1) probe.py:35's usage line still names five probes and omits `suite-coverage`; main() derives the real usage from PROBES, so only the docstring is wrong. (2) probe_suite_coverage's docstring says "`.agent-guild/state/` is gitignored in full" -- `.gitignore:5-6` negates `archive/` back in, and those 383 files are tracked. The clause's conclusion survives (probe.py itself is ignored and does vanish), and the tracked archive is what let me use `git status --porcelain` as the leak tripwire for the symlink. (3) The preamble's "Every probe carries a control in the opposite direction" is still not true of `r21-safe`, which r2 also noted; the hole is closed by C-3's `assert_clean` rather than by r21-safe itself, so the sentence is what needs narrowing, not the probe. | .agent-guild/state/checks/probe.py:35, :627-637; .gitignore:5-6; `git ls-files .agent-guild/state/ \| wc -l` -> 383. constitution.md:23-27. |
| C-9 | info | The symlink does not leak, checked rather than reasoned about. probe_suite_coverage symlinks the real `.agent-guild/state/archive` into its temp tree and then rewrites `check-job-spec.py` inside that tree, which is the `kendrick/dotfiles#22` shape; nothing reaches back through it. The suite reads the corpus through `shutil.copy`/`copytree` into its own temp dir and mutates only the copy (add_owns, add_weight_line, add_dep_rationale all take a state_dir under tempfile), and check-build.sh's log lands under the temp tree's own `state/log`. I checksummed all 596 archive files before the first C-9 run and again after every run in this round: identical. The stub half for R10 and R2 is dead weight as the clause concedes -- both fire through cases that predate the job -- but it is cheap and it catches a rename, so I would keep it rather than cut it; what needed adding was a revert-shaped mutation for R2, which is the blocker filed above. | 596-file shasum manifest of .agent-guild/state/archive taken before probe.py suite-coverage and re-taken after the full round: `diff` empty both times. _corpus.py:20-24 derives ARCHIVE_117 from the script's own location, so under the probe it resolves through the symlink for reads only; test_check_job_spec.py:85-96 copies before mutating. |
| C-2 | info | C-2 is a live green blocker rather than one passing by construction, re-derived rather than inherited. The over-relaxed reading C-1 leaves open -- any plural noun later on the line -- fails it on two archives, so the pair really does settle the fork between a bounded adjective window and an unbounded scan. r2's remaining minor about the census counting directories rather than constitutions that parse is unchanged and I judge it immaterial today: all 11 archive directories carry a constitution that loads with a non-zero clause count and a tasks/ dir (9/6, 8/4, 8/1, 5/1, 6/1, 6/1, 5/1, 9/7, 10/5, 9/4, 15/9 clauses/tasks), so no directory is being swept vacuously. `>= 11` sits exactly on today's census, which means it behaves as a census now and degrades to a floor only if an archive is ever removed. | Variant unbounded (apparatus/CON-audit-r3/patch.py:UNBOUNDED), venue /tmp/conaudit-r3-var-unbounded-Hsedb4: C-1 rc=0 PASS but C-2 rc=1 FAIL, '2026-08-10-issue-117: R10 silent -- msg="R10 count-vs-list: tasks/T-007.md:81 says two (2) but the following list has 3 item(s)"' and '2026-08-11-issue-100: tasks/T-005.md:57 says one (1) but the following list has 4 item(s)'. load_context over all 11 archives: no errors, no zero-clause constitutions. |
| C-4 | info | C-4 and C-3's discrimination both hold against implementations built this round rather than inherited from r1 or r2. R21 running after the heuristics fails C-3 on both pinned rules -- the R2 pairing and the R10 pairing each report the waivable heuristic at exit 4 instead of the proof at exit 1 -- so r2's one-heuristic finding is closed, and the R2 leg is the binding one since run_rules reaches it first. An R21 that names the task's first cited clause instead of the offending one fails C-3 on two assertions. C-4 stays green through both, and stays green on the reference, so the two clauses split the safe and harmful directions the way their texts claim. | Variant r21-after, venue /tmp/conaudit-r3-var-r21-after-LXmhbc: C-3 rc=1 with all four placement assertions failing (placement/R2 and placement/R10, each 'R21 is reported ahead of it' and 'at the unwaivable proof exit'), C-4 rc=0. Variant r21-firstclause, venue /tmp/conaudit-r3-var-r21-firstclause-Wf2bow: C-3 rc=1, 'message names the offending clause C-2' and 'message does NOT name the innocent clause C-1' both failing on 'but cites C-1'. |
| C-7 | info | C-6, C-7 and C-8 are jointly satisfiable with C-1 through C-5 and C-9, proved by building all three changes plus the suite cases and running every clause in one tree. The rebuild C-6 demands touches exactly six files -- the two authored files and their four generated copies -- and C-7's allowlist admits all six and nothing else. C-8 refuses the import class its failing example names; both its legs are live through check-build.sh's nested `bash -c`. The relaxed R10 does not trip on this job's own constitution either, which is worth stating because dispatch-guard lints this state tree on every dispatch once the worker's change lands: the reference linter exits 0 on the real .agent-guild/state. | Reference implementation at /tmp/conaudit-r3-ref-LQvbNO (apparatus/CON-audit-r3/patch.py ref): C-1 through C-9 all rc=0. `git status --porcelain` there lists exactly .agent-guild/scripts/check-job-spec.py, .agent-guild/scripts/test_check_job_spec.py and their plugin/ and plugins/ counterparts. Reference linter against the live tree: 'OK: /Users/k.arnett/repos/agent-guild/.agent-guild/state passes check-job-spec (CON-audit)', rc=0. |
| C-8 | info | Scope of this round. All nine clauses were executed three ways: against the tree as found, against a reference implementation of all three changes built from clause text alone, and against at least one variant built to violate the clause's own text -- twelve variants in all, each in its own mktemp -d venue outside the repo. None blocked, none judgment-only, none unexecuted; every red clause failed at its own assertion rather than at a precondition. The baseline sweep was re-derived rather than trusted and matches every declaration. The working tree is exactly as I found it: `git status --porcelain` is empty, and the tracked archive's 596 files checksum identical to the manifest I took before the first run. Venues, all kept for reference and all outside the project: /tmp/conaudit-r3-ref-LQvbNO, /tmp/conaudit-r3-var-{said-same-KmgoWZ, no-r2-case-m6hjdH, allowlist-I6NhNR, strict-elsewhere-N60NV5, hash-tJ3yJ8, linecount-3RPX90, synthetic-kfGrfr, unbounded-Hsedb4, window1-RcmcNT, omit-anchor-line-N196sp, no-quote-T1sqof, r21-after-LXmhbc, r21-firstclause-Wf2bow}. Apparatus at .agent-guild/state/apparatus/CON-audit-r3/. | check-baselines.py exit 0, 'ran 9 (4 red, 5 green), skipped 0'. git status --porcelain empty at start and at filing. Untouched-tree failing assertions recorded per red clause: C-1 9 assertions incl. 'fires through key/new/separate/brief'; C-3 9 incl. 'R21 named'; C-5 4 incl. 'A: diagnostic quotes the anchor text it matched'; C-9 2, 'no function named rule_R21' and 'the old strict behavior turns the suite red'. |

## Diagnosis

- **C-5** (blocker): C-5's re-derived text sells the anchor's position as sayable "by repeating the number or by saying so in words, the wording being the author's choice", and the words branch re-admits the exact implementation r1 caught. probe.py:574 matches `same line|that line|the citing line` anywhere on the R2 line, and `that line` is phrasing an R2 message naturally carries about the CITED line in the target file, which is a different line in a different document. I built r1's variant again -- the anchor's own position printed when it differs from the citation's and omitted when it does not -- worded `but the anchor `...` is not on that line; it is at src/fixture.py:29`, and C-5 passes. Case B's assertion, "the anchor's position is stated though it equals the citation's", goes green on a message that states no position at all. This is not a corner: the #117 corpus's own R2 case has its anchor ON the citing line (verified through the suite's M1 mutation), so case B is the ordinary shape, and what ships is a diagnostic that still leaves an author permuting prose in the common case. Fourth consecutive round with a defect in C-5, with the one cut already spent, against conventions.md:53's "when two rounds find a blocker in the same mechanism, cut the mechanism rather than patch it a third time". Fix, and it has to stop settling this in prose: require the anchor's position as a `label:line` pair in both cases -- in case B that is `tasks/T-001.md:22` appearing twice on the line -- and rewrite the text to say the position is reported as a path:line pair, retiring the "wording being the author's choice" sentence. That closes the fork by choosing rather than by admitting both readings. If the reading-well property is what the clause actually wants, the honest form is a `checker-judgment:` rubric, which routes to checker-judgment and stops pretending a regex can tell the two apart.
  evidence: Variant said-same (apparatus/CON-audit-r3/patch.py:SAIDSAME_R2_MSG), venue /tmp/conaudit-r3-var-said-same-KmgoWZ: C-5 rc=0 PASS. Case A message: 'R2 citation-anchor: tasks/T-001.md:22 cites src/fixture.py:12 but the anchor `if len(val) >= 2 and val[0] == val[-1]:` (found at tasks/T-001.md:24) is not on that line; it is at src/fixture.py:29'. Case B message, anchor at task line 22: 'R2 citation-anchor: tasks/T-001.md:22 cites src/fixture.py:12 but the anchor `...` is not on that line; it is at src/fixture.py:29' -- no anchor position anywhere in it. probe.py:573-577; constitution.md:156.
- **C-9** (blocker): C-9's text says "Disabling any one of the three shipped behaviors ... turns `test_check_job_spec.py` red, so each change has a case in the repo's own suite", and its check cannot see whether the R2 diagnostic -- one of the three -- has one. Stubbing `nearest_anchor` turns the suite red through cases that predate this job: I measured it in the reference tree, and the stub fails M1 and `--self-test`, both of which cover R2 as it already was. So a delivered tree carrying the whole new anchor-naming diagnostic and not one suite case for it passes C-9, C-6 and C-5 together. The clause's own escape hatch is false: "R2 carries the same gap and closes it through C-5 instead, whose three cases a stubbed suite cannot satisfy" -- C-5's three cases live in `.agent-guild/state/checks/probe.py`, which is gitignored and deleted at teardown, which is the premise C-9 exists on. C-5 covers the behavior; nothing covers the behavior surviving the harness that proved it. Net effect: R2's diagnostic ships with no live regression net, which is exactly the hole r2's C-6 major reported and C-9 was written to close. Fix: give the R2 half a revert-shaped mutation the way R10 got one -- restore the shipped message body (`but the quoted code is at {path}:{actual_line}`) inside `check_citation_rules` and require the suite to go red -- or assert the mutation-detection per NEW behavior rather than per function name.
  evidence: Variant no-r2-case (apparatus/CON-audit-r3/patch.py), venue /tmp/conaudit-r3-var-no-r2-case-m6hjdH: C-5 rc=0 PASS, C-6 rc=0 PASS, C-9 rc=0 PASS with the suite carrying archive-drawn cases for the R10 relaxation and R21 and none for R2. Per-mutation case failures measured against the reference tree: stub nearest_anchor -> 5 failing cases, of which `M1 stale code citation` and `--self-test` predate this job; stub rule_R21 -> 3, all new; revert _governs_plural_noun -> 2, all new. constitution.md:188-194.
- **C-1** (major): C-1's text claims R10 fires "when adjectives separate the number from the plural noun it governs", and r2's remedy -- five adjectives sharing no stem or suffix -- narrows the evasion without closing it, because the five words are readable out of `probe.py` by the worker they gate. A six-word allowlist keyed to exactly the words the probe uses passes C-1, C-2, C-6 and C-9, and is silent on `Four such rules constrain how:` (the phrase r2's own finding used), `Four additional rules`, `Four remaining rules` and `Four unrelated rules`, all four of which the reference implementation fires on. No fixed example set can pin an open word class against an implementation that can read the examples. Fix: generate one nonce adjective per run -- `Four glorptic rules constrain how:` -- alongside the five fixed ones. A correct predicate fires on it because the relaxation is about position, not vocabulary, and C-8 rules out any implementation that would legitimately need a real word (stdlib-only, no POS tagger); an allowlist cannot anticipate a token that does not exist until the probe runs.
  evidence: Variant allowlist (apparatus/CON-audit-r3/patch.py:ALLOWLIST, `_R10_ADJECTIVES = ("further", "key", "new", "separate", "brief", "load-bearing")`), venue /tmp/conaudit-r3-var-allowlist-I6NhNR: C-1 rc=0, C-2 rc=0, C-6 rc=0, C-9 rc=0. Direct call of find_r10_violation over eight phrases, allowlist vs reference: 'Four such rules' silent/FIRES, 'Four additional rules' silent/FIRES, 'Four remaining rules' silent/FIRES, 'Four unrelated rules' silent/FIRES. probe.py:222-233; constitution.md:128.
- **C-9** (major): C-9 refuses a conforming implementation and misreports why, and the guard that should have caught it can never fail. `revert_adjacency` (probe.py:701-715) returns `"\n".join(out) != src`, and `src` ends in a newline while the join does not, so it returns True on every input -- including one where it wrote the file back byte for byte. Run it against the untouched tree, where `_governs_plural_noun` is already strict: it reports the revert landed and the file does not change by a single byte. That makes the preamble's "Every probe that mutates its input asserts the mutation landed before it asserts anything else" false of this mutation, which is the `kendrick/skills#27` shape the probe's own docstring cites as its reason. The consequence is a false FAIL. I built the relaxation in the caller, leaving `_governs_plural_noun` identical to its shipped strict form: C-1, C-2 and C-6 pass, the suite carries the archive-drawn relaxation case M7 and M7 is green -- and C-9 fails, saying "the suite has no case for the relaxation itself", which is exactly backwards. A worker handed that diagnosis writes a test that already exists and burns a retry. Fix, two parts: compare against real bytes (`out_text = "\n".join(out) + "\n"; return out_text != src`), and say in C-9's text that the relaxation must live in `_governs_plural_noun` -- the clause requires it today and does not say so -- so a no-op revert reports "the predicate is not where the relaxation lives" instead of blaming the suite.
  evidence: revert_adjacency against the untouched .agent-guild/scripts/check-job-spec.py: returned True, `file actually changed: False`, zero diff lines. Variant strict-elsewhere (apparatus/CON-audit-r3/patch.py:STRICT_ELSEWHERE_HELPER), venue /tmp/conaudit-r3-var-strict-elsewhere-N60NV5: C-1 rc=0, C-2 rc=0, C-6 rc=0, C-9 rc=1 'R10 relaxation: the old strict behavior turns the suite red -- rc=0 the suite has no case for the relaxation itself', while that venue's own suite reports `ok M7 adjective between count and list: exit 4 (heuristic)` and `ok M7: R10 named`. probe.py:701-715, :677-687; constitution.md:188-194.
- **C-9** (major): C-9 is satisfiable with no behavioral case for any of the three changes. Mutation-detection is a proxy for coverage, and any assertion that reads the linter's bytes goes red under all four mutations while testing nothing. I added one line to the suite -- a sha256 of `check-job-spec.py` compared against its shipped digest -- and deleted every other new case: C-6 and C-9 both pass. This is #141's defect recurring inside the clause built to escape it; the preamble's "A check cannot tell a real test from a written one" is still true here, one level up. It takes deliberate gaming rather than ordinary sloppiness, which is why it sits below the two findings above, but the whole reason C-9 exists is that the probe harness vanishes and the repo's suite is what remains. Fix: pin each mutation to a named case rather than to the suite's exit code -- assert that the suite's own failure output names a case id the clause requires (M7/M8/M9-shaped), so a red run has to be red for the behavior that was disabled. A digest assertion turns everything red and names nothing.
  evidence: Variant hash (apparatus/CON-audit-r3/patch.py:HASH_CASE), venue /tmp/conaudit-r3-var-hash-tJ3yJ8: C-6 rc=0 PASS, C-9 rc=0 PASS with the suite's only new assertion being `check("the shipped linter is intact", _digest == "<sha256>")`. The weaker line-count form does NOT satisfy it (venue /tmp/conaudit-r3-var-linecount-3RPX90, C-9 rc=1), because revert_adjacency replaces a 3-line function with a 3-line function -- so the hole is content-shaped, not size-shaped. probe.py:657-668.
- **C-9** (minor): The second half of the spec's sixth acceptance criterion is still covered by nothing. spec.md:56 asks for the suite to pass "with a case per change drawn from the archive rather than from invented fixtures, matching that suite's existing convention". r2 filed that as a major against C-6 because nothing covered it; C-9 closed the "a case per change" half and left the provenance half open. I built the three new cases as invented fixtures with no corpus mutation anywhere: C-6 and C-9 pass, and that variant's linter is byte-identical to the reference, so the remaining seven clauses hold unchanged. Two honest ways out, and picking either is fine: assert the provenance (require each new case to route through `copy_corpus_state` + `mutate`, which is mechanical and matches how M1-M6 are written), or rule the convention non-binding and record the divergence in the preamble the way the R2 criterion's divergence is already recorded. What is not fine is a criterion that reads as required and is checked by nothing, since Phase 1 will excerpt it into a brief and a checker will have no clause to hold it to.
  evidence: Variant synthetic (apparatus/CON-audit-r3/patch.py:SYNTHETIC_CASES), venue /tmp/conaudit-r3-var-synthetic-kfGrfr: C-6 rc=0 PASS, C-9 rc=0 PASS; `diff` of that venue's check-job-spec.py against the reference venue's is empty. spec.md:56; constitution.md:188-194.
- **C-6** (minor): Third round on the same false sentence. C-6's text says the four suites matter "because `check-job-spec.py` is parsed by `dispatch-guard` and rebuilt into both published plugin trees". dispatch-guard does not parse it -- it shells out to it and reads the exit code, as its own docstring says in as many words. r1 filed this as info, r2 filed it as minor, and the sentence is unchanged. C-8 states the same relationship correctly two clauses later ("hooks run it as a bare `python3` call"). It costs nothing to fix and it is the kind of stated reason a worker reasons from: someone who believes the file is parsed will worry about its shape rather than its exit code. Fix: "because `check-job-spec.py` is executed by `dispatch-guard` on every dispatch and rebuilt into both published plugin trees".
  evidence: .agent-guild/hooks/dispatch-guard.py:167 'Shells out to check-job-spec.py rather than importing it', and :186-240 building the subprocess argv and branching on proc.returncode. constitution.md:163.
- **preamble** (minor): Two stale numbers in the preamble, both in sentences a later reader will take as fact. (1) "Three fail today and two pass" was written for five probes and five behavioral clauses; there are now six probes and nine clauses, and the observed split is four red and five green. I re-derived it rather than trusting it: check-baselines.py reports `ran 9 (4 red, 5 green), skipped 0 (0 judgment, 0 no-baseline)`, matching every clause's declaration. (2) The weight line calls the harness "a ~500-line probe harness"; `probe.py` is 743 lines. The weight decision itself is sound and I am not disputing it -- deep is right, and I confirm the reason below -- but the retrospective skill quotes this line verbatim as the record of how the weight was derived, so a number half again off is the wrong thing to archive. Fix both numbers.
  evidence: python3 .agent-guild/scripts/check-baselines.py .agent-guild/state --repo-root . -> 'check-baselines: ran 9 (4 red, 5 green), skipped 0 (0 judgment, 0 no-baseline)', exit 0. wc -l .agent-guild/state/checks/probe.py -> 743. constitution.md:19-20, :3.
- **C-3** (minor): Unchanged from r2, and I re-derived it rather than inheriting it: nothing requires R21 to be declared in `RULE_CLASS`. `rule_class_of` defaults to PROOF, so C-3's exit-1 assertion holds either way and the behavior is correct -- which is why this stays minor. What breaks is the diagnostic one layer out: with no entry, a mistaken `**Lint exception**: R21` is refused by rule_R20 with "which is not a rule this linter has", sending an author looking for a typo in a rule that exists. The linter's own `--self-test` claims to catch a missing entry ("Every rule run_rules dispatches needs a RULE_CLASS entry") and cannot, because it iterates a hard-coded id list ending at R20. Fix, either: add a sentence to C-3 requiring the declaration, or -- cheaper and self-maintaining -- have the self-test derive its id list from `run_rules` instead of hard-coding it, at which point C-6 enforces it for free.
  evidence: .agent-guild/scripts/check-job-spec.py:578-583 (rule_class_of's PROOF fallback), :747-752 (rule_R20's unknown-id branch), :2242-2247 (the hard-coded id tuple in self_test). My reference implementation added the entry; a variant omitting it still passes C-3, since the probe asserts only rc == 1.
- **preamble** (info): Three small inexactnesses in the harness's own prose, none of which changes a check's behavior, all cheap to correct while the file is open. (1) probe.py:35's usage line still names five probes and omits `suite-coverage`; main() derives the real usage from PROBES, so only the docstring is wrong. (2) probe_suite_coverage's docstring says "`.agent-guild/state/` is gitignored in full" -- `.gitignore:5-6` negates `archive/` back in, and those 383 files are tracked. The clause's conclusion survives (probe.py itself is ignored and does vanish), and the tracked archive is what let me use `git status --porcelain` as the leak tripwire for the symlink. (3) The preamble's "Every probe carries a control in the opposite direction" is still not true of `r21-safe`, which r2 also noted; the hole is closed by C-3's `assert_clean` rather than by r21-safe itself, so the sentence is what needs narrowing, not the probe.
  evidence: .agent-guild/state/checks/probe.py:35, :627-637; .gitignore:5-6; `git ls-files .agent-guild/state/ | wc -l` -> 383. constitution.md:23-27.
- **C-9** (info): The symlink does not leak, checked rather than reasoned about. probe_suite_coverage symlinks the real `.agent-guild/state/archive` into its temp tree and then rewrites `check-job-spec.py` inside that tree, which is the `kendrick/dotfiles#22` shape; nothing reaches back through it. The suite reads the corpus through `shutil.copy`/`copytree` into its own temp dir and mutates only the copy (add_owns, add_weight_line, add_dep_rationale all take a state_dir under tempfile), and check-build.sh's log lands under the temp tree's own `state/log`. I checksummed all 596 archive files before the first C-9 run and again after every run in this round: identical. The stub half for R10 and R2 is dead weight as the clause concedes -- both fire through cases that predate the job -- but it is cheap and it catches a rename, so I would keep it rather than cut it; what needed adding was a revert-shaped mutation for R2, which is the blocker filed above.
  evidence: 596-file shasum manifest of .agent-guild/state/archive taken before probe.py suite-coverage and re-taken after the full round: `diff` empty both times. _corpus.py:20-24 derives ARCHIVE_117 from the script's own location, so under the probe it resolves through the symlink for reads only; test_check_job_spec.py:85-96 copies before mutating.
- **C-2** (info): C-2 is a live green blocker rather than one passing by construction, re-derived rather than inherited. The over-relaxed reading C-1 leaves open -- any plural noun later on the line -- fails it on two archives, so the pair really does settle the fork between a bounded adjective window and an unbounded scan. r2's remaining minor about the census counting directories rather than constitutions that parse is unchanged and I judge it immaterial today: all 11 archive directories carry a constitution that loads with a non-zero clause count and a tasks/ dir (9/6, 8/4, 8/1, 5/1, 6/1, 6/1, 5/1, 9/7, 10/5, 9/4, 15/9 clauses/tasks), so no directory is being swept vacuously. `>= 11` sits exactly on today's census, which means it behaves as a census now and degrades to a floor only if an archive is ever removed.
  evidence: Variant unbounded (apparatus/CON-audit-r3/patch.py:UNBOUNDED), venue /tmp/conaudit-r3-var-unbounded-Hsedb4: C-1 rc=0 PASS but C-2 rc=1 FAIL, '2026-08-10-issue-117: R10 silent -- msg="R10 count-vs-list: tasks/T-007.md:81 says two (2) but the following list has 3 item(s)"' and '2026-08-11-issue-100: tasks/T-005.md:57 says one (1) but the following list has 4 item(s)'. load_context over all 11 archives: no errors, no zero-clause constitutions.
- **C-4** (info): C-4 and C-3's discrimination both hold against implementations built this round rather than inherited from r1 or r2. R21 running after the heuristics fails C-3 on both pinned rules -- the R2 pairing and the R10 pairing each report the waivable heuristic at exit 4 instead of the proof at exit 1 -- so r2's one-heuristic finding is closed, and the R2 leg is the binding one since run_rules reaches it first. An R21 that names the task's first cited clause instead of the offending one fails C-3 on two assertions. C-4 stays green through both, and stays green on the reference, so the two clauses split the safe and harmful directions the way their texts claim.
  evidence: Variant r21-after, venue /tmp/conaudit-r3-var-r21-after-LXmhbc: C-3 rc=1 with all four placement assertions failing (placement/R2 and placement/R10, each 'R21 is reported ahead of it' and 'at the unwaivable proof exit'), C-4 rc=0. Variant r21-firstclause, venue /tmp/conaudit-r3-var-r21-firstclause-Wf2bow: C-3 rc=1, 'message names the offending clause C-2' and 'message does NOT name the innocent clause C-1' both failing on 'but cites C-1'.
- **C-7** (info): C-6, C-7 and C-8 are jointly satisfiable with C-1 through C-5 and C-9, proved by building all three changes plus the suite cases and running every clause in one tree. The rebuild C-6 demands touches exactly six files -- the two authored files and their four generated copies -- and C-7's allowlist admits all six and nothing else. C-8 refuses the import class its failing example names; both its legs are live through check-build.sh's nested `bash -c`. The relaxed R10 does not trip on this job's own constitution either, which is worth stating because dispatch-guard lints this state tree on every dispatch once the worker's change lands: the reference linter exits 0 on the real .agent-guild/state.
  evidence: Reference implementation at /tmp/conaudit-r3-ref-LQvbNO (apparatus/CON-audit-r3/patch.py ref): C-1 through C-9 all rc=0. `git status --porcelain` there lists exactly .agent-guild/scripts/check-job-spec.py, .agent-guild/scripts/test_check_job_spec.py and their plugin/ and plugins/ counterparts. Reference linter against the live tree: 'OK: /Users/k.arnett/repos/agent-guild/.agent-guild/state passes check-job-spec (CON-audit)', rc=0.
- **C-8** (info): Scope of this round. All nine clauses were executed three ways: against the tree as found, against a reference implementation of all three changes built from clause text alone, and against at least one variant built to violate the clause's own text -- twelve variants in all, each in its own mktemp -d venue outside the repo. None blocked, none judgment-only, none unexecuted; every red clause failed at its own assertion rather than at a precondition. The baseline sweep was re-derived rather than trusted and matches every declaration. The working tree is exactly as I found it: `git status --porcelain` is empty, and the tracked archive's 596 files checksum identical to the manifest I took before the first run. Venues, all kept for reference and all outside the project: /tmp/conaudit-r3-ref-LQvbNO, /tmp/conaudit-r3-var-{said-same-KmgoWZ, no-r2-case-m6hjdH, allowlist-I6NhNR, strict-elsewhere-N60NV5, hash-tJ3yJ8, linecount-3RPX90, synthetic-kfGrfr, unbounded-Hsedb4, window1-RcmcNT, omit-anchor-line-N196sp, no-quote-T1sqof, r21-after-LXmhbc, r21-firstclause-Wf2bow}. Apparatus at .agent-guild/state/apparatus/CON-audit-r3/.
  evidence: check-baselines.py exit 0, 'ran 9 (4 red, 5 green), skipped 0'. git status --porcelain empty at start and at filing. Untouched-tree failing assertions recorded per red clause: C-1 9 assertions incl. 'fires through key/new/separate/brief'; C-3 9 incl. 'R21 named'; C-5 4 incl. 'A: diagnostic quotes the anchor text it matched'; C-9 2, 'no function named rule_R21' and 'the old strict behavior turns the suite red'.

---
task: CON-audit
checker: auditor
vendor: anthropic
model: claude-opus-5
verdict: FAIL
checked_at: 2026-08-18T15:55:00Z
duration_ms: None
cost_usd: None
---

<!-- GENERATED FILE—do not hand-edit. Rendered by render-verdict.py
from the verdict JSON, the record of record. Edit the JSON and
re-render instead. -->

## Per-clause results

| clause | severity | description | evidence |
| ------ | -------- | ------------ | -------- |
| C-9 | blocker | The fourth seam's mutation never runs, so C-9 is red against every possible implementation. STRIP_ANCHOR_WRAPPER is appended to the end of the copied check-job-spec.py, and that file ends with `if __name__ == "__main__":\\n sys.exit(main())`. The suite invokes the linter as a subprocess (test_check_job_spec.py:15 'Every script runs as a subprocess', :46-48 `subprocess.run([sys.executable, SCRIPT, ...])`), so SystemExit fires at the __main__ guard and the appended redefinition is never executed. It executes only on import, which nothing in the suite does. I built a full reference implementation of all three changes plus three archive-drawn suite cases, one of which (N3) asserts in so many words that the R2 diagnostic quotes the anchor; the probe still reported 'no case asserts the anchor is quoted'. This is the kendrick/skills#27 shape: a check no conforming worker can turn green, and it will read as 'you wrote no R2 case' to a worker who wrote one. FIX: inject the redefinition where module execution reaches it rather than appending past the guard -- insert it immediately before the LAST `if __name__ == "__main__":` in the copied source (that literal occurs twice; the first is inside the _FIXTURE self-test strings, so a naive replace patches a fixture), or rebind it via ast right after the `def r2_anchor_message` block. Then give this seam the landed-mutation proof its three siblings already have: every other mutation in probe_suite_coverage asserts `after != pristine` or equivalent, and the R2 wrapper is the only one that asserts nothing about whether it took effect, which is exactly why an inert append survived a round. A cheap and sufficient assertion: run the linter once over the R2 fixture after wrapping and confirm no backticked span remains in the message before running the suite. | probe.py:672-687 (STRIP_ANCHOR_WRAPPER, appended by probe.py:769-772) against check-job-spec.py:2400-2401; reference implementation /tmp/conaudit-r4-ri-VpQQ5y/repo -> 'FAIL R2 diagnostic: removing the quoted anchor turns the suite red rc=0 -- no case asserts the anchor is quoted' while the other three seams go green; same strip applied inside the function body -> '227 passed, 1 failed' with N3 the only failure; wrapper inserted above the last __main__ guard -> '227 passed, 1 failed', also N3 only |
| C-1 | blocker | The pid nonce fails a faithful implementation and still does not close the allowlist hole it was added for. (a) FALSE FAIL: `zqx{pid % 9973}ish` carries digits, so an implementation whose intervening-modifier token class mirrors the shipped predicate's own alphabet -- `[A-Za-z][A-Za-z-]*`, the exact character class at check-job-spec.py:1681 -- refuses to step over it. My reference implementation passes every one of the five real adjectives, the bare-count control, the correct-count silence control and the whole archive sweep, and fails on the single assertion 'fires through zqx6142ish'. C-1's text asks for adjectives; `zqx6142ish` is not a word in any reading of that sentence, so the probe is demanding behavior the clause does not state and a correct worker eats a retry for it. The fork the clause leaves unpinned is one gate expression wide: `[A-Za-z][A-Za-z-]*\\b\\s+` fails, `[A-Za-z][\\w-]*\\s+` passes, and I built both. (b) FALSE PASS, fourth round running: the nonce's affixes `zqx` and `ish` are fixed and probe.py is readable by any worker, so a six-word allowlist plus one `endswith('ish')` clause passes r10-adjective AND r10-corpus while firing on nothing else. That variant is silent on 'sizable', 'recent' and 'narrow' -- a flat violation of C-1's text -- and the check cannot see it. FIX: generate the WHOLE modifier set at runtime from random ASCII letters (no digits) with a randomly chosen suffix per token, assert on three or more of them, and drop the fixed affixes. Random letters cannot be pre-empted by any allowlist and require no digit-tolerant token class, which closes both directions at once. | probe.py:225-243 (nonce and the fixed adjective list); /tmp/conaudit-r4-ri-VpQQ5y (letters reading) -> 'FAIL fires through zqx6142ish', every other assertion ok; /tmp/conaudit-r4-ri2-W96iPP (digit-tolerant reading) -> 'ok fires through zqx8865ish', probe exit 0; /tmp/conaudit-r4-var1-UVGRDI (six-word allowlist + *ish) -> r10-adjective exit 0, r10-corpus exit 0, and R10 silent on 'sizable'/'recent'/'narrow' |
| C-5 | major | The rubric points the checker at the printed diagnostics and never tells it that the probe's own assertions decide anything, so the silence requirement lost its gate when C-5 stopped being a script check. Nothing else in the constitution runs `probe.py r2-anchor`; check-baselines skips it as judgment. I built an implementation that reports R2 even when the quoted span is absent from the whole target file -- the exact defect case C exists to catch, and the one CON-audit r1 shipped -- and its [A] and [B] diagnostics are word-for-word identical to the faithful implementation's: anchor quoted, position given, both readable. A checker doing literally what the rubric says ('read the three diagnostics it prints and rule on each: is the anchor quoted, and can a reader tell where that anchor sits') passes it. The FAIL lines are in the output, but the rubric directs attention away from them and case C's whole assertion is an absence. FIX: open the rubric with the mechanical half -- 'the probe must exit 0; a nonzero exit or any `FAIL` line is a FAIL of this clause regardless of how the diagnostics read, and the run must show `ok C: an anchor absent from the target file stays silent`' -- and only then ask for the judgment on the two printed messages. | constitution.md:195 (C-5's check); /tmp/conaudit-r4-var9-N8KTOs, an implementation with the R2 silence guard removed -> probe exit 1 with 'FAIL C: an anchor absent from the target file stays silent', while its [A] and [B] diagnostics are byte-identical to the conforming implementation's at /tmp/conaudit-r4-ri-VpQQ5y |
| C-5 | major | The rubric says 'read the three diagnostics it prints' and the probe prints two. Case C prints no diagnostic at all -- that is its whole point, and the rubric's very next sentence says so: 'Case C must print nothing at all.' The two halves of one instruction contradict each other, and a checker hunting for a missing third diagnostic can only conclude the implementation is short one. The second sentence is also literally false of every conforming run: case C prints two `ok` lines, so 'nothing at all' is never what a passing run looks like, and 'print' is left ambiguous between what R2 emits and what the probe emits. This is the R10 count-vs-list shape inside the constitution of the job that exists to catch it. FIX: 'read the two diagnostics it prints, for cases A and B, and rule on each ... case C emits no R2 diagnostic; its silence is the assertion, and the probe reports it as `ok C: an anchor absent from the target file stays silent`.' | constitution.md:195 ('read the three diagnostics it prints' / 'Case C must print nothing at all') against probe.py:562-604, which prints `[A]` and `[B]` blocks only, and the observed run at .agent-guild/state/apparatus/CON-audit-r4/ri-letters-r2-anchor.txt |
| C-10 | major | 'read the diff to test_check_job_spec.py' names neither a command nor a base ref, and the base is the difference between a real check and a vacuous one. This repo's own convention is to commit each piece as it verifies; against a committed implementation `git diff` is empty, and the checker is left reading nothing and ruling on it. I confirmed this on a committed copy of the reference implementation: `git diff --stat` produced no output. The same commit also empties C-7's check, which is why the base cannot be left to the checker's taste. FIX: name the command in the rubric, with a base that survives a commit -- `git diff $(git merge-base HEAD main)...HEAD -- .agent-guild/scripts/test_check_job_spec.py`, falling back to the working tree when the branch has no commits yet -- and say that an empty diff is a FAIL of this clause rather than a pass, since a job that added no cases has not satisfied C-9 either. | constitution.md:239; /tmp/conaudit-r4-var7-iM6M4M/repo, the reference implementation committed on its branch -> `git diff --stat` empty and `check-diff-scope: OK: 0 path(s) in scope` |
| C-10 | major | The synthetic escape hatch asks the checker to verify nothing, and the archive turns out to carry an instance of all three shapes, so the hatch buys only a free pass. 'A case may legitimately be synthetic where the archive carries no instance of the shape -- say so and name which, rather than failing it' instructs the checker to accept a claim about the corpus without checking it, while the clause's own failing example says three synthetic cases are a failure. Both cannot hold. I built all three cases from the archive by one-line mutation of real shipped paperwork -- T-006's 'record three findings' over its real three-item list for R10, T-001's real `checker: checker-judgment` line for R21, T-001's real compose-brief.py:64 citation for R2 -- so no shape here needs the hatch. FIX: either cut the hatch, or make it cost something: require the checker to name the shape, run a grep over `.agent-guild/state/archive/` that comes back empty, and paste the command. Add the three shapes above as the worked answer, so a worker reaching for `write_synthetic_state` has to explain why the archive line the spec itself quotes was not usable. | constitution.md:239 against constitution.md:241; archive instances confirmed by building all three cases at /tmp/conaudit-r4-ri-VpQQ5y/repo/.agent-guild/scripts/test_check_job_spec.py (N1/N2/N3, all deriving state through copy_corpus_state -> ARCHIVE_DIR), suite result '228 passed, 0 failed' |
| preamble | major | Three preamble claims went stale when C-5 became a rubric and C-10 was added, and the decompose step reads this preamble into worker briefs. (1) 'Four fail today and five pass' is now three fail and five pass, with two clauses declaring no baseline at all: check-baselines reports 'ran 8 (3 red, 5 green), skipped 2 (2 judgment)'. The fourth failing clause was C-5, which no longer has a baseline field. A count sentence disagreeing with what it counts is R10's own defect shape. (2) 'Every behavioral clause below runs a probe at .agent-guild/state/checks/probe.py rather than reading the test suite' is contradicted by C-10, which exists precisely to have a person read the suite, and by C-5, whose check is now a rubric that names the probe as an instrument rather than running it as a gate. (3) 'FIXTURES ARE SYNTHETIC, with one deliberate exception ... C-2 is the exception and uses the real archive' is contradicted by C-9, which symlinks the real archive into its mutation tree, and by C-10, which requires the shipped cases to be archive-drawn. FIX: re-derive the census from check-baselines' own output, qualify (2) as 'every clause checked by a script', and name C-9 and C-10 alongside C-2 in (3). | constitution.md:18-21, :10-11, :29-34 against `python3 .agent-guild/scripts/check-baselines.py .agent-guild/state --repo-root .` -> 'ran 8 (3 red, 5 green), skipped 2 (2 judgment, 0 no-baseline)', and against constitution.md:232 (C-9's check) and :237-241 (C-10) |
| C-5 | minor | C-5's severity dropped from blocker to major in this revision and no revision note records why. It carries a required acceptance criterion of the spec and the r2 note called cutting it a coverage loss, so the downgrade is a decision rather than housekeeping. The practical effect is nil -- validate-verdict.py treats blocker and major alike, both fail a verdict -- which is why this is minor rather than major, but the r4 note explains the move to a rubric at length and says nothing about the severity, and the next reader will not be able to tell a decision from a slip. FIX: either restore blocker or add the reason to the r4 revision note. | constitution.md:196 (`- **severity**: major`) against constitution.md:116-128 (the r4 revision note, silent on severity) and validate-verdict.py:149 (`DEFECT_SEVERITIES = ("blocker", "major")`) |
| C-7 | minor | C-7 passes vacuously against a worker who commits. check-diff-scope.py builds its path set from `git status --porcelain` union `git diff --name-only`, both working-tree only, so a committed implementation reports 'OK: 0 path(s) in scope' and the clause's own failing example -- a stray edit to dispatch-guard.py -- rides through undetected. The clause's text is honest about this ('The working tree's diff'), and the behavior is house-standard rather than anything this job introduced, so this is a scope note rather than a defect in the drafting. FIX, if it is worth one: say in the clause text that the check is run before the work is committed, so the decompose step puts it in the task's check_method in that order. | check-diff-scope.py:34-35 (docstring: 'The path set is the union of `git status --porcelain` and `git diff --name-only`'); /tmp/conaudit-r4-var7-iM6M4M/repo after committing the reference implementation -> 'OK: 0 path(s) in scope', exit 0; the same tree uncommitted -> 'OK: 6 path(s) in scope' |
| C-2 | info | Sound, and it discriminates in both directions. Green on the untouched tree and green against the reference implementation; red against an over-broad relaxation that treats any plural noun later on the line as governed, which fires falsely on two archived jobs. The census sweeps every directory carrying a constitution rather than a hardcoded list, and its control fixture proves the same call can still fire. | untouched tree -> exit 0; /tmp/conaudit-r4-ri-VpQQ5y -> exit 0; /tmp/conaudit-r4-var6-UrvgxV (over-broad) -> exit 1, 'FAIL 2026-08-10-issue-117: R10 silent msg="R10 count-vs-list: tasks/T-007.md:81 says two (2) but the following list has 3 item(s)"' and a second false positive on 2026-08-11-issue-100 |
| C-3 | info | Sound, and it discriminates. Red on the untouched tree (no rule_R21 exists), green against the reference implementation, and red against the variant this clause was rewritten to catch -- an R21 that reads the task's own check_method paraphrase instead of the constitution stays silent on the two-clause fixture and the probe catches it. The placement assertions bind against both R2 and R10, and the explicit PROOF entry in RULE_CLASS is checked directly. | untouched tree -> exit 1; /tmp/conaudit-r4-ri-VpQQ5y -> exit 0; /tmp/conaudit-r4-var3-lJPXJp (R21 gated on the task's paraphrase) -> exit 1 |
| C-4 | info | Sound, and it discriminates. Green on the untouched tree as a no-regression baseline and green against the reference implementation; red against an R21 that compares checker and clause kind for equality rather than testing the one harmful direction, which is the clause's own failing example. | untouched tree -> exit 0; /tmp/conaudit-r4-ri-VpQQ5y -> exit 0; /tmp/conaudit-r4-var4-l4C8Sa (equality comparison) -> exit 1 on r21-safe while r21-unsafe stays green |
| C-6 | info | Sound, and it discriminates. Green on the untouched tree and green against the reference implementation once the plugin trees were rebuilt; red against the same implementation before the rebuild, which is exactly the clause's failing example ('R21 is added to the source tree and build-plugin.py is never re-run'). The four commands in the text and the four in the check are the same four. | untouched tree -> exit 0; /tmp/conaudit-r4-ri-VpQQ5y before `python3 scripts/build-plugin.py` -> exit 1; after -> exit 0, 'shared-core wrappers, both published packages, and both marketplaces match fresh builds' |
| C-8 | info | Sound, and it discriminates. Green on the untouched tree and against the reference implementation; red against the same tree with `import yaml` added, which is the clause's failing example in miniature. Both halves earn their place -- the exec catches an import that is present but unresolvable, the AST scan catches one that resolves on this host and would not on another. | untouched tree -> exit 0; /tmp/conaudit-r4-ri-VpQQ5y -> exit 0; /tmp/conaudit-r4-var5-YAu28D with `import yaml` -> exit 1 |
| C-9 | info | The other three seams are sound and they discriminate, which is worth recording separately from the blocker above so the repair does not disturb them. Against the reference implementation all three go green; against the same implementation with the suite left untouched, all three go red with the right reasons ('the suite has no case that depends on this', 'the suite has no case for the relaxation itself'). revert_adjacency's false-FAIL bug is genuinely fixed: the no-op revert now reports as a no-op on the untouched tree and as landed against a real relaxation. The `r2_anchor_message` seam itself is a legitimate constraint, honestly declared in the clause text and cheap to satisfy -- my implementation adopted it without strain -- it is simply unpaid until the wrapper actually runs. | /tmp/conaudit-r4-ri-VpQQ5y -> R10 stub, R21 stub and adjacency revert all 'turns the suite red'; /tmp/conaudit-r4-var2-EuJojp (same linter, pristine suite) -> all three red with 'no case' diagnostics; untouched tree -> 'FAIL R10 relaxation: revert to strict adjacency landed', correctly reporting the no-op |
| scope | info | Scope of this round. Every one of the eight script-checked clauses was executed twice, against the tree as I found it and against a reference implementation I built from the constitution alone, and each also got at least one variant built to violate its own text; C-5 and C-10 are rubrics with nothing to execute, so both were judged by reading, with C-5's instrument run to see what its checker would actually be handed. The baseline sweep ran clean -- 'ran 8 (3 red, 5 green), skipped 2 (2 judgment, 0 no-baseline)' -- and I re-derived every one of those eight by hand, so the declared baselines are accurate as of this text. The weight line's recorded reason holds up: probe.py is 791 lines, this round did rebuild a full reference implementation, and C-10 does name a property no existing command can check; deep has no ceiling so no overrun line is owed, and no **Lint exception** line exists to audit. `git status --porcelain` was empty when I started and empty when I filed. Everything I acted on lived in its own mktemp -d, twelve of them, listed in the evidence above and in apparatus/CON-audit-r4/VENUE-ref.txt; nothing was built or mutated inside the project. One housekeeping note: apparatus/CON-audit-r4/ already existed when I was dispatched, carrying a patch.py and a runclauses.sh from an r4 attempt that did not file. I did not read or reuse them -- round-scoping is a finding source and inheriting a predecessor's reading would trade it away -- and moved them to apparatus/CON-audit-r4-aborted-preexisting/ rather than deleting them. | .agent-guild/state/apparatus/CON-audit-r4/ (baseline-sweep.txt, tree-*.txt, ri-letters-*.txt, ri-wordish-*.txt, var-*.txt, build_ri.py, variant_c1_allowlist.py, holdout_adjective.py, VENUE-ref.txt, VENUE-probe.txt) |

## Diagnosis

- **C-9** (blocker): The fourth seam's mutation never runs, so C-9 is red against every possible implementation. STRIP_ANCHOR_WRAPPER is appended to the end of the copied check-job-spec.py, and that file ends with `if __name__ == "__main__":\n    sys.exit(main())`. The suite invokes the linter as a subprocess (test_check_job_spec.py:15 'Every script runs as a subprocess', :46-48 `subprocess.run([sys.executable, SCRIPT, ...])`), so SystemExit fires at the __main__ guard and the appended redefinition is never executed. It executes only on import, which nothing in the suite does. I built a full reference implementation of all three changes plus three archive-drawn suite cases, one of which (N3) asserts in so many words that the R2 diagnostic quotes the anchor; the probe still reported 'no case asserts the anchor is quoted'. This is the kendrick/skills#27 shape: a check no conforming worker can turn green, and it will read as 'you wrote no R2 case' to a worker who wrote one. FIX: inject the redefinition where module execution reaches it rather than appending past the guard -- insert it immediately before the LAST `if __name__ == "__main__":` in the copied source (that literal occurs twice; the first is inside the _FIXTURE self-test strings, so a naive replace patches a fixture), or rebind it via ast right after the `def r2_anchor_message` block. Then give this seam the landed-mutation proof its three siblings already have: every other mutation in probe_suite_coverage asserts `after != pristine` or equivalent, and the R2 wrapper is the only one that asserts nothing about whether it took effect, which is exactly why an inert append survived a round. A cheap and sufficient assertion: run the linter once over the R2 fixture after wrapping and confirm no backticked span remains in the message before running the suite.
  evidence: probe.py:672-687 (STRIP_ANCHOR_WRAPPER, appended by probe.py:769-772) against check-job-spec.py:2400-2401; reference implementation /tmp/conaudit-r4-ri-VpQQ5y/repo -> 'FAIL R2 diagnostic: removing the quoted anchor turns the suite red  rc=0 -- no case asserts the anchor is quoted' while the other three seams go green; same strip applied inside the function body -> '227 passed, 1 failed' with N3 the only failure; wrapper inserted above the last __main__ guard -> '227 passed, 1 failed', also N3 only
- **C-1** (blocker): The pid nonce fails a faithful implementation and still does not close the allowlist hole it was added for. (a) FALSE FAIL: `zqx{pid % 9973}ish` carries digits, so an implementation whose intervening-modifier token class mirrors the shipped predicate's own alphabet -- `[A-Za-z][A-Za-z-]*`, the exact character class at check-job-spec.py:1681 -- refuses to step over it. My reference implementation passes every one of the five real adjectives, the bare-count control, the correct-count silence control and the whole archive sweep, and fails on the single assertion 'fires through zqx6142ish'. C-1's text asks for adjectives; `zqx6142ish` is not a word in any reading of that sentence, so the probe is demanding behavior the clause does not state and a correct worker eats a retry for it. The fork the clause leaves unpinned is one gate expression wide: `[A-Za-z][A-Za-z-]*\b\s+` fails, `[A-Za-z][\w-]*\s+` passes, and I built both. (b) FALSE PASS, fourth round running: the nonce's affixes `zqx` and `ish` are fixed and probe.py is readable by any worker, so a six-word allowlist plus one `endswith('ish')` clause passes r10-adjective AND r10-corpus while firing on nothing else. That variant is silent on 'sizable', 'recent' and 'narrow' -- a flat violation of C-1's text -- and the check cannot see it. FIX: generate the WHOLE modifier set at runtime from random ASCII letters (no digits) with a randomly chosen suffix per token, assert on three or more of them, and drop the fixed affixes. Random letters cannot be pre-empted by any allowlist and require no digit-tolerant token class, which closes both directions at once.
  evidence: probe.py:225-243 (nonce and the fixed adjective list); /tmp/conaudit-r4-ri-VpQQ5y (letters reading) -> 'FAIL fires through zqx6142ish', every other assertion ok; /tmp/conaudit-r4-ri2-W96iPP (digit-tolerant reading) -> 'ok fires through zqx8865ish', probe exit 0; /tmp/conaudit-r4-var1-UVGRDI (six-word allowlist + *ish) -> r10-adjective exit 0, r10-corpus exit 0, and R10 silent on 'sizable'/'recent'/'narrow'
- **C-5** (major): The rubric points the checker at the printed diagnostics and never tells it that the probe's own assertions decide anything, so the silence requirement lost its gate when C-5 stopped being a script check. Nothing else in the constitution runs `probe.py r2-anchor`; check-baselines skips it as judgment. I built an implementation that reports R2 even when the quoted span is absent from the whole target file -- the exact defect case C exists to catch, and the one CON-audit r1 shipped -- and its [A] and [B] diagnostics are word-for-word identical to the faithful implementation's: anchor quoted, position given, both readable. A checker doing literally what the rubric says ('read the three diagnostics it prints and rule on each: is the anchor quoted, and can a reader tell where that anchor sits') passes it. The FAIL lines are in the output, but the rubric directs attention away from them and case C's whole assertion is an absence. FIX: open the rubric with the mechanical half -- 'the probe must exit 0; a nonzero exit or any `FAIL` line is a FAIL of this clause regardless of how the diagnostics read, and the run must show `ok C: an anchor absent from the target file stays silent`' -- and only then ask for the judgment on the two printed messages.
  evidence: constitution.md:195 (C-5's check); /tmp/conaudit-r4-var9-N8KTOs, an implementation with the R2 silence guard removed -> probe exit 1 with 'FAIL C: an anchor absent from the target file stays silent', while its [A] and [B] diagnostics are byte-identical to the conforming implementation's at /tmp/conaudit-r4-ri-VpQQ5y
- **C-5** (major): The rubric says 'read the three diagnostics it prints' and the probe prints two. Case C prints no diagnostic at all -- that is its whole point, and the rubric's very next sentence says so: 'Case C must print nothing at all.' The two halves of one instruction contradict each other, and a checker hunting for a missing third diagnostic can only conclude the implementation is short one. The second sentence is also literally false of every conforming run: case C prints two `ok` lines, so 'nothing at all' is never what a passing run looks like, and 'print' is left ambiguous between what R2 emits and what the probe emits. This is the R10 count-vs-list shape inside the constitution of the job that exists to catch it. FIX: 'read the two diagnostics it prints, for cases A and B, and rule on each ... case C emits no R2 diagnostic; its silence is the assertion, and the probe reports it as `ok C: an anchor absent from the target file stays silent`.'
  evidence: constitution.md:195 ('read the three diagnostics it prints' / 'Case C must print nothing at all') against probe.py:562-604, which prints `[A]` and `[B]` blocks only, and the observed run at .agent-guild/state/apparatus/CON-audit-r4/ri-letters-r2-anchor.txt
- **C-10** (major): 'read the diff to test_check_job_spec.py' names neither a command nor a base ref, and the base is the difference between a real check and a vacuous one. This repo's own convention is to commit each piece as it verifies; against a committed implementation `git diff` is empty, and the checker is left reading nothing and ruling on it. I confirmed this on a committed copy of the reference implementation: `git diff --stat` produced no output. The same commit also empties C-7's check, which is why the base cannot be left to the checker's taste. FIX: name the command in the rubric, with a base that survives a commit -- `git diff $(git merge-base HEAD main)...HEAD -- .agent-guild/scripts/test_check_job_spec.py`, falling back to the working tree when the branch has no commits yet -- and say that an empty diff is a FAIL of this clause rather than a pass, since a job that added no cases has not satisfied C-9 either.
  evidence: constitution.md:239; /tmp/conaudit-r4-var7-iM6M4M/repo, the reference implementation committed on its branch -> `git diff --stat` empty and `check-diff-scope: OK: 0 path(s) in scope`
- **C-10** (major): The synthetic escape hatch asks the checker to verify nothing, and the archive turns out to carry an instance of all three shapes, so the hatch buys only a free pass. 'A case may legitimately be synthetic where the archive carries no instance of the shape -- say so and name which, rather than failing it' instructs the checker to accept a claim about the corpus without checking it, while the clause's own failing example says three synthetic cases are a failure. Both cannot hold. I built all three cases from the archive by one-line mutation of real shipped paperwork -- T-006's 'record three findings' over its real three-item list for R10, T-001's real `checker: checker-judgment` line for R21, T-001's real compose-brief.py:64 citation for R2 -- so no shape here needs the hatch. FIX: either cut the hatch, or make it cost something: require the checker to name the shape, run a grep over `.agent-guild/state/archive/` that comes back empty, and paste the command. Add the three shapes above as the worked answer, so a worker reaching for `write_synthetic_state` has to explain why the archive line the spec itself quotes was not usable.
  evidence: constitution.md:239 against constitution.md:241; archive instances confirmed by building all three cases at /tmp/conaudit-r4-ri-VpQQ5y/repo/.agent-guild/scripts/test_check_job_spec.py (N1/N2/N3, all deriving state through copy_corpus_state -> ARCHIVE_DIR), suite result '228 passed, 0 failed'
- **preamble** (major): Three preamble claims went stale when C-5 became a rubric and C-10 was added, and the decompose step reads this preamble into worker briefs. (1) 'Four fail today and five pass' is now three fail and five pass, with two clauses declaring no baseline at all: check-baselines reports 'ran 8 (3 red, 5 green), skipped 2 (2 judgment)'. The fourth failing clause was C-5, which no longer has a baseline field. A count sentence disagreeing with what it counts is R10's own defect shape. (2) 'Every behavioral clause below runs a probe at .agent-guild/state/checks/probe.py rather than reading the test suite' is contradicted by C-10, which exists precisely to have a person read the suite, and by C-5, whose check is now a rubric that names the probe as an instrument rather than running it as a gate. (3) 'FIXTURES ARE SYNTHETIC, with one deliberate exception ... C-2 is the exception and uses the real archive' is contradicted by C-9, which symlinks the real archive into its mutation tree, and by C-10, which requires the shipped cases to be archive-drawn. FIX: re-derive the census from check-baselines' own output, qualify (2) as 'every clause checked by a script', and name C-9 and C-10 alongside C-2 in (3).
  evidence: constitution.md:18-21, :10-11, :29-34 against `python3 .agent-guild/scripts/check-baselines.py .agent-guild/state --repo-root .` -> 'ran 8 (3 red, 5 green), skipped 2 (2 judgment, 0 no-baseline)', and against constitution.md:232 (C-9's check) and :237-241 (C-10)
- **C-5** (minor): C-5's severity dropped from blocker to major in this revision and no revision note records why. It carries a required acceptance criterion of the spec and the r2 note called cutting it a coverage loss, so the downgrade is a decision rather than housekeeping. The practical effect is nil -- validate-verdict.py treats blocker and major alike, both fail a verdict -- which is why this is minor rather than major, but the r4 note explains the move to a rubric at length and says nothing about the severity, and the next reader will not be able to tell a decision from a slip. FIX: either restore blocker or add the reason to the r4 revision note.
  evidence: constitution.md:196 (`- **severity**: major`) against constitution.md:116-128 (the r4 revision note, silent on severity) and validate-verdict.py:149 (`DEFECT_SEVERITIES = ("blocker", "major")`)
- **C-7** (minor): C-7 passes vacuously against a worker who commits. check-diff-scope.py builds its path set from `git status --porcelain` union `git diff --name-only`, both working-tree only, so a committed implementation reports 'OK: 0 path(s) in scope' and the clause's own failing example -- a stray edit to dispatch-guard.py -- rides through undetected. The clause's text is honest about this ('The working tree's diff'), and the behavior is house-standard rather than anything this job introduced, so this is a scope note rather than a defect in the drafting. FIX, if it is worth one: say in the clause text that the check is run before the work is committed, so the decompose step puts it in the task's check_method in that order.
  evidence: check-diff-scope.py:34-35 (docstring: 'The path set is the union of `git status --porcelain` and `git diff --name-only`'); /tmp/conaudit-r4-var7-iM6M4M/repo after committing the reference implementation -> 'OK: 0 path(s) in scope', exit 0; the same tree uncommitted -> 'OK: 6 path(s) in scope'
- **C-2** (info): Sound, and it discriminates in both directions. Green on the untouched tree and green against the reference implementation; red against an over-broad relaxation that treats any plural noun later on the line as governed, which fires falsely on two archived jobs. The census sweeps every directory carrying a constitution rather than a hardcoded list, and its control fixture proves the same call can still fire.
  evidence: untouched tree -> exit 0; /tmp/conaudit-r4-ri-VpQQ5y -> exit 0; /tmp/conaudit-r4-var6-UrvgxV (over-broad) -> exit 1, 'FAIL 2026-08-10-issue-117: R10 silent  msg="R10 count-vs-list: tasks/T-007.md:81 says two (2) but the following list has 3 item(s)"' and a second false positive on 2026-08-11-issue-100
- **C-3** (info): Sound, and it discriminates. Red on the untouched tree (no rule_R21 exists), green against the reference implementation, and red against the variant this clause was rewritten to catch -- an R21 that reads the task's own check_method paraphrase instead of the constitution stays silent on the two-clause fixture and the probe catches it. The placement assertions bind against both R2 and R10, and the explicit PROOF entry in RULE_CLASS is checked directly.
  evidence: untouched tree -> exit 1; /tmp/conaudit-r4-ri-VpQQ5y -> exit 0; /tmp/conaudit-r4-var3-lJPXJp (R21 gated on the task's paraphrase) -> exit 1
- **C-4** (info): Sound, and it discriminates. Green on the untouched tree as a no-regression baseline and green against the reference implementation; red against an R21 that compares checker and clause kind for equality rather than testing the one harmful direction, which is the clause's own failing example.
  evidence: untouched tree -> exit 0; /tmp/conaudit-r4-ri-VpQQ5y -> exit 0; /tmp/conaudit-r4-var4-l4C8Sa (equality comparison) -> exit 1 on r21-safe while r21-unsafe stays green
- **C-6** (info): Sound, and it discriminates. Green on the untouched tree and green against the reference implementation once the plugin trees were rebuilt; red against the same implementation before the rebuild, which is exactly the clause's failing example ('R21 is added to the source tree and build-plugin.py is never re-run'). The four commands in the text and the four in the check are the same four.
  evidence: untouched tree -> exit 0; /tmp/conaudit-r4-ri-VpQQ5y before `python3 scripts/build-plugin.py` -> exit 1; after -> exit 0, 'shared-core wrappers, both published packages, and both marketplaces match fresh builds'
- **C-8** (info): Sound, and it discriminates. Green on the untouched tree and against the reference implementation; red against the same tree with `import yaml` added, which is the clause's failing example in miniature. Both halves earn their place -- the exec catches an import that is present but unresolvable, the AST scan catches one that resolves on this host and would not on another.
  evidence: untouched tree -> exit 0; /tmp/conaudit-r4-ri-VpQQ5y -> exit 0; /tmp/conaudit-r4-var5-YAu28D with `import yaml` -> exit 1
- **C-9** (info): The other three seams are sound and they discriminate, which is worth recording separately from the blocker above so the repair does not disturb them. Against the reference implementation all three go green; against the same implementation with the suite left untouched, all three go red with the right reasons ('the suite has no case that depends on this', 'the suite has no case for the relaxation itself'). revert_adjacency's false-FAIL bug is genuinely fixed: the no-op revert now reports as a no-op on the untouched tree and as landed against a real relaxation. The `r2_anchor_message` seam itself is a legitimate constraint, honestly declared in the clause text and cheap to satisfy -- my implementation adopted it without strain -- it is simply unpaid until the wrapper actually runs.
  evidence: /tmp/conaudit-r4-ri-VpQQ5y -> R10 stub, R21 stub and adjacency revert all 'turns the suite red'; /tmp/conaudit-r4-var2-EuJojp (same linter, pristine suite) -> all three red with 'no case' diagnostics; untouched tree -> 'FAIL R10 relaxation: revert to strict adjacency landed', correctly reporting the no-op
- **scope** (info): Scope of this round. Every one of the eight script-checked clauses was executed twice, against the tree as I found it and against a reference implementation I built from the constitution alone, and each also got at least one variant built to violate its own text; C-5 and C-10 are rubrics with nothing to execute, so both were judged by reading, with C-5's instrument run to see what its checker would actually be handed. The baseline sweep ran clean -- 'ran 8 (3 red, 5 green), skipped 2 (2 judgment, 0 no-baseline)' -- and I re-derived every one of those eight by hand, so the declared baselines are accurate as of this text. The weight line's recorded reason holds up: probe.py is 791 lines, this round did rebuild a full reference implementation, and C-10 does name a property no existing command can check; deep has no ceiling so no overrun line is owed, and no **Lint exception** line exists to audit. `git status --porcelain` was empty when I started and empty when I filed. Everything I acted on lived in its own mktemp -d, twelve of them, listed in the evidence above and in apparatus/CON-audit-r4/VENUE-ref.txt; nothing was built or mutated inside the project. One housekeeping note: apparatus/CON-audit-r4/ already existed when I was dispatched, carrying a patch.py and a runclauses.sh from an r4 attempt that did not file. I did not read or reuse them -- round-scoping is a finding source and inheriting a predecessor's reading would trade it away -- and moved them to apparatus/CON-audit-r4-aborted-preexisting/ rather than deleting them.
  evidence: .agent-guild/state/apparatus/CON-audit-r4/ (baseline-sweep.txt, tree-*.txt, ri-letters-*.txt, ri-wordish-*.txt, var-*.txt, build_ri.py, variant_c1_allowlist.py, holdout_adjective.py, VENUE-ref.txt, VENUE-probe.txt)

# A-001: mechanical transform

Sort the following lines case-insensitively, ascending. Output only the sorted
lines, one per line, with nothing else—no explanation, no code fence, no
heading.

```
Orange
apple
Mango
banana
Cherry
date
Fig
elderberry
```
